# archive/ — Notes for Claude

Frozen history: retired skill workspaces (`skill-workspaces/`), past cleanups (`root-cleanup-2026-07/`, `cleanup-2026-07-19/`), and worktree captures (`worktree-capture-*/`).

The `CLAUDE.md`/docs copies nested under `worktree-capture-*/` are STALE DUPLICATES of a past tree — never read them for current work; use the real `docs/`, `backend/`, etc. at the repo root instead.
