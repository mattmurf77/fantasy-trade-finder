# reference/ — Notes for Claude

Competitor app screenshots captured for teardowns (e.g. `dynasty-nerds-app/`). Images only, gitignored — not analysis. Teardown writeups live in `docs/business/product/` and `docs/competitor-teardown-*.md`.
