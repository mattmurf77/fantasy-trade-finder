# Delta verification — Guided Onboarding v2 PRD vs. post-Phase-A `main`

> **Date:** 2026-08-15 · **Verified against:** `/Users/teresadickens/Documents/Claude/Projects/FTF-worktrees/guided-onboarding-v2` @ `d3fe3ac` (= `origin/main`; Phase A `0d8d7bb`, S-43 `c2e44b5`).
> **Purpose:** re-classify every load-bearing PRD fact after open-access Phase A shipped and Maestro was retired. Read this **before** the PRD's §2.1 / §5.1 / §5.2 / §5.3.1 citations — those citations are stale by 80–140 lines in `TradesScreen.tsx` and materially wrong in four places.
> **Method:** every claim re-derived from the worktree; line numbers below are worktree-current.

---

## TL;DR — what materially changed

1. **§2.1's central thesis is MOOT.** `onboarding.landing`, `.trades_first`, `.quickset_prompt`, `.apple_save_moment` are all `true` (`config/features.json:79-83`). The "sixteen of twenty entries unreachable" measurement no longer holds: S0.x, S2.x, S3.x, S5.x, S6.x and S8.1 are all reachable today. **Phase 0's headline job — "make it reachable" — is already done by flag flip.** What remains of Phase 0 is honesty, arbitration, measurement and copy, not reachability.
2. **S-43 (`c2e44b5`) fixed the reveal.** The regen-diff effect now counts over `job.cards` with a late-bound `jobId` and package-key identity (`TradesScreen.tsx:2801-2805`, `:2840-2873`). `s5.1`/`s5.0` **render for the first time ever**. The PRD's §5.2 treatment of `s5.0`/`s5.1`/`s5.5` still applies as *copy* work, but "unreachable in practice" is gone — and this raises R1 stakes because live users now see `s5.1`'s emphatic payoff line as shipped.
3. **`s7.1` is now the only genuinely-dark beat, and it's worse than the PRD says.** `onboarding.rank_routing` is still `false` (`features.json:85`), so `trades.trio-entry` mounts only at `TradesScreen.tsx:5283-5303` under `rankRoutingOn`. But `deck.replenishment` is `true` (`:162`) so `summaryVisible` displaces that whole branch (`:5241` precedes `:5279`). `s7.1` still fires ungated at `:2927-2928`. **The live deictic-degrade incoherence FR-E6 exists to catch is confirmed still shipping.** CUT verdict STANDS.
4. **`quickset_completed` is server-fired only, and its client emitter was deliberately REMOVED** (`analytics_taxonomy.py:299-305`, `:344`). The PRD's §5.3.1 row calls it "EXISTS — registered (`:280`)" and wires it to `s3.2`'s `adoptionEvent` and `N1`/`s3.2`'s `invalidateOn`. **That is now the same trap the PRD itself names for `trio_swipe`**: the client can never observe it, so those retirements would never fire. New required work (or use `quicksetCompletedPositions`, already persisted client-side and read at `TradesScreen.tsx:2676`).
5. **`guide_tour_reenabled` is now REGISTERED** (`analytics_taxonomy.py:328` + props `:895`). FR-E8's named cleanup item is ALREADY DONE.
6. **Maestro is retired (D-056).** `mobile/.maestro` holds six flows; **`guide-no-false-signoff@release.yaml` does not exist anywhere in the tree** — the PRD asserts it ships and uses it as the assertion vehicle for the `s8.1` rewire. NFR-4, D6, D8, G-7's "zero occurrences in Maestro runs", M-column Maestro references, §4.1's "Maestro assertion flows", and §5.4's "do not fix at QA" all need re-expression as structural `mobile/tests/check-*.js` suites + operator TestFlight checklist. Precedent set by S-43: `mobile/tests/check-s51-regen-diff.js` (32 assertions, sabotage-verified) is now "primary evidence" per the commit message.
7. **The engine is byte-for-byte as the PRD describes.** No eligibility fields on `GuideStep`, no `degradeLine`, no suppression event, no `guideScriptVersion`, guide absent from `InterruptSurface`, `claim()` still first-come-no-preemption. **FR-E2…E7, E9, E10 all STAND unchanged.** Phase A touched zero mobile client files (its diffstat is flags + fixtures + docs only).
8. **`onboarding.guide_v2` does not exist** in `config/features.json` — still to be created (§8, D7).
9. **Open-access Phase B is unbuilt.** Zero repo hits for `grade_count`/`grade_required`; `ProvenanceChip.tsx:47` still renders `CONSENSUS VALUES` / `YOUR BOARD` with no progress affordance. NG-2, D5 and the Phase-B-gated progress beat STAND.
10. **All PRD `TradesScreen.tsx` line citations are wrong** — even pre-Phase-A (the `s8.1` predicate was at `:2671` at `c2e44b5^`, `:2703` now, vs. the PRD's `:2588`). The PRD was authored against a tree older than `7057d86`/`6158e65`. Use §"Corrected citations" below. `SendInSleeperButton.tsx`, `LeagueSummaryScreen.tsx`, `MatchesScreen.tsx`, `OutlookBiasReceipt.tsx` citations are all still accurate.

---

## Verdict table

### A. §2.1 gating table (the reachability thesis)

| Row | PRD claim | Verdict | Evidence (worktree) | What the build should now do |
|---|---|---|---|---|
| S0.x — `onboarding.landing` false | Analyst never introduces himself | **MOOT** | `landing` = `true` (`config/features.json:79`); gate unchanged at `SignInScreen.tsx:152`; steps requested `:155`, `:159`, `:371-372` | Nothing to un-gate. FR-E1's "replace with product-state predicate" is now cosmetic for S0.x — keep it only for the one-gate invariant, not for reachability |
| S2.x — `firstRun` ← `trades_first` false | Deck act dead | **MOOT** | `trades_first` = `true` (`:80`); `firstRun` latch unchanged at `TradesScreen.tsx:369-377` | Deck act is **live in production today**. Copy trims (`s2.1`, `s2.3`) are now user-facing fixes, not dark-code cleanup — raises their priority |
| S3.x — `onboarding.quickset_prompt` false | Never fires | **MOOT** | `quickset_prompt` = `true` (`:82`); trigger `maybeShowQuicksetPrompt` `TradesScreen.tsx:2724-2750`, guided arm `:2739-2747` | Live. `s3.1` CUT + `s3.2` absorb is now a live-copy change |
| S5.x — reachable only via S3→S4 | Unreachable in practice | **MOOT + CHANGED** | S3 live; **and** S-43 fixed the effect that made `fresh` structurally 0 (`TradesScreen.tsx:2840-2873`, guided arm `:2853-2861`) | `s5.1`/`s5.0`/`s5.5` are live. Treat their copy trims as **R1-critical** (`s5.1`'s "Your board, your market" now actually ships) |
| S6.2 — `onboarding.apple_save_moment` false | Dead | **MOOT** | `apple_save_moment` = `true` (`:83`); `appleAskEligible` `TradesScreen.tsx:2969-2974` | Live. The `s6.2` 17w/2600ms timing violation is shipping to users now |
| S8.1 — predicate `guideSeen['s2.2'] && guideSeen['s6.1']` | Unreachable because s2.2 dead | **STANDS (predicate) / MOOT (unreachability)** | Predicate **unchanged**, now at `TradesScreen.tsx:2701-2707`; `completeTour` chain `:2713-2718` | The `guideSeen['n6.1'] \|\| guideSeen['s6.1']` rewire STANDS as required work. Its assertion vehicle must become a `check-*.js` suite, not the (nonexistent) Maestro flow |
| **S7.1 — fires pointing at nothing** | Live deictic incoherence | **STANDS — confirmed still live, and displaced twice over** | Request ungated `TradesScreen.tsx:2927-2928`; target mounts only under `rankRoutingOn` (`:319`, flag `false` at `features.json:85`) at `:5283-5303`; and `summaryVisible` (`:2939-2943`, `deck.replenishment` = `true` at `:162`) renders first at `:5241` | CUT `s7.1` as specced. This is the one §2.1 row that survives intact |

**Net:** G-1's reachability table is still worth producing, but it will come back nearly all-green. The PRD's framing — "v2's first job is not new content, it is making content reachable" — is **superseded**. Re-scope Phase 0 around FR-E2…E10 + the event inventory + copy, and drop the un-gating narrative.

### B. Engine requirements

| FR | Verdict | Evidence | What the build should now do |
|---|---|---|---|
| **FR-E1 — One gate** | **MOOT in substance, STANDS as hygiene** | All four sibling flags now `true` (`features.json:79-83`); `rank_routing`/`guided_layer` still `false` (`:85`, `:87`) | Keep the invariant (step eligibility reads only `onboarding.v2` + `guided_avatar` + product state), but the *test* ("with all other `onboarding.*` false a fresh install reaches `s8.1`") is now a regression guard, not a fix. **D4 (`feat/open-access-phase-a` coordination) is CLOSED** — it merged at `0d8d7bb` and touched no mobile file. OQ-6 is moot |
| **FR-E2 — Declarative eligibility** | **STANDS, verbatim** | `GuideStep` has exactly `id/line/pose/flip/target/advance/autoMs/ctas/once/side/screen` (`useGuide.ts:28-47`) — no `retireAfter`, `maxDisplayCount`, `invalidateOn`, `adoptionEvent`, `degradeLine`. `requestStep` checks only `guidedAvatarActive()`, `once`+`guideSeen`, `active` (`:89-103`) | Unchanged |
| **FR-E3 — Behavioral retirement** | **STANDS**, and its stated precondition got harder | No retirement anywhere; `once` is the only cap. **Two** events in the §5.3.1 retirement set are now server-only (`trio_swipe` **and** `quickset_completed`) | Re-audit every `retireAfter`/`invalidateOn` against client-observability before authoring. Prefer persisted client state (`quicksetCompletedPositions`, `useOnboardingState.ts`) over event joins for anything server-fired |
| **FR-E4 — Guide/modal arbitration** | **STANDS, verbatim** | `claim()` still first-come-no-preemption (`useInterruptCoordinator.ts:50-56`); `SURFACE_PRIORITY` still doc-only (`:33-38`); guide absent from `InterruptSurface` (`:26-30`); both root modals still test `activeSurface !== null` (`PushPrimingModal.tsx:37`, `AppleSaveMomentSheet.tsx:40`) | Unchanged. Note G-7's "zero modal-over-bubble occurrences in Maestro runs" needs a new vehicle |
| **FR-E5 — Suppression measured** | **STANDS** | Silent drop at `useGuide.ts:94`; no `guide_step_suppressed` in taxonomy (`analytics_taxonomy.py:78-79`, `:553-557`) | Unchanged |
| **FR-E6 — Degrade observable** | **STANDS** | `AnalystGuide.tsx:42-49` measures, `:79-86` renders cutout-or-nothing, same line either way; no `spotlight` prop (`analytics_taxonomy.py:553`); 250 ms ceiling `guideTargets.ts:28` | Unchanged. `s7.1` remains the live exhibit (row A7) |
| **FR-E7 — Copy lint** | **STANDS** — recount the violations against the *live* script | `analystScript.ts` unchanged: `s0.1` `:13`, `s2.1` `:41`, `s2.3` `:51`, `s3.1` `:55`, `s5.0` `:78`, `s7.1` `:100`; `s6.1` 12w/2200 ms `:90`, `s6.2` 17w/2600 ms `:94` | Unchanged — but every listed violation is now **shipping to users**, not dark |
| **FR-E8 — Taxonomy first** | **PARTIALLY ALREADY DONE** | `guide_tour_reenabled` **is registered**: name `analytics_taxonomy.py:328`, props `frozenset()` `:895` | Drop the "register or remove `guide_tour_reenabled`" task. The rest of FR-E8 (row-before-emitter + read-back) STANDS |
| **FR-E9 — Script versioning** | **STANDS** | No `guideScriptVersion` in `OnboardingPersisted` (`useOnboardingState.ts:46-48`, defaults `:75-77`) | Unchanged. Now more load-bearing: v1 upgraders **have** seen live beats since the flag flip, so the "returning v1 user" population is real, not hypothetical |
| **FR-E10 — Opt-out sacred, re-enable sane** | **STANDS, and the diagnosis sharpens** | `enableTour()` = `resetGuideProgress()` + track (`useGuide.ts:132-135`); `resetGuideProgress` clears `guideDismissed`, `guideSeen`, `guideTourCompleted` wholesale (`useOnboardingState.ts:147-159`) | Unchanged. "Replays into an unreachable opening" is now "replays into a **reachable** opening" — i.e. a Settings re-enable today re-runs `s0.1`-onward for real. Still wrong (full wipe, not trigger-aware), still FR-E10 work |
| **FR-E11 — Kill-switch honesty** | **STANDS** | `onboarding.guided_layer` still `false` (`features.json:87`); `guidedAvatarActive()` off ⇒ passive branches at `TradesScreen.tsx:2572-2601` (`guidedOn && tradesFirstOn`) never run | The fallback is still *nothing*. **OQ-7's "flip the flags" default is now a one-key change** (`guided_layer` only — `quickset_prompt` is already true), which makes it cheap. Recommend flipping `guided_layer` as the runbook fix |

### C. Beats

| Beat | Verdict | Evidence | What the build should now do |
|---|---|---|---|
| **`N1`** (provenance chip, calibration copy) | **STANDS** — with one broken dependency | Target registered `TradesScreen.tsx:2624`; chip copy `ProvenanceChip.tsx:47`; per-user Elo confirmed (`server.py:10315`/`:10324` → `ranking_service.py:334`, `trade_k_like` 8.0 / `trade_k_pass` 4.0 at `:57-58`); no `grade_count` anywhere | Build as specced. **Re-source `invalidateOn`**: `quickset_completed` is server-only (see §D). `adoptionEvent: quickset_started` still NEW |
| **`s3.2`** (two-minute ask) | **STANDS** — trigger now live, not dark | Guided arm `TradesScreen.tsx:2739-2747`; chain `:2663-2671` off `guidedS3Pending`, never `guideSeen['s3.1']` (the PRD's CUT-safety argument re-verified, now `:2746` + `:2663`) | Build as specced. `adoptionEvent: quickset_completed` **must be re-sourced** to a client-observable receipt |
| **`N2`** (re-aim the deck, two-form) | **STANDS, unchanged** | `consolidateOn = fullSheetOn && !!finderMode` at `:607`; strip gated `consolidateOn && showPrefsChangedStrip` at `:3867`, emits `handleFindTrades('prefs_changed_strip')` `:3872`; receipt guard `OutlookBiasReceipt.tsx:46-54` / `:89`, `.change` target `:103`; all three owning flags on: `trade.outlook_direction` (`features.json:56`), `trades.finder_hub` (`:11`), `trades.edit_full_sheet` (`:182`; read at `TradesScreen.tsx:538`) | Build as specced. Form A/B split, `degrade: fall back to Form B`, `outlook_saved` NEW emitter — all still correct |
| **`N4`** (empty deck / pin hand-off) | **STANDS, unchanged** | `summaryVisible` `:2939-2943`; card render `:5241-5278`, testIDs `trades.deck-summary` `:5246`, `.see-liked` `:5264`, `.done` `:5271`; `targetingEnabled` `:1116` (`trade.finder_targeting` = `true`, `features.json:36`); player-mode board `:4227` / `:4518`; legacy "Target players" `:4337-4341` | Build as specced. Button-budget rule (one primary) applies to the card at `:5262-5276` |
| **`N6.1`** (first-like router) | **STANDS** — and its `onSuccess` gate is now confirmed viable | `swipeMutation` `:1620`, `onSuccess` `:1646`, `{matched, match_id}` read at `:1651-1653`; first-like detection `:3448` (`celebrationsShown.first_like`); request-then-consume `:3461-3467`; **note:** likes bypass the undo hold and `mutate()` immediately (`:3421-3432`), so the `onSuccess` gate fires without delay | Build as specced. The `guideSeen['n6.1']` write on the `matched` suppression path and on timeout both STAND |
| **`N6.2`** (awaiting send spotlight) | **STANDS, unchanged** | `TradeCard.tsx:662-671` mounts `SendInSleeperButton surface="awaiting"`; per-testID last-mount-wins `guideTargets.ts:9-16`; router early-returns `SendInSleeperButton.tsx:411-413` (MFL) / `:431-433` (ESPN) / `:458-472` (copy) before `:483` | Build as specced. `trades.send-control.guide` per-instance registration STANDS |
| **`N7`** (Trios rung) | **STANDS, unchanged** | Nested nav precedent `TradesScreen.tsx:2685-2688`; `Trios` registered unconditionally in the Rank stack; `trio_entry_tapped` `:5299` unregistered; `trio_swipe` server-only (`analytics_taxonomy.py:342`) | Build as specced. The trio client-receipt requirement STANDS |
| **`N3`** (mutual-match send) | **STANDS, all citations exact** | `SendInSleeperButton.tsx`: `:104` kill switch read, `:452` `if (!enabled) return null`, `:109` MFL flag, `:115` ESPN flag, `:252` `sleeper_send_failed`, `:358` `sleeper_send_attempted`, `:483` testID. `espn.send` = `true` (`features.json:69`); build-≥103 precondition documented at `:68`. **`SendInMflButton.tsx` and `SendInEspnButton.tsx` still contain zero `track()` calls** (verified by count) | Build as specced. Stale-comment cleanup task STANDS (`SendInSleeperButton.tsx:428-431` still says ESPN flag "absent from…") |
| **`N5`** (league standing) | **STANDS — every citation still exact** | `league_pos_candidates_viewed` `LeagueSummaryScreen.tsx:920` with `divider ∈ shown\|no_median\|no_split` `:922`; `league_candidate_pinned` `:1086`; basis chips `:1426`/`:1432`/`:1438`; median divider `:2085`. `league.pos_candidates` + `league.player_trade_handoff` both `true` (`features.json:148-149`) | Build as specced, zero corrections |
| **Ladder / `rankLadder`** | **STANDS** | No ladder state in `useOnboardingState.ts` | Unchanged |
| **Phase-B progress beat** | **STANDS as deferred** | Zero hits for `grade_count`/`grade_required` in `mobile/` or `backend/` | Unchanged; D5 still open |

### D. §5.2 script verdicts

| v1 id | PRD verdict | Delta verdict | Note |
|---|---|---|---|
| `s0.1` | CHANGE — trim; un-gate | **CHANGE (trim only)** | Un-gate clause MOOT; live today at `SignInScreen.tsx:155` |
| `s0.2` | KEEP | **STANDS** | `analystScript.ts:15-19` |
| `s0.err-notfound` | CHANGE — trim | **STANDS** | `:20-23`; now reachable (`SignInScreen.tsx:371-372`) |
| `s0.err-down` | KEEP | **STANDS** | `:24-27` |
| `s1.1` | CHANGE — trim | **STANDS**, with a new caveat | `:28-32`. `onboarding.league_autoskip` is now `true` (`features.json:81`) — verify the picker still mounts for multi-league users before spending copy on it |
| `s2.wait` | CHANGE — itemize | **STANDS** | `:33-38`; live via `TradesScreen.tsx:2634-2640` |
| `s2.1` | CHANGE — trim + soften | **STANDS, priority up** | `:39-42`; **shipping to users now** — the un-softened confidence claim is live, so this is an R1 exposure, not a dark-code trim |
| `s2.2` | KEEP | **STANDS** | `:43-47`; requested `TradesScreen.tsx:2658-2659`, advanced `:3330` |
| `s2.3` | REPLACE → `N1` | **STANDS** | `:48-52`; live via `:3332-3334` |
| `s3.1` | CUT — merged into `s3.2` | **STANDS; safety re-verified** | `setGuidedS3Pending(true)` unconditional at `:2746`; `s3.2` chains off `guidedS3Pending` at `:2663`, never `guideSeen['s3.1']` |
| `s3.2` | CHANGE — absorbs s3.1 | **STANDS** | `analystScript.ts:57-67` |
| `s4.1` | CHANGE — trim; `degradeLine` | **STANDS** | `:68-71`. Note: `s4.1` has `advance:'tap'` and **no `target`** (`:69`) — the PRD calls it deictic and needing a degrade line; verify intent, since with no target there is nothing to measure or degrade |
| `s5.1` | KEEP | **CHANGED — now actually renders** | `:72-75`; S-43 (`TradesScreen.tsx:2840-2873`, request `:2857-2859`). Copy is live for the first time; re-review under R1 rather than rubber-stamping KEEP |
| `s5.0` | CHANGE — trim 27→13 | **STANDS, priority up** | `:76-79`; same S-43 note |
| `s5.5` | CHANGE — trim | **STANDS** | `:80-88`; chain `TradesScreen.tsx:2675-2691` |
| `s6.1` | REPLACE → `N6.1` + s8.1 rewire | **STANDS** | `:89-92`; request `TradesScreen.tsx:3461`; s8.1 predicate `:2701-2707` |
| `s6.2` | CHANGE — trim, fix `autoMs`, re-sequence chain; **`maybeAskApple` must stop consuming before show** | **STANDS — consume-before-show confirmed still live** | `analystScript.ts:93-96`; chain `TradesScreen.tsx:3468-3475`; `maybeAskApple` sets `appleAskShownThisSession = true` and patches `applePromptShownFor` at `:2978-2983` **before** the 700 ms `setTimeout` that calls `setAppleAsk` at `:2985-2988`. Bug intact |
| `s7.1` | CUT as pushed beat | **STANDS** (see row A7) | `:97-101` |
| `s8.1` | KEEP | **STANDS** | `:102-105` |
| S2.identity, S2.re-entry, S4.2, S4.idle | CUT from script doc | **STANDS** | Absent from `analystScript.ts` — doc-only cleanup |

### E. §5.3.1 event inventory

| Event | PRD state | Delta verdict | Evidence |
|---|---|---|---|
| `guide_step_shown/_advanced/_skipped` | EXISTS; `spotlight` NEW | **STANDS** | `analytics_taxonomy.py:78`, props `:553-555`. `via` union incl. `'timeout'` confirmed `useGuide.ts:61` |
| `guide_tour_reenabled` | Emitted, **unregistered** | **ALREADY DONE** | Registered `analytics_taxonomy.py:328`; props `frozenset()` `:895` |
| `guide_step_suppressed` | NEW | **STANDS** | Absent |
| `find_trades_tapped{source:'prefs_changed_strip'}` | EXISTS, registered | **STANDS, verified** | `analytics_taxonomy.py:495` (`{"source","mode"}`) + the "`source` is a BUG FIX" comment `:485-494` naming `prefs_changed_strip` as a live value; emitter `TradesScreen.tsx:3872` → `handleFindTrades` `:808-813`. Strip gated on `consolidateOn` `:3867` (defined `:607`) |
| `deck_regenerated` allowlist `{position, new_trades}` | Do not splice | **STANDS** | `analytics_taxonomy.py:674` unchanged; sole emitter now `TradesScreen.tsx:2848-2852`, still armed only by the Quick-Set-return path (`:2776-2822`) |
| `outlook_saved` | NEW | **STANDS** | Absent; `TradeDnaSheet.tsx` is a `<Modal>` at `:482` (PRD said `:475`) — NG-11 unaffected |
| `finder_target_pinned` | NEW | **STANDS** | Absent |
| `league_pos_candidates_viewed` | EXISTS `:920` | **STANDS, exact** | `LeagueSummaryScreen.tsx:920-922`; taxonomy `:212` + props `:741` |
| `league_candidate_pinned` | EXISTS `:1086` | **STANDS, exact** | `LeagueSummaryScreen.tsx:1086` |
| `sleeper_send_attempted/_failed` | EXIST; MFL + ESPN NEW | **STANDS, exact** | `SendInSleeperButton.tsx:358`/`:252`; taxonomy `:128`, props `:608`/`:614`. `SendInMflButton.tsx` and `SendInEspnButton.tsx`: **0 `track()` calls each** |
| **`quickset_completed`** | **EXISTS — registered `:280`** | **CHANGED — materially** | **SERVER-fired only** (`analytics_taxonomy.py:344`); explicitly excluded from client events with the client emitter **removed** (`:299-305`, "would trip the import-time disjointness assert… and take the app down at boot"). Wiring `s3.2`'s `adoptionEvent` or `N1`/`s3.2`'s `invalidateOn` to it from the client is the FR-E3 worse-than-none case. **Use the persisted `quicksetCompletedPositions` (read at `TradesScreen.tsx:2676`) or add a distinctly-named client receipt** |
| `quickset_started` | NEW | **STANDS** | Zero hits |
| `awaiting_segment_viewed` | NEW | **STANDS** | Zero hits. `MatchesScreen.tsx` has no client event on awaiting mount; the dismiss path is explicitly server-fired (`trades.ts:576`) |
| trio client receipt | NEW; `trio_swipe` server-only | **STANDS, exact** | `trio_swipe` `analytics_taxonomy.py:342` (SERVER_FIRED); `trio_entry_tapped`/`trio_session_started` **are now registered** as client events (`:323`) — but neither is a *submit* receipt, so the requirement is unchanged |
| `tiers_saved` | In code, no taxonomy row | **STANDS** | Zero taxonomy hits; all `tiers_saved` matches are the DB column (`database.py:185`) / `accounts.py` board flags — i.e. it is **not an event name at all**. Prefer dropping it from `invalidateOn` or replacing with a real receipt |
| `ranks_imported`, `all_positions_ranked`, `account_verified` | NEW (no emitter) | **STANDS** | Absent |

### F. Cross-cutting PRD sections

| Section | Verdict | Note |
|---|---|---|
| §3.1 G-1 (reachability goal) | **MOOT in substance** | Table still worth producing as a regression artifact; it will come back all-green except `s7.1` |
| §3.1 G-7 ("zero modal-over-bubble in Maestro runs") | **CHANGED** | Re-express as a structural assertion + TestFlight checklist item |
| §3.2 NG-1 (no in-app accept) | **STANDS, exact** | `mobile/src/api/trades.ts:484-487`: "Mobile READS dispositions and does not write them… Accept/decline UX on mobile is a NEXT.md item" |
| §3.2 NG-11 (no beat inside a `<Modal>`) | **STANDS** | `TradeDnaSheet.tsx:482`; `AnalystGuide` mounted once in `RootNav.tsx:810` |
| §4.x metrics | **STANDS**, except Maestro references in M-notes and §4.1 | Substitute structural suites + operator walk |
| §5.5 NFR-4 (Maestro) | **MOOT** | D-056. Replace with: new `testID`s pass `mobile/scripts/testid-lint.sh` (still live, still in CI) + a per-beat `mobile/tests/check-guide-*.js` suite following the `check-s51-regen-diff.js` pattern |
| §6 Phase 0 exit gate | **CHANGED** | "G-1 table with zero failing rows" is now nearly free; "operator TestFlight walk under the pinned flag set" becomes the *primary* gate, not a supplement |
| §7.1 D4 (`feat/open-access-phase-a`) | **CLOSED** | Merged `0d8d7bb`; zero mobile-file overlap. OQ-6 moot |
| §7.1 D6 / D8 (Maestro flows, sim gate) | **MOOT** | D-056 retired both |
| §8 Flags | **STANDS** | `onboarding.guide_v2` still to be added to `config/features.json` |
| §9 OQ-6 | **RESOLVED by events** | Phase A landed |
| §9 OQ-7 | **STANDS, cheaper** | Only `onboarding.guided_layer` (`features.json:87`) is left false; flipping it is a one-key runbook fix |

---

## New facts the PRD doesn't know

1. **Phase A was config-only.** `0d8d7bb` touched `config/features.json`, five flag fixtures, `test_seed_ui_test_db.py`, and three docs. **No `mobile/src` file changed.** Every engine/screen fact in the PRD survives structurally; only reachability and line numbers moved.
2. **Flag-fixture mirror enforcement exists.** `backend/tests/test_seed_ui_test_db.py:107` mirror-asserts `backend/tests/fixtures/flags/release.json` against `config/features.json`. **Adding `onboarding.guide_v2` requires updating `release.json` (+ `profiles-on.json`, `release-300.json`, `release-espn-send-off.json` as documented derivations) or the backend suite goes red.** Not in the PRD's D-list.
3. **`backend/tests/fixtures/flags/onboarding-v2.json` inverted its job** and now holds `onboarding.league_autoskip` false deliberately, with a pinning test that "fails loudly if the Phase A flip is ever reverted." Any FR-E1 test that assumes all-`onboarding.*`-false must not fight this fixture.
4. **`onboarding.league_autoskip` is now `true`** (`features.json:81`) — the PRD never mentions it. It is the flag that can make `s1.1`'s screen skip itself. Verify `s1.1`'s reachability before spending copy on it (§5.2 row).
5. **S-43 established the post-Maestro evidence standard**: `mobile/tests/check-s51-regen-diff.js` — 32 assertions, sabotage-verified, including a section that *lifts the shipped expression verbatim and executes it against synthetic data*. This is the template the PRD's D3 ("structural CI test + copy lint") should follow, and it is the substitute for D6.
6. **No `check-*.js` suite runs in CI** (recorded in the 2026-08-12 CHANGELOG entry). D3's "structural CI test" therefore requires *adding CI wiring*, not just a script. `mobile/scripts/testid-lint.sh` **does** run in CI.
7. **Likes are not undo-held.** `ux.swipe_undo` delays only *pass* POSTs (`TradesScreen.tsx:3421-3429`); a like calls `swipeMutation.mutate` synchronously at `:3431`. N6.1's `onSuccess`-gated design has no added latency for likes.
8. **`matchIdByTradeRef`** (`TradesScreen.tsx:1651-1653`, declared `~:1713`) already caches `trade_id → match_id` from swipe responses for the share landing. N6.1's `matched` branch can reuse it rather than re-reading the response.
9. **`trio_entry_tapped` and `trio_session_started` are now registered client events** (`analytics_taxonomy.py:323`) — the PRD says "neither registered." The N7 requirement is unchanged (still needs a *submit* receipt), but the taxonomy work is smaller than stated.
10. **`tiers_saved` is not an event name anywhere** — it is a `users` table column (`backend/database.py:185`). The PRD's "in code (44 files) but no taxonomy row" reads as if an emitter exists. It does not.
11. **`guide-no-false-signoff@release.yaml` does not exist** in `mobile/.maestro` (six flows + `capture/` + `flows/`). It is referenced only in `docs/plans/audit-p0-remediation/*` and `living-memory/TEST_LEDGER.md`. The PRD leans on it in §2.1, §5.5 NFR-4 and R-handling; all three need a new vehicle.
12. **`DECISIONS.md` IDs were renumbered 2026-08-15**: Maestro retirement is **D-056** on `main` (the brief's "D-056/D-057" reflects the pre-renumber fork). Cite D-056.

---

## Corrected citations (build agents: use these, not the PRD's)

**`mobile/src/screens/TradesScreen.tsx`** — all PRD citations for this file are stale; the file is 6,576 lines.

| Thing | PRD said | Actual |
|---|---|---|
| `firstRun` latch | `:334-337` | `:369-377` |
| `consolidateOn` | `:572` | `:607` |
| `finderMode` | `:531/:541` | `:576` (`finderHubOn` `:574`) |
| `outlookReceiptShown` | — | `:909-915` |
| `swipeMutation` def / `onSuccess` / `{matched, match_id}` | `:1517/:1543/:1548` | `:1620` / `:1646` / `:1651-1653` |
| `targetingEnabled` | — | `:1116` |
| `s8.1` predicate | `:2588-2594` | `:2701-2707` |
| guide target registration | — | `:2622-2631` (`provenance-chip` `:2624`, `trio-entry` `:2625`, `card-body` `:2623`) |
| `s3.2` chain (`guidedS3Pending`) | `:2550` | `:2663-2671` |
| `setGuidedS3Pending(true)` | `:2630-2633` | `:2746` |
| `s5.5` chain | — | `:2675-2691` |
| Quick-Set-return focus effect | — | `:2776-2822` |
| regen-diff effect (S-43) / `deck_regenerated` emitter | `:2699` | `:2840-2873` / `:2848-2852`; guided arm `:2853-2861` |
| `s7.1` request | `:2775-2776` | `:2927-2928` |
| `summaryVisible` | `:2787-2791` | `:2939-2943` |
| `appleAskEligible` | — | `:2969-2974` |
| `maybeAskApple` (consume-before-show) | `:2824-2837` | `:2976-2989` (consume `:2978-2983`, show `:2985-2988`) |
| first-like detection | — | `:3448` |
| `s6.1` request | — | `:3461` |
| `s6.2` + Apple chain | `:3316-3322` | `:3468-3475` |
| prefs-changed strip gate / `find_trades_tapped` | `:3707` / `:3712` | `:3867` / `:3872` (helper `:808-813`) |
| `OutlookBiasReceipt` mount | — | `:3855-3860` |
| legacy outlook row | — | `:4099` |
| targeting board (player mode) | `:4047/:4337` | `:4227` and `:4518` |
| legacy "Target players" | `:4157` | `:4337-4341` |
| summary card render | `~5060-5090` | `:5241-5278` (`.see-liked` `:5264`, `.done` `:5271`) |
| exhausted non-summary branch / `trio-entry` | `:5096-5117` | `:5279-5312`; button `:5291-5303`, `trio_entry_tapped` `:5299` |

**Other files — PRD citations verified EXACT, no change needed:**
`SignInScreen.tsx:152` · `useGuide.ts:94` / `:132-135` / `:134` · `guideTargets.ts:9-16` · `useInterruptCoordinator.ts:50-56` · `PushPrimingModal.tsx:37` · `AppleSaveMomentSheet.tsx:40` · `OutlookBiasReceipt.tsx:46-54` / `:79` / `:87` (`.change` at `:103`, `.details` at `:122`) · `SendInSleeperButton.tsx:115` / `:252` / `:358` / `:428-433` / `:483` (kill-switch return at `:452`, copy fallback `:458-472`, MFL branch `:411-413`) · `TradeCard.tsx:666`→ now `:662-671` (`surface="awaiting"` at `:671`; `surface="match"` at `:654`) · `MatchesScreen.tsx:112-119` → now `:132-137` · `LeagueSummaryScreen.tsx:920` / `:922` / `:1086` / `:1432` / `:2085` · `analystScript.ts:89-96` · `analytics_taxonomy.py:530-531` → now `:608`/`:614`; `:596` → now `:674`; `:417` → now `:495`; `:278` → now `:342`; `:280` → now `:344` (**and see §E — server-fired**).

**Backend:** swipe route `server.py:10274-10276`; `record_trade_signal` calls `:10315`/`:10324`; `ranking_service.py:334` (Ks at `:57-58`); `trade_match` notifications `server.py:10446`/`:10455` (PRD said `10321-10339`).

**`config/features.json`:** `trade.send_in_sleeper` `:45` · `trade.send_in_mfl` `:46` · `trade.outlook_direction` `:56` · `_comment_espn_send` `:68` · `espn.send` `:69` · `trade.finder_targeting` `:36` · `trades.finder_hub` `:11` · `trades.edit_full_sheet` `:182` · onboarding block `:74-89` (`v2` `:78`, `landing` `:79`, `trades_first` `:80`, `league_autoskip` `:81`, `quickset_prompt` `:82`, `apple_save_moment` `:83`, `rank_routing` `:85` **false**, `guided_layer` `:87` **false**, `guided_avatar` `:88`) · `league.pos_candidates` `:148` · `league.player_trade_handoff` `:149` · `deck.replenishment` `:162` · `ux.prompt_arbiter` `:117`.
