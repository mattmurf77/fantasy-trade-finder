# Handoff prompt — Rookie-draft M5 (MFL parity) + M6/M6b (slot values)

Copy everything below into a fresh session.

---

You are the **orchestrator** for the next build wave of the rookie-draft feature set in the Fantasy Trade Finder repo (`/Users/teresadickens/Documents/Claude/Projects/Fantasy Trade Finder`).

**Your role, explicitly:** you manage subagents who write the code. You brief them, enforce file ownership, merge their branches, run the gates, and push. **You do not write the feature code yourself** (small merge-conflict resolutions and gate fixes are fine). When the wave is complete, the operator returns to a separate session for review, revision, and sign-off before anything is released — so leave every new flag OFF, do not cut a TestFlight build, and do not flip anything on for users. Your deliverable is merged, gated, pushed-to-`main`, flag-dark code plus a clear report of what was built and what the reviewer should scrutinize.

**Read first, in order:**
1. `docs/plans/rookie-draft/plan.md` — the dual-agent-converged plan. §1's D-criteria are the acceptance contract; §M5 and §M6 are your milestones; the **"Operator decisions — 2026-08-06"** block at the bottom is binding and CHANGES M6 (see below).
2. `docs/plans/rookie-draft/lld.md` — implementer-grade spec; follow its M5 and M6 sections. Note the LLD predates the operator decisions where they conflict — the plan's decisions block wins, and any conflict must be called out in code comments and the status doc (a prior wave hit exactly this on O10 and handled it that way).
3. `docs/plans/rookie-draft/hld.md` — component boundaries.
4. `docs/plans/rookie-draft/build-m4.md` — what the route wave deliberately deferred to M5 (MFL binding, `draft.mfl` flag) and to M6 (`slot_value` is already typed optional client-side, so M6 is additive).
5. `docs/feedback/items/214-stud-tax/` — the precedent for shipping a repricing safely (mode toggle + matrix replay + deck sanity diff). M6b follows this pattern.
6. Repo conventions: root `CLAUDE.md`, `docs/CLAUDE.md` (doc-update triggers), `docs/coding-guidelines.md`.

**Current state (all on `origin/main`, all dark):** M0 data foundation + M1 draft fixture/replay harness (unflagged, live), M2 rookie scope backend seam + mobile surfaces + consolidated rookie view (`ranks.rookie_subset` OFF), M3 `backend/draft_board_service.py` + `GET /api/draft/board` (`draft.room` OFF), M4 Draft Room UI (`draft.live_poll` OFF). Backend suite baseline **1685 passed / 1 skipped**; tsc clean. A separate session is running QA + TestFlight on that work — coordinate by not touching what it touches (it should only be flipping flags and fixing QA findings, but re-check `git log origin/main` before you start and re-baseline your test counts if it landed fixes).

## The three milestones

**M5 — MFL parity for the Draft Room.** `draft_board_service` already renders MFL fully and has passing fixture tests (`test_m5_*`) against four committed live grids (`made0` / `partial` / `complete` / `multi-unit`), but the route deliberately does NOT bind MFL — an unbound MFL league currently returns an honest `platform_unsupported` payload via `draft_board_service.unsupported_board()`. M5 wires the production MFL fetchers behind a new `draft.mfl` flag (4-touch: `feature_flags.py`, `config/features.json`, `backend/tests/fixtures/flags/release.json` — the mirror is test-enforced — and `docs/config-reference.md`), including the MFL player-id crosswalk so the undrafted list isn't silently under-counted (the service suppresses it unless `mfl_player_ids` is injected — that's the seam). Honor `mfl_service._REQUEST_SPACING_SECONDS`. Per the plan: **mid-draft MFL update latency is UNVERIFIED** — a timed probe against a genuinely drafting MFL league is a release gate for MFL live mode; until it passes, MFL ships upcoming + manual refresh only. Snapshot age (`as_of`) always surfaced; auth failure falls back to the stored snapshot with a reconnect notice, never stale-as-live.

**M6 — slot values, display axis.** Read DynastyProcess `files/values.csv` (the repo currently reads only `values-players.csv`) for its `pos:"PICK"` rows — per-slot current-year prices (`2026 Pick 1.01…5.12`) plus future-year rungs. Serve `slot_value` on the draft board payload (it is already typed optional on the client). Needs its own hermetic test seam: `FTF_TEST_MODE` only pins `values-players.csv` today, so a second remote file would be live egress in the harness — add an env override before any fetch lands. Non-12-team leagues use a **percentile map**, labeled an approximation (operator decision O3).

**M6b — slot values in the TRADE ENGINE (operator decision O2 overrides the plan's original "display-only" position).** This is a repricing, not a plumb: DP's current-year slot curve is much steeper than our shipped ladder (1.01 ≈ Elo 1817 vs "Early 1st" 1720), and tier bands are absolute Elo anchored to pick values and mirrored across five clients (`docs/cross-client-invariants.md`). Ship it the way #214 shipped the stud-tax retune: a **user-facing mode toggle** (e.g. `pick_pricing`: market-slots | tier-ladder, defaulting to today's behavior until the operator says otherwise), a **before/after matrix replay** so the value deltas are visible per asset, and a **deck sanity diff** on a real league proving suggestions don't flood or degrade. `GENERIC_PICK_SEEDS` and the tier ladder stay untouched under the legacy mode — flag/mode off must be byte-identical. **The operator also flagged a future third mode** — pick values derived from the user's OWN rookie rankings (personalized pick pricing) — which is NOT in scope now, but the toggle architecture must not preclude it; say in the status doc how a third mode would slot in.

## How to run the wave

Use **Opus subagents in isolated worktrees**, briefed one milestone each, with **disjoint file ownership** — this repo's `backend/server.py` (~17.5k lines) and `backend/database.py` are single-writer resources; never put two agents in the same file in one wave. Suggested sequencing: M5 and M6 can run in parallel *only if* you can split their `server.py` edits into non-overlapping regions (M5 = route binding + MFL fetchers; M6 = payload enrichment) — if that's not cleanly separable, run M5 first, merge, then M6, then M6b (M6b depends on M6's data reader). M6b is a solo wave regardless: it touches `trade_service.py` pricing.

Every subagent brief must include: `git fetch origin && git merge origin/main --no-edit` as its FIRST action (abort and report on conflict); the exact plan/LLD sections that bind it; its file-ownership list and its do-not-touch list; verify-failing-first tests where the LLD names them; gates (`python3 -m pytest backend/tests -q` green + `cd mobile && npx tsc --noEmit` clean); the 4-touch flag convention; doc updates per `docs/CLAUDE.md`; a status doc under `docs/plans/rookie-draft/`; commit on its own branch; **do NOT merge or push** (you do that).

You merge each branch, resolve registry-file conflicts by union-dedupe of table lines, run the full gate yourself with the exit code unmasked (a `tail -1` on pytest output hides failures — check `$?`), and push to `main` from a clean worktree if the shared working tree holds another session's uncommitted WIP (never commit, stash, or discard foreign WIP).

**Report at the end:** what each milestone built, the gate numbers, every deviation from plan/LLD with its justification, the M6b before/after value table and deck-diff numbers, which flags exist and confirmation they are all OFF, and a ranked list of what the reviewing session should scrutinize hardest — the repricing math above all.
