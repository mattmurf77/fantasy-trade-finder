# Parent specification — remaining focal-alignment mechanism

September 22, 2026. Read-only, development-only audit while the parent validates
the round8 freshness repair. Agent B owns a concise mechanism report; Agent C
independently challenges it. No source changes, new generation, scorer changes,
weight fitting, provider/DB calls or release decisions are authorized here.

## Question and evidence

The owner prioritizes bilateral willingness to execute, with personal rankings
plus outlook/needs identifying targets and outgoing candidates, and market value
setting consideration. Higher enthusiasm alone must not raise the asking price.
Unknown counterparty preferences are not evidence of success. Useful integrity
or diversity improvements do not themselves establish this product objective.

The locked69-context comparison and policy3 retained replay remain the evidence
baseline. See evaluation-evidence.md, presentation-evidence.md if present,
release-decision.md, and scope.md. In the original policy2 first30 development
subsets, A's known focal-buy alignment declined80.99%→76.65%; policy3 recovered
to78.23%, almost identical to its native78.25%. Changing known coverage and card
composition makes those percentages unsuitable as matched causal effects or
acceptance estimates. We need a mechanism diagnosis, not another aggregate score.

Private artifacts (never commit raw participant data):

- `/private/tmp/fleeced-revision-panel-v2-20260922`: request inputs, per-arm exact
  inventories/results and summary from the fixed comparison.
- `/private/tmp/fleeced-revision-policy3-regression-20260922`: same retained inputs
  replayed through current presentation, with exact native inventory bridges.
- `/private/tmp/bilateral-revision-locked-panel-v2-20260922.json`: locked split.

Select at most three explicitly development, real-request contexts that already
show known focal-buy erosion, based on their existing aggregate diagnostics.
Do not inspect holdout to choose a change. Decompose by native construction,
eligibility and final survivor order; preserve exact multiset occurrence identity.
For a few exact examples determine whether an incumbent high-conviction target
disappeared, remained but ranked later, gained more expensive companions, or was
replaced by Unknown/differently covered assets. Compare both managers, market
terms, outlook/needs evidence and existing term-efficiency precedence; do not
assume moving a favored A target upward is unconditionally a bilateral gain.

Trace the relevant current algorithm, distinguish observed effects from plausible
explanations, and give at most one minimal mechanism-level regression or proposed
change if supported. A valid finding can be that current artifacts cannot isolate
the cause. Do not turn a feature contribution into an acceptance probability or
recommend arbitrary retuned weights. Any semantic change needs a separate parent
specification, registered new independent panel and full six-dimension evaluation.

## Execution limits

No benchmark/replay/runtime import while the parent's exclusive source/CPU guard
is active. Static source reads and lightweight extraction from the selected saved
artifacts are allowed; do not load every large corpus file at once. Do not edit
runtime, tests, existing frozen artifacts or graders. Use apply_patch for your
report and any private read-only extraction script. Report missing evidence rather
than silently reconstructing historical projections or user preferences.

Write `focal-mechanism-audit.md`, with split selection, exact diagnostic definitions,
privacy-safe example labels, limitations and a bounded next decision. C reviews
its reasoning against the owner's priorities before parent decides further work.
