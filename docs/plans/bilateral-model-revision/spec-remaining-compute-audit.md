# Parent specification — remaining exact compute opportunities

September22,2026. Agent C, read-only bounded audit while A integrates the narrow
final-freeze change and B reviews it. The authenticated-boundary prototype remains
private; do not introduce its authority/configuration changes into shared code.

Use the existing private full-worker/profile artifacts and current source to
identify at most two remaining dominant redundant computations. Distinguish
already removed freeze/parse work from current remaining work; older profile
seconds are not updated timing evidence. No new benchmarking during A's guard,
no provider/database call and no shared runtime or scorer edit.

Prefer exact code/data-lifetime improvements inside current generation and
presentation: repeated same-input lookups, repeated preparation of value/tier or
lineup substrates, or avoidable materialization later discarded. Show the exact
consumer, complete transitive dependencies and existing cache boundaries. Identify
whether an operation is really duplicate or required to retain full valid
inventory, independent evidence and final order. Do not propose output caps,
reduced pools/budgets, proxy approval, learned acceptance claims or the rejected
unconditional simple-first order. Avoid a new platform as the default response.

For each defensible opportunity give one minimal implementation seam, an exact
parity falsification test, state/lifetime/privacy risks, and a measurable bounded
prototype criterion. If no additional worthwhile exact optimization is supported,
say that clearly. Do not write or tune another prototype under this spec.
Write remaining-compute-audit.md; parent decides the next work rather than
automatically widening the experiment until a favorable timing appears.
