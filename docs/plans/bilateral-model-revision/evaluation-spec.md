# Frozen evaluation specification — revision 1

Frozen on 2026-09-22 before inspecting any revision-2 output. The executable contract is [gates.json](../../../backend/tests/fixtures/model-evaluation/revision/gates.json); its content hash is included in every run. The incumbent is version `owner-v2-bilateral-1` at immutable `ecda17d1`, not owner-v1 and not the edited candidate. Candidate and incumbent run in separate fresh processes against identical captured inputs and configuration with socket connections blocked and scratch databases. Source hashes bind uncommitted candidate changes; baseline file hashes must match the named commit. [Constructed archetypes](../../../backend/tests/fixtures/model-evaluation/revision/archetypes.json) freeze all 16 outlook pairs across full/sparse/no personal boards plus nine target/sell preference, explicit-selection, format and invalid-pin counterfactuals before candidate output inspection.

The existing independently defined six-dimension evaluator supplies A/B give, receive and package cells, raw features, Unknown/N/A and bilateral verdicts. Constructor utilities are never labels. Construction, common significance projection, survivor ordering and observed exposure/outcomes are separate layers. The last layer is unavailable unless independently captured and validated; no cards are fabricated as viewed. For each available stage retain full inventory and first 1/5/10/30. Those cuts are reporting lenses, never search caps. Errors and true zero-card requests remain denominators.

Critical properties and expected constructed cases are frozen in the machine-readable contract. Tests include incomplete/sparse boards, all outlook pairs, real legal assignment/cut arithmetic, own-next-draft picks, explicit pins, significance, shape, independently annotated stud bands and preference perturbations. Increasing only personal enthusiasm may improve targeting, but must not raise the cheapest retained price for the same exact target. Focal and full-package diagnostics remain separate. Synthetic reference bands are fixture facts, not universal empirical thresholds. Adverse/deliberately corrupted cases must cause the relevant assertions to fail.

Primary development comparison reports per-request differences in raw reference terms, focal personal coverage/direction, RB/pick portfolio, package size, repetition, surfaced opportunities, native latency and search budget. Independent grades do not become automatic release gates without human calibration. Positive-only accepted trades and KTC/DP reference corpora may explain price/shape disagreement, but cannot label these generated offers as accepted/rejected. No outcome confidence interval is invented from generated cards.

Each original request is one captured input context; repeated hashes, repeated owner/league clusters and counterfactual copies are counted separately. Preserve the bounded production-export selection frame and excluded/invalid counts. Private rosters/boards/identities remain only in mode-0600 local run files; committed results contain aggregates. Missing boards, market publication timestamps, pick ledger, projection/rule/cut support and fixed eligible outcome cohorts are explicit limitations.

Before any candidate output, the fresh export's managers and leagues are connected into components whenever an initiating or counterparty manager occurs across leagues. Component hashes define a deterministic order; every third component is held out. A single component cannot create an independent holdout. The captured 76 requests produce only two connected components, assigning 74 requests to development and two to holdout. This small holdout is reported honestly rather than relabeling the 19 initiating manager/league combinations as independent clusters. The private split binds the exact capture hash. Both constructors receive the same common captured configuration; original per-request configuration remains part of raw provenance. Separate `--variant incumbent` and `--reuse-incumbent` phases permit completing model code before inspecting candidate results; reused results must match the exact normalized input and common configuration hashes.

G1/G2 require zero critical correctness or membership regressions. G3 requires inspection of independent diagnostic changes and no newly known harm across four asset directions; counts alone are not improvement. Failed code properties trigger a second round. G4/G5 remain evidence-limited while observed coverage, threshold ratification, CI/device/load or other parent release requirements are absent. A second code round cannot manufacture these observations. No empirical margin is added after seeing results.
