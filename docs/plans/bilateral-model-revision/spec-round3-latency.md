# Round 3 specification — remove measured revision overhead without changing offers

The independent full-worker smoke failed the no-material-first-action-regression
goal: 8.69→18.66s first durable30 and11.13→22.90s completion, with2354→3306
served/impression occurrences. These localPython3.14/SQLite measurements are not
product p95, but are sufficient to withhold release and investigate.

Agent A owns a **private experimental harness only at first**, plus
latency-evidence.md. Do not edit shared frozen constructor/policy/server sources
or commit HEAD while C's locked quality comparison runs. Profile the new stages
and identify repeated serialization/parsing, repeated exact-proof work, redundant
same-focal computations and persistence costs. Prototype a narrow equivalent
implementation inside an isolated process/private copied module using apply_patch.
The existing helper/harness or a temporary subclass/monkeypatch may be used only
for this offline experiment; do not install global monkeypatching in production.

Preserve ALL exact candidate membership, terms, prices, order, both-manager
private evidence, occurrence multiplicity, selection rules, budgets and final
eligibility/publication checks. No cap, narrower pool, weaker proof, omitted
diagnostics, moved durability boundary or partial false-first-action shortcut.
Only generated IDs, observation timestamps and elapsed measurements may be
normalized for parity, with an explicit exclusion list. Cache only within the
same immutable dependency/context; never reuse changed offers or stale inputs.

Measure at least one same-input current-candidate versus optimized-candidate
pair and exact semantic digests. If it cannot preserve behavior or materially
reduce overhead, retain the negative finding. Send parent the smallest tested
patch recommendation, risks and timing; do not apply runtime edits yourself until
parent authorizes after C's source freeze ends. Parent will then integrate,
rerun affected tests and bind exact parity against retained locked inventories
before relying on the earlier independent quality comparison.

This is a performance repair, not permission to tune model weights/rules using
held-out quality results. Remaining independent grade/coverage/rollout gates stay
unchanged even if latency is fixed.
