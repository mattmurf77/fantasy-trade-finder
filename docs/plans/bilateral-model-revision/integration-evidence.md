# Integration and parent verification

Branch `codex/bilateral-revision-20260922`, based on independent evaluator commit
`ecda17d1` on fresh upstream `73bfa41e`. Isolated worktree; preserved canonical
and legacy dirty checkouts. Parent wrote specs before dispatching three Astra
Ultra agents, authored integration, reviewed their work, and requested mutual
adversarial reviews. No production mutations or release actions have occurred.

## Versioned integration

Exactly-1, default-0 numeric selector registered in both runtime and DB seeds;
unchanged arm and unrelated settings. Dark selector removed from canonical input
hash to preserve incumbent assignment parity. Incumbent helper refactor has a
frozen digest regression. Version fences cover queued jobs, safety signatures,
final exact-proof reuse, selected requests, pending inventory, cached/polled jobs,
overhaul generation identity and durable initial/continuation batch publication.
Old undecided inventory is withheld after a flip; existing likes, original
valuation evidence and historical rows remain. A revision does not relabel old
proofs as new.

Pure survivor ordering runs once after final dispositions/significance/validity
and before owner publication. Owner-exclusive mode already disables ghosting;
the legacy presentation policy is excluded on that path. Private occurrence
indices therefore correspond to the same final list and durable card ordinal.
No public board/policy diagnostics or new mobile fields are introduced. Polling
does not rerun ordering. Batch size 30 then100 remains delivery mechanics, not
an output/search cap.

## Executed checks

- Final frozen policy3/server/benchmark source collection: **6,690 passed,
  1 skipped in372.75s**, Python3.14.4, isolated in-memory SQLite. Skip is the
  opt-in captured2025 outlook backtest. No runtime/test edits occurred during
  this run. Subsequent private performance prototypes are not shared runtime.
- Initial integrated full backend: **6,582 passed, 1 skipped, 381.25s**, local
  Python3.14.4, isolated SQLite. This run began before the last evaluator/capture
  review repairs; it is broad regression evidence, not a claim every subsequent
  test was collected. Later exact-source validation must be recorded below.
- Selector/publication/admission/legacy-profile group: **88 passed**.
- Revised-route plus incumbent-route group: **29 passed**.
- Same-arm overhaul upgrade and rollback added after independent integration
  review: **9 overhaul tests passed**, including preservation of liked evidence.
- Latest combined candidate/presentment/revised-route/overhaul/capture/independent
  benchmark checks after evaluator repairs: **166 passed, 4.72s**.
- Native TypeScript `tsc --noEmit`: passed. Dependencies installed from the lock
  with offline `npm ci --ignore-scripts`; no manifest/lock changes.
- All **99 native structural suites** passed; native test-ID lint passed.
- Web structural **195/195** checks passed; `git diff --check` clean.

Agent A independently reviewed parent dispatch, final/pending/batch fences and
private order logging; no blocking defect found. Agent B's evaluator review did
find input-dedup, cached-baseline provenance and first-offer-price measurement
gaps; these were repaired rather than treating prior results as release evidence.
Generator/presentation round2 and negative controls are detailed in their own
evidence files. No simulator/Maestro was run.

A later adversarial integration pass found two response-time races missed by
that earlier review. A revision flip during selected-offer persistence could
return revoked cards, and a flip during pending-card disposition reads or
serialization could escape the initial filter. Parent added four real-route
regressions, observed all four fail on the prior implementation, then added the
post-durability selected-response check and a final pending-version re-filter.
The filter preserves inbound/demo exemptions and original committed evidence;
it never deletes rows or reintroduces already excluded cards. The updated
route/incumbent/overhaul group passed **56 tests in3.12s**. These later checks
supersede the earlier 'no blocking defect found' review for these boundaries.

The supplemental actual-worker performance check did **not** pass the
nonregression objective: first durable30 rose8.69→18.66s in an isolated paired
smoke. Parent withheld release and issued a separately bounded private
performance-prototype spec while preserving the locked evaluation source.
See [timing/evidence](latency-evidence.md); green tests do not waive that result.

## Not yet established

Hosted exact-head CI on production Python3.12, physical-device tap-to-actionable
latency, production load, full live serving replay, independent blinded grading,
ratified empirical release margins and mature fixed-cohort outcomes are not
established by local unit tests. Backend-only implementation does not itself
require a TestFlight binary, but cannot claim physical-device qualification.
