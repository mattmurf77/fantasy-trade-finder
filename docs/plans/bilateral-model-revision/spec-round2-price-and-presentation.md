# Round 2 specification — preserve efficient terms through publication

Independent cross-review exposed two red controls before release: stronger liking
could still put a more expensive same-target package first; and a fresh
support-only presentation sort would undo a constructor-only fix. Round 1 is not
release eligible. Retain the incumbent support weights; do not fit coefficients
to held-out requests.

Agent A owns candidate/constructor tests: define deterministic, symmetric
same-focal/partner dominance using captured market cost plus both managers'
independent personal and portfolio benefit. Retain all eligible occurrences;
rank avoidable consideration later rather than hard deleting inventory. Do not
compare unrelated focal transactions as though their additional cost were
avoidable. Explicit anchors stay authoritative. Expose the minimal immutable
private evidence needed to preserve this ranking, never a claimed probability.

Agent B owns presentment/tests: coordinate with A's evidence contract. Preserve
the constructor's efficient-term precedence through final survivor ordering;
do not silently restore a support-only baseline that contradicts it. Novelty
must not promote a dominated same-focal package ahead of its surviving efficient
alternative. All existing exact-occurrence, protected-slot, no-cap, bounded
lookahead and BOTH-manager quality-tolerance guarantees remain. If preserving
the constructor baseline is necessary, document the changed baseline explicitly
and retain tests showing diversity cannot erode either manager's baseline score.

Both agents must add a composed real constructor → final survivors → presentment
regression, including the preference-only perturbation, not just isolated helper
tests. Mirror, permutation, missing-board, all four outlooks and explicit-pins
controls must remain green. Report any unavoidable trade-off; parent independently
evaluates and reviews, and only then considers release.
