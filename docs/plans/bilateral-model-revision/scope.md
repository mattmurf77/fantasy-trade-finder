# Scope — bilateral model revision

Date: 2026-09-22. Owner explicitly requests parent-authored agent specs, implementation, independent evaluation, then conditional live release or a second coding round. No express waiver. Fresh origin/main remains 73bfa41e; branch starts at ecda17d1, the reviewed offline evaluator commit built on that main. Preserve canonical/legacy dirty worktrees.

## Product contract

Both managers should willingly execute the exact offer. Personal tiers/order plus outlook/needs identify focal targets and sells; reasonable market consideration prices the package. Higher conviction alone does not authorize extracting extra assets. Tankers prioritize picks and favored long-horizon WR/TE/QB, sell RBs, protect favored youth/own-next-draft picks except explicit intent. Rare personally elite/discounted-prospect RB exceptions stay. No artificially capped deck, no forced padding or quotas. Preserve small organic shapes and deliberate larger selections. Unknown counterparty preferences are not consensus conviction; dynasty proxies are not projected starts.

## Scope and boundaries

Round 1: separately versioned bilateral candidate for price-efficient support/intent, pure post-significance survivor ordering, independent expanded offline benchmark, parent-only selector/capture/integration. Retain incumbent callable/version/config rollback; no changes to owner-v1 or fit. Add actual starter evidence only where existing fresh compatible sources suffice; label missing sources and leave broader ingestion unresolved rather than invent data. No global pick-price overhaul without separate empirical evidence.

Analytics: existing bakeoff config/request snapshots, immutable owner proof and final impression features carry constructor/version, raw support/market/intent and private presentation provenance. No new client events in this round; actual viewed/eligible cohort gaps remain explicit release gates. Schema: no new tables/columns. Planned numeric knob `owner_bilateral_revision_enabled`, default 0, existing config registry/admin workflow; exact 1 selects candidate, 0 retains incumbent. Not a new public/client flag. Parent owns config docs and rollback binding. No production settings changed before reviewed evaluation and release gates.

Round8 follows independently reproduced prepared-job freshness failures. Extend
the existing job input epochs to consumed counterparty board/outlook identities,
retaining original viewer epochs and all historical offer evidence. This shared
job-boundary repair protects all models on that path without changing their
algorithms, weights, eligible package inventory or configuration. No new reads,
tables, flags, client/API fields, scheduler or prepared cache; existing
`inputs_changed` response semantics cover these newly tracked dependencies.
Full provider/market/ownership authority remains explicitly out of this narrow
repair. Parent spec, actual-route RED/GREEN regressions, independent review,
fresh full tests and worker parity precede acceptance. Update architecture and
the existing API lifecycle documentation, plus initiative evidence and ledger.

## Evidence and canonical docs

Each owned subsystem: hermetic pytest, named red control followed by restored green, independent cross-review. Parent: flag-off parity, final revalidation/version fences, no cap/permutation invariants, publication identity tests, frozen baseline/candidate framework comparison, full backend/web/mobile CI checks, hosted exact-head CI before release. Manual physical TestFlight checklist required to claim device latency/flow; no simulator. No client changes expected; if needed, scope/review before TestFlight build. Update architecture/config-reference/runbook and initiative evidence/ledger. API/schema/client constants/glossary/HLD/LLD N/A (no route/schema/client vocabulary change; compatibility stubs not update targets).

## Decision gates fixed before implementation

G1: zero critical identity/legality/expiry/pin/mirror/known-preference safety regressions; default-off incumbent behavior and version unchanged. G2: no output cap or hidden candidate-budget reduction; presentation preserves exact eligible inventory and fixed inbound/explicit positions. G3: candidate changes improve the intended raw diagnostics without a new known four-side contradiction or quality regression in reviewed synthetic/archetype and held-out contexts; no acceptance probability claim. G4: six-dimensional independent evidence/coverage reported for both managers; missing evidence cannot pass. G5: full tests/review/CI, rollback/readback and load/device evidence required by the reviewed plan before broad promotion. Unknown outcomes or unratified empirical thresholds are not waived by this implementation request.

If G1–G3 fail, run a second bounded coding round from independent findings. If G4–G5 remain genuinely unavailable, do not fabricate them or deploy broadly: finish safe fixes/measurement, report remaining evidence/approval needs. User authorized conditional deployment, not lower standards. If all required gates pass, push reviewed commit/PR, attach it, merge and deploy exact green commit using established Render workflow, change only the candidate knob and read back; preserve unrelated arms/settings and old interests.
