# Bilateral revision model and presentation-policy card

## Identity and intent

Candidate constructor: `owner-v2-bilateral-2`; arm: `owner_v2_bilateral`.
Incumbent comparator: `owner-v2-bilateral-1` at `ecda17d1`. Deployment selector:
`owner_bilateral_revision_enabled=1`, default 0, conditional on existing bilateral
selection and owner serving controls. Implementation branch:
`codex/bilateral-revision-20260922`. Ongoing human modeling/measurement/release
owners remain to be named; agents' reviews are implementation evidence, not a
signed operational ratification record.

Hypothesis: same-focal efficient consideration and coherent future-portfolio
selection reduce needless cost while preserving both managers' reasons to trade.
Personal rankings, outlook and roster context jointly select assets; market
references price the package. This is an interpretable heuristic, not a trained
or calibrated bilateral acceptance predictor.

## Changed components and controls

- Constructor support weights remain .45 preference, .35 adjusted market, .20
  team plan. Market-source blending, stud convention, computational search budgets,
  explicit-selection authority and other arms are unchanged.
- Tank/rebuild portfolio features distinguish favored horizon-appropriate youth
  from mere youth, emphasize pick receipt and RB sale, retain personally elite or
  discounted-prospect RB exceptions, and preserve incumbent pick/youth guards.
- Local same-focal comparisons use both managers' captured personal cost/benefit,
  portfolio direction, positional/availability roles and audited companion
  benefit. Valid but inefficient alternatives remain in inventory. A bounded
  family audit is not an exhaustive Pareto-optimality claim.
- Actual projected-starter effects are **Unknown** when fresh compatible
  projections/horizon/availability/cuts are unavailable. Existing dynasty-value
  lineup fit is labeled a proxy rather than an in-season improvement.
- Separate policy `bilateral-survivors-3` permutes post-filter organic survivors,
  preserving efficient native precedence, both-manager quality bounds, protected
  slots, fixed-position known focal directions and exact occurrence inventory.
  The locked first comparison evaluated policy2; policy3 is its separately recorded
  regression repair, not silently substituted into that result. Explicit searches are unchanged. It has
  no cap, quota, padding or eligibility veto.

## Evaluation and development history

Parent wrote scoped specs before dispatching three Astra Ultra workstreams.
The independent evaluation contract/archetypes were frozen before candidate
outputs. Cross-review failed the first round: increased liking could raise
first asking price, and a display support-sort could undo a constructor fix.
A separately written second-round spec required composed constructor→survivor
regressions and local—not global—price precedence. Both were repaired without
retuning weights to a held-out cohort. Named destructive controls confirm that
tests detect missing price precedence, fabricated starter evidence, removed
valid inventory and loss of bilateral presentation bounds.

See [generator evidence](generator-evidence.md), [presentation evidence](presentment-evidence.md),
[independent evaluation contract](evaluation-spec.md) and
[evaluation results](evaluation-evidence.md). No synthetic or generated card is
counted as an observed acceptance. All six independent dimensions retain A/B
give, receive and package cells; absent independent contextual annotations or
compensation bands remain Unknown, not a favorable grade.

## Limitations and deferred work

Counterparty personal boards are sparse. Missing ages can already have been
imputed to 25 upstream; a captured numeric age alone cannot establish authentic
youth provenance. Historical market publication timestamps and actual league
projection/cut context are incomplete. The bounded price audit may not discover
every surviving dominance relation after filtering. Proxy thresholds are not
calibrated willingness probabilities. Global pick-year pricing was deliberately
not changed without separate owner-aligned validation.

Future candidate requests now preserve validated source-age/imputation evidence
and existing fallback-outlook fields in a detached private capture. That envelope
is explicitly collection-only: the current model still consumes the same Player
ages, including legacy defaults. This closes a future auditing gap, not the
age-model limitation or missing historical provenance. See [capture evidence](age-evidence.md).

The offline benchmark is a common-input constructor/significance/presentation
comparison, not a full production serving or first-action measurement. Actual
provider completions, mutual-interest outcomes, fixed eligible windows, blinded
four-side adjudication, numeric promotion margins and sufficient independent
cohorts are separate evidence obligations. A new score or larger output count
cannot satisfy them.

## Release and rollback

Consult [status](status.md) for the actual decision; this card itself authorizes
nothing. The owner has now authorized a [production learning release](production-learning-release.md)
despite unresolved latency and quality evidence; this is not a calibrated or
validated acceptance claim. One audited knob restores incumbent -1, with version-fenced new-search
inventory and unchanged historical interest evidence. No mobile code changes are
part of this revision, so a new TestFlight binary is not inherently needed to
serve it. Physical-device/load/three-second validation is still required before
claiming that performance target.
