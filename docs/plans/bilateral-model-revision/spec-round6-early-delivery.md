# Round 6 — early complete simple-package phase

Parent specification, September 22, 2026. The owner renewed conditional release
authorization; the three-second first actionable tile remains the target. Round5
equivalent serialization changes did not close the regression. This round tests
a delivery architecture, not another global cache or a smaller search.

## Agent A: private feasibility prototype

Use the existing frozen input and actual worker harness. First measure the time
and inventory at the completion of organic one-for-one evaluation across all
partners. Existing `candidates()` yields simple terms before companion expansion
within each partner, but the round-robin loop does not expose a global phase.
Do not assume the first 30 constructed cards are fully ranked or safe to publish.

Prototype one explicit completed-simple-phase checkpoint. Rank its complete
eligible subset with the candidate's native ranker, then apply the SAME complete
owner, roster, significance, disposition, presentation, serialization, durable
impression and live-policy checks used for normal publication. Measure first
durable30 (or the honest smaller eligible count), not a raw callback. Continue
the unchanged full search in the prototype where feasible; compare all final
exact terms, candidate counts, per-partner budgets and proofs to the frozen full
run. No candidate/output cap and no new eligibility rule. Explicit searches,
inbound cards and mixed-arm paths are excluded from this first prototype.

It is acceptable to measure a phase-only worker lower bound first, but label it
as such: that is not a production streaming implementation or proof of full
inventory/stable-prefix safety. Do not claim completion until those are tested.

Early publication necessarily changes final presentation order. Record a named
delivery version and compare the first30 against full candidate and incumbent
using the independent diagnostics on development contexts only. Never claim old
prefix parity, global optimum, or actual acceptance. Evaluate repetition, both
manager focal direction, package pricing/support, and meaningful coverage. Keep
the held-out panel locked; no tuning to its outcomes. Previously exposed panel
is only a regression set, not a new blind holdout.

Propose the smallest implementation with append-only published prefix, exact
impression IDs, immutable metadata, full continuation, same budgets, invalidation
and cancellation. An early like/pass must not return in the suffix. Ranking the
completed search may not mutate an already published object/proof. A fresh policy
flip, expired evidence, changed roster or changed source input must revoke stale
publication at every existing boundary. Never skip a durability or safety gate.

Own private scripts under the existing latency directory and
`early-delivery-evidence.md` only. No shared runtime edit before parent review.
Coordinate CPU windows and source/HEAD freezes. Stop after this one architectural
prototype and concrete recommendation; no unbounded optimization loop.
