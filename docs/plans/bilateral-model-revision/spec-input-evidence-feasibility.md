# Existing-input feasibility — actual roster benefit and age provenance

Agent B performs a bounded READ-ONLY source/data-contract investigation while
the performance lane runs. Do not edit runtime/tests, call external providers,
read credentials, write production, or manufacture projections. Record findings
in input-evidence-feasibility.md; parent writes any implementation spec later.

The ambiguous development offer cannot establish in-season benefit from a
dynasty-value lineup proxy. The original accepted plan includes actual marginal
starter evidence. Determine whether existing app modules already supply fresh,
format-compatible projections, legal league slots, availability and roster/cut
facts that can be captured cheaply in `_owner_generation_context` for BOTH teams.

Trace current Win Now/projection/outlook caches or persistent records, normalized
Sleeper/ESPN/MFL league lineup rules, and exact timestamp/dependency-version
contracts. Distinguish persisted available inputs, provider calls triggered on
demand, estimated defaults and historical-only artifacts. No new network fetch
belongs on Find a Trade's critical path. If the data do not exist, state the
smallest asynchronous/preparation capture and evidence contract needed, with a
specific code location and clear Unknown fallback; do not design new infrastructure.

Also locate upstream missing-age→25 imputation and whether provenance remains
recoverable from existing in-memory or captured inputs. A true young player and
an imputed age must not silently mean the same thing for tanking logic. Suggest
a narrow provenance addition rather than a global player-schema/value rewrite.

Stop at a concrete implementation-feasible recommendation or demonstrated data
gap. Do not broaden into all analytics collection or propose fitting model weights
to the three audited cards. User-facing tiers/pick equivalents and other managers'
private rankings remain unchanged/private.
