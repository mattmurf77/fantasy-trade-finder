# Round 4 — novelty must not erode known personal focal direction

## Why this bounded repair

The locked comparison's DEVELOPMENT-only stage attribution shows the candidate
survivor policy reducing known focal sell alignment82.56→80.68% and buy
alignment78.25→76.65% while improving repetition. The existing bilateral support
tolerance is therefore insufficient to guarantee that novelty stays secondary
to personal intent. This does not establish contextual harm or justify forbidding
all trades involving a less-preferred player. Constructor eligibility and explicit
selections remain unchanged.

## Agent B ownership

Own only `backend/trade_bilateral_presentment.py`, its existing test file and
presentment evidence. First finish the parent integration review. Do not change
the constructor, independent grader, captured fixture, coefficients, eligibility,
offer counts, pricing precedence, search budgets or selected-search behavior.
Version the revised policy separately from `bilateral-survivors-2` so previous
locked results cannot silently be attributed to it.

Read the existing exact proof's per-asset personal/market/source evidence. Add a
bounded presentation comparison protecting each manager's known focal BUY and
SELL direction independently. A novelty swap may not turn a favorable known
focal direction into neutral/adverse direction or conceal it by moving to unknown
evidence. Do not invent preferences from consensus-filled values. Protect both
destinations against their fixed baseline and prevent chained swaps from eroding
the guarantee, just as the support bound does. Unknown is neither favorable nor
adverse; choose and document a conservative comparable-evidence rule.

Keep the guarantee categorical/directional, using existing evidence conventions,
not fitted thresholds or independent grader scores. No new benefit score or
probability. Same-focal efficient-term precedence, protected positions, explicit
intent, exact occurrence multiplicity, fixed first representative, support bounds
and bounded32 lookahead stay authoritative. All original offers remain available.

### Tied-headliner clarification before final implementation

Independent review reproduced a1x2 tie: keeping the constructor's first named
buy target favorable can conceal a newly adverse equally valuable second focal
asset for both managers. Protect **all equally highest-market-value assets on
each side**, not just the tie-broken intent ID. A conservative implementation may
require equal co-headliner counts and equal known/unknown counts, then compare
sorted known directional categories componentwise at each fixed destination.
This prevents losing a favorable co-headliner when another adverse one already
exists. Do not replace the categories with their minimum alone, or invent a
preference for an unknown tied piece. Different tie shapes can stay in native
order; this is not an eligibility rule. Add permutation/tie/partial-evidence
regressions and keep the runtime comparator independent of the evaluator.

## Tests / review

Write synthetic mechanism regressions BEFORE changing policy: equally supported
novel cards with worse known buy direction, worse sell direction, swapped manager
roles, missing source evidence, unchanged direction with beneficial novelty, and
multi-swap fixed-baseline erosion. Demonstrate a failing current-policy case and
green repaired behavior. Use real constructor proof in a composed test where
possible. Do not copy development trade identities into committed tests.

Agent C independently checks the repaired policy over retained exact constructor
inventories; no constructor re-generation is required if all constructor bytes
are unchanged and policy provenance is explicit. Report all stages and evidence
coverage, not only favorable metrics. The same locked panel is now a regression
panel, NOT a fresh blind release test. Do not inspect/tune to the holdout; a new
independent benchmark and performance repair remain required before promotion.

Parent reviews and reruns integration/full tests. This round does not waive the
measured latency failure or missing bilateral outcome evidence. Stop after this
bounded mechanism repair and report remaining constructor-level disagreements;
do not broaden the search/model or chase a selected aggregate score.
