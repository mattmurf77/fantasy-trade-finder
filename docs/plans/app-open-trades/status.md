# App-open trade preparation — implementation evidence

2026-09-20: implemented in isolated `codex/app-open-trades`, starting from production commit 57c37c26. Session initialization already started a worker but hard-coded fairness 0.75 while native searches defaulted to 0.5. A completed warm-up therefore missed the normal cache. Error jobs also prevented subsequent startup retries.

The server now defaults startup preparation to 0.5 and accepts the native persisted threshold. Matching completed/running work is reused, failed jobs retry, and ordinary warm-up preserves an active user search. Explicit generate requests supersede incompatible running fairness/shape jobs instead of serving the wrong search. Removed redundant onboarding-gated client generate calls: every successful session-init path already starts server preparation.

Evidence: 116 focused backend tests passed (startup defaults/explicit values, cache adoption, mismatched running settings, force supersession, presentation boundaries). Native executable lifecycle check: 39 passed, including real startup transport for unset/off/on preferences. Shared-fairness and session-ownership guards passed; TypeScript passed. Broad final mobile and backend checks tracked in release updates below. Web: 195/195 passed. No simulator used; device checklist in scope.md remains unrun.

Code trace: `mobile/App.tsx` restores local state then calls `revalidateSession` on launch and foreground; `state/useSession.ts` uses the existing guarded initialization lifecycle; `api/auth.ts:sessionInit` sends the saved fairness threshold. `backend/server.py:session_init` starts background preparation after authoritative league assembly. `generate_trades` reuses compatible cached jobs; existing owner publication gates still require final checks and durable evidence. No offer cap, candidate budget, safety rule or persistence guarantee changed.

Limits: initialization/provider fetches still precede generation; custom searches may require fresh work. This changes when work starts and removes avoidable regeneration, not the measured 37–42-second fresh-search duration. Server cache remains in process and expires after the existing TTL. Native preference transport/removal of duplicate kickoff requires a new TestFlight build. Backend default alone helps installed native clients using their default fairness setting.
