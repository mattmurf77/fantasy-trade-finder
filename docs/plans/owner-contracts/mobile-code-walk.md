# Mobile search continuity — code-walk evidence

2026-09-05. Base: `5cf34182374a80159e13d22ba7989eba1697b6ff` (fresh main). Scope: [mobile-scope.md](mobile-scope.md). Authority: the shared-root owner decisions engineering handoff through R09. This is source-derived evidence, not observed native navigation. No simulator, Maestro, production mutation or build was run.

## Implemented contract

| Scenario | Source trace and limit |
|---|---|
| GET-only canvas search | `mobile/src/screens/TradeCalculatorScreen.tsx:864` carries both explicit sides in the store handoff; `mobile/src/state/useFinderTargets.ts:66` types that optional client-only field. `mobile/src/screens/TradesScreen.tsx:3034` forwards it, `:3191` consumes a pushed request, and `:3252` supplies the direct inline request. `mobile/src/utils/tradeSearchRequest.ts:34` gives an explicit canvas priority over legacy pin state, including an explicitly empty canvas. `TradesScreen.tsx:1892` sends its receive IDs through the existing generator API. This fixes transport loss; it does not define new backend multi-selection coverage or substitute rules. |
| Find More / Retry / preferences refresh | `TradesScreen.tsx:1120` routes both layouts through the same handler. A saved fair request reuses its anchor and partner; a model repeat uses the saved request at `:1892`, with the current explicitly selected fairness/shape. Explicit target changes clear the saved requests at `:2912`; unpin and Search all clear them at `:3541` and `:3735`. No temporary canvas selection is persisted into saved player rules. |
| Fairness and shape cross the pushed boundary | The existing local fairness preference hydrates at `TradesScreen.tsx:1097`; the initial dispatch choke waits at `:3083`. Pushed requests include hydrated fairness and trade intent at `:3035` and `:3253`; arrival adopts them at `:3192`. A push before hydration omits the default rather than misrepresenting it as the user's value. The receiving deck waits for its own hydration. `:3612` passes the existing `fairness_threshold` argument to fair-packages; model requests already use that argument. No account persistence schema or price policy is introduced. |
| Back retains the builder | `TradesScreen.tsx:3227` prevents a duplicate results push. Its push arm does not write the landing's sheet opponent; only the results instance adopts search scope. `mobile/src/components/TradeBuildCanvas.tsx:218` saves the exact searched sides/opponent before any scope-driven remount. Its `:182` reconciliation distinguishes a matching search-scope synchronization from a later explicit partner change: a different partner/Any supersedes the old partner, keeps the latest SEND and clears incompatible GET. The pure rule is `mobile/src/utils/tradeSearchRequest.ts:19`. `TradesScreen.tsx:3663` pops a pushed deck to the still-mounted builder; legacy Back restores saved request sides rather than assuming canvas requests wrote pins. An explicitly empty model request does not revive stale pins. |
| League switch clears only temporary context | `TradeBuildCanvas.tsx:157` clears local prefill on league change, and `:212` includes league identity in the calculator key. `TradesScreen.tsx:2205` invalidates old responses and clears request snapshots, draft prefill and temporary partner. Existing lane/shape/pin resets remain. No ranking, account-fairness or durable Untouchable/Target writes were added. |
| Anchored pending, failure and zero results | `TradesScreen.tsx:3585` marks a fair request pending before awaiting it, clears old cards, snapshots its request, and checks the generation epoch on success/error/finally. Existing failure recovery now repeats that request. `:7303` and `:7505` disable both manual search CTAs during hydration/fair generation. Classic results use the existing skeleton while pending; a completed fair zero reaches `:8763`, not the first-run skeleton or never-searched copy. Its Back action uses the same calculator recovery handler. The dormant in-canvas browse branch checks `fairDeck && !fairPending` at `:7939`, retaining its existing looking state until completion. |
| Model regeneration after rankings/Undo | Quick Set/rank-return dispatch at `TradesScreen.tsx:4671` now requests `force:true, repeat:true`, preserving a consumed GET-only request while refreshing board-derived results. Suppression Undo at `:3749` retains the request across the shared reset; an epoch check prevents an older Undo response from regenerating into a new context. This is not an implementation of TradeBrowseAll pass Undo. |

The only added visible treatment is the existing Card/Text/Button empty-state vocabulary for an anchored zero. No new trade-card panel, receipt, score, tag taxonomy, tier display or action semantics were introduced. Win Now, telemetry outcomes, provider transmission, queue/like and private ranking displays were not edited.

## Mechanical evidence

- `npm ci --ignore-scripts --no-audit --no-fund --prefix mobile` installed dependencies only in this isolated worktree. Shared dependencies were not modified. The native patch-package postinstall was not run; no native runtime claim follows from this install.
- All 93 `mobile/tests/check-*.js` scripts passed, including Win Now, analytics, results push, canvas-results, inline-home, calculator behavior, handoff and owner-search-continuity.
- The new guard executes the actual transpiled pure selection/scope helpers and AST-extracted screen mutation function. Cases cover GET-only, explicit empty canvas versus absent canvas, legacy targeting flag/package mode, multi-GIVE inclusion mode, copied arrays, new partner/Any versus matching scope synchronization, and rank-return/Find More after the one-shot canvas is consumed. Its source assertions separately pin the actual semantic-regeneration callers and React/navigation wiring.
- An independent reviewer executed the actual transpiled TradeBuildCanvas with a minimal hooks/effects harness: searched B, matching parent B, edited SEND, explicit parent C, explicit clear and league change all passed. This is component logic execution with mocked native dependencies, not TestFlight or visual proof.
- Existing guards were updated to pin the actual new dependency/reset/dispatch census, shared event source and request-based Back path; no events were removed from user actions and no catch-all test waiver was added.
- `mobile/node_modules/.bin/tsc --noEmit`, `bash mobile/scripts/testid-lint.sh` and `git diff --check` passed.
- Native navigation, asynchronous timing and visual states still require [mobile-testflight.md](mobile-testflight.md). These checks are not claimed as runtime proof.

## Deliberately not implemented

- Stud-tax Off removal/legacy normalization: held for the owner decision; no hidden Off effect was introduced by hiding its control.
- Exact “both options” partial-selection coverage, initial More Offers partner/package keep-release defaults, new fallback groups or target substitution.
- Account-wide fairness synchronization/schema, saved-tag lifetime changes, ranking/tier learning or backend package policy.
- The existing model-regeneration family used by rank-return is retained; no new fair-package rank-return workflow or regeneration comparison UI is introduced.
- Parent-identified P1: TradeBrowseAll Undo currently restores local cards after an already-dispatched pass. It remains deferred and is not fixed by this mobile continuity work or a within-tier learning clamp. The existing held-pass deck Undo was not changed.

Parent must review and integrate this lane, update the central evidence ledger/documentation and run integrated CI before push. No push or release is authorized by this artifact alone.
