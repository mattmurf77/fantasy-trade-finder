# Private research captures

Competitor captures are external reference evidence. Analysis lives in this directory and relevant business/initiative documents. Fleeced's own frozen capture library remains in `screens/` at the repository root.

The logged-in Dynasty Nerds capture set and its original source index are retained in `_local/research/competitors/dynasty-nerds-app/` at the repository root. `_local/` is ignored by Git and excluded from EAS archives. Do not publish logged-in or paywalled captures without an explicit operator decision. Keep collection dates and source URLs with the local set.

The previously unnamed RTF package is preserved as `_local/research/competitors/unclassified-notes.rtfd/`; classify it before using it as evidence. No data was discarded to rename the package.
