# docs/business/ — Notes for Claude

Not for code work. Company-ops deliverables from the role skills (`/mkt-*`, `/pm-*`, `/an-*`, `/fin-*`, etc. — see `README.md`) by department: `analytics/`, `finance/`, `marketing/`, `product/`. Engineering sessions can skip this tree entirely.

Buildable specs live elsewhere: `docs/plans/` (in-flight builds) + `docs/feedback/items/` (per-feedback-item fixes).

Overlap warning: analytics/monetization content here is STRATEGY (market sizing, pricing options, positioning). The build-side counterpart is `docs/plans/analytics-platform/` + `docs/plans/monetization/` — don't confuse a strategy memo here with an implemented spec there.
