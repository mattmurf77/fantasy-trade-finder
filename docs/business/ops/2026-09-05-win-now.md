# Win Now experimental beta release — 2026-09-05

**Status: backend/web live; iOS 1.17.0 (147) uploaded, Apple processing.** The operator authorized season/Win Now/championship as an experimental, uncalibrated beta. Backend/web is live at `c28ec6d8` after PR #280 merged on 2026-09-05 at 05:06:23 UTC. All four final CI checks passed for `abb1af118d3fe39f32292e99cecc64cebff1d2f3` (run `33945722395`). Render deployment `dep-dadq6k97lnhs73e8o970` is LIVE since 05:07:28.48285 UTC with all three beta flags verified true. iOS **1.17.0 (147)** finished building and uploaded successfully; Apple processing and tester availability are separate, with tester availability still unverified. Forecast/title calibration and physical-device QA remain unproven.

## Integration update

The release includes security main `a927e3a7`; superseded preliminary CI and build results are not the final release evidence. Final CI and parent review cover the integrated ownership and data-lifecycle changes described below.

## Release scope

Backend/web is serving `outlook.season_projections=true`, `trades.win_now=true` and `outlook.championship_probabilities=true`. Mobile **1.17.0 (147)** built and uploaded successfully; Apple processing, tester availability and physical installation remain distinct stages. The championship flag and snapshot capability must both permit output. Legacy outlook `odds.title_pct` remains prohibited.

Win Now uses external weekly projections, supported league scoring and legal lineups, constrained season search, edited evaluation and isolated scenario decisions. Likes/passes do not train dynasty Elo or send real league proposals. League/rule/forecast/availability/freshness failures remain explicit unavailable responses. Source accessibility does not imply a commercial license or calibrated predictions.

[Build contract](../../plans/win-now/BUILD.md) · [Scope](../../plans/win-now/scope.md) · [Configuration](../../config-reference.md#win-now-beta-controls)

## Evidence and limits

Final Python 3.12 backend suite: **5,353 passed, 1 skipped in 732.08 seconds**. Parent merged focused run: **220 passed in 15.25 seconds**. Client verification: all **92** structural guards, TypeScript and testID lint passed; **190** web structure checks, **23** auth checks and actual isolated Chromium/MV3 runtime passed. All four hosted CI checks succeeded on tested head `abb1af118d3fe39f32292e99cecc64cebff1d2f3`, run `33945722395`; PR #280 squash-merged as `c28ec6d802463e048d59a97967e9bb5bb9fdc6f9`. See [final and historical evidence](../../plans/win-now/EVIDENCE.md) and [production smoke artifact](../../plans/win-now/production-smoke.json).

The [exploratory result](../../plans/win-now/EXPLORATORY-RESULTS.md) uses revised historical source inputs for Lakeview 2024 at four checkpoints, with one league-season and one champion; these are not four independent championship events. Source revision timing, omitted cohorts and scoring approximations remain in the raw artifacts. Strict archived-input evaluation still rejects the revised-input cohorts. No held-out calibration, prospective validation, native physical QA, acceptance uplift or production latency claim follows from this evidence.

Normal residual assumptions omit shared NFL game/team effects and multiweek injury correlation. Current availability is not a future injury forecast. Sampling ranges do not cover model/projection uncertainty. UI receipts must retain the uncalibrated-beta disclosure. The operator accepted these limitations for release; they remain follow-up validation work.

## Scoring normalization and explicit approximation

Sleeper can retain kicker/team-defense coefficients in offense-only leagues. Finite known K or team-defense coefficients are removed only when the corresponding active K or DEF/DST/D/ST slot is absent. This does not enable active K/DEF/IDP lineups. Unknown scoring rules are not discarded by prefix matching. Original `source_scoring_settings` remain in the immutable league/snapshot identity alongside effective rules.

A separate, explicit beta approximation omits nonzero finite coefficients for exactly `st_ff`, `st_fum_rec`, `st_td` and `fum_rec_td` when the forecast provider does not declare support. These offensive-player special-teams/fumble events are not unused K/DEF rules and are not forecast as zero events. The exact excluded coefficients are retained in metadata `scoring_exclusions`; the warning is **“Rare special-teams/fumble bonuses are not projected.”** Provider-supported keys remain included. Other unsupported rules and invalid numeric values still fail validation. [Implementation detail](../../plans/win-now/BUILD.md#release-scoring-boundaries--2026-09-05).

## Execution record

| Gate or action | Verified outcome |
|---|---|
| Merge | PR #280, 05:06:23 UTC; squash `c28ec6d802463e048d59a97967e9bb5bb9fdc6f9` |
| Final CI | All four checks SUCCESS; run `33945722395`, head `abb1af118d3fe39f32292e99cecc64cebff1d2f3` |
| Render | `dep-dadq6k97lnhs73e8o970` LIVE at `c28ec6d8` since 05:07:28.48285 UTC |
| Effective flags | Season projections, Win Now and championship all true |
| Public production smoke | Root/flags 200, exact deployed JS/CSS, four unauthenticated routes 401; landing rendered without error/warning logs |
| Production configuration | CRON_SECRET and DATABASE_URL verified set; secret values never printed |
| Real source smoke | Lakeview refused `unknown_starter_availability:1:1`; authenticated production forecast unverified |
| Mobile | iOS 1.17.0 (147), build FINISHED and submission uploaded; Apple processing, tester availability unverified |
| Physical QA and calibration | Not performed/proven; remain follow-ups |

Production smoke verified HTTP 200 for root and flags, all three serving flags true, exact deployed bytes for `app.js`, `win-now.js` and Win Now CSS, and HTTP 401 from four new unauthenticated endpoints. The live in-app-browser landing rendered with no error/warning logs. This does not establish an authenticated production forecast or a physical TestFlight pass. The real-source Lakeview check still refused `unknown_starter_availability:1:1`; no forecast was fabricated.

EAS build `69b53400-1d8d-42ec-ad7a-5d0e3c756059` for iOS **1.17.0 (147)** finished. It compiled `77824c02`; runtime code was byte-identical to tested head `abb1af11` and released main. Submission `6184a831-a685-497b-8e97-734ed30fa4b9` uploaded successfully and entered Apple processing; tester availability and physical installation remain unverified. The first optional `--what-to-test` attempt was rejected as Enterprise-only before submission; retrying without that field uploaded the same binary successfully. Superseded build 146 was canceled and was not submitted.

Run the [manual TestFlight and browser checklist](../../plans/win-now/EVIDENCE.md#manual-testflight-and-browser-checklist). Preserve explicit unavailable reasons and the beta/scoring warnings; no source refusal should be replaced by invented projections.

## Concurrent security integration checks

The release preserves security #279 verified ownership and account-data lifecycle admissions. Win Now jobs participate in lifecycle admission; private tables export/delete every resolved account alias. DB row fences remain for overlapping containers. The final CI and regression results above cover the integrated release; physical runtime proof remains outstanding.

## Worker behavior during deployment

The build-time player-cache import sets `FTF_BUILD_MODE=1`, suppressing Win Now startup. Rolling containers never requeue another container's unexpired running job. Queued jobs resume; expired work fails, and late completion cannot revive terminal/deleted/expired work. After a worker crash, its running search remains unavailable until its original 15-minute expiry, then the user can retry. Automatic ownership/heartbeat-based retries are not implemented.

## Rollback

Set **all three** serving flags false: `outlook.season_projections`, `trades.win_now`, `outlook.championship_probabilities`. Deploy the configuration or apply the supported process reload, then verify effective server flags and unavailable/hidden client entry behavior. Changing Render environment settings requires deployment; the flag reload endpoint cannot fetch newly changed Render environment values.

If code rollback is needed, restore the prior main commit **`a927e3a7`**, with the three flags false, then verify health and legacy dynasty behavior. Preserve the additive evidence tables, raw scoring and historical evaluation artifacts; serving rollback does not require deleting them. Server flags provide the immediate control for a mobile binary already delivered to testers. Record the failed revision, reason, action and verification here.

## Decisions needed

No further permission is required for the already authorized experimental release. Calibration remains future work; do not turn the release authorization into a statement that it passed. A materially different scope or paid provider purchase would require a separate operator decision.

## Handoffs

Confirm Apple processing completion and tester availability, then run the physical TestFlight checklist. Continue prospective forecast evaluation with timestamp provenance and independent league/season evidence. Parent owns living-memory and release cleanup; raw exploratory artifacts remain unchanged.
