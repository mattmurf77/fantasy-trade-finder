# docs/reviews/ — Notes for Claude

Point-in-time audit snapshots — not current truth. Check the date before trusting a claim; re-verify against the live code/docs.

- `2026-05-22-*` (SUMMARY, api-layer, backend, mobile-render, silent-bugs) + `REVIEW_BRIEF.md` — historical deep code review, largely acted on.
- `2026-08-08-branch-triage.md` + `2026-08-08-context-overload-audit.md` — current ops references (branch/worktree backlog, context-load remediation this file is part of). Branch/worktree *deletions* since then are ledgered in `docs/recovery/`, not here.
- `2026-08-16-p0-remediation-status.md` — seven independent verifiers checking the `docs/plans/audit-p0-remediation/` specs against the **`origin/main` tree**, not a working checkout. The closest thing to ground truth on what the P0 batch actually landed.
- `2026-08-18-valuation-age-audit.md` — read-only investigation into age penalties in the value ladder (triggered by the Davante Adams reports). No engine code changed.
- `2026-08-19-pick-year-valuation.md` (D-079, the *year* axis) + `2026-08-19-ktc-pick-value-comparison.md` (D-084, the *round* axis — round 2 repriced to market rank) — the pick-pricing pair.
- `2026-08-21-market-curve-comparison.md` — the *player-curve* axis: FTF's value-at-rank shape vs KTC/FantasyCalc (D-084 method). Refutes the "our curve is too flat" thesis (we sit inside the market band, steep end; the KTC blend imports ordering, never shape) and locates the mid-package-buys-stud symptom in `_package_value_market`'s own-best-asset depth benchmark instead. Also documents the 2026-08-21 QB-knob market-rank consequence (Allen → #71 overall). Both carry measured, sourced market comparisons; read the KTC one before quoting any competitor's raw pick ratio, because raw ratios across value scales are not comparable.
- `2026-08-19-pick-badge-scale.md` (D-088) — the third of the pick trio, and it **corrects a diagnosis D-084 drew from the KTC memo** (not a claim the memo itself makes: the memo predates the symptom and never mentions badges). A current-year 3rd badging `second` was read as the seed map's floor compression becoming visible; it was actually a wrong value→Elo inverse in `GET /api/league/picks`. **The compression measurement stands and is re-derived here** — 100 ranks inside 54.9 Elo points at ranks 200–300 — it simply was not the cause. Read this one before acting on Q-020.
- `2026-08-19-web-parity-audit.md` — four-auditor snapshot of the live site vs the mobile app: P0 breakage (self-trapping demo path, prod mockup, `[STATE]` in Terms, zero analytics events), the one-stylesheet-thirteen-copies root cause, and a backend-free parity backlog. Live browse against prod; source claims verified against `origin/main`. Phase 0 shipped on `fix/web-phase0` — see `docs/plans/web-parity/`.
- `2026-08-19-knockout-waterfall.md` + `2026-08-19-knockout-waterfall/` — the **knockout report**: all 3,073,019 candidate trades the engine enumerates for league `1312140920132497408` (the only league with >2 boarded members), and which rule killed each, for arm B (both its divergence and consensus paths) and arm C. Carries *first-kill* (real execution order) **and** co-kill data, so it can answer the question a plain waterfall cannot: which rules exclude trades **nothing else would**. Headline: `#141` junk-filler alone accounts for more unique exclusions than every other rule combined, in all three pipelines; the Elo-gap guard first-kills half the divergence universe and uniquely kills zero. Read this before tuning any gate knob — several rules that look expensive are wholly redundant, and one that looks cheap (the 60-point dual-surplus floor) is 97% unique. Data files include the complete per-trade sheet.
- `2026-08-22-trade-model-restrictiveness.html` — operator-facing (HTML, plain language) review of **why the deck feels thin**, joining three parallel code audits (acceptance gates / search space / mutual-gain math) to read-only prod telemetry over all 10,560 served cards. Three findings worth carrying: (1) generation breaks at `global_target`, so a 12-team deck reaches a **median 6 of 11 partners** — predicted from `trade_service.py` and then confirmed in the data as a sharp mode at exactly 6 with a cliff at 5 cards/partner; (2) the **bake-off is serving in prod** (`bakeoff_serve_interleaved = 1`), so arms have real user decisions — `gen_v2` likes at 57% vs `current` 18% (n=30, p≈9e-5, one user dominant: a strong lead, not a result), correcting the several audit memos that call the arms dark; (3) the `consensus_both_ways` "doubles the deck" figure is **not what the knob delivers** — it governs only the package test, not `user_gain_ok_1for1`, so as built it is ≈+18%. Knob values were read from the prod `model_config` table, not code defaults.
- `2026-08-22-trade-model-second-read.html` — a **second-model re-review** of the restrictiveness report above (HTML, operator-facing). Re-verified seven code claims at their cited lines and four measurements; all held. Two framings corrected: the 80%-repeat/novelty figures are mostly an unswiped-refresh artefact (372 of 424 decks had zero decisions), and the #370 re-serve "leak" is 151-of-177 the operator's own 2026-08-17 pass amnesty (`pass_cooldown_start_epoch`), residual = 2 liked trades. Three new findings: (1) the six-partner ceiling is a **fixed set** in single-board leagues — partners sort ranked-first then roster order (`trade_service.py:5093`), so 5 of 13 and 7 of 13 leaguemates never appeared across 6–11 decks; (2) like rate is a viewer-appeal meter — consensus cards (viewer wins by construction) 30% vs divergence 19% — so D-099's guardrail-not-verdict rule applies to every knob experiment; (3) the two-sided scoreboard: 465 likes → 15 matches → 3 accepted ever, and 1 partial / 0 exact of 127 linked real Sleeper trades. gen_v2 survives rank-, deck- and basis-matching (57% vs current's **6%** on divergence). Adds R0 (rotate the unranked sweep order — zero cost, fixes the blind spot) ahead of R1, and **retracts its own first-draft claim** that `calc_opened` is dead — it is set on 3 of 525 outcome rows against 7 `trade_edit_in_calculator_tapped` events ever; the 0.00 was rounding.
- `2026-08-22-knockout-rules-judged.html` — the **knockout-rule verdict** (HTML, operator-facing): three Opus agents worked the 8/19 waterfall sheet in parallel with opposed briefs (convict / rewrite / defend), reconciled by the lead. Rulings: **nothing removed; five refined** — `#141`'s *relative* bar (`filler_min_frac` 0.25 → 0.15, sweep first), R5 (dual-need rescue, standalone, do first), R1 (reprice in `package_value_v2`, same 0.25, still two-sided), R2 (starter-depth predicate), the 3-for-1 shape rule (knob at 2). **Keep the 450 floor** — 91% of #141's unique kills carry a sub-450 body and deleting the rule floats padded cards to the top of the deck (100% of the top 200), which OVERTURNS the restrictiveness report's R4. The diversity pass is a selector into 5–8 slots, not a 91% deleter — OVERTURNS its R3; supply is ~5.4 distinct ideas per pair. `trade_elo_gap_max` → 0 only in the same change as #141 (0 unique kills; nests). Viewer-must-win gate: never alone; operator picks conditional-slack (fairness ≥ 0.85) or both-ways + 0.75 (Q-028a). Unanimous keep on the dual-surplus floor (96% of its kills have a side losing) — audit `waiver_slot_cost` = 425 instead.
- `2026-08-27-calc-vs-guided-finder-audit.md` — **feature-parity audit** of the manual in-league calculator (`InLeagueCalculator`, `calc.merged_layout`) against the guided trade finder (`TradesScreen`, `trades.finder_hub`), run ahead of a possible deprecation of the guided page. Audit only; no code or flags changed. Headline: the operator's premise ("the manual calc has most, if not all features of the guided") **does not hold** — 24 guided-only capability clusters plus 5 partials, and the calculator's own *Find a Trade* button renders nothing without the guided deck (`TradeCalculatorScreen.tsx:868` `popTo('TradesHome')`). Second headline: the merge is **already built, in the opposite direction** — `calc.inline_home` (D-158, Wave B0, ships dark) makes the guided landing host the calculator inline and preserves every guided capability; it is blocked on Wave B, the tour rebuild, which has **not** been built. Carries the row-by-row parity table with file:line evidence, the deprecation-risk list with preservation costs, 7 operator decisions, 6 open questions, 10 design-alignment items, and the analytics/flag impact — including that removing the deck kills `trade_card_viewed`, an INTENT event the session definition is built on.
- `trade-engine-deep-dive.md` + `trade-engine-external-research.md` — trade-engine proposal history; see `docs/adr/adr-002-trade-engine-v2-v3-rebuild.md` for what actually shipped.

Add a row here when you add a review. An unlisted audit in this folder is indistinguishable from a stale one.
