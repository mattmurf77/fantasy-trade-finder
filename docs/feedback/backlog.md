# Feedback Backlog — deferred, not declined on the merits

> Items the operator explicitly deprioritized ("not a priority — backlog it") rather
> than rejected. Each entry keeps enough context to pick the work up cold. When one
> is picked up, move it through the normal feedback pipeline and delete the row.

| ID | Date filed | Deferred | Screen | Item |
|---|---|---|---|---|
| #160 | 2026-07-20 | 2026-08-08 | QuickSetTiers | **Tweener spots between tiers** — "Tweener spots would be nice." Allow a half-step placement when a player doesn't cleanly belong to either adjacent tier band. Needs a design decision on how a tweener maps to tier values (tier bands are absolute Elo anchored to pick values — see docs/cross-client-invariants.md), and how it renders in the tier board without breaking the color encoding. Operator: "Not a priority." Feedback item set to `declined` to clear the open queue; this row is the durable record. |
