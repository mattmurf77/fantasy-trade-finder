# docs/feedback/

Per-item work starts at `items/INDEX.md` (index of all feedback items). `items/README.md` holds the folder-naming convention (`<id>-<slug>/`).

The dated files at this level (`2026-04-29-web-experience.md`, `inbox.md`, `perf-audit-2026-05-21.md`) are historical intake/audit snapshots, not the current queue.
