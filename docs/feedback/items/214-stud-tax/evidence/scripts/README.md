# Preserved evaluation scripts

These scripts were promoted from ignored `feedback-workspace/214/` because this item's durable reports reference them. They are historical reproduction aids, not scheduled or production commands. Their original relative paths and data assumptions may refer to archived local output. Inspect arguments and preserve the original baseline before attempting a rerun; do not run against live accounts by default.

Exact originals are also retained in `_local/recovery-2026-09-06/local-artifacts.tar.gz` at the repository root. Promotion did not execute these scripts or assert that historical tuning remains current.
