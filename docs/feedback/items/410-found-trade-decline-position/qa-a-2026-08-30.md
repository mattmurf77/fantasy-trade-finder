# QA-A findings — FB-410/411/412 (2026-08-30)

Independent mechanical verification of build commit `03a90652` in a fresh worktree
(`qa-410`, base chain confirmed: `03a90652` → `966df00a` → `14d06ba2` → `11c8903c` → `bd83fe94`).
Nothing here is taken from the build report; every command was re-run.

**Overall: 17/17 requirements PASS. No defect found.**

*(Saved by the orchestrator from QA-A's final report.)*

## 1. Floors

| Command | Result |
|---|---|
| `npm ci` (fresh, no symlink) | 801 packages; draggable-flatlist patch applied ✔ |
| `npx tsc --noEmit` | **clean** (re-run at session end: still clean) |
| 87 `npm run` check-suites | **87 PASS / 0 FAIL** (re-run after all sabotages: 87/87 again) |
| `check-mascot-ram.js` (no npm script — orphan) | ran directly: 35 assertions, all passed |
| `testid-lint.sh` | `testid-lint OK` |
| `pytest backend/tests/test_calc_trade_queue.py` | **33 passed** — the FB-409 backend fix is undisturbed (and `backend/` is absent from the build's diff) |

## 2. Sabotage cycles — 27 run (21 mandated + 8 own + 5 coverage probes), **27/27 red, 27/27 restored green**

Headline: **S-2 "helpful cleanup"** — decline branch presses the host prop *and then* calls
`clear()`, with every id, glyph, flex and label left correct → RED on
`4m1` + **`4m2. the decline branch never calls clear() — the browse-session data-loss defect
stays closed`**. This is the batch's most important direction and it is proven in isolation.

Other mandated cycles, each red on its named assertion and restored: S-1b "disabled instead of
replaced" (14 red), S-3 "one cell, one Pressable" (9 red), S-5 kill-switch blindness (`4b2`),
S-6 "belt and braces" pager ✕ re-added (`3-bis` + `4b3`), S-7/S-7b required-prop (`4l2`, both
files), S-9 double chip (`17c/17d/17e`), S-10 `fontSize: 10` (`17f` + `17g`), S-11 global `sm`
badge (`17i`), S-12 slot above Add (`17j`), S-14 hedged blame copy (`18j`), S-15 carve-out
deleted (`18k`).

QA-A's own additions: S-1 local pass path (`4m1b`), S-4 shared `disabled` (`4m4`), S-8 restored
double haptic (`4m2c` + `4m5`), S-13 symmetric slot on the receive column (`12i2c`), S-16
50/30/20 nudged to 25 (`4m7` — D-157 survives D-169), S-17 `minWidth: 0` dropped (`17`), S-18
ice chrome on the decline cell (`4m6c`), S-19 price slot allowed to shrink (`17b`). Plus five
coverage probes for assertions no sabotage individually hit.

## 3. Self-satisfaction audit

Every new/changed assertion was classified. Real red direction confirmed for the whole `4m`
family, `4b2`, `4b3`, `3-bis`, `4l2`, `12i*`, `17`–`17j`, `18j`.

**Weak-direction pins, stated honestly:**
- **`17g`** (no numeric fontSize < 11) is **vacuous while green** — `TradeSide.tsx` holds zero
  numeric `fontSize` literals, so it quantifies over an empty array. It has a real independent
  red direction (probe P-17g: a sub-floor literal on an unrelated style fired `17g` alone), and
  it pairs with `17f` (which catches a wrong size *on the name*). Complementary, not redundant.
- **`18k`** (header carve-out) carries **zero behavioral coverage** — `18j` carries 100% of the
  behavioral pin, with a proven near-miss sabotage (S-14 hedged wording that still names the
  partner). `18k` exists only to stop a future editor restoring the blame. Documentation-grade,
  admitted as such; probe P-18k showed it fires on more than its bare negation.
- **`4l3`** and **`17k`** are presence pins the build report did *not* flag; both are
  `!/token/.test(file)` shapes whose sabotage is near their own negation. Neither is
  load-bearing (`tsc` + `4l2` cover `4l3`'s intent).
- **`4m3c` did not fire on S-3**, the sabotage it was written for (S-3's collapsed cell puts the
  conditional in the testID position, so the AST fork resolves to two literals). S-3 was still
  caught by nine other rules.

## 4. Untouched-suite verification

`git diff --stat bd83fe94 03a90652 -- mobile/tests/` → **exactly three files** changed.

**Whole-file byte-identical** (verified by `git show` comparison, not eyeball) and green:
`check-any-partner.js` (FB-406's guard from hours earlier), `check-shop-deck.js`,
`check-picker-chip-alignment.js`, `check-inline-home.js`, `check-calc-tour.js`.
**Block-identical:** `check-calc-merged-layout` rules 16 / 16b, `check-canvas-results` 12i5.

**Six edits to pre-existing assertions — all six declared re-specs, no green assertion silently
altered or deleted:** §3 id-loop element removed (replaced in place by `3-bis` asserting
absence); `4b2` predicate re-specced to the new prop expression; `4l` **message only**
(predicate byte-identical); `12i` inverted to assert the pager id is gone (+ `12i-bis`); `12i2`
re-specced with `12i2b` carrying the gate forward; layout `17` now requires `flexShrink: 1`
**and** `minWidth: 0` (+ `17b`). Everything else is purely additive (+2 / +10 / +28).

## 5. Verdict per requirement

**R-1 … R-17 all PASS**, each with file:line evidence and a red sabotage. Highlights:
R-6 (data-loss defect) PASS via S-2 in isolation; R-7 kill-switch parity PASS
(`browseDecline={browseLive && sortedDeck.length > 0 && declineReasonProps ? {...} : null}`,
`TradesScreen.tsx:7411`); R-14 PASS with layout rules 16/16b **byte-identical** to the
pre-batch tree — the byte-identity charter was not weakened to accommodate the change;
R-9 PASS (D-169 entry + index row + the canvas-results-spec §4 amendment all in the code commit).

**Three PRD/build-narrative inaccuracies** (documentation, not code or evidence defects):
R-2's rationale claims retained `.maestro/` flows reference `calc.action.clear` — they do not;
R-16's "register the rename with testid-lint" clause is a no-op (the lint is flow→source
directional and no flow references these ids); R-5's `canvas-results` mentions in
`InLeagueCalculator.tsx` are comment-only (the assertion strips comments).

## 6. Final state

`git status` empty · `git diff HEAD` empty · `tsc` clean · 87/87 suites · HEAD `03a90652`.
Every sabotage restored. Left for sweep: branch `qa-410` + scratchpad worktree `wt-qa-410`,
zero commits of their own.
