# FB-418 — reconciliation log (Phase 1 critic pass)

> Critic review of [`prd.md`](prd.md) · [`scope.md`](scope.md) · [`plan.md`](plan.md)
> against `mobile/src/components/ShopOffersBody.tsx` (whole file),
> `mobile/src/utils/queueCalcTrade.ts`, `mobile/tests/check-shop-deck.js`,
> `mobile/src/screens/ShopAssetScreen.tsx`, `mobile/src/components/Toast.tsx`,
> `mobile/src/api/trades.ts`, `mobile/src/api/client.ts`, and the #402
> precedent folder (`rulings-2026-08-27.md`, `rulings-2026-08-28.md`,
> `prd.md`, `lld-delta.md` §6). Tree: `origin/main` @ `c7e75666`, 2026-09-03.
> Every `file:line` the Author cited was re-read; all hold.

**Verdict: READY FOR BUILD.** Zero BLOCKING objections. Eleven NON-BLOCKING
objections, all fixed in place; three claims rebutted with evidence; four
premises verified and recorded so the build cannot silently drift off them.

| # | Objection | Label | Resolution |
|---|---|---|---|
| O-1 | **D-2 mis-describes its own mechanism.** `handleLike` closes over `index` (`useState`, `:300`), a per-render *constant*; the value is identical before and after the `await`, so `const at = index` "before the await" cannot change anything. The consequence claim ("lands one tile back from where they swiped to") is also wrong for a backward swipe — the pager lands on the tile that took the liked tile's slot (`:735` clamp: former `k+1` at `k`), which is one tile *ahead* of a backward swipe and exactly where one forward swipe left them. | NON-BLOCKING (`ShopOffersBody.tsx:300`, `:735`, `:975-979`, `:1039`) | **Fixed.** D-2 rewritten: render-time index, the capture is a readability option not a correctness one, `k3` accepts either identifier, "no live ref" is the actual constraint, the swipe consequence stated per direction. Added the second residual: a refetch landing mid-round-trip rewinds to 0 (`:678`) and the like's resolution then jumps to `at` — accepted, sub-second, never a wrong tile removed. `plan.md` Risks row updated to match. |
| O-2 | **R-9 left the `suppressed` block lying in a second place.** Beyond "Keys enter this set only when a held dismiss COMMITS" (`:308`), the block says "Cross-session memory is the SERVER's … the next fetch simply doesn't offer a recently dismissed idea" and "This set only bridges the in-instance gap between a commit and that next fetch" (`:314-319`). For a queued like there is no server-side exclusion (prd §6 — asset-ideas does not consult the queue), so the next fetch *will* re-offer it and this set is the only filter for the session. One clause about the gate leaves the memory claim false. | NON-BLOCKING (`ShopOffersBody.tsx:314-319`) | **Fixed.** R-9 now requires the `suppressed` clause to state the like's memory posture in one sentence; `plan.md` approach paragraph says the same. |
| O-3 | **R-8 and R-9 had no mechanical pass criterion** ("code review of the diff" only), against the item's own §8 rule that every requirement maps to a named guard or step. Both are cheaply pinnable with the suite's existing helpers. | NON-BLOCKING (`check-shop-deck.js:141-161` helpers; no existing `busyKey`/`finally` assertion — `grep -n "busyKey\|finally"` over the suite is empty) | **Fixed.** Added **k7** (the `TryStatement`'s `finallyBlock` references `setBusyKey`; the `setSuppressed` call sits inside the `tryBlock` *after* the `AwaitExpression` — pins R-8 and the never-optimistic half of R-4) and **k8** (textual tripwire: `#418` at three sites, one inside `commitDismiss`'s range — pins R-9). Sabotages S-7a/b, S-8 named. §8.4 rows, §9 ownership, scope §3/§5, plan Files/Sequence updated to k1–k8. |
| O-4 | **k4 as written has an inverted-branch hole.** "Nearest ancestor `IfStatement` whose expression references `queued`" passes `if (res.queued) { onToast } else { setSuppressed }` and `if (!res.queued) … else { setSuppressed }`; the second is semantically fine, the first removes the tile on refusal. **k3 as written could pass vacuously** if either identifier is missing and the comparison is against `undefined`. | NON-BLOCKING (prd §8.1) | **Fixed.** k4 additionally requires the call inside the `thenStatement` range and no `!` in the condition text (sabotage S-4b added); k3's spec now says a missing identifier on either side is a fail, with the `findAll`-in-range shape to use. |
| O-5 | **Checklist step 7 could produce a false failure.** Reopening the window re-serves the liked idea only while the `['shop-ideas', league, asset, '']` row is warm (`staleTime: 60_000`, `:410`, key at `:388`). After that the sweep re-runs, and because the like moved the Elo board (ruling 2026-08-27 A, `trade_k_like`) the idea may legitimately be gone or re-binned — the operator would read that as "R-1 failed". | NON-BLOCKING (`ShopOffersBody.tsx:388`, `:410`; `rulings-2026-08-27.md` §A) | **Fixed.** Step 7 says "within a minute", explains why, and states that an absence outside the window is not a failure. |
| O-6 | **Checklist coverage gap: no middle-tile case.** Step 2 is `k = 0`, step 6 is `k = last` / `N = 1`; the general `0 < k < N-1` clamp case (`clamped = k`, pager holds position, does not rewind) had no runtime step. Step 8 also gave no expectation for the 15 s client deadline (`api/client.ts:230`) if iOS does not fail the request at once. | NON-BLOCKING (`ShopOffersBody.tsx:735`; `api/client.ts:230`) | **Fixed.** Added step **2b** (send on tile 2 of ≥ 3 → `2 / N-2`, position held) and promoted it to a must-run; step 8 notes the possible deadline wait and that the toast + re-enabled pair is the pass. Scope §3/§5 step counts updated (10 steps). |
| O-7 | **R-7 asserted the toast replacement without checking the mount, and missed a reachable ordering.** Verified: `Toast.tsx:76-92` re-arms the hold timer on `effectiveHoldMs` change (5000 → 1500), so "no second toast" in step 9 is true; `undoToastRef` stays set and a later early flush's retract is the `ShopAssetScreen.tsx:68` by-reference no-op. Missed: `busy` is per-key (`:982`), so while B's like is in flight the user can swipe and ✕ tile C — a pending dismiss armed *mid-round-trip*, whose consumed `requestPagerScroll` is then superseded by B's. Bounded by the clamp; C's timer/key/toast untouched. | NON-BLOCKING (`Toast.tsx:76-92`; `ShopOffersBody.tsx:982`; `ShopAssetScreen.tsx:68`) | **Fixed.** Both verified details and the reverse ordering added to R-7. |
| O-8 | **R-3 asserted the `N = 1` path without tracing it.** Traced: the effect fires on the new `visibleIdeas` reference with `clamped = 0`; the pager branch has unmounted (`:878`), React nulls `listRef.current` in the commit before effects run, so the `?.` at `:738` skips `scrollToOffset` — identical to today's last-tile dismiss. | NON-BLOCKING (`ShopOffersBody.tsx:735-742`, `:878`) | **Fixed.** Arithmetic for `k < N-1`, `k = N-1`, `N = 1` written into R-3 with the ref-nulling reason. |
| O-9 | **D-1's premise ("a like changes no idea reference, ticks nothing") was asserted, not verified.** If the queue call invalidated `['shop-ideas', …]`, a like *would* enter the `ideasUpdatedAt` flush-and-rewind effect (`:676-679`) and D-1 would be false by the back door. | NON-BLOCKING (verified: `git grep -n "invalidateQueries\|refetchQueries\|setQueryData" -- api/trades.ts utils/queueCalcTrade.ts components/ShopOffersBody.tsx` is empty; `queueTradeForOpponent` `api/trades.ts:601-620` is a bare `api.post`) | **Fixed.** Premise and the grep recorded in D-1 with the rule that a future invalidation breaks D-1 by construction; mirrored as a Risks row in `plan.md`. |
| O-10 | **The guard file's own header would contradict the fix.** `check-shop-deck.js:50-51` says "the suppression set is commit-only, never cleared by data" — the second half stays true, the first becomes false. R-9 covered only the body's comments. | NON-BLOCKING (`check-shop-deck.js:50-51`) | **Fixed.** R-9, §9 and `plan.md` Files now include one clause on that bullet. |
| O-11 | **Plan snippet omitted the `try`/`finally` frame**, leaving open where the write sits relative to `finally` (which also runs on refusal). | NON-BLOCKING (`plan.md` Approach) | **Fixed.** Snippet shows the full frame with the write inside the `try` after the `await`, `finally` unchanged, then-branch only. |
| R-a | *Should a like use `locallyRemoved` with an Undo, mirroring the dismiss?* | — | **Rebutted.** No un-queue route exists (`api/trades.ts` has no such call; prd §6 already lists "Undo for a sent offer" out of scope). A pending state with no cancel path would make "Undo" the exact dishonest copy R-9 (402) was written to prevent. `suppressed` stands; k2 pins it. |
| R-b | *Should `alreadyQueued: true` leave the tile in place, since nothing new happened?* | — | **Rebutted.** `queueCalcTrade.ts:87-101` returns `queued: true` for it — the server's own idempotence signal that the offer *is* in the queue. By prd §1's definition the tile is stale either way; leaving it would re-create the bug for the case the operator's report explicitly describes ("Already queued" as proof of staleness). k5 stands. |
| R-c | *Is Fix A violated by adding a second gate?* | — | **Rebutted.** R-A (`rulings-2026-08-28.md`) rules the set *client-authoritative and never cleared by data*; it is about subtraction, not addition. The like is a second **commit** gate with the same posture (session-scoped, no data tick clears it, `n2d` still green). `commitDismiss` stays a gate (`n2b`), `handleDismiss`/`undoDismiss` still never touch it (`n2c`). The one legal subtraction (`:553-560`) is unchanged and cannot collide with a liked key — no key can be both dismissed and liked in one session (each removes the tile from the pager). |
| V-1 | P-1 (`:718-726`): the fix adds only `requestPagerScroll`; `scrollToOffset` stays at one call site — `n3a` green. | verified | No change. |
| V-2 | R-5 counter honesty: no count is touched; `e`, `n2e`, `n2f` prove derivation from the filtered list. | verified | No change. |
| V-3 | B-4 (`:568-578`): the like path neither retracts nor re-issues the Undo toast; replacement is the shipped semantic. | verified | No change (details folded into R-7, O-7). |
| V-4 | Every cited line — `ShopOffersBody.tsx` (`:77-82`, `:300`, `:306-326`, `:534-561`, `:614-648`, `:676-679`, `:696-715`, `:728-747`, `:878-934`, `:955`, `:982`, `:1033-1039`), `queueCalcTrade.ts` (`:76-78`, `:82-86`, `:87-110`), `ShopAssetScreen.tsx` (`:43-54`, `:64-68`), `check-shop-deck.js` (`:141`, `:147`, `:154`, `:790-846`, `:1480-1595`), `TradesScreen.tsx:3223`, `ci.yml:47`, `package.json:85`, `INDEX.md` row 418 — re-read and holds. | verified | No change. |

## What remains for the orchestrator

Nothing blocking. Two notes for the build agent, already in the docs:

1. k1–k8 count only after the red/green sabotage pair is observed and logged
   (`scope.md` §5); k8 is textual by design and is a tripwire, not a proof.
2. The `suppressed` block's clause must correct the *memory* claim (O-2), not
   just name the gate — a one-clause edit there is the R-9 trap.
