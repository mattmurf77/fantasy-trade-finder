# QA round 2 — agent A — 2026-09-08

## Summary: PASS (0 defects; 1 minor doc-wording observation)

Full re-verification of G-428 at the resolved tip `646034ee` (Phase 4 commit `c4f8a6f7` applied). Every round-1 ruling is in the code and proven: `completed_draft_seasons` is shape-blind (a completed startup draft excludes its season in the sync **and** the send window starts at the next class — proven on the real sync, propose and validate paths, not only the predicate); guard check 9 walks the `Alert.alert` message-argument AST (RED with only the `detail ||` operand removed, comment kept); the dead `season_window: null` / `?–?` branches are gone and `season_window` is always a 2-list on a refusal; an unknown season still abstains and never refuses. All 18 defect tests are RED on the pre-fix sources and GREEN on the tip; the full suite is green; `tsc` and all 98 structural guards pass. Nothing was fixed or committed; the worktree is clean.

## Environment

- Worktree: detached at `646034ee` (`G-428: reconciliation log — round 1 resolution (c4f8a6f7)`), base `15dabe05`. Diff under test `git diff 15dabe05..646034ee` (21 files, +1,174/−71; code files: `backend/draft_status.py`, `backend/server.py`, `backend/sleeper_write.py`, `backend/analytics_taxonomy.py`, 4 test files, `mobile/src/api/sendInSleeper.ts`, `mobile/src/components/SendInSleeperButton.tsx`, `mobile/tests/check-send-button-platform.js`).
- Python 3.14.4 (`python3 -m pytest`, `-p no:cacheprovider`); Node v24.14.1; `mobile/node_modules` is the populated symlink.
- Scratch SQLite only: the suite used the worktree's own `data/trade_finder.db` (absent at start, created by the run); the two ad-hoc probes ran on isolated `DATABASE_URL=sqlite:///…/scratchpad/probe_g428_startup/probe*.db`. No `DATABASE_URL` in the ambient env; production never touched.
- Flags: none changed by the diff (`config/features.json` not in the diff; `picks.owned_sync`, `picks.league_horizon`, `trade.send_in_sleeper` as on `main`).
- Sleeper public API read with `curl` (no auth) for fixture realism only. No simulator, no Maestro (D-056). Every sabotage reverted with `git checkout 646034ee -- …`; `git status --short` empty at the end.

## Results

| Test | Verdict | Evidence |
|---|---|---|
| Targeted set: `test_owned_picks.py test_sleeper_write_route.py test_sleeper_write.py test_sleeper_pick_tradability.py test_pick_assignment.py test_trade_send_validate.py test_pick_horizon.py -q` | PASS | `173 passed in 30.00s` (matches the Phase 4 count) |
| Full `python3 -m pytest backend/tests -q -p no:cacheprovider` (single uninterrupted process; stdout → `scratchpad/full-suite-A-round2.log` because the run outlives the 600 s tool ceiling and was moved to the background by the harness) | PASS | `5892 passed, 1 skipped in 829.20s (0:13:49)` — exit 0, zero `FAILED`/`ERROR` lines (5,893 collected: the 5,891 of round 1 + the two Phase 4 unit tests). Stderr carries the pre-existing `[receipts_*] failed: (sqlite3.OperationalError) no such table …` log lines from the receipts tests' stubbed engine — logged best-effort warnings, not failures, unrelated to this diff |
| RED-first — `git checkout 15dabe05 -- backend/draft_status.py backend/server.py backend/sleeper_write.py`, then `test_sleeper_pick_tradability.py test_owned_picks.py test_sleeper_write_route.py test_sleeper_write.py -q -rf` | PASS (RED) | `18 failed, 87 passed in 12.42s` — the 16 defect tests the build report names **plus** the two Phase 4 additions (`test_completed_draft_seasons_is_shape_blind`, `test_sleeper_pick_window_post_startup_starts_next_class`). Full list + headline lines below. No #428 defect test stayed green pre-fix |
| RED-first — restore `646034ee` files, rerun the same 4 files | PASS (GREEN) | `105 passed in 12.05s`; `git status --short` empty after restore |
| Guard check 9 RED proof — remove ONLY the `detail` / `\|\|` operand from the `sleeper_pick_untradable` `Alert.alert` (branch comment mentioning `detail` kept), run `node tests/check-send-button-platform.js` | PASS (guard RED) | `FAIL  sleeper button: sleeper_pick_untradable renders the server detail` · `1 check(s) failed.` · **exit 1** (the sibling `has its own Alert branch, no goConnect` check still PASSes, so the failure is specifically the argument walk) |
| Guard check 9 — restore, rerun | PASS | `PASS  sleeper button: sleeper_pick_untradable renders the server detail` · `All send-button platform-routing checks passed.` · exit 0 |
| `cd mobile && npx tsc --noEmit` | PASS | exit 0 |
| Every `mobile/tests/check-*.js` (98 tracked files at this tip — round 1 reported 100; `git ls-files` at `26ab3147` also gives 98 and the diff touches only `check-send-button-platform.js`, so the round-1 count was a counting difference, not a removed guard) | PASS | `98 passed, 0 failed of 98` |
| Fixture realism vs live FFV3 (`1312140920132497408`), re-checked 2026-09-08 | PASS | `/drafts` → 1 entry `{draft_id: 1312140920136699904, season: "2026" (str), status: "complete", type: "linear", last_picked: 1787707582674, settings.rounds: 4}`; `/traded_picks` → 57 rows, keys exactly `owner_id, previous_owner_id, roster_id, round, season`, Counter 2026: 34 / 2027: 17 / 2028: 6; roster 11's 2026 R4 held by roster 1 (`{round:4, season:"2026", roster_id:11, owner_id:1, previous_owner_id:11}`) — byte-identical to `TRADED_2026`; roster 1's 2027 R1 held by roster 5 (checklist step 8 still valid); `/league` → `season:"2026"`, `status:"in_season"`, `total_rosters:12`, `draft_rounds:4`, `trade_deadline:12`, `disable_trades:0`. `DRAFTS_DONE` / `META` / grid-row shapes in `test_sleeper_write_route.py:314-325` and `test_sleeper_pick_tradability.py:34-42` still reproduce them |
| Real payload through the predicate (`646034ee`) | PASS | `completed_draft_seasons(real, "2026") = {2026}`; `sleeper_pick_window("2026", real drafts, real traded) = (2027, 2029)`; flake + no verdict → `(2026, 2028)`; flake + cached `drafted/medium` → `(2027, 2029)`; flake + cached `not_drafted` → `(2026, 2028)`; live `pre_draft` + cached `drafted` → `(2026, 2028)` (live wins); season `None` → `None`; `sleeper_pick_tradable` over `(2027, 2029)`: 2025 F / 2026 F / 2027 T / 2028 T / 2029 T / 2030 F; `(2026, None)` → True; `("x", window)` → True; malformed entries (`None`, `"x"`, non-numeric season, missing season) → `set()` |
| **Resolution (1) — shape-blind exclusion, predicate level** | PASS | `_is_rookie_shaped(startup) = False` yet `completed_draft_seasons(startup, 2026) = {2026}`; `sleeper_pick_window(2026, startup, real traded) = (2027, 2029)` and with no traded rows `(2027, 2029)`; startup `complete` + rookie `pre_draft` same season → `(2027, 2029)` (any complete draft consumes the class); 2025 startup complete + 2026 rookie `pre_draft` → `(2026, 2028)` (a prior season's startup does not exclude the current one). Unit tests `test_completed_draft_seasons_is_shape_blind`, `test_sleeper_pick_window_post_startup_starts_next_class` green and RED pre-fix |
| **Resolution (1) — shape-blind exclusion, real paths** (ad-hoc probes on isolated SQLite, `scratchpad/probe_g428_startup/`, not committed) | PASS | Sync: `_sync_sleeper_owned_picks` with `/drafts` = the 25-round `complete` 2026 startup → grid seasons `{2027, 2028, 2029}` (no 2026 rows). Propose: `SPENT_2026_4TH` under the same list → **422 `sleeper_pick_untradable`**, `picks == [id]`, `season_window == [2027, 2029]` (2-list), `detail == message`, message names `2027–2029`, `propose_trade` not called, no `sleeper_send_succeeded` row. Propose in-window: `MY_2027_2ND` under the startup list → 200, `draft_picks == ["1,2027,2,1,2"]`. Validate (#180 fixture): `SPENT_2026_4TH` → `checked: true`, `_codes == ["pick_untradable"]`, blocking, message names `2027–2029`; `MY_2027_2ND` → `warnings == []`. `4 passed` + `2 passed` |
| **Resolution (3) — `season_window` always a 2-list; abstain never refuses** | PASS | Code-walk (d) below: `"season_window": list(window)` (`server.py:18446`) is reached only through `if untradable:` (`:18443`), and `untradable` is non-empty only when `sleeper_pick_tradable` returned False (`:30369-30370`), which requires `window is not None` (`draft_status.py:226-227`). `_sleeper_untradable_copy` unpacks `first, last = window` (`:30420`) with no fallback. Probe: meta **without** `season` → `sleeper_pick_window` returns None (`draft_status.py:208-211`) → the spent pick **sends** (200, `["11,2026,4,1,2"]`), i.e. abstain never refuses. `test_sleeper_pick_window_unknown_season_is_none` / `test_sleeper_pick_tradable` green |
| **Resolution (2) — guard check 9 on the `Alert.alert` argument** | PASS | Diff read (`check-send-button-platform.js:485-513`): finds the `if` whose condition matches `UNTRADABLE`, walks its `thenStatement` for a `CallExpression` whose callee text is `Alert.alert`, then walks `arguments[1]` for an `Identifier` named `detail`; comments are not AST nodes. RED/GREEN evidence above |
| Code-walk (a) sync: live read wins; cached verdict only on `[]` | PASS | below |
| Code-walk (b) `sleeper_pick_window` semantics | PASS | below |
| Code-walk (c) encoder order grid → window → holder | PASS | below |
| Code-walk (d) propose 422 shape + nothing sent | PASS | below |
| Code-walk (e) validate `pick_untradable` blocking | PASS | below |
| Code-walk (f) `/drafts` + `/league` fetched only with a pick | PASS | below |
| Code-walk (g) 502 `detail` = first `errors[].message`, `message == detail` | PASS | below |
| Code-walk (h) in-window 2027 pick under a completed 2026 draft unchanged | PASS | below |
| Hunt: overhaul path inherits the clean grid and the guarded core | PASS | `backend/overhaul_api.py:130-134` `pick_rows` → `load_picks(league_id, source=pick_source_platform)`; sends select `sleeper_propose` (`:281`), wired to `_sleeper_propose_core` at `backend/server.py:31639` |
| Hunt: MFL / ESPN / schema / flags untouched | PASS | `git diff 15dabe05..646034ee --stat -- backend/mfl_write.py backend/espn_write.py backend/mfl_service.py backend/espn_service.py backend/overhaul_api.py backend/database.py config/features.json` is empty; `_sleeper_encode_ftf_picks` has exactly two callers (`server.py:18432`, `:30104`), both Sleeper-branch |
| Hunt: `_is_rookie_shaped` still has its #207 consumers (not dead after the Phase 4 removal) | PASS | `backend/draft_status.py:342` (verdict) and `backend/draft_board_service.py:598` still call it; `completed_draft_seasons` no longer does |
| Docs rows (api-reference propose/validate rows + 422 table, cross-client-invariants enum 18 + validate vocabulary, sleeper.md §1.1 rows 6–7 / §3.2 / §3.3 / §5) | PASS | Diff read; the api-reference 422 row now says `season_window` is "always present … an unknown season abstains and never produces this refusal", matching the code. One wording nit → F-1 (minor) |
| PRD §7 step 1 — prod `SELECT season, COUNT(*) FROM draft_picks WHERE league_id='1312140920132497408' GROUP BY season` | BLOCKED | no production access from QA (and none permitted); operator item |
| Runtime sends (PRD §7 steps 2–5) | BLOCKED | TestFlight-only evidence — checklist below |

### RED lines (pre-fix sources `15dabe05`, tests from `646034ee`)

`18 failed, 87 passed in 12.42s`. Failed:

```
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_completed_draft_seasons_reads_only_complete_current_or_later
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_completed_draft_seasons_is_shape_blind            (Phase 4)
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_post_startup_starts_next_class (Phase 4)
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_post_draft_is_next_three_classes
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_pre_draft_anchors_current
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_flake_with_cached_drafted_excludes_current
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_flake_without_verdict_abstains
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_cached_verdict_never_overrides_a_live_drafts_read
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_unknown_season_is_none
test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_tradable
test_sleeper_pick_tradability.py::test_spent_current_season_pick_flags_pick_untradable
test_owned_picks.py::test_daemon_step_flaked_drafts_uses_cached_drafted_verdict
test_sleeper_write_route.py::test_propose_refuses_spent_current_season_pick
test_sleeper_write_route.py::test_propose_refuses_untradable_on_receive_side
test_sleeper_write_route.py::test_propose_untradable_reported_after_unmapped_before_not_owned
test_sleeper_write_route.py::test_propose_502_detail_is_sleepers_sentence
test_sleeper_write.py::test_graphql_error_detail_is_first_message
test_sleeper_write.py::test_graphql_error_auth_words_still_classified_from_full_dump
```

Headline assertions, verbatim:

```
test_propose_refuses_spent_current_season_pick
E   AssertionError: {"status":"proposed","transaction_id":"TX9"}
E   assert 200 == 422
test_propose_untradable_reported_after_unmapped_before_not_owned
E   AssertionError: assert 'sleeper_pick_not_owned' == 'sleeper_pick_untradable'
test_daemon_step_flaked_drafts_uses_cached_drafted_verdict
E   assert False   (all(r["season"] != 2026 for r in rows))
test_spent_current_season_pick_flags_pick_untradable
E   AssertionError: assert [] == ['pick_untradable']
test_propose_502_detail_is_sleepers_sentence
E   KeyError: 'message'
test_graphql_error_detail_is_first_message
E   - These draft picks cannot be traded.
E   + [{"message": "These draft picks cannot be traded.", "path": ["propose_trade"], "locations": [{"line": 2, "column": 3}]}]
TestSleeperPickWindow::* (10)
E   AttributeError: module 'backend.draft_status' has no attribute 'completed_draft_seasons' / 'sleeper_pick_window' / 'sleeper_pick_tradable'
```

### Code-walk proofs (file:line against `646034ee`)

**(a) Sync — live `/drafts` wins; cached verdict only on `[]`.** `_sync_sleeper_owned_picks` (`backend/server.py:13287`): `_drafts = _fetch_sleeper_drafts(league_id)` (`:13352`; `:16005-16021` returns `[]` on a non-digit id, any exception, or a non-list body) → `_exclude = completed_draft_seasons(_drafts, _cur_season)` (`:13353` → `backend/draft_status.py:165-187`: `status == "complete"` `:179`, `int(season) >= current_season` `:185`, **no shape test** — the Phase 4 removal of the `_is_rookie_shaped(d) is False` skip is visible in `git diff c4f8a6f7~1..c4f8a6f7`). The cached verdict is reached only through `if not _drafts:` (`:13354`) → `_sleeper_cached_draft_verdict` (`:13271-13284`: one `get_league_draft_context` read, `database.py:10653`, → `DraftStatus(status, confidence or LOW)`; None on no row / no status / read error) → `_exclude.add(_cur_season)` iff `.drafted` (`:13356-13357`), logged at `:13358-13360`. A non-empty list takes `elif _exclude:` (`:13361`) and never consults the cache. `_exclude` then feeds `save_draft_slot_order` (`:13369-13373`, order is None when the current season is excluded) and `sync_draft_picks(exclude_seasons=_exclude)` (`:13378-13391`, unchanged). Daemon ordering: `_sync_sleeper_owned_picks` (`:21723`) runs before `_refresh_league_draft_status` (`:21739`) in the same pass, so a consulted verdict is always from an earlier pass. Does-not-fire: `_drafts` non-empty (any content — including a list with only `pre_draft` entries, `test_cached_verdict_does_not_leak_into_the_sleeper_sync`), or empty with a NULL / `unknown` / `not_drafted` row (`test_daemon_step_flaked_drafts_without_verdict_keeps_current_season`). Startup-shaped: the probe above shows a 25-round `complete` 2026 draft yields grid seasons `{2027, 2028, 2029}`.

**(b) `sleeper_pick_window` (`draft_status.py:190-219`).** Unparseable `current_season` → `return None` (`:208-211`); `exclude = completed_draft_seasons(...)` (`:212`); cached verdict adds the current season only under `if not drafts and cached_verdict is not None and cached_verdict.drafted` (`:213-214`); `observed` = every dict `traded_picks[].season` (`:215-218`) → `pick_horizon(current, exclude, observed)` (`:219` → `:92-139`): the anchor walks past excluded seasons (`:126-128`), and an observed season widens `last` only when `first <= season <= ceiling` (`:136-137`), so a spent season (< `first`) can never re-enter — `test_sleeper_pick_window_post_draft_is_next_three_classes` (2026 + 2028 rows → `(2027, 2029)`), and the real FFV3 list (34 season-2026 rows) → `(2027, 2029)`.

**(c) Encoder order (`server.py:30327-30387`).** Per pick (`:30355-30358`, give then receive): `row = grid.get(str(pid))` → `unmapped` (`:30359-30362`); unparseable `season`/`round`/`original_roster_id` → `unmapped` (`:30363-30368`); **`if not sleeper_pick_tradable(season, window): untradable`** (`:30369-30371`); holder lookup (`:30372-30378`); `if holder != from_rid: not_owned` (`:30379-30381`); `encode_draft_pick` (`:30383-30384`); 4-tuple returned (`:30387`). An in-window pick that changed hands still lands in `not_owned`; a spent pick lands in `untradable` before its holder is compared — `test_propose_untradable_reported_after_unmapped_before_not_owned` runs both orderings.

**(d) Propose 422 + nothing sent (`_sleeper_propose_core`, `server.py:18349`; reached from the route wrapper `propose_trade_to_sleeper` `:18282` and the overhaul send loop `:31639`).** Guard `if give_picks or recv_picks:` (`:18418`) → grid (`:18423`) → traded (`:18424`) → `window = _sleeper_pick_window_for_send(league_id, _fetch_sleeper_league_meta(league_id), traded)` (`:18430-18431` → `:30390-30400`: `season` from `meta` `:30396`, one `_fetch_sleeper_drafts` `:30397`, cached verdict only `if not drafts` `:30398`, → `sleeper_pick_window` `:30399`) → encoder (`:18432-18434`). Returns: `unmapped` 422 (`:18437-18442`), then **`{"error": "sleeper_pick_untradable", "picks": untradable, "season_window": list(window), "message": _msg, "detail": _msg}` 422 (`:18443-18447`)**, then `not_owned` 422 (`:18448-18452`) — all before `req = ProposeTradeRequest(...)` / `_sleeper_write.propose_trade(token, req)` (`:18461`). `season_window` is always a 2-list here: `untradable` is non-empty only when `sleeper_pick_tradable` returned False, which needs `window is not None` (`draft_status.py:226-227`); with `window is None` every pick abstains (`untradable == []`) and the route proceeds to Sleeper (probe: meta without `season` → 200). Tests: `test_propose_refuses_spent_current_season_pick` (`propose_trade` `assert_not_called`, `_send_succeeded_rows() == []`, deck-outcome mock not called, `season_window == [2027, 2029]`, `detail == message`), `…_on_receive_side`, `…_drafts_flake_without_verdict_does_not_block` (drafts `[]`, no leagues row → 200).

**(e) Validate (`trades_validate`, `server.py:30008`).** `meta = _fetch_sleeper_league_meta(league_id)` (`:30045`, the route's own read, required for `checked: true`) → `if give_picks or recv_picks:` (`:30097`) → grid (`:30099`) / traded (`:30100`) → `window = _sleeper_pick_window_for_send(league_id, meta, traded)` (`:30103`) → encoder (`:30104-30106`) → `asset_unmapped` (`:30109`), **`{"code": "pick_untradable", "severity": "blocking", "message": _sleeper_untradable_copy(...)}` (`:30114-30115`)**, `pick_moved` (`:30118`). `test_spent_current_season_pick_flags_pick_untradable` asserts `_codes == ["pick_untradable"]`, blocking, window text; probe repeats it under a startup draft.

**(f) Pick-only fetches.** The only `/drafts` read on the send paths is `:30397` inside `_sleeper_pick_window_for_send`, called only at `:18430` and `:30103`, both inside the `if give_picks or recv_picks:` blocks; the propose-side `/league/{id}` read is the inline `_fetch_sleeper_league_meta` at `:18431` in the same block (the only other upstream read in `_sleeper_propose_core` is `_fetch_league_rosters` at `:18391`). `test_propose_pick_free_send_makes_no_drafts_or_meta_fetch` asserts `drafts_mock`, `meta_mock`, `grid_mock`, `traded_mock` all not called on a players-only 200; `test_pick_free_validate_makes_no_drafts_fetch` mirrors it.

**(g) 502 body.** `sleeper_write._post_graphql` (`backend/sleeper_write.py:383-398`): `dump = json.dumps(errors)[:500]` (`:385`), `first = errors[0]` (`:392`), `msg = first.get("message")` (`:393`), `detail = str(msg).strip()[:500] if msg else dump` (`:394`), `low = dump.lower()` drives the auth-word scan (`:395-396`), `SleeperAuthError` / `SleeperWriteError(..., detail=detail)` (`:397-398`). Route except (`server.py:18475-18486`): `_detail = str(e.detail)[:200]` (`:18480`), body `{"error": "sleeper_write_failed", "kind", "detail": _detail, "message": _detail}` (`:18481-18486`). Tests `test_graphql_error_detail_is_first_message`, `…_auth_words_still_classified_from_full_dump`, `test_propose_502_detail_is_sleepers_sentence`. Fielded build 154 renders `detail` in the catch-all (`SendInSleeperButton.tsx:335-340`), so the user reads Sleeper's sentence with no EAS build.

**(h) #413 unchanged.** `git diff 15dabe05..646034ee -- backend/tests/test_sleeper_write_route.py` only adds fixtures and gives `_propose` `drafts=DRAFTS_PRE` / `meta=META` kwargs (`:314-345`); no pre-existing test body is touched, and T-5 (`THEIR_2026_1ST` under a pre-draft list) keeps its fixture. `test_propose_in_window_pick_still_sends`: `MY_2027_2ND` under `DRAFTS_DONE` → 200 with `draft_picks == ["1,2027,2,1,2"]`; the probe repeats it under `STARTUP_DONE` with the same string.

**Mobile branch.** `SendInSleeperButton.tsx:268` `const detail = body?.detail`; ladder root `:270` (`sleeper_not_linked || sleeper_expired`); `sleeper_pick_unmapped` `:306`; **`sleeper_pick_untradable` `:317-327`** (`Alert.alert('Couldn’t send', detail || <count-aware fallback>)`, no `goConnect`); `sleeper_pick_not_owned` `:328`; catch-all `:335-340` unchanged. Enum comment 17 → 18 at `:252-254`; `sendInSleeper.ts:5-7, 214`.

## Findings

### F-1: docs / comments still describe the refusal as "its rookie draft already ran" although the rule is now shape-blind
- Severity: minor (wording only; no behavior or contract impact)
- Where: `docs/api-reference.md` 422 `sleeper_pick_untradable` row and the validate row ("its rookie draft already ran"), `mobile/src/components/SendInSleeperButton.tsx:318-319` branch comment, `mobile/tests/check-send-button-platform.js` check-9 header comment. Since Phase 4 any `complete` draft at or after the current season — a startup draft included — produces the refusal (`draft_status.py:165-187`, `test_completed_draft_seasons_is_shape_blind`). `docs/integrations/sleeper.md` §3.2 already uses the shape-neutral "a pick whose class has been drafted".
- Expected vs actual: reconciliation-log ruling (1) says "any complete draft with season ≥ current excludes that season"; the api-reference row's parenthetical explains the mechanism with the rookie case only. Accurate for FFV3 and every currently linked league (none is in its startup season), so this is a follow-up wording touch, not a Phase 4 item. No test is wrong.

Non-findings worth recording (checked, no defect): the `elif _exclude:` log at `server.py:13361` never fires on an empty read (the D-189 line at `:13358` covers it); `_is_rookie_shaped` keeps its #207 consumers (`draft_status.py:342`, `draft_board_service.py:598`), so the Phase 4 removal left no dead code; round-1 A F-2 / F-4 and B F-3 stand as accepted observations (D-189 text now carries the confidence sentence — `build-report.md` §4).

## TestFlight checklist (operator-run) — FFV3 (Sleeper `1312140920132497408`, 2026 draft complete 2026-08-26), build ≥ 1.17.2 (154) after the Render deploy of `646034ee`; EAS optional except step 12

1. **Pre-deploy (Render shell / read-only prod)** → `SELECT season, COUNT(*) FROM draft_picks WHERE league_id='1312140920132497408' GROUP BY season` → expect 2026 rows present (root cause confirmed); log the counts in TEST_LEDGER.
2. **Deploy → force-quit and reopen Fleeced with FFV3 selected** (session init runs the sync) → re-run the query → expect **2027, 2028, 2029 only**, 48 rows each, **no 2026**. Render log for that init carries either `owned-pick sync for 1312140920132497408: excluding [2026] — Sleeper reports the draft complete` or (flaked read) `… drafts read empty — excluding 2026 on the cached drafted/… verdict (D-189)`. Neither line and 144 picks ⇒ both signals absent — reopen once more.
3. **League → Picks list** (FFV3) → scroll the full list → expect no 2026 pick anywhere; earliest class shown is 2027. Any 2026 row ⇒ STOP, sync fix failed (report the Render line from step 2).
4. **Trades → Calculator → In-league → pick rows** → expect the picker offers only 2027–2029 picks; your side shows the 2028 4th and 2029 1st–4th, and "2026 4th (orig. roster 11)" is gone.
5. **Trades → Deck** → swipe until a card carries a pick → expect every pick on every card is a 2027–2029 class; no card names a 2026 pick.
6. **Positive send, own pick:** Calculator → give your **2028 4th** for any player → Send in Sleeper → pre-flight is the plain "Send this trade?" → expect **"Sent"** and a pending proposal in the Sleeper app → cancel it in Sleeper.
7. **Positive send, far class:** same with your **2029 1st** → expect "Sent" (confirms Sleeper's window reaches 2029 at runtime) → cancel in Sleeper.
8. **Positive send, acquired pick on the receive side:** ask roster 5 for "2027 1st (orig. you)" for a player (live `traded_picks` still shows roster 5 holding it as of 2026-09-08) → expect "Sent" → cancel in Sleeper (#413 TF-3; runtime close of Q-037).
9. **Negative send, spent pick (only if reachable):** on the web calculator (`web/`) hand-add the pick id `1312140920132497408_2026_4_11` to your give side → Send → expect the alert **"Couldn’t send — 1 draft pick in this trade can’t be traded in Sleeper right now — 2026 picks are no longer tradable (Sleeper is trading 2027–2029 picks). Rebuild the trade with one of those."**, **not** "Please try again", and **no** `sleeper propose write-failed` line in Render (a 422 is returned before the GraphQL call). If the id is not reachable from any surface, log "not reachable" — the server-side refusal is covered by `test_propose_refuses_spent_current_season_pick`.
10. **Validate mirror (same trade as step 9, before tapping Send):** the pre-flight shows the blocking warning "This trade will likely fail" with the same sentence (`pick_untradable`); tapping "Send anyway" yields step 9's alert, never a reconnect prompt.
11. **Players-only send unchanged:** Calculator → one player for one player → Send in Sleeper → expect "Sent"; the Render lines for that `/api/trades/propose` show **no** `→ Sleeper GET …/drafts` and **no** `…/league/1312140920132497408` read → cancel in Sleeper.
12. **Mobile parity branch (only after the next EAS build ships `646034ee`'s `SendInSleeperButton.tsx`):** repeat step 9 on the device → the alert title is "Couldn’t send" with the server sentence, and it does **not** open the Sleeper reconnect screen.
13. **First-year startup league (only if one is linked — FFV3 is not one; otherwise log "no startup-season league available"):** a league whose Sleeper `/drafts` shows a `complete` current-season draft with `settings.rounds` ≥ 15 (the startup) → after one app open, League → Picks list and the Calculator picker show **no current-season picks** (only the next three classes, e.g. 2027–2029); a pick-bearing send of a next-class pick → "Sent" → cancel in Sleeper. If a current-season pick id is still reachable on the web calculator → expect step 9's alert with "(Sleeper is trading 2027–2029 picks)". Any current-season pick shown on any surface ⇒ report: the shape-blind exclusion did not reach the sync.
