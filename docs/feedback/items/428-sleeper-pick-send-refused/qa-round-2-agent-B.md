# QA round 2 — agent B — 2026-09-08

## Summary: PASS (0 findings that block; 3 minor observations, none an app bug or a wrong test)

Full re-verification of the resolved tip `646034ee` (Phase 4 commit `c4f8a6f7` on top of the build `26ab3147`), not a delta check. Every round-1 test re-ran green, the three round-1 rulings are each proven by a RED→GREEN pair (18 defect tests RED on the base sources, 5 RED on the pre-Phase-4 `draft_status.py`, guard check 9 RED on the comment-kept sabotage), the startup-shaped completed draft now excludes its season in the **sync** and starts the **send window** at the next class (proven with throwaway route + sync probes, not just the predicate unit tests), `season_window` is a 2-list on every refusal, abstain never refuses, and the FFV3 fixtures still match Sleeper's live public data byte-for-byte.

## Environment

- Worktree: detached at `646034ee61582f7aadd26b4faab9a9005ef3b4b3` (`G-428: reconciliation log — round 1 resolution (c4f8a6f7)`), base `15dabe05`. Diff under test `git diff 15dabe05..646034ee` (21 files, +1174/−71; code files: `backend/draft_status.py`, `backend/server.py`, `backend/sleeper_write.py`, `backend/analytics_taxonomy.py`, 4 test files, `mobile/src/api/sendInSleeper.ts`, `mobile/src/components/SendInSleeperButton.tsx`, `mobile/tests/check-send-button-platform.js`).
- Python 3.14.4 (`python3 -m pytest`, `-p no:cacheprovider`); Node v24.14.1; `mobile/node_modules` is the populated symlink to the parent worktree.
- Scratch SQLite only: `data/trade_finder.db` removed before each pytest run (recreated by the suite; gitignored); no `DATABASE_URL` in the environment; production never touched. Sleeper public API read with `curl`, no auth.
- Flags: none changed by the diff (`trade.send_in_sleeper`, `picks.owned_sync`, `picks.league_horizon` as in `config/features.json`).
- No simulator, no Maestro (D-056). Every sabotage was restored with `git checkout 646034ee -- <file>`; the throwaway probe file was deleted after each run; `git status --short` is empty at the end of the round and `git log -1` still reads `646034ee`. Nothing committed, nothing fixed.

## Results

| Test | Verdict | Evidence |
|---|---|---|
| Targeted set: `test_owned_picks.py test_sleeper_write_route.py test_sleeper_write.py test_sleeper_pick_tradability.py test_pick_assignment.py test_trade_send_validate.py test_pick_horizon.py -q` | PASS | `173 passed in 10.77s` (matches the Phase 4 count in the reconciliation log) |
| Full `python3 -m pytest backend/tests -q -p no:cacheprovider` (one uninterrupted foreground-launched process; the harness moved it to the background at the 600 s ceiling, summary read from the tee'd log `scratchpad/full-suite-B-r2.log`) | PASS | **`5892 passed, 1 skipped in 1022.42s (0:17:02)`**, `EXIT=0`, 0 `FAILED`/`ERROR` lines. (Round 1 was 5890 + the 2 Phase 4 unit tests = 5892, as expected.) Stderr carries the pre-existing `[record_event] system:receipts/receipts_grade_run failed: … no such table: user_events` log lines from the receipts tests' stubbed engine — logged warnings, not failures, unrelated to this diff |
| RED-1 — `git checkout 15dabe05 -- backend/draft_status.py backend/server.py backend/sleeper_write.py`, run the 4 defect test files at `646034ee` | PASS (RED) | **`18 failed, 87 passed in 4.25s`** — the 16 round-1 defect tests plus the 2 Phase 4 additions (`test_completed_draft_seasons_is_shape_blind`, `test_sleeper_pick_window_post_startup_starts_next_class`). Verbatim lines below |
| RED-1 restore (`git checkout 646034ee -- …`) and rerun | PASS (GREEN) | `105 passed in 5.20s`; `git status --short` empty |
| RED-2 — `git checkout 26ab3147 -- backend/draft_status.py` (the pre-Phase-4, shape-filtered predicate) vs `test_sleeper_pick_tradability.py` + my sync/route probes | PASS (RED) | **`5 failed, 14 passed`**: `test_completed_draft_seasons_reads_only_complete_current_or_later` (`assert {2026} == {2026, 2027}`), `test_completed_draft_seasons_is_shape_blind` (`assert set() == {2026}`), `test_sleeper_pick_window_post_startup_starts_next_class` (`assert (2026, 2028) == (2027, 2029)`), probe `test_sync_excludes_startup_shaped_complete_draft` (`assert {2026, 2027, 2028} == {2027, 2028, 2029}` — the grid re-populated 2026), probe `test_route_refuses_spent_pick_after_startup_draft` (`assert 200 == 422` — the spent pick reached the mocked Sleeper). Restored → `14 passed` |
| Resolution proof — startup-shaped complete draft (throwaway `backend/tests/_qa2_startup_probe.py`, copied in from the scratchpad, run, deleted; source kept at `scratchpad/_qa2_startup_probe.py`) | PASS | `5 passed in 2.23s` on `646034ee`: **sync** `_sync_sleeper_owned_picks` with `/drafts = [{"season":"2026","status":"complete","type":"snake","settings":{"rounds":25}}]` → rows' seasons `{2027, 2028, 2029}`, 24 rows (2 rosters × 3 × 4); **route** `POST /api/trades/propose` giving `…_2026_4_11` under the same startup draft → 422 `sleeper_pick_untradable`, `season_window == [2027, 2029]` (a 2-list), `detail == message`, "2027–2029" in the copy, `propose_trade` not called; `MY_2027_2ND` under the startup draft → 200, `draft_picks == ["1,2027,2,1,2"]`; receive-side refusal (`THEIR_2026_1ST`, `DRAFTS_DONE`) → `season_window == [2027, 2029]`; **abstain** — meta `{"status":"in_season"}` (no season), meta `None`, meta `{"season":"n/a"}` × spent pick × `DRAFTS_DONE` → 200 each, encoded `["11,2026,4,1,2"]`, no `season_window` key in the body |
| Guard RED — `check-send-button-platform.js` check 9 with ONLY the `detail\n ||` operand removed from the untradable `Alert.alert` (four comment lines kept, both mentioning `detail`) | PASS (guard RED) | `FAIL  sleeper button: sleeper_pick_untradable renders the server detail` · `1 check(s) failed.` · **exit 1** (captured directly, `SABOTAGED_GUARD_EXIT=1`). Round-1 B F-1's sabotage A now fails the guard. Restored → exit 0 |
| `cd mobile && npx tsc --noEmit` | PASS | exit 0 |
| Every `mobile/tests/check-*.js` | PASS | 98 files, 0 non-zero exits (`ls mobile/tests/check-*.js \| wc -l` = 98; the diff touches only `check-send-button-platform.js`, so the "100" in the round-1 reports was a counting difference, not a removed guard) |
| `check-send-button-platform.js` on the clean tree | PASS | `sleeper_pick_untradable has its own Alert branch, no goConnect` · `renders the server detail` · `all three pick branches sit inside the ladder before the catch-all else` · `the catch-all else still renders the generic failure copy` |
| Fixture realism vs Sleeper public API (FFV3 `1312140920132497408`, 2026-09-08) | PASS | below |
| Real payload through the predicate | PASS | `completed_draft_seasons(real drafts, "2026") = {2026}`; `sleeper_pick_window("2026", real drafts, real traded_picks) = (2027, 2029)`; flake + no verdict `(2026, 2028)`; flake + cached `drafted/medium` `(2027, 2029)`; flake + cached `not_drafted/high` `(2026, 2028)`; live `pre_draft` + cached `drafted/high` `(2026, 2028)` (live wins); season `None` / `"abc"` → `None`; `sleeper_pick_tradable` over `(2027, 2029)`: 2025 F · 2026 F · 2027 T · 2028 T · 2029 T · 2030 F; `(2026, None)` → T; `("x", window)` → T; malformed entries (`None`, `"x"`, season `"abc"`, missing season, `in_progress`) → `set()`; a lone 2025 `complete` → `set()`; a 2027 `complete` with 2026 pending → `{2027}` and window `(2026, 2028)` |
| Code-walk (a)–(h) | PASS | below, all cites against `646034ee` |
| Hunt: overhaul / MFL / ESPN / DB untouched | PASS | `git diff --stat 15dabe05..646034ee -- backend/overhaul_api.py backend/mfl_write.py backend/espn_write.py backend/database.py backend/win_now_service.py` is empty. `git grep` for `_sleeper_encode_ftf_picks` / `_sleeper_pick_window_for_send` / `_sleeper_cached_draft_verdict` / `completed_draft_seasons` / `sleeper_pick_window` / `sleeper_pick_tradable` outside tests: only `server.py:13353,13355` (sync), `:18430-18432` (propose), `:30103-30104` (validate), the encoder body, plus docstring mentions (`overhaul_api.py:142`, `server.py:18290`, `sleeper_write.py:24`). No caller still unpacks the old 3-tuple. Overhaul's `pick_rows` (`overhaul_api.py:130-137`) reads `load_picks(…, source=pick_source_platform)` and its send selects `sleeper_propose` = `_sleeper_propose_core` (`server.py:31639`, `overhaul_api.py:281`), so it inherits both the clean grid and the 422 |
| Hunt: `_is_rookie_shaped` still used only by the #207 verdict | PASS | `draft_status.py:342` (verdict path) is its sole remaining caller; `completed_draft_seasons` (`:165-187`) no longer references it |
| Docs rows (api-reference propose/validate rows + 422 table; cross-client-invariants enum 18 + validate vocabulary; `sleeper.md` §1.1 rows 6–7, §3.2 tradability rule, §3.3 field-1 + F3, §5 call budgets; `analytics_taxonomy.py:1069-1074`; addendum; `sendInSleeper.ts:5-7,215`; `SendInSleeperButton.tsx:252-254`) | PASS | read in the diff; the api-reference 422 row now says `season_window` is "always present … an unknown season abstains and never produces this refusal", matching `server.py:18446` `list(window)` and the abstain probe |
| PRD §7 step 1 — prod `SELECT season, COUNT(*) FROM draft_picks WHERE league_id='1312140920132497408' GROUP BY season` | BLOCKED | no production access from QA (and none permitted); operator item |
| Runtime sends (PRD §7 steps 2–5) | BLOCKED | TestFlight-only (D-056) — checklist below |

### RED-1 lines (sources at `15dabe05`, tests at `646034ee`)

```
FAILED backend/tests/test_owned_picks.py::test_daemon_step_flaked_drafts_uses_cached_drafted_verdict
E           assert False   (all(r["season"] != 2026 for r in rows))
FAILED backend/tests/test_sleeper_write_route.py::test_propose_refuses_spent_current_season_pick
E       AssertionError: {"status":"proposed","transaction_id":"TX9"}
E       assert 200 == 422
FAILED backend/tests/test_sleeper_write_route.py::test_propose_refuses_untradable_on_receive_side
E       assert 200 == 422
FAILED backend/tests/test_sleeper_write_route.py::test_propose_untradable_reported_after_unmapped_before_not_owned
E       AssertionError: assert 'sleeper_pick_not_owned' == 'sleeper_pick_untradable'
FAILED backend/tests/test_sleeper_write_route.py::test_propose_502_detail_is_sleepers_sentence
E       KeyError: 'message'
FAILED backend/tests/test_sleeper_write.py::test_graphql_error_detail_is_first_message
E       - These draft picks cannot be traded.
E       + [{"message": "These draft picks cannot be traded.", "path": ["propose_trade"], "locations": [{"line": 2, "column": 3}]}]
FAILED backend/tests/test_sleeper_write.py::test_graphql_error_auth_words_still_classified_from_full_dump
E       - request failed
E       + [{"message": "request failed", "extensions": {"code": "UNAUTHENTICATED"}}]
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_completed_draft_seasons_reads_only_complete_current_or_later
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_completed_draft_seasons_is_shape_blind
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_post_startup_starts_next_class
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_post_draft_is_next_three_classes
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_pre_draft_anchors_current
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_flake_with_cached_drafted_excludes_current
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_flake_without_verdict_abstains
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_cached_verdict_never_overrides_a_live_drafts_read
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_window_unknown_season_is_none
FAILED backend/tests/test_sleeper_pick_tradability.py::TestSleeperPickWindow::test_sleeper_pick_tradable
E       AttributeError: module 'backend.draft_status' has no attribute 'completed_draft_seasons' / 'sleeper_pick_window' / 'sleeper_pick_tradable'
FAILED backend/tests/test_sleeper_pick_tradability.py::test_spent_current_season_pick_flags_pick_untradable
E       AssertionError: assert [] == ['pick_untradable']
18 failed, 87 passed in 4.25s
```

Survivor tests (green by design on the base code, pinning behavior that must not change): `test_propose_in_window_pick_still_sends`, `…_drafts_flake_without_verdict_does_not_block`, `…_pick_free_send_makes_no_drafts_or_meta_fetch`, `test_daemon_step_flaked_drafts_without_verdict_keeps_current_season`, `test_in_window_pick_has_no_untradable_warning`, `test_validate_drafts_flake_without_verdict_does_not_flag`, `test_pick_free_validate_makes_no_drafts_fetch`, `test_cached_verdict_does_not_leak_into_the_sleeper_sync`, every #413 T-1…T-14 — all within the 87 passed.

### Fixture realism (Sleeper public API, 2026-09-08, `curl`, no auth)

| Live read | Observed | Fixture | Match |
|---|---|---|---|
| `GET /v1/league/1312140920132497408/drafts` | one entry: `season "2026"` (str), `status "complete"`, `type "linear"`, `settings.rounds 4`, `last_picked 1787707582674`; keys `created, creators, draft_id, draft_order, last_message_id, last_message_time, last_picked, league_id, metadata, season, season_type, settings, sport, start_time, status, type` | `DRAFTS_DONE` (`test_sleeper_write_route.py:316-318`, `test_sleeper_pick_tradability.py`) | yes — same names, string season, same `rounds`, same `last_picked` |
| `GET …/traded_picks` | 57 rows, `Counter({'2026': 34, '2027': 17, '2028': 6})`, keys `owner_id, previous_owner_id, roster_id, round, season` (season str, others int); roster 11's 2026 R4 = `{round:4, season:"2026", roster_id:11, owner_id:1, previous_owner_id:11}` | `TRADED_2026` (`:323-324`) | yes — byte-identical; the spent season is still listed live 13 days after the draft (the fix's premise) |
| `GET /v1/league/1312140920132497408` | `season "2026"` (str), `status "in_season"`, `total_rosters 12`, `settings.trade_deadline 12`, `disable_trades 0`, `draft_rounds 4`, `previous_league_id "1181674778942836736"` | `META` (`:314-315`) | yes — string season, `settings.draft_rounds`; `total_rosters 2` in the fixture is the 2-roster test league, immaterial to the window |

None of the public league ids is startup-shaped (all `rounds: 4`), so — as in round 1 — Sleeper's answer for a post-startup current-season pick cannot be observed here; the shape-blind rule rests on the ruling (a completed draft consumes its class) and the pre-#428 sync behavior it restores.

### Code-walk proofs (file:line against `646034ee`)

**(a) Sync — live `/drafts` wins; cached verdict only on `[]`; startup-shaped drafts now excluded.** `_sync_sleeper_owned_picks` (`backend/server.py:13287`): `_cur_season` from the live meta (`:13334-13339`); `_drafts = _fetch_sleeper_drafts(league_id)` (`:13352`; the fetcher `:16005-16021` returns `[]` on a non-digit id, any exception, or a non-list); `_exclude = completed_draft_seasons(_drafts, _cur_season)` (`:13353` → `backend/draft_status.py:165-187`: `isinstance(d, dict) and status == "complete"` `:180`, `int(season)` `:183`, `season >= current_season` `:186` — **no shape test**; `_is_rookie_shaped` is now referenced only by the #207 verdict at `:342`). `if not _drafts:` (`:13354`) → `_sleeper_cached_draft_verdict` (`:13271-13284`: one `get_league_draft_context`, exceptions → None, no status → None, else `DraftStatus(status, confidence or LOW)`) → `_exclude.add(_cur_season)` iff `.drafted` (`:13356-13357`, logged `:13358-13360`); a non-empty list takes `elif _exclude:` (`:13361`) and never consults the cache. `_exclude` feeds `save_draft_slot_order` (`:13369-13376`, `None` when the current season is excluded) and `sync_draft_picks(exclude_seasons=_exclude)` (`:13378-13391`, unchanged) whose `pick_horizon` walks the anchor past the excluded class. Daemon ordering: the sync (`:21723`) precedes `_refresh_league_draft_status` (`:21739`) in the same pass, so a consulted verdict always comes from an earlier pass. Does-not-fire: non-empty `_drafts` of any content; `[]` with NULL / `unknown` / `not_drafted` (`test_daemon_step_flaked_drafts_without_verdict_keeps_current_season`). Startup shape at the sync level: proven by the probe above (`{2027, 2028, 2029}`, RED on the pre-Phase-4 predicate).

**(b) `sleeper_pick_window` (`draft_status.py:190-219`).** Unparseable `current_season` → `None` (`:208-211`); `exclude = completed_draft_seasons(...)` (`:212`); the cached verdict adds the current season only under `not drafts and cached_verdict is not None and cached_verdict.drafted` (`:213-214`); `observed` = every `traded_picks[].season` (`:215-218`) → `pick_horizon(current, exclude, observed)` (`:219` → `:92-139`): `first` walks past `exclude` (`:124-127`), `last = first + 2` (`:129`), an observed season widens `last` only when `first <= season <= first + 4` (`:130-137`), so a spent season (< `first`) can never re-enter. Startup: `(2026, STARTUP_DONE, TRADED_2026) → (2027, 2029)` and 2026 untradable (`test_sleeper_pick_window_post_startup_starts_next_class`).

**(c) Encoder order grid → window → holder (`server.py:30327-30387`).** Per pick: `row = grid.get(str(pid))` → `unmapped` (`:30359-30362`); unparseable `season/round/original_roster_id` → `unmapped` (`:30363-30368`); **`not sleeper_pick_tradable(season, window)` → `untradable`** (`:30369-30371`); holder lookup (`:30372-30378`); `holder != from_rid` → `not_owned` (`:30379-30381`); `encode_draft_pick` (`:30383-30384`); 4-tuple at `:30387`. `sleeper_pick_tradable` (`draft_status.py:222-233`): `None` window → True (`:226-227`), unparseable season → True (`:228-231`), else `first <= season <= last` (`:233`). T-17 pins both orderings.

**(d) Propose 422 shape + nothing sent (`_sleeper_propose_core`, `server.py:18349`).** `if give_picks or recv_picks:` (`:18418`) → grid (`:18423`) → traded (`:18424`) → `window = _sleeper_pick_window_for_send(league_id, _fetch_sleeper_league_meta(league_id), traded)` (`:18430-18431`) → encoder (`:18432-18434`) → `unmapped` 422 (`:18437-18442`) → **`{"error":"sleeper_pick_untradable","picks":untradable,"season_window":list(window),"message":_msg,"detail":_msg}` 422 (`:18443-18447`)** → `not_owned` 422 (`:18448-18452`) — all before `ProposeTradeRequest` (`:18454`) and `_sleeper_write.propose_trade` (`:18461`), so no GraphQL call, no `sleeper_send_succeeded` row, no deck outcome (`_save_deck_outcome_safe` at `:18496` is after the send). **`season_window` is always a 2-list:** `list(window)` is only evaluated when `untradable` is non-empty, and `untradable` can only be appended at `:30370` when `sleeper_pick_tradable` returned False, which requires `window is not None` (`draft_status.py:226`). The `None` branch removed by Phase 4 was therefore dead, and the abstain probe (meta without a parseable season → 200, no `season_window` key) confirms the other side. `_sleeper_untradable_copy` (`:30403-30423`) unpacks `first, last = window` at `:30420` under the same guarantee.

**(e) Validate (`trades_validate`, `server.py:30008`).** `meta` fetched at `:30045` and required to be a dict for `checked:true` (`:30047`); same gate `:30097` → grid/traded `:30099-30100` → `window = _sleeper_pick_window_for_send(league_id, meta, traded)` (`:30103`) → encoder (`:30104-30106`) → `warnings.append({"code":"pick_untradable","severity":"blocking","message":_sleeper_untradable_copy(...)})` (`:30114-30115`), between `asset_unmapped` (`:30108`) and `pick_moved` (`:30117`). The #180 `META` has no `season`, so V-7…V-9 re-patch it with `"2026"` (`test_sleeper_pick_tradability.py:136-146`); without a season the validate window abstains (V-9's flake case abstains for the D-089 reason).

**(f) Pick-only fetches.** The only `/drafts` read on the send paths is `_fetch_sleeper_drafts` inside `_sleeper_pick_window_for_send` (`:30390-30400`, `:30397`), called only at `:18430` and `:30103`, both inside `if give_picks or recv_picks:`; the propose-side `/league/{id}` read is the inline `_fetch_sleeper_league_meta` at `:18431` in the same block (the only other upstream read in the core is `_fetch_league_rosters` at `:18391`). The cached-verdict DB read (`:30398`) happens only when `drafts` is empty. T-20 (`drafts_mock`, `meta_mock`, `grid_mock`, `traded_mock` all `assert_not_called`) and V-10 pin the players-only path.

**(g) 502 body.** `sleeper_write._post_graphql` (`backend/sleeper_write.py:384-398`): `dump = json.dumps(errors)[:500]` (`:385`), `first = errors[0]` when a non-empty list (`:392`), `msg = first.get("message")` when a dict (`:393`), `detail = str(msg).strip()[:500] if msg else dump` (`:394`), `low = dump.lower()` drives the auth-word scan (`:395-396`), both `SleeperAuthError` and `SleeperWriteError` carry `detail=detail` (`:397-398`). Route except (`server.py:18475-18486`): `_detail = str(e.detail or "")[:200]`, body `{"error":"sleeper_write_failed","kind","detail":_detail,"message":_detail}`. Pinned by `test_graphql_error_detail_is_first_message`, `…_auth_words_still_classified_from_full_dump`, T-21. The 409 `sleeper_rejected` body (`:18464-18473`) still carries `detail` only — unchanged, out of scope (round-1 note).

**(h) In-window pick under a completed draft unchanged.** T-18 (`MY_2027_2ND`, `GRID_WITH_SPENT`, `TRADED + TRADED_2026`, `DRAFTS_DONE`) → 200, `draft_picks == ["1,2027,2,1,2"]`, the same string as T-3/T-4 before #428; my probe repeats it under `STARTUP_DONE` with the same result. `encode_draft_pick` and the `orig,season,round,from,to` order are untouched (the `sleeper_write.py` diff is confined to `_post_graphql`). The `_propose` helper's `drafts=DRAFTS_PRE` default keeps T-5 (`THEIR_2026_1ST`) on its original fixture; no pre-existing test body changed.

**Mobile branch (`SendInSleeperButton.tsx`).** `const detail = body?.detail` at `:268`; `else if (code === 'sleeper_pick_untradable')` at `:317-327` sits between `sleeper_pick_unmapped` (`:306`) and `sleeper_pick_not_owned` (`:328`) in the ladder; renders `detail || <count-aware fallback>` (`:323-326`); no `goConnect` reference; catch-all `:335-340` unchanged. Guard check 9 (`mobile/tests/check-send-button-platform.js:483-513`) locates the `Alert.alert` `CallExpression` inside `thenStatement`, walks `arguments[1]` for an `Identifier` named `detail` — the comment-kept sabotage now fails it (exit 1 above).

## Findings

No blocker or major finding. Three minor observations, none an app bug and none a wrong test:

### O-1: the "web calculator" negative-path step in PRD §7 step 5 / both round-1 checklists is not reachable as written
- Severity: minor (checklist quality only)
- `git grep -n "trades/propose\|trades/validate\|Send in Sleeper" -- web extension` matches only `web/terms.html:104` (the terms text). `web/` has no Send-in-Sleeper flow and no propose call, so "hand-add the 2026 4th's id on the web calculator" cannot be performed. The four `SendInSleeperButton` mounts (`InLeagueCalculator.tsx`, `TradeCard.tsx`, `MatchesScreen.tsx`, `TradesScreen.tsx`) are the only send surfaces. Rewritten below as step 9: a cached deck/match card that still names a 2026 pick, or a `curl` against the operator's own verified session — otherwise "not reachable", with the server-side refusal covered by T-15/T-16 and the probes above.

### O-2: guard-file count is 98, not the 100 both round-1 reports state
- Severity: minor (bookkeeping)
- `ls mobile/tests/check-*.js | wc -l` = 98 on `646034ee`; `git diff --stat 15dabe05..646034ee -- mobile/tests` touches only `check-send-button-platform.js`, so no guard was removed by this diff. All 98 exit 0. Worth one line in TEST_LEDGER so the number is not chased later.

### O-3: living-memory write-back is not in the diff (expected)
- Severity: minor (informational)
- `git grep -n "D-189" living-memory/DECISIONS.md` and `grep -n 428 living-memory/TEST_LEDGER.md living-memory/CHANGELOG.md` are empty on this tip; the build report §4 explicitly leaves D-189's id allocation, the Q-037 close, TEST_LEDGER and CHANGELOG to the orchestrator at write time (concurrent sessions, id from `origin/main`). Not a defect; listed so Phase 5 does not forget: D-189 text (with the Phase 4 shape-blind + confidence-blind sentences), Q-037 closed on `investigation.md` §2, TEST_LEDGER (suite line `5892 passed, 1 skipped`, both QA rounds, the RED evidence), CHANGELOG, and the A F-4 2027-rollover item for NEXT.md.

Round-1 items re-checked and confirmed resolved or accepted: A F-1 / B F-2 (shape-blind — resolved, RED-2 proves both the unit and sync/route levels); A F-3 (dead `season_window: null` / `?–?` — removed; api-reference corrected); B F-1 (guard check 9 — resolved, comment-kept sabotage exits 1); A F-2 (confidence-blind corroboration — one sentence in the D-189 text, build-report §4); A F-4 / B F-3 — accepted as pre-existing / verbatim-spec copy, unchanged, not re-raised.

## TestFlight checklist (operator-run) — FFV3 (Sleeper `1312140920132497408`, 2026 draft complete 2026-08-26), build 1.17.2 (154) or later, after the Render deploy of `646034ee`'s merge

Pre-flight (not TestFlight):
0. Read-only prod: `SELECT season, COUNT(*) FROM draft_picks WHERE league_id='1312140920132497408' GROUP BY season;` — record the result in TEST_LEDGER. 2026 rows present ⇒ root cause confirmed. (BLOCKED for QA agents: no prod access.)

1. **Force-quit and reopen Fleeced → land on Trades home with FFV3 selected** (this runs `session/init`, whose daemon pass runs the owned-pick sync). Render log for that init should show either `owned-pick sync for 1312140920132497408: excluding [2026] — Sleeper reports the draft complete` or, if `/drafts` flaked, `… drafts read empty — excluding 2026 on the cached drafted/… verdict (D-189)`. Expect one of the two lines; a sync with neither line and 144 picks means both signals were absent — re-open once more.
2. Re-run step 0's query. Expect **2027, 2028, 2029 only, 48 rows each** (12 rosters × 4 rounds); zero 2026 rows.
3. **League → Picks list** (FFV3): scroll the full list. Expect **no 2026 pick anywhere**; earliest class shown is 2027. Any 2026 row ⇒ STOP, sync fix failed (report the Render line from step 1).
4. **Trade Calculator (in-league) → add a pick from "My picks"**: the picker offers only 2027/2028/2029 picks. Expect no 2026 entry.
5. **Deck**: swipe until a card carries a pick on either side. Expect every pick on every card to be a 2027–2029 class.
6. **Positive send, own pick:** Calculator → give your **2028 4th** for any player from another team → **Send in Sleeper** → the pre-flight is the plain "Send this trade?" (no "This trade will likely fail") → confirm → expect **"Trade sent"**. Open the Sleeper app → the pending offer is there with the 2028 4th → **cancel it in Sleeper**.
7. **Positive send, far class:** repeat step 6 with your **2029 1st**. Expect "Trade sent" and the offer in Sleeper → cancel. (Proves Sleeper's window really is 2027–2029 at runtime.)
8. **Positive send, receive side, acquired pick (#413 TF-3 / Q-037 runtime close):** Calculator → receive **"2027 1st (orig. you)"** from the roster that holds it (roster 5 per `traded_picks`) for a player of yours → Send → expect "Trade sent" and the offer in Sleeper → cancel.
9. **Negative send, spent pick — only if reachable (O-1):** the web app has no send flow, so the only in-app route is a Deck or Matches card cached **before** step 1 that still names a 2026 pick. If one exists: Send in Sleeper → expect the pre-flight **"This trade will likely fail"** with the bullet "**1 draft pick in this trade can’t be traded in Sleeper right now — 2026 picks are no longer tradable (Sleeper is trading 2027–2029 picks). Rebuild the trade with one of those.**"; tap **Send anyway** → expect the alert **"Couldn’t send"** with that same sentence (build 154 renders `detail`), **not** "Something went wrong … Please try again", and **no** reconnect prompt. Render: **no** `sleeper propose write-failed` line for this attempt (the 422 returns before the GraphQL call). Alternative without a cached card: from a terminal, `curl -X POST …/api/trades/propose` with your own verified session header and body `{"league_id":"1312140920132497408","their_roster_id":<any other roster>,"give_player_ids":["1312140920132497408_2026_4_11"],"receive_player_ids":["<their player id>"]}` → expect HTTP 422 `{"error":"sleeper_pick_untradable","picks":["1312140920132497408_2026_4_11"],"season_window":[2027,2029],…}` and nothing pending in Sleeper. If neither is possible, log "not reachable" — that is itself the step-3 pass, and the refusal is covered by `test_propose_refuses_spent_current_season_pick`.
10. **Players-only send unchanged:** Calculator → one of your players for one of theirs (no picks) → Send → expect "Trade sent" → cancel in Sleeper. Render for this request: `→ Sleeper GET` lines for `/league/…` and `/rosters` only — **no** `/drafts`, **no** `/traded_picks` (pick-free sends make no pick reads).
11. **Sleeper-refusal wording (F3), if it can be provoked safely:** any send Sleeper itself refuses should show "Couldn’t send" followed by Sleeper's own sentence, never a `[{"message": …` JSON fragment. If nothing provokes a refusal, log "not provoked".
12. **Analytics:** after steps 9 and 10, `user_events` for your user should show `sleeper_send_failed` with `error_code = 'sleeper_pick_untradable'` (step 9, if performed in-app) and `sleeper_send_succeeded` (steps 6–8, 10). Confirm `error_code` is exactly that string (closed enum, 18 values).
13. **First-year startup league (Phase 4 expectation, only if the operator has one):** in a dynasty league whose **startup** draft (≥15 rounds) is `complete` for 2026 and which has no separate 2026 rookie draft, expect exactly the FFV3 behavior — after one app open the Render line reads `excluding [2026] — Sleeper reports the draft complete`, the Picks list starts at 2027, and a send of any 2027–2029 pick reads "Trade sent". A 2026 pick surfacing anywhere in such a league ⇒ report it (that is the round-1 A F-1 / B F-2 regression the shape-blind rule closes). No such league is known among the public ids, so log "no startup-year league available" if that is the case.
