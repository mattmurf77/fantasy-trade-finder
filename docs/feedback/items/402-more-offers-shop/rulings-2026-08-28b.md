# Operator rulings — 2026-08-28b (post-ship: the four surprises)

> Recorded from the operator's messages after v1.16.9 (build 135) shipped.
> These supersede the shipped presentation and the same-value constraint.
> The rev-3 spec (`rev3-spec.md`) is the buildable contract; this file is
> the why.

## R-2026-08-28b-1 — the calc/guided merge: clarified, still parked

Operator: *"I thought we were merging the find a trade and league manual
calc?"* — Answer given: that merge is `calc.inline_home`, built and dark,
blocked on Wave B (tour rebuild), and was explicitly parked by the operator
on 2026-08-27 ("not making a decision"). **No ruling yet on starting
Wave B** — awaiting the operator's go. Nothing in the shop rework conflicts
with it; a pushed shop window fits the merged page BETTER than the inline
strip did.

## R-2026-08-28b-2 — the shop is a pushed WINDOW, not an inline strip

Operator: *"I thought the 'shop' experience would launch into a new window
rather than present at the bottom of the trade card (with back button
navigation to the find a trade card)."* — Ruled: re-host into a pushed
screen (the rev-1 D-1 shape). The 2026-08-27 "presented in line" reading is
superseded. Entry unchanged (give-side "More offers" + chooser). Back
returns to the untouched deck. The inline-only machinery (pan gating,
deck-holds-still button/VoiceOver gates, close-on-card-change, kill-switch
mount seams) is DELETED, not preserved — a pushed window makes those
problems structurally impossible.

## R-2026-08-28b-3 — position filters on ALL modes; Same value defaults populated at his own position

Operator: *"I expected the position filter to be present for all trade
variations (tier up, down, similar value) and by default the similar value
suggestions would return offers for the same position (currently shows '0'
and requires the user to select a position to search)."* — Ruled: the
position chip row moves to the top of the shop window and applies to every
mode. Clarification recorded: the shipped default DID search his own
position — the "0" was an honest empty under the ±10% band (spike S-2
predicted it) — but the UX verdict stands and R-4 below is what actually
fixes the dead-end default.

## R-2026-08-28b-4 — Same value = same TIER, not the fairness band

Operator: *"The returned options for same value are tighter constraints
than needed.. I think we can present all players in the same tier of pick
valuations rather than the fairness gate currently applied."* — Ruled: the
same-value pool is TIER membership (the 8-tier pick-valuation ladder,
`backend/tier_config.json` / `ranking_service.tier_for_elo`), replacing the
±10% band + #108 gain gate for this group. Scoping decision (orchestrator,
flagged): shipped as a request parameter the shop sends — the single-pin
panel's existing lateral behavior is NOT silently changed; unify later if
the operator wants tier semantics there too.
