# Operator rulings — 2026-08-28 (post-QA round 2)

> Continuation of `rulings-2026-08-27.md`. Recorded verbatim from the
> operator's reply to the QA round-1 report: **"Fix B3, 2. Offer it.
> Resolve the runtime concerns by ensuring consistent/universal approach."**

## R-2026-08-28-A — B-3 is fixed

The warm-cache resurrection of a committed dismiss (QA reviewer B, finding
B-3) is fixed. Mechanism note: this and P-2 are the same defect family
(a dismissed idea reappearing from stale data), so one universal mechanism
covers both — a committed dismissal stays locally suppressed for the strip
session regardless of cache row, refetch timing, or payload contents; only
Undo restores a tile.

## R-2026-08-28-B — the own-position chip IS offered ("Offer it")

Reviewer A defect 1 (mockup-vs-LLD contradiction) is ruled for the **LLD**
(§3.4): the shopped player's own position renders as a selectable chip, so
"WR laterals **plus** RB laterals" is expressible. Empty selection keeps its
meaning (same-position swaps, field omitted). PICK stays excluded; pick
pins still render no picker. The mockup's Same-value frame is updated to
match the ruling.

## R-2026-08-28-C — P-1..P-4 resolved with consistent/universal semantics

Not one-off patches — each fix states the general rule it enforces:

- **P-1 (pager/counter desync on last-tile undo):** rule — pager position is
  derived from the rendered data; scrolls react to data changes, they never
  race them.
- **P-2 (dismiss vs refetch race):** covered by the R-A mechanism above —
  suppression is client-authoritative for the session, so server/cache
  ordering can never resurrect a tile.
- **P-3 (doubled/phantom `shop_opened`):** rule — an event fires exactly
  once, at the single point where the thing it names actually happens. The
  emit moves into the one code path that opens the strip; the chooser's
  open emits nothing; Cancel emits nothing.
- **P-4 (queue-route prerequisite):** rule — a feature's entry is gated on
  every flag its actions require. `shopEnabled` adds `calc.merged_layout`
  (the ✓'s route 404s without it), and the flag comment names the full
  prerequisite chain including the pre-existing hidden one
  (`trade.finder_targeting`, which the keep chips already require).
