# G419 — Phase-1 reconciliation

## Parent phase-1 exit — 2026-09-06

The original planner independently re-read the completed revision at `481b1809` and approved backend implementation with no remaining specification blockers. The parent read the revised contract, ratified the exact-interest and held-policy boundaries, and refreshed `origin/main` immediately before build; it remains `4026ebc8` with no overlap. The queue renewal exception, truthful durable `passed` evidence/repair, real DB timestamp tests and limited local mobile guarantee are now the reviewed contract. Earlier planned/pending language below records the author phase, not the current gate state.

### Parent build adjudication — missing synthetic source prerequisite

The builder's strict public projection exposed `test_bakeoff_serving.py::test_likes_you_injection_does_not_reorder_the_interleave`: its fake injector stamped `likes_you=True` on a synthetic `te1 → rb3` card, while the harness's only source like described another package. The parent independently read the fixture and its order/attribution assertions. R1/R3 require actionable source evidence, so weakening production validation to retain an unverified badge was rejected. Approved correction: add the actual synthetic counterparty's matching `rb3 → te1` source row in the existing isolated harness; keep every interleave, arm-rank and policy-version assertion unchanged. No golden file recapture, profile change or new product rule. An unknown-source control must remain excluded. This records fixture adjudication, not final QA success.

**Status:** planned · 2026-09-06 · `codex/feedback-419-trade-disposition-20260906`

Author pass against plan commit `19b927aa2ce5bd7f4abb3ba0df2635ed311994a5` and code base `4026ebc81eaae50b345b42421641125c5b8d413e`; initial docs commit `fd74cbf1`. The original planner's critique was relayed by root and its two blockers are incorporated in Round 2. Root ratified R1 and all held policy boundaries. [PRD](prd.md) and [scope](scope.md) now await focused planner review of the corrections. **No runtime source, tests, flags, schema, private history or shared ledger was edited by this author; no tests were run.** This log does not imply focused review is complete.

## Round 1 — author verification and resolutions

| Concern from plan / current code | Author resolution and review target |
|---|---|
| Older source like survives a later receiver pass after amnesty/cooldown | Confirmed `database.py:5860` reads only recent unretracted likes; `server.py:3621` consults current pass keys; `server.py:20448` excludes expired/pre-amnesty passes from those keys. R1 resolves the old offer read-side using later exact decisions while preserving both discovery settings. This is the incident-first requirement, T1 must be RED on baseline. |
| Source's own later pass cannot invalidate its old like in current readers | Confirmed `save_trade_decision` (`database.py:5714`) appends; `find_mirror_like` (`:8820`) reads live likes only. R1/R2 apply exact latest-action interpretation to candidate sources before injection and exact/fuzzy match selection. Do not translate a pass into another actor's withdrawal. |
| Fresh re-like and receiver positive precedence were open | PRD R1 table pins both sequences: a later source like is new evidence subject to current discovery cooldown; receiver re-like is a new opposite-direction offer and does not revive resolved old source consent. Automatic matching still requires a currently actionable counterparty like and the existing caller swipe. Planner must critique these explicit choices. |
| Queue can permanently mistake a resolved row for a queued offer | `database.py:5923` ignores later decisions and is windowless; `server.py:14580` probes it before writes. R2 keeps this ordering and deterministic ID, returns `already_queued:false` only for a deliberate new offer after resolution, and preserves ordinary retries and withdrawal revival. No new queue refusal or queue-time matching. |
| Awaiting and derived R4/summary would contradict an injector-only fix | `database.py:9228` selects 500 live historical likes then subtracts matches; `server.py:6739` derives R4 from that reader; `database.py:7968` derives the summary count. Include actionable-like projection there with existing bounds/output/partner resolution. This removes resolved offers from the one-sided bucket, so scope calls for the shared invariant text to be clarified by root. |
| Stored rows lack target-user identity | Confirmed `load_awaiting_trades` docstring and roster index at `database.py:9305`. No schema fix/backfill. Use explicit counterpart context where available, existing roster evidence otherwise; keep account/league boundaries. Test different counterpart/league and ambiguous membership. Do not turn the same union of players into a global rejection. |
| Per-candidate DB probes could turn polling or generation into an N+1 read | R2 requires one batched scoped history projection reused across candidates, bounded by existing candidate selection and relevant time/actors/leagues. Later negatives must not be lost behind a cap. Snapshot readers do DB work outside `_trade_jobs_lock`. Test query count on multi-card fixtures. |
| Reason-only pass does not bind live exclusions | Confirmed ordinary pass binds both sets at `server.py:14125`; `_apply_reasoned_pass` at `:14771` lacks that bind. Include a shared pass-only helper, preserve first-progressive-write and Elo rules. `trade_pass_reason` can store a reason without a recoverable card (`:15002`); tests must use valid echoed context and must not treat a contextless reason bank as proof of a reconstructable pass. |
| Mobile reason write omits restart context and edited identity | Additional author finding: `api/declineReasons.ts:90` serializes no give/receive/target context; `TradesScreen.tsx:5698` uses the raw card ID while ordinary swipes use the edited variant. R4 includes populating the server's existing optional context and actual acted card ID. This closes the valid-client missing-card seam before relying on a boolean reason success, and prevents a reason from passing the unedited package. T7 includes serializer wiring and a real endpoint reconstruction case; no new request field or response schema. |
| Cached/provisional job dictionaries ignore updated service state | Confirmed `_trade_job_public_view` (`server.py:2994`) returns stored cards; cached/running `/generate` branches (`:13357`, `:13363`) and old-ID `/status` (`:13928`) share it. Include current exact-pass and actionable source projection on public reads/publication. Drop resolved cached interested cards as artifacts; do not guess a pre-boost score or rewrite impression history. |
| Job/session ownership can drift | `_TradeExecutionContext` (`server.py:6779`) captures account/league/format while retaining live decision sets; `/status` only compares job account before serving. The projection must use the job's captured league and current relevant action state even if the caller switches leagues. It must not use the active session's unrelated league service to filter an old job. |
| Replenishment restores only seven days | Confirmed `server.py:21793`, unlike normal `:20448`. Include narrowly shared per-type window/amnesty restoration; verify actual helper/builders with day 8/day 13 and exact boundary fixtures. Preserve malformed-stamp conservative handling and defaults. |
| Local lane reset and append-only merge can replay regardless of server fix | Confirmed append-by-ID at `TradesScreen.tsx:2173`, lane projection at `:3882`, reset at `:3908`. R4 includes exact working-set disposition, edited package identity and stale merge/reset protection. A separate mobile owner handles this after backend contracts are ratified. |
| Undo/failure and reason panel can be broken by eager removal | Confirmed held-POST semantics at `TradesScreen.tsx:2397`, flush/Undo at `:2695`/`:2707`, reason-disabled hold at `:5567`; layer-1 discards the reason response at `:5740` and keeps layer 2 attached until `commitReasonAdvance` (`:5712`). Keep reversible pending state, recognize either successful pass request and defer visual removal until the existing panel transition. T7 must prove no next-card skip, no late callback into another league, and retry after total failure. |
| Post-match Decline / broad fatigue exemption | Held. The evidence's only match is a different 1x2; `server.py:18578` decline suppression and `:5904` likes-you exemption are different policy. Preserve `test_deck_fatigue` exemptions/newest-only suppression Undo. No lowering fuzzy thresholds or near-duplicate rejection. |
| Standing offers | Their source is separate general player-for-pick intent (`server.py:3740`), not the incident's exact player-for-player historical like. Include a shared active exact-pass regression only; expiry/target semantics remain held. |

## Evidence provenance and limitations

The author inspected only the supplied local bounded extraction's aggregate/schema metadata: read-only marker, one league, 31 decisions, one match, and neither limit reached. The planner's sanitized chronology is the detailed event source. No production connection, credentials or real-user generation was used. Raw artifact data remains outside tracked docs. Exact producing job, installed build number and final impression attribution remain unknown; none blocks the synthetic incident reproduction.

Root authorized continuing the selected fix and explicitly asked to preserve D-067 amnesty/cooldown, tiers/policy/arms/history and different packages. Root also directed consistent source eligibility across readers, batched history projection and separately reviewable mechanical seams. Those constraints are captured in R1–R4; this author does not expand them into match-Decline or standing-offer lifecycle redesign.

## Scope/document reconciliation

The source plan is diagnosis and proposed scope; this PRD is the executable author contract for critique. It includes the four contradictory like readers because leaving queue/matching/Awaiting unchanged would create stale consent or prevent a fresh offer. It includes reason/cache/restoration/mobile fixes as separate bounded commits because they can independently re-show the newly passed card. They are confirmed secondary defects, not asserted incident provenance.

No analytics waiver: existing events/history suffice for new controlled evidence, without filling historical attribution gaps. No schema/flag/config change. Root owns API/LLD/architecture, the Awaiting invariant clarification, the non-obvious exact-source precedence decision, central status/index and test ledger. HLD/glossary/data-dictionary/config-reference have explicit n/a reasons in scope. D-056 replacement evidence is unit/AST/state tests, final code-walk and manual TestFlight; no Maestro/simulator work.

## Round 2 — original planner critique incorporated

Root supplied two blocking findings and two scope clarifications. The author re-read the relevant code; this round supersedes Round 1's assumption that populating context and reading the adapter boolean alone proves a pass.

| Review finding | Evidence / incorporated contract | Focused verification |
|---|---|---|
| **Blocker 1: fresh requeue still hits the ten-second same-like guard** | `database.py:5780` selects the last same-decision live row; `:5802` can suppress the fresh like even after the revised queue probe returns none. `server.py:14580` and `:14623` are two separate dedupe layers. PRD R2 now requires a narrow explicit-queue exception verified against an intervening exact own or receiver-mirrored pass after the old source row. No client bypass, new global dedupe behavior or unrelated-pass exception. | T3 executes real queue at t=0, source-own or receiver-mirrored swipe pass at t=1, requeue at t=2 and repeats. Exactly one new positive row/Elo/event for renewal; no duplicate signal/events on retries. Ordinary swipe guards remain green. |
| **Blocker 2: HTTP 200 / reason creation is not durable pass commitment** | `server.py:15002` banks a reason when no card resolves; `:15017` only applies a pass when `state.created`; `:15028` reports `passed=bool(created)` regardless of persistence. `_apply_reasoned_pass` swallows DB failure. Thus contextful retry of an already banked row never repairs the pass. PRD R3 now requires durable exact-pass verification/once-only repair on contextful progressive requests and truthful existing `passed` state; true includes verified previously committed retries, false includes contextless/failed/unverified disposition. `ok` still acknowledges the reason, with existing error/status surface. | T4 adds contextless bank→valid-context repair without swipe, failure→recovery, repeat/reordered requests beyond ten seconds, service loss and actual edited identity. Assert decision/outcome/Elo/event counts and preserved reason data. No new table or marker field. |
| Client must consume response evidence, not transport success | `api/declineReasons.ts:90` currently resolves true after any successful HTTP response. R4 changes the internal adapter to expose existing response fields and distinguish transport failure, accepts only `passed === true` as reason commitment, and sends existing optional echo fields for the actual acted/edited ID. A false/absent response does not erase an already confirmed local pass. | T7 pins 200/ok/false, missing evidence, verified true, retry true, sibling failure and delayed callbacks. Fielded clients discard the body and keep the existing companion swipe; field names/types/status codes stay compatible. Tests pinning duplicate passed false are intentionally updated while all count/invariant assertions remain. |
| Append-only local merge cannot prove removal of remote-only invalidation | R4 is narrowed to passes locally observed in the current screen/session. Remote-only invalidation of an already retained local deck with no local pass record is held. R3 still filters fresh/cached server responses; a newly loaded deck receives corrected state. | T7/manual steps explicitly state the local-action boundary and do not claim a server-only filtered snapshot deletes existing local cards. |
| Legacy recipient and normalized time evidence | R1 keeps unknown historical recipients unknown. Current roster containment is actionability evidence only. Mixed timezone/naive/equal timestamps are tested through actual DB readers and cutoff selection, preventing a pure helper test from masking SQL text-order bugs. | T2 must exercise DB-backed source/match consumers, including reordered IDs and unrelated account/league/package controls. |

The response change is explicit: `passed` means verified durable disposition state rather than “this request created a reason row.” Ordinary `/swipe` best-effort behavior is preserved and its response is not upgraded to a durable persistence guarantee. Reason repair retains the existing once-only Elo claim and outcome dedupe, makes no new synthetic action, and does not use a claimed Elo slot or in-memory mask as pass evidence. Ancillary telemetry/ranking failures do not erase an already durable decision or justify re-running all side effects.

## Focused review queue

1. Original planner verifies R2/T3's intervening-pass writer exception closes the sub-ten-second renewal blocker without relaxing ordinary retries.
2. Verify R3/R4/T4/T7's truthful passed state, durable repair evidence, idempotency and response consumption close the banked-reason blocker while keeping fielded clients compatible.
3. Confirm T2's DB-backed timestamp/legacy identity evidence and the explicit local-action-only client boundary; R1 and held policy choices are already ratified by root.

No new operator choice is requested by the author. Runtime work remains untouched until the assigned focused review is resolved; this log does not declare that review complete.
