# QA round 1 — agent A — 2026-09-06

## Summary: FAIL (1 major finding)

**F-1 blocks this batch:** reason-pass verification accepts an old same-card-ID pass from another episode, reports `passed:true`, and leaves a later renewed counterpart like actionable. This violates #419 R3/T4 despite all checked-in regressions passing. No runtime/test fix was made by QA-A. A repair requires a fresh full two-agent QA round under the feedback QA skill; this is not a ship verdict.

Independent executed results: **5,622 backend passed, 1 skipped**; **869 focused backend passed**; **95/95 mobile guard programs passed**; focused native checks **28/28** (#419) and **38/38** (#420/421); TypeScript and test-ID lint passed; **190/190 web structure checks passed**. These are this agent's runs, not inherited builder/root/QA-B counts.

## Environment and boundaries

- Sole checkout: `/private/tmp/ftf-feedback-419-421-f1uMyt/qa-a`, clean detached HEAD at tested parent **`aa4637899a9233a4afffca22f3f90847fca76a62`**. Runtime diff reviewed against **`4026ebc81eaae50b345b42421641125c5b8d413e`**. The report commit's parent is the tested SHA; no runtime changes are included in the report commit.
- Darwin arm64; Node **v24.14.1**; Python **3.12.14**, `/private/tmp/ftf-context-venv/bin/python`. Independently installed `mobile/node_modules` exists and is not a symlink.
- Read relevant root/backend/mobile/subtree CLAUDE instructions, coding guidelines, feedback skill, lessons and QA reference; mobile design-system/component instructions; all three PRD/scope/reconciliation contracts and their complete build/code-walk evidence. Reviewed every changed runtime/test file against the stated baseline. Shared reference documentation was inspected for the implemented contracts.
- Full backend used exactly the required normal provider posture: in-memory DB, no bytecode/cache, **no global player-curve pin** and no conftest change. Only the separate targeted backend/repro imports pinned the two existing DP fixtures. Tests supply isolated synthetic sessions/engines and local provider seams. No private DB material or secrets were needed.
- D-056: no simulator, Maestro, captures or physical-device execution. No production/network/user-session actions, deployment, flag changes, EAS, proposal, message or feedback closure. No web UI changed, so no local web interactive server was required; web structural regression still ran.
- The earlier denied source-disabling sabotage was not retried or bypassed. Baseline evidence below executes unchanged source. The one private repro file was explicitly authorized by parent, is outside the checkout, and changes only isolated test data/fixtures, not runtime logic.

## Executed commands and results

Commands run from the sole checkout unless another directory is stated. Every foreground session was polled to completion.

| Check | Verdict and observed result |
|---|---|
| `DATABASE_URL=sqlite:///:memory: PYTHONDONTWRITEBYTECODE=1 /private/tmp/ftf-context-venv/bin/python -m pytest backend/tests -q -p no:cacheprovider --tb=short` | PASS, **5622 passed, 1 skipped in 455.32s**, exit 0. Background receipt workers emitted missing-table diagnostics (`receipts_grade_runs`, `deck_impressions`, `user_events`); these were not pytest failures. No fixture was altered to silence them. |
| Focused 33-file backend command below | PASS, **869 passed in 37.04s**, exit 0. Includes all new backend suites and surrounding source/match/queue/reason/restoration/worker/policy/session/Win Now regressions. |
| Supplemental actual presentation helper command below | PASS, **6 cases**: zero/sparse and all-large equal-cost supply sizes 0, 1, 2, 6, 13, 17 stay stable, preserve objects/values and retain every original occurrence index. |
| All independent `mobile/tests/check-*.js` processes, command below | PASS, **95 programs passed, zero failed**, exit 0. Each file received its own Node process. |
| `node mobile/tests/check-trade-disposition.js` | PASS, **28 passed, 0 failed**, separate focused execution. |
| `node mobile/tests/check-win-now-recovery.js` | PASS, **38 passed, 0 failed**, separate focused execution of real transpiled modules/handlers and transport. |
| `npx tsc --noEmit` in `qa-a/mobile` | PASS, exit 0. |
| `bash mobile/scripts/testid-lint.sh` | PASS, lint OK, exit 0. |
| `/private/tmp/ftf-context-venv/bin/python qa/web/check_web_structure.py` | PASS, **190/190**: DS 95, TOK 1, SEO 50, A11Y 38, HYG 6. |
| `git diff --check`; `git status --short`; `git rev-parse HEAD`; `test ! -L mobile/node_modules` | PASS; clean runtime/test checkout at exact stated SHA. |
| `FTF_TRADE_DISPOSITION_BASELINE=4026ebc8 node mobile/tests/check-trade-disposition.js` | Expected original-defect RED: **0 passed, 4 behavioral failures**, exit 1. Baseline adapter discards structured `passed:false`/true evidence and edited context; original projection keeps the committed package. No missing-export/import failure counted. |
| Verified seven baseline modules exactly equal `git show 4026ebc8:<path>`; then `FTF_MOBILE_TEST_ROOT=/private/tmp/ftf-feedback-419-421-f1uMyt/419-provenance-baseline/mobile FTF_BASELINE_ONLY=1 node mobile/tests/check-win-now-recovery.js` | Expected original-defect RED: **0 passed, 7 behavioral failures**, exit 1. Modules: `src/api/{client,auth,winNow,espn,platformLink}.ts`, `src/state/useSession.ts`, `src/screens/WinNowScreen.tsx`. Failures: 15.2-second completion, stale 403 callback, wake-up copy, absent mounted-screen repair, early foreground join, premature B POST before A acknowledgement, automatic restart after uncertain POST. |
| Private F-1 reproduction below | **FAIL as expected on tested runtime**, real 200 reason route plus behavioral assertion; verified imported module paths are in `qa-a`. |

Independent all-guard runner:

```sh
node -e 'const fs=require("fs"),cp=require("child_process");const files=fs.readdirSync("mobile/tests").filter(f=>/^check-.*\.js$/.test(f)).sort();let failed=[];for(const f of files){const r=cp.spawnSync(process.execPath,["mobile/tests/"+f],{encoding:"utf8"});process.stdout.write(f+": "+(r.status===0?"PASS":"FAIL")+"\n");if(r.status!==0){failed.push(f);process.stdout.write(r.stdout+r.stderr);}}process.stdout.write(JSON.stringify({total:files.length,passed:files.length-failed.length,failed})+"\n");process.exit(failed.length?1:0)'
```

Focused backend command:

```sh
DATABASE_URL=sqlite:///:memory: PYTHONDONTWRITEBYTECODE=1 \
FTF_DP_VALUES_FILE=backend/tests/fixtures/outlook-hypotheses/dp-values-players-2026-08-09.csv \
FTF_DP_PICK_VALUES_FILE=backend/tests/fixtures/dp_values_picks_2026-08-06.csv \
/private/tmp/ftf-context-venv/bin/python -m pytest -q -p no:cacheprovider --tb=short \
 backend/tests/test_small_trade_presentment.py \
 backend/tests/test_trade_interest_disposition.py backend/tests/test_trade_disposition_replay.py \
 backend/tests/test_trade_disposition_restoration.py backend/tests/test_decline_reasons.py \
 backend/tests/test_trade_match_flow.py backend/tests/test_pass_cooldown.py \
 backend/tests/test_awaiting_dismiss.py backend/tests/test_deck_fatigue.py \
 backend/tests/test_trade_decision_idempotency.py backend/tests/test_swipe_reconstruct.py \
 backend/tests/test_bakeoff_serving.py backend/tests/test_calc_trade_queue.py \
 backend/tests/test_deck_replenishment.py backend/tests/test_standing_offers.py \
 backend/tests/test_trade_job_read_amplification.py backend/tests/test_scoring_execution_context.py \
 backend/tests/test_deck_first_session.py backend/tests/test_force_supersedes_running_job.py \
 backend/tests/test_trade_policy.py backend/tests/test_trade_policy_wiring.py \
 backend/tests/test_trade_roster.py backend/tests/test_trade_roster_wiring.py \
 backend/tests/test_bakeoff_arm_a_golden.py backend/tests/test_bakeoff_runner.py \
 backend/tests/test_bakeoff_composition.py backend/tests/test_bakeoff_challenger.py \
 backend/tests/test_engine_quality_golden.py backend/tests/test_session_init_sleeper_calls.py \
 backend/tests/test_verified_sessions.py backend/tests/test_persistent_sessions.py \
 backend/tests/test_win_now_api.py backend/tests/test_win_now_service.py
```

Supplemental T2/T7 all-large/equal-cost and sparse-supply command (unchanged production helper, same targeted fixture environment):

```sh
DATABASE_URL=sqlite:///:memory: PYTHONDONTWRITEBYTECODE=1 \
FTF_DP_VALUES_FILE=backend/tests/fixtures/outlook-hypotheses/dp-values-players-2026-08-09.csv \
FTF_DP_PICK_VALUES_FILE=backend/tests/fixtures/dp_values_picks_2026-08-06.csv \
/private/tmp/ftf-context-venv/bin/python - <<'PY'
import copy
from backend import small_trade_presentment as sp
from backend.tests.test_small_trade_presentment import helper, six_cards
print('actual module:', sp.__file__)
for count in [0,1,2,6,13,17]:
    cards = [copy.deepcopy(six_cards()[0]) for _ in range(count)]
    for i, card in enumerate(cards):
        card.trade_id = f'all-large-{i}'
    before = copy.deepcopy([vars(c) for c in cards])
    after, records = helper(cards)
    assert [id(c) for c in after] == [id(c) for c in cards]
    assert [vars(c) for c in cards] == before
    assert [r['original_index'] for r in records] == list(range(count))
    print(f'PASS T2/T7 all-large equal-cost stable supply n={count}')
print('6 supplemental cases passed')
PY
```

## Findings

### F-1 — A historical same-ID pass falsely verifies a new reason episode

- Severity: **major / release-blocking contract violation**.
- Requirement: #419 **R3**, “durable exact pass for this reason episode”; “never use an unrelated historical pass”; **T4** repair/verified response. It also prevents R1 from resolving fresh source evidence when the user's new reason is the only successful write.
- Source: `backend/database.py:6770` locks/validates the current reason row, but `:6776` queries every live historical pass for actor/league/trade ID. `:6786` accepts equal oriented sets and `:6788` returns `(True, False)` without tying that decision to this reason/impression episode or its chronology. `backend/server.py:15136` trusts that result; `:15147` skips new-pass side effects; `:15155` consumes the reason Elo claim as though a companion ordinary swipe supplied this episode's signal.
- Exact synthetic repro: seed a current owned impression/card; persist this actor's same-ID/same-package pass dated **2026-08-01T12:00:00Z**; persist the counterpart's renewed mirrored like dated **2026-09-05T12:00:00Z**; POST the actual current `/api/trades/pass-reason` with `detail:value_giving`. The older pass is expired/pre-amnesty and is not the current reason episode. Deterministic fair-package IDs and reused card identities make this a valid runtime shape, not a test-only collision.
- Expected: one real current pass after the renewed like, `passed:true` only after that commit, renewed source no longer actionable, one current pass outcome, then same-episode repeats without duplicate pass/outcome/Elo/event.
- Actual: HTTP 200 `{detail:'value_giving',elo_written:false,ok:true,passed:true,reason:'value',switched_from:null}`. There remain **only two decision rows** (the old pass and renewed like), **zero pass outcomes**, and `load_recent_league_likes` still returns the renewed source. The new pass was never persisted. Existing live-memory binding can temporarily hide this discrepancy; restoration/read-side chronology cannot honor a decision that was not written. A mobile reason-only success trusts this false acknowledgement.
- Reproduced against unchanged implementation, with database/server module paths printed and confirmed. The script stops at `assert len(rows) == 3`; later expected retry assertions are deliberately not represented as executed success.

Executable private artifact: `/private/tmp/ftf-feedback-419-421-f1uMyt/qa-a-reason-episode-repro.py` (synthetic data only). From the tested checkout:

```sh
DATABASE_URL=sqlite:///:memory: PYTHONDONTWRITEBYTECODE=1 \
FTF_DP_VALUES_FILE=backend/tests/fixtures/outlook-hypotheses/dp-values-players-2026-08-09.csv \
FTF_DP_PICK_VALUES_FILE=backend/tests/fixtures/dp_values_picks_2026-08-06.csv \
PYTHONPATH=. /private/tmp/ftf-context-venv/bin/python \
 /private/tmp/ftf-feedback-419-421-f1uMyt/qa-a-reason-episode-repro.py
```

Existing `test_decline_reasons.py:483` covers an ordinary pass near an existing/new reason and correctly protects duplicate Elo, but does not contrast that companion pass with an expired previous episode plus a fresh positive. The missing negative control explains the green suite. Repair must retain same-episode/restart/reordered retry and ordinary-swipe idempotency, rather than simply accepting every old exact pass or emitting a new pass on every refinement. QA made no fix.

Shared reference divergence: `docs/api-reference.md:264` and `docs/architecture.md:235` describe truthful verified/repairable exact-pass state; F-1 shows that claim is not met for a new episode reusing a historical card ID. The documentation describes the approved requirement and should not be weakened to bless this defect. Other inspected shared references matched the bounded implementation; no unrelated documentation blocker found.

## Requirement/test matrix and independent code walk

Line references below identify the tested parent, not just the builder report's historical SHA. “PASS” is bounded mechanical/code-walk evidence, not device execution or proof that every prospective sabotage ran. F-1 remains an overall FAIL.

### #419: R1–R4 and T1–T8

| Requirement/test | Verdict | Production trace and exercised evidence |
|---|---|---|
| R1 / T1 incident and T2 chronology | PASS for source reader contract | `database.py:5901` normalizes each selected decision to UTC with numeric row-ID tie ordering and keeps oriented sets/actor/league; `:5972` checks own and receiver-mirrored passes; `:5983` excludes resolved/retracted/non-orderable positive evidence, and `:6009` selects current source rows. Discovery expiry/amnesty is separately `:5947`, so expiry cannot revive an old like. Real injector `server.py:3583` consumes `database.py:6067` before cap/boost; no history mutation or fabricated recipient. `test_trade_interest_disposition.py:49`, `:68`, `:79`, `:95`, `:107`, `:130`, `:165` execute the incident, deliberate renewal, receiver-consent distinction, actor/league/package/orientation controls, malformed/tied/offset/naive dates, withdrawal/no fallback and UTC cutoff. |
| R2 / T3 readers, renewal, counts and query shape | PASS | `database.py:9040` filters each candidate's own exact source before unchanged exact/fuzzy matching; `:6092` windowless queue probe and `:5992` renewable evidence preserve legitimate new consent. `server.py:14758` probes before writes, `:14808` supplies server-derived queue context; the narrow writer exception does not globally disable ordinary ten-second dedupe. Real `/queue`→`/swipe pass`→`/queue` at 0/1/2 seconds in `test_calc_trade_queue.py:419` runs both own and receiver pass, then repeated calls. `database.py:9425` preserves Awaiting candidate cap and current roster resolution; unknown/conflicting ownership cannot supply an interested counterparty, known player anchors mixed pick packages. Awaiting/R4/summary regressions, fuzzy/D-170 controls, ordinary swipe idempotency, and `test_trade_interest_disposition.py:173`/`:214` query assertions ran. |
| R3 / T4 reason bank, repair, binding, once-only effects | **FAIL — F-1**; other existing mechanical cases pass | `server.py:14913` resolves owned/surrogate reason identity; `:15120` banks reason separately; `:15136` verifies/repairs; `:14954` shared bind touches relevant live formats only when pass verified. `database.py:6756` serializes check/write but fails episode association as above. Stored claim + signal persistence and `server.py:15160` allow deferred Elo only after a pass; no repeated outcome is intended. Existing tests `test_decline_reasons.py:393`, `:417`, `:439`, `:460`, `:472`, `:483` cover actual edited repair, failed persistence/recovery, Elo rollback, formats/alias, old-league fences and swipe ordering. They do not rescue F-1. |
| R3 / T5 snapshots and T8 publication wiring | PASS when the durable pass/source history exists | Public job view `server.py:2997` calls `:3032` using the job key. One history query plus optional owned same-league source-link query (`database.py:6037`) distinguishes specific old source from a newer identical package; null legacy links use current exact evidence, not invented provenance. Generate/status copy under lock then project outside it (`server.py:13528`, `:14117`); pending cards use their own league (`:14147`). Final cut `:7969` precedes both impression writers, leaving survivors/frozen history untouched. `test_trade_disposition_replay.py:52`, `:84`, `:96`, `:112`, `:151`, `:184`, `:194`, `:211`, `:253`, `:279` exercise cached/running/new-ID/held worker, cross-league, expired source, explicit provenance and fail-closed controls. Standing intent remains distinct but active exact passes win. |
| R3 / T6 restoration and replenishment | PASS | Shared `server.py:3095` loads discovery keys; interactive `:20588` and headless `:21889` use it, preserving separate configured pass and seven-day like windows. `:22004` projects before inventory counts. `test_trade_disposition_restoration.py:27` executes both builders at day 8/day 13, fractional boundary, amnesty on/off, malformed and UTC-offset cases; replenishment/cache/force/context tests ran. No permanent organic ban or frozen-row rewrite. |
| R4 / T7 acted identity and source cursor | PASS for local mechanism; upstream F-1 not hidden | `tradeDisposition.ts:11` keys actor/league/known counterpart/oriented sets, not card ID or values. `:52` projects edited cards against the persistent source coordinate and holds the current reason card through layer 2; `:72` maps visible navigation back to source indices. `TradesScreen.tsx:293`–`:337` owns a shared account/league session record and epoch; `:1964` freezes actual edited card, `:5849` echoes it; `:4037` projects every render. `:3659`/`:3702` retain pre-single-pin source cursor. Native 28-case guard executes new IDs/same-length/reordered/lane/restored/deferred projection and actual browse handlers. |
| R4 / T7 independent evidence, Undo, retry and fences | PASS for local mechanism | `tradeDisposition.ts:31` accumulates true reason evidence; `:38` independently accepts acknowledged swipe. `TradesScreen.tsx:2832` flushes captured held attempt; `:2846` Undo commits/sends nothing. Four reason paths observe responses at `:5897`, `:5929`, `:5940`, `:5959`; `:5868` advances once, not on layer-2 bank. `:2453` total-failure repair waits for sibling evidence without blocking the mutation and avoids rollback after a late success. Context/deck/newer-action guards at `:1970` and current action comparisons fence stale callbacks; `:6104`/`:6157` restore failed browse removals once and preserve later fronted cards. `:8557`/`:9061` retry key re-arms panels. All 28 native checks and decline/browse/Undo/shop regressions ran. |

Scoped-history cost is explicitly disclosed, not a production-speed claim: `database.py:6017` selects 11 columns bounded by chosen league/actor but does not apply a textual date cutoff, so older scoped rows are scanned before correct normalization. The builder code walk documents this tradeoff at lines 77–85. QA reran the 5,000-row/20-card one-query checks and linked-source two-query check; no per-card or per-arm query and no DB work under the global job lock was found. This does not establish a production latency bound.

### #420/#421: lifecycle requirements and T1–T12

| Requirement/test | Verdict | Production trace and exercised evidence |
|---|---|---|
| T1 one exact missing-context repair; T2 shared readiness | PASS | Entry routes remain `LeagueSummaryScreen.tsx:1518` and `TradesScreen.tsx:6674` → `RootNav.tsx:787`. Account/league keyed `WinNowScreen.tsx:55`, effect `:90`/`:99` calls `useSession.ts:728`. Attempt captures before await, joins `leagueSession.ts:80`; `useSession.ts:756` requires typed exact 409/body code, `:758` performs one force, `:759` one logical GET replay. Replay errors escape, not recurse. Mounted-screen and real helper fake-clock cases prove delayed init joins without early reads and available/unavailable restoration without another tap. |
| T3 failed repair/explicit Refresh and one cache retry | PASS | `leagueSession.ts:118` only successful current completion advances readiness; failures do not throttle as success. Screen `:97` consumes actual Refresh authorization. `auth.ts:517` and `:611` retain exactly one recognized cache-missing retry under original lifecycle/deadline. Executed refusal/second-409/cache-denial/explicit recovery cases terminate; no new POST/general write recovery. |
| T4 acknowledged same-token A→B; T5 supersession/detach | PASS (bounded client guarantee) | `leagueSession.ts:38` advances generation before asynchronous selection, `:41` binds account/revision, `:45` captures intent. `:89` abandons obsolete pre-POST preparation; `:97` waits prior lane settlement; actual submit `:105` fences dispatch. Current seed/readiness only at `:116`. `useSession.ts:386` and `:663` invalidate synchronously; consumer subscription `:744` detaches projection on new intent. `leagueSession.ts:130` caller wait abort does not cancel another valid shared consumer. Executed prepared-A/current-B and dispatched-A→ACK→B, two-consumer abort, sign-out, token replacement, late failures and verification tests; full lifecycle guards supplement rapid intent/blur/flag paths. No unconditional server final-writer guarantee is claimed. |
| T6 installed guard restoration | PASS | `test_verified_sessions.py:538` persists/restores a synthetic verified token, removes in-memory state, calls the installed route/guard: exact 409 and zero forecast calls before actual authorized init, typed unavailable afterward, foreign league denied. Full verified/persistent-session/Win Now API suites retain invalid membership/unverified/signed-out denials. No fake-ready replacement was used as restoration proof. |
| T7 exact 30-second logical GET and composed deadline | PASS | `client.ts:332` normalizes exact method/path; only projection GET gets 30 seconds, lookalikes/POST/ordinary requests retain rules. `:456` captures deadline once before retries; `:298` bounds token/preparation/fetch/body/backoff; guards at `:535` and `:590` reject late resumed dispatch/publication. `useSession.ts:728` starts immutable 90 seconds; `leagueSession.ts:84` uses earlier operation/caller budget and never extends on join. Executed 15.2-second completion, exact 30-second expiry, token/body stalls, retry/backoff and suspended-JS checks; unchanged search polling remains separate. |
| T8 error matrix and current/stale 401/403 | PASS | Current 401 receipt at `client.ts:614` is an exact revision transition recognized by `:212`, preserving its own server error after clearing only its current token; callback `:620` cannot follow replacement sign-in. `:633` fences verification callback before store publication with current token/revision/signal/deadline/context. `useSession.ts:765` identity-only error guard suppresses stale results while retaining current timeout/401. Mounted Win Now, picker and resync 401 tests prove loading releases and message survives. Existing parser/error guards plus 38 recovery tests cover truthful available/unavailable/refusal/network/service/timeout handling and no obsolete wake-up diagnosis. |
| T9 feature/source/freshness/search/evaluation behavior | PASS regression/code walk | `WinNowScreen.tsx:90` retains season-flag effect dependency and cleanup; baseline handler alone changes, while source readiness/expiry, title gates, independent search/evaluate/decision epochs remain. All existing Win Now/forecast mobile guards and backend API/service/forecast suites ran in full regression. `api/winNow.ts:7`–`:16` writes/search remain outside automatic recovery. No missing-source-as-zero or stale data fallback added. |
| T10 persistent uncertainty and fixed authorization/deadline | PASS (held server limit explicit) | `leagueSession.ts:91` latches uncertainty and clears readiness after ambiguous dispatched init; `:99` rejects already-queued selection. `:59`/`:75` require a later explicit authorization, not merely force/focus/new generation; `:133` dates Connect intent before target resolution. `useSession.ts:600`/`:601` and `:650` carry original deadline/ticket. Executed repeated automatic foreground/refocus-style entry, throttle expiry, late A settlement, explicit retry, relatch, replacement token, delayed Connect and near-90-second repair composition. No automatic dependent init/read or success publication follows uncertainty. |
| T11 all writers and imported account-only compatibility | PASS | Sole initializer/seed owner `useSession.ts:694`; callers are revalidate `:358`, switch `:444`, Connect `:592`, picker `LeaguePickerScreen.tsx:436`/`:443`, resync `LeagueScreen.tsx:144`/`:163`. Picker init precedes guarded league persistence/Main; errors use identity-only ownership to release current busy state. `auth.ts:258` requires shared control for actual init POST and `:311`/`:336` fences verification mirrors. Imported builders at `:421` precede Sleeper account-only guard `:449`; actual ESPN/MFL/Fleaflicker modules exercised, real league works while `no_league` makes no protected read. Existing seed/ownership guards retain behavioral contracts. |
| T12 telemetry/dependency/scope | PASS | `client.ts:287` exact neutral timeout; existing outer failure boundary emits once per logical request, caller abort/supersession excluded. Event-spy case strips query IDs, omits background duration and keeps allowed fields only; no lane-only fabricated HTTP event. `auth.ts` callback plumbing avoids API→store import; no new telemetry/schema/config for recovery, write replay, forecast/ranking or web UI change. Version-only native metadata is consistent 1.17.1. |

Mechanical coverage is executed production modules with deterministic seams, not a complete physical UI or every possible timing interleaving. Named prospective mutation variants such as every missing-generation boundary, rapid A→B→A at every await, and removed installed backend guard were **not separately sabotaged** by this reviewer; their structural/behavioral coverage is not mislabeled as executed sabotage. The approved safety exception and device boundary remain applicable.

### Small-player packages: R1–R7 and T1–T8

| Requirement/test | Verdict | Production trace and exercised evidence |
|---|---|---|
| R1/R7 / T1/T3/T7 eligibility, exemptions and one knob | PASS | Only new Float default `simple_player_presentment=0.0` in `trade_service.py:1293` and `database.py:2942`; exact numeric mode at `small_trade_presentment.py:18`. Single call `server.py:7857` requires successful live policy, mode on, no give/receive/partner/raw-selection exemption, no active ghost, no dark/single-arm validation. Manual/Win Now/asset-ideas paths never call it. `test_small_trade_presentment.py:100`, `:127`, `:416` exercise both formats/group modes, F1 on/off and all worker exclusions; arm/generator goldens remained unchanged. |
| R2/R3 / T1/T2/T3 stable six-slot full-class permutation | PASS | `small_trade_presentment.py:25` uses real pick parsers/canonical pseudo-asset semantics and known player positions; unknown/malformed/pure-pick locked, mixed picks count zero and never break ties. `:57` requires actual source arm, group when applicable, card lane/basis and final policy lane; valid ungrouped nulls accepted, missing expected metadata locks. `:96` stable sorts only identical class positions in disjoint six absolute slots using max-side/total players; ranks travel, no rank in class. No asset/value/eligibility/object mutation; every occurrence retained and movement ≤5 before removal. `test_small_trade_presentment.py:136`, `:167`, `:174`, `:188`, `:209`, `:218` cover holes/short windows/duplicates/all-large/known special locks and missing attribution. Actual three-arm first-three load **16→7**, first-six **23→23**, each fixture move three slots, unchanged arm sequence. |
| R4 / T5 capture, cache and rollback | PASS | `server.py:3119` captures mode/version/base once; `:3127`/`:3133` compatibility is used by generate completed/running `:13528`, pregen `:21158`, replenish `:21963`. Kickoff `:8234` does not recapture supplied mode and `:8284` stores it. Raw receive with targeting off remains presentation-exempt, bypasses organic mode-on cache and cannot seed its index (`:8292`), without changing ignored generator pins/default .75 fairness. `test_small_trade_presentment.py:296`, `:322`, `:354`, `:372`, `:395`, `:480` execute hot flip, missing/unchanged/0↔1 completed/running/cache modes and raw selection controls. Old-ID polls never re-sort. |
| R5 / T4 first evaluated and final publication | PASS | Final policy evaluation succeeds before `server.py:7857`; first evaluated snapshot `:7868` consumes the single permutation before releasing safety protection `:7879`. Final #419 projection `:7969` then removes authoritative dispositions without reranking/refill. Republish `:7988` is independent of F1. Held real worker test `test_small_trade_presentment.py:237` inspects first publication, injects an actual pass, resumes through actual final projection/writers with all four F1/suggestion-telemetry combinations and helper count one. |
| R6 / T6 per-occurrence frozen provenance | PASS | `small_trade_presentment.py:133` retains the exact surviving occurrence subsequence, not an identity-keyed collapse/stale zip. Legacy writer `server.py:8020` and F1 `:8054` enumerate identical survivors; feature positions `:4889`, serving version `:4982`/`:5022` preserve original arm/group ranks and actual attribution/propensity, separate from valuation policy. Held duplicate fixture proves original indices `[3,5,4,2,1,6,7]`, final indices `0..6` equal F1 index. Locked unattributed first-row case `test_small_trade_presentment.py:456` retains uniform presentation/version with unknown counts null; later poll removal cannot rewrite frozen rows. |
| R7 / T7/T8 safety, supply, logging and regression | PASS mechanically, integrated release still blocked by F-1 | Mode off/exempt outputs unchanged; no generator/profile price/normalization/fairness edits, hard cap or supply relaxation. Empty/failed live policy remains honest. Existing suggestion telemetry gate controls candidate-row logging independently; F1 off does not synthesize F1 rows; no private value/token/identity telemetry added. Full backend plus focused worker/roster/policy/standing/bakeoff/cooldown/context suites and all mobile/web checks passed. The package pass-removal guarantee requires actual durable input; it cannot repair F-1's missing new decision. |

## RED evidence qualification and unrun work

- Independently reran the **four #419 mobile** and **seven Win Now** original-defect behavioral REDs on verified unchanged baseline modules, then current GREEN. This proves those guards detect the original defects, not every future possible sabotage.
- Reviewed the build evidence's unchanged-runtime backend #419, package **26 + 2 raw-receive** REDs, baseline path verification and discarded invalid attempts. Those backend baseline runs are **reviewed builder evidence, not QA-A reruns**. Current corresponding backend cases ran both focused and full. No missing source/export/import was counted as a meaningful failure.
- Prospective source-resolution/bind/publication/restoration/transport/guard/attribution sabotages were **UNRUN under the approved safety exception** unless specifically recorded as unchanged-baseline behavior above. No blanket every-test sabotage qualification or physical runtime proof is claimed. The fresh F-1 behavioral RED is additional independent evidence.
- Physical TestFlight, production latency/parallel read measurement, CI-on-pushed-SHA, Apple processing/tester availability, and real staged knob activation/rollback are **UNRUN here**. The conditional anonymous corpus shape diagnostic was reviewed only and is not acceptance or a production no-regression promise.
- Held limits are not new findings: remote-only invalidation of retained mobile deck without local pass record; standing/post-match lifecycle; ordinary-swipe best effort; lack of server cancellation/final-writer guarantees after ambiguous accepted init; inability to unconditionally cancel/order already dispatched native storage; corpus conditionality. Owner's unattended-ship authorization is conditional on successful QA and does not fabricate any of these observations.

## Consolidated TestFlight checklist — operator-run, ALL UNRUN

Use authorized synthetic/staging accounts A/B/C and known packages only. Record physical device/iOS, binary version/build, backend SHA, effective test configuration, exact entry route and sanitized outcome/timing for each step. No real provider proposals, production learning writes or production restart merely for QA. **Do not run these against the failing build as a ship acceptance sign-off.**

1. **Acquire → Find a Trade / results:** seed old A like → later B pass that is now expired/amnestied; generate fresh and cached results. No “They're interested” card/boost from that old source. An otherwise eligible organic same package may return unbadged; changed-package control remains eligible. Repeat both scoring formats.
2. **Acquire → results → Value / Fit / Neither reason tiles:** create fresh exact A interest, pass as B through layer 1 only, layer-2 selection, layer-2 bank and layer-2 Send separately. Current layer 2 stays usable and the deck advances once. Change Value/Outlook/All lanes, leave/reopen, Find more, refresh, change scoring format; confirmed exact package remains absent with no next-card skip.
3. **Acquire → editable card / browse More Offers:** edit one side and pass the edited package. Only actual edited identity is removed, raw original/different counterpart/package remains independent. Inject total write failure, then retry; browse removal/tally restore once and reason controls are enabled. A late successful reason plus failed swipe must not rewind/toast over success.
4. **Acquire → results with reason capture disabled in staging:** pass then Undo in the held window. No pass POST/history commitment, original card actionable. Expire the hold separately and verify captured original action is written. Repeat after account/league/deck switch and with a newer action; old callbacks cannot change current cursor/toast/panel.
5. **Acquire → reason capture with controlled route failure:** contextless bank and failed pass persistence return `passed:false` without local confirmation; valid-context retry repairs once. Verify **F-1 regression**: old same-ID pass → renewed counterpart like → new owned impression/current reason; a new current pass resolves renewed interest, one outcome/signal as allowed, repeated refinement does not duplicate it. Repeat after service loss.
6. **Acquire → held/running result, then old job:** pause staged generation after first evaluated publication, pass a displayed package, release and poll/reopen old job. Passed package disappears before final impressions, survivors keep order. Switch league/format before polling; old job remains bound to its captured identity. Restore interactive/headless sessions containing an eight-day pass; suppression/counts agree.
7. **Calculator → In-league partner → ✓; Matches → Awaiting them / mutual:** A queue → own pass → requeue within ten seconds, then receiver mirrored-pass variation. Renewal creates one fresh offer; immediate retries no extra signal/row/event. Awaiting/counts lose resolved old offers. Receiver re-like alone cannot match resolved source; source subsequently supplies new consent through ordinary swipe and normal match can occur. Active cooldown is not lifted by a new positive. Repeat reversed/changed-package controls.
8. **League → Season projections & Win Now** (`league-summary.win-now`), then **Acquire → Win Now** (`trades.win-now`): enter during slow initialization. Loading and Back stay responsive, no premature protected read; acknowledged init yields actual available data or truthful refusal without another tap.
9. **Win Now → Refresh after staging session restoration:** restore persisted verified token across authorized staging process rollover, without sign-out. One bounded exact missing-context repair and logical replay; a second exact refusal terminates. Other 400/401/403/404/409 refusals do not trigger repair.
10. **Win Now → Back → League picker / account sign-out:** defer preparation, then select B; separately delay already-dispatched successful A init and select B. A must not overwrite B's cache/league/navigation/data/busy/error. Repeat rapid A→B→A and sign-out/replacement login. Delayed old 401 must not clear replacement token; delayed old verification 403 must not change its banner; current 401/403 still show their authoritative state.
11. **Win Now after ambiguous staged init timeout:** queued B fails honestly. Refocus/remount/foreground and let throttle expire without tapping Refresh: zero new init/projection requests, no ready hint. A new deliberate Refresh/selection after the uncertainty allows one bounded new attempt; a second ambiguity relatches. No message claims the server request was canceled or final ordering is guaranteed.
12. **Win Now → baseline loading/error/Refresh:** controlled response at about 16 seconds renders; projection beyond 30 seconds times out neutrally. Stall token/body/provider/lane/repair near the whole 90-second deadline; no reset or late dispatch/publication. Leave at deadline: no late alert. Test unavailable live-week/source, offline, 503 with/without server message and recovery; no zero standings or enabled search on unavailable data. Verify Dynamic Type and VoiceOver for existing controls.
13. **League picker → Connect; League → ESPN resync:** slow target/token/provider work ends with truthful retryable timeout and released busy state; init acceptance precedes Main. Old delayed Connect gesture cannot count as new uncertainty authorization. Imported ESPN/MFL/Fleaflicker account-only real leagues retain existing builders/selection; sentinel/no-league accounts make no projection read.
14. **Win Now with valid current baseline:** objective/budget/protection/title gates, source expiry, search cancel/refresh, editable evaluation, independent decision action and Back remain unchanged. Observe an ordinary read during cold forecast work and record contention, not a throughput-fixed claim.
15. **Acquire → new organic Find a Trade, controlled mode 1:** known six-card three-arm fixture serves `1x1,1x2,1x1,3x3,1x4,2x3`; larger cards remain reachable and values unchanged. Repeat both formats/grouped and ungrouped supply. Pick-heavy mixed packages count only players; pure-pick/unknown/special cards retain protected positions. No new visual panel/badge/value treatment.
16. **Acquire explicit SEND / GET / named partner; Calculator / More Offers:** original explicit search behavior is unaffected, including nonempty raw GET selection while receive targeting is disabled. No organic mode-on cache returned for that exempt raw selection, and the explicit result does not replace organic cache inventory.
17. **Acquire retained result across staged mode 1→0 and 0→1:** old-ID/Back/poll order stays captured; fresh generation obtains compatible mode/version. Pass during publication with F1 on and off: no refill/re-sort, final survivor indices and impression provenance agree. Operator checks sanitized server evidence, not private values/tokens. Record activation/rollback separately from binary availability.

All 17 steps remain **UNRUN**. The owner's authorized unattended release path can resume only after the blocker is repaired and the required new QA round succeeds; it is not evidence that a device test, Apple processing, or deployment has happened.
