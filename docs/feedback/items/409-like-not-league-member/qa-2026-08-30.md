# QA findings — FB-409 (2026-08-30)

**Under test:** `11c8903c` "fix(#409): synthesize the caller member on the trade-queue route (G-063)" on `bd83fe94`.
**Verdict: PASS** — no blocking findings. Fix is correct, minimal, sabotage-proven; the new
tests are genuinely production-shaped.

*(Saved by the orchestrator from the QA agent's report. Line cites below are as at
`11c8903c`; the orchestrator's follow-up commit added a 4-line comment at `:13328`, shifting
`/api/session/init` cites by +4 — see the corrected cites in `living-memory/GOTCHAS.md` G-063.)*

## Suites (re-run independently, foreground)

- `pytest backend/tests/test_calc_trade_queue.py -q` → **33 passed in 4.02s**
- `pytest backend/tests -q` → **4478 passed, 1 skipped in 336.40s** (baseline 4471 + 7 new; no new skips/xfails)

## Sabotage (re-proven independently)

Reverted ONLY the synthesized-member fallback → **5 of 7 new tests RED**, each reporting the
fielded symptom verbatim: `{'queued': False, 'reason': 'not_league_member', 'detail': 'caller
is not a member of this league'}`. Restore → **33 passed**, tree clean.

The **2 that stay green under sabotage are exactly the two that EXPECT `not_league_member`**
(`test_prod_shape_cannot_queue_a_trade_with_yourself`, `test_prod_shape_unknown_opponent_still_refused`)
— the no-over-permit guard, correct in both directions.

## Per-test shape audit (the crux — a test-shaped fixture hid this bug for 8 days)

All seven new tests build a genuinely production-shaped session (caller ABSENT from
`members`; caller roster reachable only via `sess["user_roster"]`), verified against the real
session builder (`for opp in opponent_rosters` + the DB merge's
`| {user_id, league_user_id}` exclusion) and the three mobile clients that filter the caller's
own roster out before sending. **Five would have caught the original bug**; the two that would
not are the deliberate no-over-permit guards, honestly documented as such. No test is
self-satisfying — none asserts the route's output back at itself, none passes because the
fixture was bent to fit. `test_prod_shape_give_side_still_checked_against_my_roster` is the
anti-wildcard test: it proves the synthesized roster is the session's *real* roster, not `[]`.

The caller-present `harness` fixture is retained for the 26 pre-existing cases — correct: that
shape must keep working; the bug was that it was the *only* shape.

**Per-perspective claim verified:** the likes-you mirror test must build the OPPONENT's league
(`members=[ME]`) because exclusion is per-perspective — confirmed against
`_inject_likes_you_cards_impl` (`:3376`/`:3405`/`:3415`), whose viewer roster arrives as a
separate kwarg, never from `members`.

## Adversarial review

- **(a) `caller_roster` on all paths — correct.** Assigned before the guard, so always bound:
  picks-unavailable → raw session roster (symmetric with `opponent.roster`, also un-injected);
  `_inject_owned_picks` raises → keeps pre-assignment value, no partial state; success → the
  previously-discarded `_ur`, built with the same `_strip` + `pick_assets` as every member's
  roster. No session pollution (fresh `list(...)` copy in, rebind not mutate inside).
- **(b) No field read that the synthesized object leaves wrong.** Exactly three consumers
  repo-wide: `is None`, `.user_id`, `.roster`. Everything downstream of the mirror-reason
  helper takes the **opponent's** identity or plain id lists. `elo_ratings={}` /
  `has_rankings=False` are never read on this object (the quality ladder prices off `seed_map`).
- **(c) Self-trade guard intact, by construction.** Synthesis fires only when the caller is
  absent from `members_by_id`; `opponent` is drawn from that same dict, so a self-trade attempt
  necessarily yields `opponent is None` and refuses on the other arm.
- **(d) No shared-state leak.** Fresh local `LeagueMember`; no append, no dict write, no
  `sess["league"].members` mutation — pinned by `test_prod_shape_does_not_mutate_league_members`.
- **(e) Same-defect sweep:** three other sites found, all pre-existing and non-blocking
  (below). Everything else that iterates `.members` is exclusion-aware and correct.

## G-063 FORMAT.md compliance

All checks pass (index row matches entry, H2/H3 structure, ISO date, next-ID = max+1, template
section intact). Two cite corrections were applied by the orchestrator afterwards (N-2).

## Findings

**BLOCKING:** none.

**NON-BLOCKING**
- **NB-1 — ship-gate bookkeeping:** TEST_LEDGER / CHANGELOG entries owed before merge. *(Written by the shipping session; this document is the evidence.)*
- **NB-2 — `GET /api/trades/standing-offers` carries the same defect** (`:17054-17063`): `own_roster` is always `None`, so the `stale` flag is permanently false — a standing offer whose player was traded away still shows Active. Display-only and fail-open by design. Fix: read `sess["user_roster"]`.
- **NB-3 — match-accept push shows a raw account id** instead of a display name (`:17301-17303`, `:17377-17379`). Same class, cosmetic.
- **NB-4 — co-owner mirror gap, newly reachable:** the decision row is written with `sess["user_id"]` (account key, byte-identical to `swipe_trade`) while the queue predicate resolves the caller as `_league_user_id(sess)`. For a **co-owner**, the opponent's members list carries the roster's primary `owner_id`, so the mirror silently drops the queued package despite `queued: true`. Pre-existing in the organic like path and shared with `swipe_trade`; this fix makes the path reachable, it does not introduce the gap. Sole owners unaffected.

**NOTE**
- **N-1 — `sess["username"]` doesn't exist in a real league session** (`display_name` is the populated key). Harmless since the field is never read. **Fixed by the orchestrator** in a follow-up commit.
- **N-2 — G-063 line cites had drifted.** **Fixed by the orchestrator** and re-verified line-by-line against source.
- **N-3 — pre-existing `G-062` ID collision** in GOTCHAS.md (two entries share the id). Not caused by this work; needs a reference sweep before renumbering.
- **N-4 — `_inject_owned_picks` partial-mutation asymmetry** on its exception path. Fail-closed; unchanged by this fix.
- **N-5 — `test_prod_shape_does_not_mutate_league_members` is coupled** to the happy path, so it reds for the general reason under sabotage; its own assertion remains a genuine guard against the tempting wrong fix (appending to `members`).
