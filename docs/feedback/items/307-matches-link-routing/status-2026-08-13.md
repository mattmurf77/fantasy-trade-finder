# #307 + #308 — build status (LeagueHome group, 2026-08-13 wave)

**Base:** worktree `wave-league` @ `origin/main` `60fccc7` + plan commit `6368e31`.
**Build commits:** `5b2fa94` (production), `246ac62` (test suites), Maestro/docs commits after.
**Backend:** zero changes, as planned.
**MatchesScreen.tsx:** untouched, per ownership — the §4.3 receiver is the Matches group's build; `flows/league/07-matches-tile-scoped.yaml` is merge-gated on it.

## Changes at file:line

| File:line | Item | Change |
|---|---|---|
| `mobile/src/screens/LeagueScreen.tsx:642,650` | #307 | both tiles navigate with `{ segment, leagueId, at: Date.now() }` — exact §4.3 shape (`leagueId` = active league, non-null in the tiles' render path) |
| `mobile/src/screens/LeagueScreen.tsx:637,645` | #307 | tile testIDs `league.matches-mutual-tile` / `league.matches-awaiting-tile`; file-local `StatCard` (:1073-1082,1091) gains an optional `testID` passed to its `Pressable` |
| `mobile/src/screens/LeagueScreen.tsx:237-239` | #308 | `activeFormat` hoisted above the contrarian query (was :251); `queryKey: ['league-contrarian', leagueId, activeFormat]` — the latent stale-verdict cache fix, matching the sibling `progress` key |
| `mobile/src/screens/LeagueScreen.tsx:833-834` | #308 | `foldNeeded` / `foldFormat` wired from `contrarianQuery.data` |
| `mobile/src/api/league.ts:374-399` | #308 | `getContrarianLeaderboard` return type gains optional `ranked_users`/`needed`/`format`; insufficient branch threads them from the raw payload (additive — existing consumers unaffected) |
| `mobile/src/utils/leagueUnlocks.ts:21-47` | #308 | `CONTRARIAN_UNLOCK_USERS = 3`, `FOLD_FORMAT_LABEL` (mirrors TopBar's tile labels), pure `contrarianFoldLine(needed, format)` — plan §3.2(b) verbatim shapes/copy |
| `mobile/src/components/LeagueProgressModule.tsx:43-44,114-115,222-226` | #308 | `foldNeeded`/`foldFormat` props; fold `Text` gains `testID="league.progress-foldline"` and renders `contrarianFoldLine(foldNeeded, foldFormat)`; header comment updated, conflation warning kept. Compact variant unchanged (never renders the fold line) |
| `mobile/.maestro/flows/league/07-matches-tile-scoped.yaml` (new) | #307 | scoped landing + `at` re-fire contract; header marks it merge-gated on the Matches group's chip testIDs; authored-not-run (D-P1-08) |
| `mobile/.maestro/flows/smoke/09-league.yaml` | #308 | one-line `assertVisible: id: league.progress-foldline` — the plan §6 waiver is **VOID** by its own clause: `standard` seeds exactly 2 in-format ranked users (`qa_standard` + `qa_opp_ranked`) < 3, so the fold line renders in the hermetic fixture as-is |
| `mobile/scripts/testid-lint-allow.txt` | #307 | `matches.league-chip.*` allow entry, note naming the constructing edit (MatchesScreen chip row, Matches group, §4.3) |
| `mobile/src/components/CLAUDE.md`, `mobile/src/utils/CLAUDE.md` | #308 | map rows updated per plan §7 |
| `docs/feedback/items/307-matches-link-routing/scope.md` (new) | both | fast-track scope block; analytics waived per plan §3.4/§4.5; #308 Maestro waiver voided as above |

## Sabotage matrix (all executed, actual output)

Harness: apply mutation → run suite → restore → `git diff --quiet` guard per
mutation (apply-verified: harness errors if the mutation didn't change the
file). All 8 RED as required, all restores clean.

| # | Sabotage | Suite | Verdict + first failing assertion (actual) |
|---|---|---|---|
| S1 | Early-return the shipped static string | check-league-unlocks | RED — `FAIL  S1 (1, sf_tep) names the live remaining count: Leaderboards and contrarian ranks appear here once 3 leaguemates have ranked.` |
| S2 | Hardcode the remaining count (`— 1 more to go` for any `needed`) | check-league-unlocks | RED — `FAIL  S2 needed=1 and needed=2 produce different sentences: Leaderboards and contrarian ranks appear once 3 members have ranked in SF TEP — 1 more to go.` |
| S3 | Drop the format clause (`where = ''`) | check-league-unlocks | RED — `FAIL  S3 (1, sf_tep) names "SF TEP": Leaderboards and contrarian ranks appear once 3 members have ranked — 1 more to go.` |
| S4 | `CONTRARIAN_UNLOCK_USERS = 2` | check-league-unlocks | RED — `FAIL  S4 CONTRARIAN_UNLOCK_USERS = 3: expected 3, got 2` |
| S5 | Remove `activeFormat` from the contrarian queryKey | check-contrarian-format-key | RED — `FAIL  S5 contrarian queryKey is ['league-contrarian', leagueId, activeFormat]: the key lost activeFormat — a format toggle now serves the other format's cached verdict` |
| S6 | Delete both fold props, comment left in place (comment-strip proof) | check-contrarian-format-key | RED — `FAIL  S6 foldNeeded is wired from contrarianQuery.data?.needed: the fold line silently reverts to the format-blind fallback copy` |
| S7 | Drop `leagueId` from the awaiting tile's navigate (the half-fix) | check-matches-tile-league-param | RED — `FAIL  S7 navigate('Matches', {segment: 'awaiting', …}) carries segment + leagueId + at: params were: {segment: 'awaiting', at: Date.now()}` |
| S8 | Strip the awaiting tile's testID | check-matches-tile-league-param | RED — `FAIL  S8 tile testID present: league.matches-awaiting-tile: flows/league/07-matches-tile-scoped.yaml taps this id` |

Both structural suites strip `//` and `/* */` comments before matching, so a
wiring surviving only in prose cannot pass (S6 exercised exactly that: the
explanatory comment stayed, the props were deleted, the suite went RED).

## Verification (final tree, actual output)

```
npx tsc --noEmit                                 → exit 0
bash mobile/scripts/testid-lint.sh               → testid-lint OK
node mobile/tests/check-league-drill-in.js       → 29 PASS — All League drill-in checks passed (#299 + #302).
node mobile/tests/check-analytics-297-302.js     → 35 PASS — All #297/#298/#299/#302 analytics checks passed.
node mobile/tests/check-league-candidates-300.js → 67 PASS — All #300 league trade-candidate checks passed.
node mobile/tests/check-picks-subset-invariance.js → 72 PASS — All picks-subset-invariance checks passed.
node mobile/tests/check-league-unlocks.js        → 21 ok  — All league-unlock checks passed.  (4 pre-existing + 17 new)
node mobile/tests/check-contrarian-format-key.js → 3 PASS — All contrarian-format-key checks passed.
node mobile/tests/check-matches-tile-league-param.js → 6 PASS — All matches-tile-league-param checks passed.
```

Not executed anywhere: the Maestro flows (D-P1-08 — sim apparatus retired;
07 additionally merge-gated on the Matches group), and no simulator/device
has rendered the new fold-line copy or the scoped landing.

## Proposed shared-doc text (orchestrator applies — plan §7)

For `docs/cross-client-invariants.md`, new section:

> ## League unlock thresholds
>
> Two unlocks on the League home progress module count DIFFERENT populations
> at DIFFERENT thresholds — never conflate them (feedback #265 and #308 are
> both conflation incidents):
>
> | Unlock | Threshold | Population | Source of truth |
> |---|---|---|---|
> | Mutual matches | `MATCH_UNLOCK_MATES = 1` ranked leaguemate | any format, caller EXCLUDED | `mobile/src/utils/leagueUnlocks.ts` (#265); backend basis `trade_service.py generate_trades` |
> | Leaderboards / contrarian ranks | 3 users with ≥1 stored ranking | ACTIVE scoring format only, caller INCLUDED | `/api/league/contrarian` (`backend/server.py`, `CONTRARIAN_UNLOCK_USERS` mirrors it client-side) |
>
> Scoring-format display labels are pinned now that two surfaces render
> them (TopBar format tile; #308 fold line): `1qb_ppr` → "1QB",
> `sf_tep` → "SF TEP" (`TopBar.tsx FORMAT_TILE_LABEL`,
> `leagueUnlocks.ts FOLD_FORMAT_LABEL`).

Optional (plan marks it optional): enrich `docs/api-reference.md`'s
`/api/league/contrarian` row with the insufficient-payload shape
`{insufficient_data: true, ranked_users, needed, format, message}`.

## Flags for the orchestrator

1. **`api/league.ts` grant:** confirmed in the build brief as this group's
   (additive return-field threading only); done exactly that way.
2. **Smoke-09 staleness (pre-existing, not fixed here):** the flow taps
   `tab.league` and immediately waits for `league.hero`, but since #181 the
   League tab roots at `LeagueRankings` (`league-summary.league-home` pushes
   LeagueHome) — the flow appears not to have been updated and has not run
   since the sim apparatus retired. My added assert sits after the existing
   `league.hero` wait, so it inherits whatever that step's fate is; flagged
   rather than repaired because smoke flows are outside this group's grant.
3. **Plan §8 adjacent finding** (mutual-match unlock's any-format smell) —
   logged by the plan for NEXT.md/OPEN_QUESTIONS; nothing built here touches
   it. Orchestrator owns the entry per the wave's shared-docs routing.
4. **No plan defects found.** Every question the build raised was answerable
   from the plan; the one deviation (voided #308 Maestro waiver) followed the
   plan's own void clause.
