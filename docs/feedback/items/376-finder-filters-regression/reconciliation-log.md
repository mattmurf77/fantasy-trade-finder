# Reconciliation log — #376/#379/#394 (Group A)

## Round 1 — Author deltas over the plan (2026-08-24, from prd.md §8)

| Point | Resolution |
|---|---|
| Orchestrator A1: keep `!firstRun` | Provisional, accepted; declared-first-run residual acknowledged |
| Orchestrator A2: remove Filters button outright | Provisional, accepted; coupling to R-4 flagged, "guard assertions 4+6 enforce the pair" |
| Planner cite drift | #333 block is `InLeagueCalculator.tsx:784-824` (plan said 784-818); `check-trades-banner-region.js` needs no edit |

## Round 2 — Critic (2026-08-24)

All PRD/scope cites were re-verified from disk against `ff153a0`. Confirmed exact: `features.json:11,60,84,213,217`; `outlookReceiptCovers` at `OutlookBiasReceipt.tsx:46-54`; `firstRun` latch `TradesScreen.tsx:433-440`; `receiptDetails` memo `:1100-1115` (and it is **not** gated on `outlookDirectionOn` — deps `prefsQuery.data, intentModesOn, tradeIntent, untouchableIds`, so the PRD's second-line spec is buildable today); `dnaOpenSource` default `'sheet'` `:748`; guide registration `:3229/:3246`; `outlook_saved` at `analytics_taxonomy.py:412,1290` with `source` prop and `guide|sheet|strip` values (`:1285`); zero `dna_sheet_opened` anywhere; `TradeHomeUtilityRow.tsx` contains no `track(`; `package.json:36`; the CI check-loop in `ci.yml` (§47). D-151/D-152/D-153/D-157/D-158 claims match `living-memory/DECISIONS.md:1358-1473`. Scope.md's analytics and no-schema/API/flag claims are honest as written.

### BLOCKING

**B-1 — Guard assertions 3+4 are blacklists and do NOT make A2-without-R-4 mechanically unshippable (prd.md §6a, §2).** The PRD's own safety argument for removing the Filters button ("A2 is safe only because R-1 renders for every variant… guard's assertion 4 pins this coupling") fails under aliasing: assertion 4 greps the row's condition for the literal `showInlineHome`, but a builder gating the row `homeInlineVariant !== 'control' && …` or via `homeInlineStripOn`/a derived boolean (all in scope at `TradesScreen.tsx:644-650,707`) passes assertion 4 **green** while re-stranding the control cohort with the button already deleted — the exact "entry exists but not for a cohort" failure mode the OLD guard stayed green through. Same class: assertions 3+4 also permit *any additional conjunct* (`&& presentationV2On`, `&& deck.length > 0`), so "row exists but is gated off" recurs via any third predicate and all 7 assertions stay green. **Required fix:** replace the two blacklist assertions with an exact-expression whitelist — the row's gate must equal `consolidateOn && !outlookReceiptShown && !firstRun` (whitespace/paren-tolerant), which subsumes 3+4 and closes both dodges. This is also the concrete answer to "why could the new guard not stay green through the original regression": with the exact pin, every green build renders the row unless one of exactly three predicates is false, and each false-branch has a covering surface (¬`consolidateOn` → the legacy Controls Card outlook row returns, `:5330/:5407`; `outlookReceiptShown` → the receipt renders, `OutlookBiasReceipt.tsx:96/101`; `firstRun` → one-mount latch `:433-440`). Without the exact pin that property does not hold, and the PRD's A2 arbitration rests on it.

**B-2 — R-1's value contract is ambiguous for `team_outlook === 'not_sure'`, a common persisted state (prd.md §3 R-1, §4).** R-1 says value = "`prefsQuery.data?.team_outlook` capitalized, else 'Not set'". But `'not_sure'` is a *declared* value the system itself writes: `TradeDnaSheet.tsx:494-496` persists `outlook: draftOutlook ?? 'not_sure'` whenever positions are saved without an outlook pick, and `outlookReceiptCovers` excludes it (`LEAN` has no `not_sure` key, `OutlookBiasReceipt.tsx:34-39,53`) so the row — not the receipt — renders for exactly these users even with the flag on. Two engineers diverge legitimately: `cap('not_sure')` → "Not_sure" (the legacy row's literal behavior, `:5412-5414`) vs. "Not set" vs. "Not sure". The PRD's copy examples ("Contending"/"Not set") never cover it. **Required fix:** one sentence in R-1 pinning the mapping. Critic's recommendation: display `not_sure` as **"Not sure"** (it is declared; the sheet reopens preselected on it), and use the receipt's `LEAN[…].name` labels for the four directional values so "Contending"/"Rebuilding" match the receipt's vocabulary rather than raw-enum capitalization.

### NON-BLOCKING

**N-1 — A1's banner-coverage sentence overstates; the residual itself is real and correctly logged.** Verified: `inferredOutlook` is null whenever an outlook is declared (`TradesScreen.tsx:1032-1035`), so `outlookBannerWants` (`:3038-3040`) is **false** for a first-run declared-outlook user — the banner does not cover that cell; only the §8 risk-acceptance row is accurate, not §2's "outlook remains reachable first-run via the interrupt banner". Also note the banner is slot-arbitered *below* quickset prompt / coach mark / apple banner (`:3006-3045`), so even the undeclared first-run cell can see it deferred, not instant. Fix: tighten the A1 sentence to "undeclared-outlook first-run users, when the interrupt slot frees"; the code-walk table (§6c) must show the declared-first-run cell as an accepted gap, not banner-covered.

**N-2 — Assertion 1's testID matches must be boundary-anchored.** `trades.outlook-fallback` is a prefix of `trades.outlook-fallback.change`/`.details`; an unanchored regex passes with the container deleted. Require `testID="trades.outlook-fallback"` closing-quote-exact per id (the `#384 /isDemo/` lesson, status.md W0–W4 record).

**N-3 — Assertion 5 must search within the row's extracted span.** A file-global `/setDnaSheetOpen\(true\)/` self-satisfies via five other call sites (`:787,:813,:852,:3348,:5142`); the "swap the setter" sabotage would stay green. The mandated sabotage matrix is the backstop, but the PRD table is the contract — say "within the row's span" explicitly. Same for assertion 2: locate the `!consolidateOn` legacy branch by anchor text, never by line number (`:5330` drifts the moment R-2 deletes lines above it — which this very diff does).

**N-4 — Checklist step 6 (control variant) needs its mechanism stated and a fallback.** `TestStagesScreen` exists (`RootNav.tsx`) and a fresh stage account is off the tester allowlist → `trades_home_inline` targeting (`is_tester_allowlist: true`) assigns no variant → control. Write that expectation into the step so the operator doesn't read the chip mode bar as a bug, and name the fallback (a non-allowlisted tester, e.g. jonbonjourvi) in case a stage account is ever allowlisted. Step 5's "or after clearing outlook" should be dropped or reworded: there is no clear-outlook affordance (single-select chips; the backend fallback writes `not_sure`, see B-2) — the second account is the real path.

**N-5 — Wave B0 mortality note.** D-158 replaces this whole region with an inline `InLeagueCalculator` that carries its own outlook row (`calc.outlook-row`, `InLeagueCalculator.tsx:713-744`); if B0 lands without removing `trades.outlook-fallback`, TradesHome shows two outlook surfaces. Add one line to the planned DECISIONS.md entry and to the B0 plan pointer: the row is deliberately mortal and B0's builder deletes it + retires/rewrites the rewritten guard's assertions 1-5. No contradiction found with D-151/D-152/D-153/D-157 otherwise; R-6's untouched-surface list checks out.

**N-6 — R-1 wording: "one-tap row" vs. Change-only pressable.** §4 correctly mirrors the calculator (only Change is pressable, `InLeagueCalculator.tsx:726-741`); R-1's opening "one-tap 'Outlook & filters' row" can be read as whole-row tappable. One word fixes it. (Critic leans whole-row tappable would actually be better UX, but matching the shipped calculator pattern is the safer contract — either way, pin it.)

### Verified non-issues (hunted, found sound)

- R-4's control-variant rendering path IS pinned: wrapper gated `finderMode ?` only (`:5132`), `consolidateOn ⊆ finderMode` (`:731`), mode-bar branch `:5089-5119` — the claimed render order holds.
- R-3's no-double-render: receipt's `hidden` (`OutlookBiasReceipt.tsx:96`) = `!covers(...) || !entry`, and `covers` true ⟹ `LEAN[resolved]` exists ⟹ `entry` truthy — the `|| !entry` term can never diverge from the screen's `outlookReceiptShown` complement; both read the same `['league-prefs', leagueId]` cache.
- R-5 truth table recomputed against `covers()`: flag false → row always; flag true → receipt iff resolved ∈ {championship, contender, rebuilder, jets}, row otherwise. Matches PRD.
- Checklist steps 1 and 6 would both concretely fail under the original #394 defect (no row in either variant) — regression-detecting in both cohorts.
- Scope analytics claims all verified true (see header paragraph).

## Round 3 — Author incorporates the critique (2026-08-24)

Both critic cites the author had not previously verified were re-checked from disk before adoption: `TradeDnaSheet.tsx:493-496` (persists `outlook: draftOutlook ?? 'not_sure'`) and `TradesScreen.tsx:1030-1035` (`inferredOutlook` null once `team_outlook` declared) — both exact. scope.md untouched (no fix required it; its analytics/schema claims were verified sound in Round 2).

| # | Resolution |
|---|---|
| **B-1** | **Fixed (prd.md §6a + §2).** Blacklist assertions 3+4 replaced by one exact-whitelist assertion (new #2): the row's gate must *equal* `consolidateOn && !outlookReceiptShown && !firstRun` — whitespace/paren-tolerant, no conjunct added or dropped. Proving sabotage named: prepend `homeInlineVariant !== 'control' && ` (the alias that beats a blacklist) → red; `&& showInlineHome`, `&& presentationV2On`, and dropping `!firstRun` also red. A2's safety argument completed in §2 with each predicate's false-branch covering surface cited: ¬`consolidateOn` → legacy Controls Card outlook row (`:5330`/`:5407-5424`); `outlookReceiptShown` → the receipt's own Change (`OutlookBiasReceipt.tsx:96,101-126`); `firstRun` → one-mount latch (`:433-440`), undeclared cell banner-covered, declared cell = A1 residual. Guard renumbered to 6 assertions; criterion map updated. |
| **B-2** | **Fixed (prd.md §3 R-1, one precise mapping).** Directional values render the receipt's `LEAN` names (All-in / Contending / Rebuilding / Tanking, `OutlookBiasReceipt.tsx:34-39`); `not_sure` renders **"Not sure"** (critic's recommendation adopted — it is declared, never `cap()`-literal "Not_sure", never "Not set"); "Not set" only for null/absent. Checklist step 5 gained the matching runtime check. |
| N-1 | **Applied (§2 A1, §6c, §8).** Banner coverage restated as verified truth: undeclared first-run only, and only when the interrupt slot frees (arbitered below quickset/coach-mark/apple, `:3006-3045`); declared first-run = plainly stated accepted residual, not banner-covered (`:1030-1035`). Code-walk outline shows that cell as an accepted gap. |
| N-2 | **Applied (guard assertion 1).** testID matches boundary-anchored: `testID="trades.outlook-fallback"` closing-quote-exact per id; sabotage extended to container-only deletion. |
| N-3 | **Applied (guard assertions 3+4).** Assertion 4 scoped to the row's extracted JSX span (file-global self-satisfies via `:787,:813,:852,:3348,:5142`); assertion 3 locates the legacy branch by its `{!consolidateOn ? (` anchor expression, never by line number (this diff itself shifts `:5330`). |
| N-4 | **Applied (checklist 5+6).** Step 6 states the control mechanism (fresh stage account off the allowlist → no variant → control; mode bar is *expected*, not a bug) + fallback (non-allowlisted tester, e.g. jonbonjourvi). Step 5 drops the impossible "after clearing outlook" path — a never-declared second account is the only route; `not_sure` bonus check added. |
| N-5 | **Applied (§5).** Wave B0 mortality note: B0's builder deletes `trades.outlook-fallback` and retires/rewrites guard assertions 1–4 in the same change (else two outlook surfaces beside inline `calc.outlook-row`); note also routed to the planned DECISIONS.md entry and the B0 plan pointer. |
| N-6 | **Applied (R-1 + §4).** Tap surface pinned: **Change-only pressable**, matching the shipped `calc.outlook-fallback` (`InLeagueCalculator.tsx:726-741`) and the receipt — one interaction contract across all three sibling surfaces, styles reused verbatim; whole-row tap noted as a B0 UX candidate, not shipped here. R-1's "one-tap row" wording removed. |
