# QA-A findings — FB-407 (2026-08-30)

**Verdict: PASS** — independent mechanical verification of commit
`8f722676d91e7f73403eb2c32e4b30a201ea6af7` ("fix(#407): auto-defaulted calculator
partner no longer scopes Find a Trade"). All builds/typechecks/suites green; all
five sabotage cycles independently re-proven red→green on the correct assertion;
the assertion set is not self-satisfying (empirically demonstrated). Zero
modifications left behind.

*(Saved by the orchestrator from QA-A's final report — QA-A ran in an isolated
worktree cut at 8f722676 on d2a0b0bb on e89eebb0, base confirmed.)*

## 1. Commands run + result lines

| # | Command (QA worktree `mobile/` unless noted) | Result |
|---|---|---|
| 1 | `npm ci` (foreground, sync) | `added 801 packages, and audited 802 packages in 10s` — exit 0. (27 npm-audit vulns: 1 low/10 moderate/14 high/2 critical — pre-existing dependency posture, not introduced by this change) |
| 2 | `npx tsc --noEmit` | clean, **exit 0** |
| 3 | All **86** `npm run` scripts mapping to `mobile/tests/check-*.js` (enumerated mechanically from `package.json`; excludes `test:contrast`, which maps to `scripts/check-contrast.js`) | **`SUMMARY: 86 passed, 0 failed of 86`** — includes `test:calc-merged-behavior` → `check-calc-merged-behavior: all assertions passed` |
| 4 | `bash scripts/testid-lint.sh` | `testid-lint OK` — exit 0 |
| 5 | Five sabotage cycles + full-revert probe (below) | each red on the expected assertion, each restore green |
| 6 | `git status --porcelain` (end of run) | **empty — clean**; HEAD still `8f722676` |

Minor observation (out of scope): `mobile/tests/check-mascot-ram.js` exists but has
no `npm run` wiring, so it is not part of the runnable suite set.

## 2. Sabotage cycles (independently re-applied by QA-A — not replayed from the build report)

Each cycle: apply edit → `npm run test:calc-merged-behavior` → confirm the named
assertion is the **only** failure (`1 FAILED`, suite exit 1) →
`git checkout -- src/components/InLeagueCalculator.tsx` → rerun → green, exit 0.

| Cycle | Sabotage applied | Red assertion | Restored green |
|---|---|---|---|
| i | Payload gate reverted to unconditional `opponent: opponent ? {…} : null` | `✗ 20d` | ✓ |
| ii | Chip-row `opponentChosenRef.current = true` removed (`:1074`) | `✗ 20c-bis` | ✓ |
| iii | Team-picker-sheet ref-set removed (`:1485`) | `✗ 20c-bis` | ✓ |
| iv | Ref write added inside the default-opponent effect (`:545-548`) | `✗ 20b-bis` | ✓ |
| v | Initializer changed to `useRef(false)` (`:351`) | `✗ 20a` | ✓ |

**Extra probe — full pre-fix build vs. new tests:** the entire production diff
reverse-applied (`git apply -R`) while keeping assertions 20a-20d → `3 FAILED`
(20a ✗, 20c-bis ✗, 20d ✗; 20b ✓, 20b-bis ✓, 20c ✓); restored → green. The section
as a whole cannot pass on the shipped-bug build.

## 3. Self-satisfaction audit

- **20a** — two-sided (red on pre-fix build and weakened initializer).
- **20b** — one-sided *by design*: an anchor keeping 20b-bis from going vacuous if
  the default effect is deleted/renamed. Must never be cited as fix evidence alone.
- **20b-bis** — passes vacuously on the fully pre-fix build (no ref anywhere), but
  its one-directional job (catching re-promotion of the default to a choice) is
  proven red by cycle iv, and the pre-fix build is caught by 20a/20c-bis/20d — the
  **ensemble** is sound. Residual gap: a ref write >400 chars from the effect's
  setter inside the same (currently 2-line) effect would evade the window — low risk.
- **20c** — two-sided for its purpose (site inventory drifts red); pairing is
  20c-bis's job.
- **20c-bis** — two-sided (red at paired=0 on pre-fix, paired=1 under either single
  removal). Residual gap: 200-char lookbehind could in principle catch an unrelated
  ref write — no realistic false-pass today (sites ~400 lines apart, compact bodies).
- **20d** — two-sided; full end-to-end anchoring means a weakened gate breaks the
  match. Brittleness to reformatting fails **safe** (false red, never false green).

**Conclusion:** no assertion is self-satisfying in a way that matters; every briefed
sabotage was caught by the correct assertion with no collateral failures.

## 4. Verdicts per mini-PRD requirement

| Mini-PRD requirement | Verdict | Evidence |
|---|---|---|
| Fix piece 1 — ref declared beside `opponentId` state | **PASS** | `InLeagueCalculator.tsx:351`; 20a (cycle v red) |
| Fix piece 2 — both tap sites set the ref; default effect does not | **PASS** | `:1074`, `:1485`; effect `:545-548` untouched; cycles ii/iii/iv |
| Fix piece 3 — payload gated on chosen-or-receive-side | **PASS** | `:1229-1231`; cycle i + probe |
| Surgical scope — 1 production file + 1 test file, nothing else | **PASS** | `git show --stat 8f722676` |
| Regression guard — three pinned properties, sabotage-verified, no sim/Maestro | **PASS** | 20a-20d; five cycles re-proven; pure `node` structural checks (D-056) |
| CI-gate posture verifiable here — tsc, testid-lint, structural suites | **PASS** | tsc exit 0; `testid-lint OK`; 86/86. (backend pytest not run — mobile-only diff; ledger entries correctly deferred to ship) |
| Code-walk proof | **PASS (exists; fix-side claims verified)** | changed-file claims independently verified; downstream hops consistency-read (unchanged files) |
| Manual TestFlight checklist | **PENDING (operator)** | 5-step checklist in mini-prd.md; runtime evidence reserved for the operator per D-056 |

## 5. End-state hygiene

QA worktree clean at end (empty porcelain, empty diff, HEAD 8f722676). Left for
orchestrator sweep: branch `qa-a-fb407` + scratchpad worktree `wt-qa-a-fb407`
(no unique commits — tip already on `claude/new-user-feedback-5fa613`).
