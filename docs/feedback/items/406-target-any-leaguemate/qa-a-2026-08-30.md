# QA-A findings — FB-406 (2026-08-30)

**Verdict: PASS on all 10 PRD requirements.** Independent mechanical verification of
commit `c138507c` ("feat(#406): 'Any league mate' targeting on the merged
calculator") in an isolated worktree (`wt-qa-a-fb406`, branch `qa-a-fb406`, base
verified: c138507c → 001ec915 → afc7e158 → 8f722676 → d2a0b0bb → e89eebb0).

*(Saved by the orchestrator from QA-A's final report.)*

## Environment note (orchestrator error, contained)

QA-A was mistakenly launched without worktree isolation; it began recovery steps in
the session checkout (`git checkout -b qa-a-fb406` + one `npm ci`) before the
orchestrator's correction arrived. No sabotage was ever applied there; the session
checkout was restored to `claude/new-user-feedback-5fa613` at `c138507c`, clean.
Only side effect: a lockfile-faithful `npm ci` refresh of the session
`mobile/node_modules` (untracked, harmless). All QA ran in the isolated worktree.

## Floors

| Command | Result |
|---|---|
| `npm ci` (fresh, no symlinks) | OK |
| `npx tsc --noEmit` | **clean**, exit 0 |
| All check-suite npm scripts | **PASS 87/87** — includes new `test:any-partner` |
| `node tests/check-mascot-ram.js` (direct — see note) | PASS |
| `bash scripts/testid-lint.sh` | `testid-lint OK` |

**Note (non-blocking, pre-existing):** 88 `check-*.js` files exist but only 87 npm
scripts — `tests/check-mascot-ram.js` (#186, `7ac7869e`) has never had a script.
Suggest a one-line follow-up adding `test:mascot-ram`.

## Declared minimal diffs — verified

`TradesScreen.tsx` +1/−0 (seed marker :5817) ✓ · `TradeBuildCanvas.tsx` +7/−0
(type field + pass-through) ✓ · `check-calc-merged-behavior.js` +3/−2 (exactly the
declared 20a re-spec; 20b–20d untouched, verified) ✓ · backend diff **empty** ✓

## Sabotage cycles — 20 run, every one red on the named assertion, green after restore

S-1→A-1 · S-2→A-2 · S-3→A-3 · S-4→A-4 **and cross-suite 20d** · S-5→A-5 ·
S-6→A-6 (line-740 masking ruled out) · S-7→A-7 exactly-once · S-8→A-8 union type ·
S-9→A-9 (a11y branch does not mask the value branch) · S-10a→A-10 ·
**S-10b conjunction-flip→A-10** (critic B-2's one-character sabotage caught) ·
S-11→A-11 count+adjacency · S-11b→A-11b (+A-15) · S-12→A-12 · S-13 sticky-anyone→A-13 ·
S-14 dropped-marker→A-14 · S-14b extra-marker→A-14 (two-sided count) ·
S-14c prop-drop→A-14 forwarding · S-15 chosen-seed→A-15+A-11b **and 20a red**
(negation-drop direction) · 20a declaration-drift→20a red (second direction) —
**the re-specced 20a is two-sided ✓**.

## Self-satisfaction audit

No self-satisfying assertion among A-1..A-15. Two honest weak spots, both
compensated and source-verified: A-1's above-map sub-check is vacuous when the row
is absent (sibling inside-region check catches absence); A-10's "inside the merged
branch" is a proxy heuristic (source-verified: note at :1361 inside the `{merged ?(`
branch opened :1267). A-8's negative greps are best-effort by design (PRD N-6
disclaimer present in the suite).

## Verdict per requirement

R-1 ✓ · R-2 ✓ · R-3 ✓ · R-4 ✓ · R-5 ✓ (backend zero-diff) · R-6 ✓ · R-7 ✓
(stale-verdict leak closed, gated `ev` at :657) · R-8 ✓ · R-9 ✓ (full suite set
green, only declared files in diff) · R-10 ✓ (seed marker + two-sided 20a re-spec).

## End state

All sabotages restored (`git status` clean, `git diff` empty, final tsc clean,
both key suites green). Left for orchestrator sweep: worktree
`scratchpad/wt-qa-a-fb406` + branch `qa-a-fb406` (tip = c138507c, no unique
commits).
