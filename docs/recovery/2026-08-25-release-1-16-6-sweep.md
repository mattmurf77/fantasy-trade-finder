# 2026-08-25 — v1.16.6 release-day branch sweep

| tip sha | branch | merged as |
|---|---|---|
| `cb691b25` | `claude/release-1-16-6` | squash PR [#206](https://github.com/mattmurf77/fantasy-trade-finder/pull/206) → `c092f808` |
| `2b436d3c` | `claude/gotchas-g012-correction` | squash PR [#207](https://github.com/mattmurf77/fantasy-trade-finder/pull/207) → `14ce389d` |
| `2aa495fa` | `claude/release-1-16-6-memory` | squash PR [#208](https://github.com/mattmurf77/fantasy-trade-finder/pull/208) → `b301221c` |

**Why safe — and why a tree diff was NOT the check for two of these.** `claude/release-1-16-6-memory` is byte-identical to `origin/main` (`git diff` empty). The other two are **not**, because main moved past them within the hour: #207 rewrote the G-012 text that #206 introduced, and #208 rewrote HANDOFF/CHANGELOG/TEST_LEDGER. A tree diff against a moved main shows the LATER edits and reads like missing content — the trap this repo's "verify by content, cite the evidence" rule exists for. Verified instead by content markers on `origin/main`:

- release-1-16-6: `app.json` `"version": "1.16.6"` (1 hit), `Info.plist` `1.16.6` (1), `MARKETING_VERSION = 1.16.6` (2 — Debug + Release). All four version spots.
- gotchas-g012-correction: `GOTCHAS.md` contains "Which native source actually wins" (1 hit) and the superseded "silently disagreed for a whole release" line is **gone** (0 hits) — the correction landed and replaced, not appended.

Evidence: TEST_LEDGER 2026-08-25 (build `8925880d` finished, submission `b190d30b` finished, v1.16.6/132 from `c092f808`); CI green on each PR head.

**Deleted:** 2026-08-25 (reflog recovery expires ~2026-11-25).

Recovery:
```
git branch claude/release-1-16-6 cb691b25
git branch claude/gotchas-g012-correction 2b436d3c
git branch claude/release-1-16-6-memory 2aa495fa
```

**Not swept, deliberately:** the 21 worktrees under `.claude/worktrees` (5.9 GB) that put the main checkout at 9.5 GB — the G-022 upload-failure condition. Today's build sidestepped it by building from a 561 MB worktree. That sweep needs its own pass: each worktree inspected for uncommitted work before removal, per this folder's `--force`-refusal rule.
