# 2026-09-03 — #413 ship sweep (PR #270) + the superseded parallel #414 build

Ship: PR [#270](https://github.com/mattmurf77/fantasy-trade-finder/pull/270) squash → `main` @ `5c83e8cd`. Verification **by content**: `git diff origin/main ship/2026-09-02-fb413 --stat` is empty (the ship tree is byte-identical to the squash), and `ship/…` contains `feat/fb413-sleeper-send-draft-picks` in full (a clean merge). Evidence: `living-memory/TEST_LEDGER.md` 2026-09-03.

| tip sha | branch | where it lived | why deletion is safe |
|---|---|---|---|
| `c6c95e07` | `ship/2026-09-02-fb413` (local + `origin/`) | session worktree `unruffled-meitner-3596cb` | tree identical to `main` @ `5c83e8cd` (squash of this branch) |
| `d49611be` | `feat/fb413-sleeper-send-draft-picks` | — (worktrees swept 2026-09-02) | ancestor of `c6c95e07`, whose tree is on `main` |
| `8c165533` | `feat/fb414-lopsided-one-for-one` | — | **unshipped, superseded** by D-175 (PR #268). Its engine code is NOT on `main` by design; its docs (PRD, LLD, QA reports, code-walk) ARE on `main` under `docs/feedback/items/414-lopsided-one-for-one/` with status SUPERSEDED. Kept recoverable for the G-8 test design (`test_gap_sweetener_frac.py::test_v3_gap_pass_never_uses_an_avoided_position_as_equalizer`). |
| `dd5dde16` | `claude/weekly-feedback-review-5943dd` (never pushed) | session worktree | the pre-rebase session branch: its unique content is the merge of the two group branches + the pre-renumber write-back (D-172/Q-035 ids that `main` reassigned). Everything shippable was re-applied on `ship/…` with renumbered ids; the rest is the superseded #414 build. |

Worktree `.claude/worktrees/unruffled-meitner-3596cb` hosts this session and cannot remove itself — remove from the main checkout (`git worktree remove .claude/worktrees/unruffled-meitner-3596cb`; expect it clean).

Deleted: 2026-09-03 (reflog recovery expires ~2026-12-02). Recovery: `git branch <name> <sha>`.
