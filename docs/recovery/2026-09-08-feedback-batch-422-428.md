# Feedback batch #422–#428 — branch and worktree recovery capture

2026-09-08. **Captured before deletion.** Scope is only the branches and temporary worktrees created for this batch. The original dirty checkout, unrelated branches, nested worktrees and the sibling Fleeced checkout are preserved and untouched.

Release [PR #292](https://github.com/mattmurf77/fantasy-trade-finder/pull/292) head `8329d975150d2387def5d9a6e69170fef49f1aad` squash-merged as `1371d2e5`. Render deploy of `1371d2e5` reached **live** at 2026-09-09T00:03:58Z; production smoke passed (root 200, flags correct, 0 error lines in 200 log lines, and the #427 rule verified on the live calculator: two mid firsts 4234.0 face vs 4210.2 → `even`, while two mid seconds still carry a −225.9 `package_depth` deduction).

## Captured tips

| Tip SHA | Branch | Worktree (scratchpad suffix) |
|---|---|---|
| `685f50afbf66fe5280b637b3ab596775efdfaebb` | `claude/feedback-422-428` (session branch) | — (the session worktree, retained) |
| `8cb5c193d2ec4c00ab0c98ca8a2c7e15968903fe` | `feat/fb422-win-now-slot-refusal` | `wt-g422` |
| `9d541ac7947f7ef7f25f3246e03df09c824172ca` | `feat/fb423-outlook-row-and-marker` | `wt-g423` |
| `d6d9ef5c6b8b1d4e7b99aadf63dbfd12384faa26` | `feat/fb425-overhaul-hero-replaces-draft` | `wt-g425` |
| `b9dd1a9a7760b9ff57d22984270b33125da2d7e4` | `feat/fb427-first-round-pick-tax-exempt` | `wt-g427` |
| `3be911127e6eda7142f1ad71261cd605e1789f5d` | `feat/fb428-sleeper-pick-tradability` | `wt-g428` |
| `8329d975150d2387def5d9a6e69170fef49f1aad` | `feat/feedback-2026-09-08` (release) | — |

QA worktrees (`qa-g4xx-a/b`, `qa2-g4xx-a/b`) were detached copies whose only product was a `qa-round-*.md` report; every report was copied onto its group branch and is on `main`. They were removed during the run after that copy.

## Content preservation proof

Three independent checks, all run against freshly fetched `origin/main` before any deletion:

1. **Ancestry** — every branch tip above is an ancestor of the tested release tip `8329d975` (`git merge-base --is-ancestor` returned true for all six).
2. **Complete-tree equality** — `git diff --stat 8329d975 origin/main` is empty, so the tested tree and `main` are byte-identical across the squash boundary.
3. **Owned-file equality** — for each group, `git diff --name-only origin/main <branch> -- <its owned files>` returned 0 files (G-422 `win_now_service.py` + its tests; G-423 `InLeagueCalculator.tsx`, `teamReviewCompletion.ts`, `check-outlook-row-source.js`; G-425 `OverhaulEntryCard.tsx`, `TradeHomeUtilityRow.tsx`; G-427 `trade_service.py` + `test_first_round_pick_exempt.py`; G-428 `draft_status.py` + `test_sleeper_pick_tradability.py`).

Every worktree was verified clean (`git status --porcelain` empty, including untracked files) immediately before removal. `mobile/node_modules` and `secrets.local.env` inside each worktree were **symlinks** to the session checkout; removing the worktree removes the link, never the target. No `--force` removal is authorized by this record.

Recovery: `git branch <name> <sha>` from the table, or `git fetch origin refs/pull/292/head` for the full release history. The batch's reasoning trail (PRDs, build reports, ten QA reports, reconciliation logs) is on `main` under `docs/feedback/items/42{2,3,4,5,6,7,8}-*/` and is not affected by branch deletion.
