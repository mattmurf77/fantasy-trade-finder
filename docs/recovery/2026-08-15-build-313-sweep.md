# 2026-08-15 — #313 1QB QB cap (build-313) sweep

| tip sha | branch | worktree path |
|---|---|---|
| `9ae6334` | `build-313` | `.claude/worktrees/review-313` (see note) |

- **Why safe:** merged via squash PR
  [#128](https://github.com/mattmurf77/fantasy-trade-finder/pull/128) → `main` @
  `34ebd84` (all three CI checks green on `9ae6334`). Verified **by content**:
  after the post-merge fetch, `git diff build-313 origin/main` is **empty** —
  `backend/data_loader.py`, `backend/database.py`, the three test files, both
  `docs/feedback/items/313-1qb-qb-cap/` docs, and every shared-doc row
  (config-reference, cross-client-invariants, DECISIONS D-054) are
  byte-identical on `main`. Ahead-counts and `git branch -d` are NOT evidence
  here (squash merge).
- **Deploy verified by behaviour, not just record:** prod
  `GET /api/trade/values?scoring_format=1qb_ppr` serves Josh Allen / Drake Maye
  / Lamar Jackson all at `tier: first_1` (pre-ship they read `firsts_2`) —
  first poll after merge, 2026-08-15.
- **Worktree note:** the branch was built in `.claude/worktrees/build-313`,
  whose *directory* was removed by a concurrent session's sweep on 2026-08-15
  while this session was reviewing (the branch and all commits survived; the
  worktree was clean). The review/ship continued in a fresh worktree
  `.claude/worktrees/review-313`, removed by this sweep after verification.
- Recovery: `git branch build-313 9ae6334`
- Reflog recovery expires ~2026-11-13.
