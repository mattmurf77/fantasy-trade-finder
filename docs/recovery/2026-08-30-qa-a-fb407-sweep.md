# Recovery ledger — 2026-08-30 — QA-A worktree/branch sweep (FB-407)

| Branch | Tip sha | Verification | Verdict |
|---|---|---|---|
| `qa-a-fb407` | `8f722676d91e7f73403eb2c32e4b30a201ea6af7` | Ancestry: `git merge-base --is-ancestor qa-a-fb407 <session HEAD>` → contained; `git log qa-a-fb407 --not HEAD` → 0 commits. Branch was created by the FB-407 QA-A agent purely to check out the fix commit (already on `claude/new-user-feedback-5fa613`); QA-A ended with a clean porcelain — zero unique content by construction and by check. | CAPTURED → swept (branch deleted, scratchpad worktree `wt-qa-a-fb407` removed) |

Context: QA-A's isolated worktree was cut from origin/main (known trap,
lessons.md 2026-08-24); it re-created a worktree at the fix sha in the session
scratchpad, ran the FB-407 mechanical battery (PASS — see
`docs/feedback/items/407-finder-forced-team/qa-a-2026-08-30.md`), restored every
sabotage, and left the branch + worktree for orchestrator sweep.

## Addendum — same-day FB-406 QA-A sweep

| Branch | Tip sha | Verification | Verdict |
|---|---|---|---|
| `qa-a-fb406` | `c138507c` (= the FB-406 build commit, already on `claude/new-user-feedback-5fa613`) | Ancestry: `git merge-base --is-ancestor` → contained; `git log qa-a-fb406 --not HEAD` → 0 commits; QA-A ended with clean porcelain in its worktree | CAPTURED → swept (branch deleted, scratchpad worktree `wt-qa-a-fb406` removed) |

## Addendum 2 — FB-406 QA-B agent worktree sweep

| Branch | Tip sha | Verification | Verdict |
|---|---|---|---|
| `worktree-agent-a14bd0dab402527c3` | `e89eebb0` (= origin/main tip; 0 commits not on origin/main by ancestry) | Read-only QA audit worktree; its single untracked output file (`qa-b-2026-08-30.md`, 271 lines) copied verbatim into `docs/feedback/items/406-target-any-leaguemate/` and verified before removal | CAPTURED → swept (worktree removed with --force for the already-copied untracked file; branch deleted) |

## Addendum 3 — session branch + ledger branch sweep (post-ship)

Both branches carried the 2026-08-30 feedback batch (#407 `8f722676` + #406 `c138507c`),
squash-merged to `main` as `287aed09` (PR #250) and `1c5e6543` (PR #252). This repo
squash-merges, so containment is verified **by content**, not ancestry.

| Branch | Tip sha | Verification | Verdict |
|---|---|---|---|
| `claude/new-user-feedback-5fa613` | `5c0bad56` | `git diff origin/main claude/new-user-feedback-5fa613 --stat` → **empty** (tree identical to `main` @ `1c5e6543`) | CAPTURED → swept (remote + local deleted by explicit name) |
| `docs/ship-ledger-2026-08-30` | `26578b99` | `git diff origin/main docs/ship-ledger-2026-08-30 --stat` → **empty** | CAPTURED → swept (remote + local deleted by explicit name) |
| `docs/sweep-ledger-2026-08-30` | (this entry's branch) | Merged to `main` as the commit carrying this addendum; deleted after merge | CAPTURED → swept |

**Not touched** (other sessions' branches, adjacent names — deliberately left alone per
M-005, delete by explicit name only): `claude/new-user-feedback-55320e`,
`claude/new-user-feedback-d4c47d`.

**Worktree still to remove, from the main checkout** (it cannot remove itself):
`git worktree remove .claude/worktrees/new-user-feedback-5fa613` — nothing uncommitted in it;
a `--force` requirement would mean stray files, inspect before discarding.
