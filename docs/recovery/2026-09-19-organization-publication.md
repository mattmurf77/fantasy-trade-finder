# Project organization publication — 2026-09-19

## Scope and source

The owner requested refreshing the documentation against newer work, then publishing it to the public `mattmurf77/fantasy-trade-finder` repository. This follows the earlier explicit destination/payload discussion; the newly discovered verbatim interview reading copy is excluded.

The prepared organization head `801e00ea` was based on main `4c343a48`. Fresh main `b33b4da9` contains 24 subsequent commits, including owner-led/owner-only generation, Team Overhaul, feedback fixes, database storage controls, landing-page updates and the trade-significance gate. This release integrates that source before publishing the organization changes.

Preparation uses an independent clone. The active `codex/team-overhaul-discovery` working folder, its tracked edits and untracked drafts are preserved; no existing nested worktree is traversed, modified or removed. No local credentials, recovery archive, database, dependency tree or newly created raw interview is included in the publication.

## Reconciliation

- Generated initiative/feedback indexes use normalized status records with dated evidence. Newer main's Team Overhaul contracts and owner decisions take precedence over older local drafts.
- Session memory retains current priorities within the established byte limits. Earlier release records remain dated evidence, not an assertion that production settings were read during this task.
- Current owner, comparison, fit and other trade-model implementations remain available. Model algorithms and effective feature values are preserved from fresh main; organization changes relocate a mock-draft calibration-document reference without changing its model.
- The script map keeps both the organized tooling catalog and the newer database-maintenance guidance. No seeding, data compaction, configuration activation or production data mutation is part of this task.
- A separate future request for simultaneous liked offers, one per opposing team, remains in the queue. It is not declared implemented by this release.

## Validation and release gates

Local reconciliation checks include the deterministic index validator, bounded memory validator, focused hygiene tests, affected backend tests, mobile test-ID lint and web structural checks. Backend AST and feature-value comparisons guard against unintentionally replacing newer runtime behavior. Exact results are recorded below and on the pull request.

Publish the exact reviewed branch, require green hosted CI, then merge through the repository's normal pull-request flow. Verify that the merged tree equals the tested tree. GitHub publication, backend deployment, mobile upload, tester availability and physical-device acceptance remain separate facts. No new mobile build or runtime-setting change is requested here.

## Rollback

For the published result, revert the eventual organization pull request's squash commit on a clean branch and run CI. The earlier `52630df3` command alone is not the complete rollback for this refreshed, squash-merged publication. The pull request identifies the final merge SHA; do not reset or force-push main.

The private recovery material documented in [the original organization record](2026-09-06-project-organization.md) remains local. Reverting Git does not restore loose files removed from the legacy directory; restore selected preserved files into a separate empty recovery directory. Do not apply historical source mirrors over current main or alter the excluded nested worktrees.

## Prepublication verification

After conflict resolution, both organization validators and all 29 hygiene tests pass. The 15 affected static/seed backend tests pass, mobile test-ID lint passes and all 195 web checks pass. Independent review confirms all 98 current production backend modules remain present; executable AST matches main except for the intended calibration-document reference. Feature and tier configuration values are unchanged after excluding comment metadata. Newer model implementations, mobile versions/lockfile and deployment/build configuration are retained. Added-file review found no detected secret literals or unintended private artifact names; the new raw interview is absent. Full hosted CI remains the exact-head merge gate.
