# Project organization and recovery — 2026-09-06

## Scope and baseline

The operator authorized establishing a clean current workspace and implementing the organization audit, explicitly excluding nested worktrees. This task created an independent clone from freshly fetched `origin/main` at `4026ebc8` on `codex/project-organization-20260906`. The intended durable location is the sibling `Fleeced` project. No existing worktree or branch was removed, reset, switched, rebased or otherwise managed. No remote push, merge, build submission, deployment or flag change occurred.

The original primary checkout remains at `05e87d84` / `session-2026-08-13-notif-ship`. Its 66 modified tracked files and existing deleted recovery-record working-tree entry were preserved, not overwritten with current main. The clean clone retains main's intact `2026-08-13-device-auth-s0-sweep.md`. Original nested worktrees, credentials, databases, ranking backups and historical Git recovery bundle remain protected.

## Implemented organization

- Shared `AGENTS.md` and a small Claude adapter route to one workflow and topic map. Product introduction, engineering contracts and business assumptions have distinct homes.
- Startup memory uses a 6,000-byte serialized output cap, 2,000-byte handoff, 1,500-byte/seven-item queue, complete blocks, explicit sources, and safe missing/symlink handling. Full prior memory is retained with an integrity manifest.
- Plans and feedback each have one explicit status source. Deterministic generators produce active indexes and full catalogs; uncertain legacy outcomes are labelled. Existing documented release states/evidence were preserved rather than reset.
- Twenty-three documented closed/superseded/abandoned initiative sets moved to `docs/plans/archive/2026/`. Dated reviews moved under their year. Small compatibility redirects preserve append-only recovery links.
- September owner interviews, decisions, unanswered questions and two distinct engineering-brief versions are grouped under `docs/plans/trade-engine-balance/`. Research and private capture policies are grouped under `docs/research/competitors/`.
- Historical color labs moved out of the served web root. Maestro specifications and retired shell tooling moved into `archive/retired-tooling/`; shell tools refuse execution. Active testID lint still checks retained specifications and fails if they are absent.
- Both seed CLIs retain their original profiles, identities and behavior through a shared implementation. Code City moved to `scripts/codecity/`, with corrected root resolution and launch configuration.
- Historical root smoke evidence and item 214 reproduction scripts were promoted; unresolved TikTok cron instructions now have a durable evidence path. Old synthetic benchmark/source mirrors and local research are retained in the verified private recovery archive rather than normal source retrieval.

## Preservation and local cleanup

Private files are under ignored `_local/recovery-2026-09-06/`; EAS excludes their contents. The directory includes a verified complete-history `legacy-checkout.bundle`, binary dirty/index patches and source HEAD, a tar archive of 1,544 local-artifact files, every member's SHA-256, import dispositions, original document hashes, archived-plan dispositions, and validation logs. Every archived member was verified against the original before cleanup. It is local preservation, not an off-device backup.

The artifact tar preserves unintegrated staged implementations, deferred teardown findings, raw feedback research and unique design originals. No unresolved implementation was silently declared shipped. Restore a selected path into an empty temporary directory and compare it with current source before reusing it. Do not apply old full source mirrors to current code.

Completed original-location actions and any concurrent-change holds are recorded in the private `cleanup-result.json` after persistent installation. Generated OS/Python caches, failed `dd.json`, generated Code City output and safely preserved loose/raw artifacts are cleanup targets. Existing nested worktrees and their contents are outside that operation.

## Validation

- 29 focused hygiene/archival tests passed, including Unicode serialization, oversized context, queue grammar, symlink boundaries, malformed status fences, deterministic indexes and archive-placement errors.
- TypeScript `tsc --noEmit` passed; all 93 mobile structural suites passed; testID lint passed; all 190 web structural checks passed.
- Full backend run: 5,458 passed, 1 skipped; two old assertions expected the now-archived color labs to remain served. Updated those assertions to require 404 and retained archive files. The 15 affected static/seed tests then passed.
- The two seed profiles matched original stdout and all SQLite rows across eight legacy/new execution scenarios (default, dry-run, clear, clear plus dry-run).
- Additional mock-draft checks validate the relocated calibration artifact. Detailed completion is appended below.
- Backend executable AST comparison found documentation-only changes in 16 of 17 changed modules. The remaining module updates only `CALIBRATION_ARTIFACT` to its existing archived document path; ranking/trade algorithms and schema logic were not changed.
- EAS local archive inspection completed without a remote build: mobile screens, icon, lockfile and native files are present; private recovery, data, archived-tooling and node_modules directories contain zero files. Credentials are absent.
- Code City generated successfully from the staged file inventory. Index validators and `git diff --check` are final acceptance checks.

Local tests used installed Python 3.14 and Node 24; hosted CI remains configured for Python 3.12 and Node 20. No hosted CI run was claimed. Historical evidence snapshots retain their original paths/claims; missing documents in that history are not fabricated during reorganization.

## Final acceptance — 2026-09-06

The additional calibration/mock-draft run passed all 151 tests. All 18 static-memory snapshot files match their recorded SHA-256 and byte counts. The final hook emits 3,996 serialized UTF-8 bytes; HANDOFF is 1,107 bytes and NEXT is 1,024 bytes. Both memory and status validators pass, and the final 29-test hygiene run passes. Active entry/index/product/memory documents have no missing relative file targets; both revised workflow skills pass their structural validation.

## Persistent installation and completed cleanup — 2026-09-06

The independent current workspace is installed at `/Users/teresadickens/Documents/Claude/Projects/Fleeced`, on local branch `codex/project-organization-20260906`. The old `Fantasy Trade Finder` root now has `CURRENT_WORKSPACE.md` and an `AGENTS.md` routing future work here.

Completed 25 verified cleanup actions with no holds, removing 476,706,240 logical bytes of loose artifacts from the old location plus 22 remaining generated cache entries. Unique content remains in the private recovery archive, so the removed-byte figure is not net disk savings. All cleanup targets were checked to be untracked before deletion. Before/after hashes confirm that the original tracked diff, staged diff, HEAD and branch are unchanged. Nested worktrees were excluded from traversal and mutation.

No remote publication or deployment occurred. Existing local credentials and databases remain at the old location; configure any new runtime environment deliberately rather than copying historical source or databases wholesale. The private recovery result and validation logs live under `_local/recovery-2026-09-06/`.

## Publication preparation — 2026-09-06

The owner subsequently authorized publishing this work to GitHub. Fresh main had advanced to `4c343a48`, including feedback release PR #283 (`988fa2d6`). The organization branch merges that source intact. Conflicts were confined to generated indexes and session memory; incoming release evidence remains in the ledger and canonical references. New initiative status records distinguish verified backend release from pending native tester availability. HLD/LLD remain compact pointers; the newly documented immutable reason-episode contract is retained in the data dictionary and engineering reference map.

The public-destination review found no private recovery paths or detected credential literals among the additions. Backend executable AST comparison against the new main confirms identical behavior apart from the existing calibration document relocation. The 29 hygiene tests and both validators pass after reconciliation. Hosted CI and main merge are separate publication gates; their outcomes are recorded on the GitHub pull request for branch `codex/project-organization-20260906`. No additional mobile build or runtime-setting change is part of this publication.
