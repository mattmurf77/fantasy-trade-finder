# screens/web — empty, and staying that way

Web screen captures were a planned phase 2 that never started. This directory holds
nothing but this file.

The mobile capture pipeline (`mobile/scripts/screen-capture.sh`) was the only writer under
`screens/`, and it is now out of policy per operator decision **D-056** (2026-08-15) —
see [`../CLAUDE.md`](../CLAUDE.md). There is no capture process for `web/` and no plan to
build one.

Do not populate this ad hoc.
