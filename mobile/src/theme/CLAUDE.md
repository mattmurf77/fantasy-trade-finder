# mobile/src/theme/

Design tokens.

- `colors.ts` — palette including tier colors (Elite/Starter/Solid/Depth/Bench). Must match the canonical hex values in `docs/cross-client-invariants.md` § Tier color tokens (shared with web + extension).
- `spacing.ts` — spacing scale.

Always reference tokens — never hard-code colors or px values in components.
