# mobile/src/theme/

Design tokens.

- `colors.ts` — palette including tier colors (Elite/Starter/Solid/Depth/Bench). Must match `web/css/styles.css` and `extension/content.css`.
- `spacing.ts` — spacing scale.

Always reference tokens — never hard-code colors or px values in components.
