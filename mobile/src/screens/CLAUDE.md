# mobile/src/screens/

One file per top-level route.

| Screen | Purpose |
|---|---|
| `SignInScreen` | Sign in with Apple is the primary portal behind `auth.accounts` (P2.6 account-first: a new Apple identity lands in an account-only session, no league needed); Sleeper username demoted to "Continue with Sleeper" below it (flow unchanged). With `onboarding.landing` on (ADR-006 account-later), the layout inverts: username field primary ("See trades for your team"), Apple demoted to a quiet re-entry link, voice-doc not-found copy, and a Sleeper-down demo escape in the error state |
| `LeaguePickerScreen` | Pick which league to use |
| `LeagueScreen` | League home (since #181 the pushed `LeagueHome` sub-route of the League tab's stack — the tab lands on the rankings view; this classic page is reached from its "League home" row): hero (IDENTITY ONLY since #223 — the hero Pressable/chevron and the bottom "Switch league" button are gone; switching + the sheet live in the global TopBar), Matches tiles, Explore rows, activity, contrarian, coverage, leaderboards. **#229/#230/#234 low-activity treatment (2026-08-02, ships live — approved mock `empty-states-progress-v3.html`):** while any unlock is outstanding the page adds an action row (Rank players secondary \| Find a trade solid-ice, `league.action.rank`/`league.action.find`), ONE `LeagueProgressModule` (`league.progress-module`: positions ring + ranked-members bar + unlock sentence + invite + fold line) and a "Works right now" card (`league.works-now`: flare-labeled EXAMPLE trade rendering the real TradeValueBar, solid-ice Find a trade); confirmed-zero sections fold (Matches tiles both-0, joined chip 0, coverage ranked-0, contrarian+leaderboards on server `insufficient_data`) and return automatically once counts > 0; a fully unlocked league (ring 4/4 + matches exist + contrarian live) renders exactly the classic populated layout. **#243 (2026-08-03, approved mocks `league-home-fold.html` V1 + `risers-fallers-cards.html` D1):** divider double-margin bug fixed (`styles.divider` dropped its `marginTop` — the ScrollView `gap` already separates siblings; applies to every divider on the screen, populated state included), Explore reflowed from 3 stacked hairline rows (~201pt) to ONE 3-across tile row (`ExploreTile`; `league.rookie_board_entry` adds the 3rd tile; testIDs unchanged), hero Card padding 16→12, and the progress module's invite button became an inline text link — the low-activity state's progress module now ends fully above the 658pt fold. `MarketPulseStrip` (flag `market.movers`) mounts below Explore — operator override of the D1 mock's top-of-page slot; renders null without flag/data |
| `RankHomeScreen` | Build-your-board chooser — reached from Quick Set's "More ways to rank" header link (no longer the no-pref default since #122); describes the five ranking flows by process (guided → manual) with Quick set leading as the lowest-effort "recommended" card (#119), saves `rankingMethodPref` so later launches route straight to the chosen flow |
| `LeagueScreen` | League home (since #181 the pushed `LeagueHome` sub-route of the League tab's stack — the tab lands on the rankings view; this classic page is reached from its "League home" row): hero (IDENTITY ONLY since #223 — the hero Pressable/chevron and the bottom "Switch league" button are gone; switching + the sheet live in the global TopBar), Matches tiles, Explore rows, activity, contrarian, coverage, leaderboards |
| `RankHomeScreen` | Build-your-board chooser — reached from Quick Set's "More ways to rank" header link (no longer the no-pref default since #122); saves `rankingMethodPref` so later launches route straight to the chosen flow. **#232 consolidation (2026-08-02, approved mocks `rank-method-consolidation-v2/-v3`):** THREE primary cards labeled by outcome — FASTEST = Quick set (recommended, #119, + Quick-rank follow-on subrow) · MOST PRECISE = Head-to-heads (Trios) · MOST CONTROL = Tiers board — with Pick Anchors / Overall ranks / Trends behind a collapsed "More ways to rank" disclosure; content from the shared model `navigation/rankChooserModel.ts` (also rendered by the RankMenu sheet, so the surfaces can't diverge). Flag `ranks.import` adds the v3 Variant A import entry right of the heading ("Have rankings already?", ice underline + upload glyph, testID `rank-home.import`) opening `RankImportSheet`; a successful apply toasts + lands on Overall ranks |
| `RankScreen` | 3-player swipe matchup loop (Trios). **#243 3-up (2026-08-03, approved mock `mockups/polish-lab-2026-08/trios-three-up-v2.html` Variant A):** the three stacked full `PlayerCard`s are now three side-by-side 128pt mini-cards built inline in `TrioPlayerCard` (PlayerCard no longer used here) — badge row, reserved TWO-LINE name box (13/18 bodySm metrics, fixed 36pt so 1-line names top-align and all cards stay equal height; whole-word wrap only via `textBreakStrategy="simple"` + hyphenation off; `glueSuffix()` NBSP-joins "Harrison Jr." so a suffix never orphans), `TEAM · AGE` meta. A2 winner tap-state: ranked card = ice border + ink2 fill + check tick + "RANKED 1ST/2ND/3RD" label, un-ranked cards dim 55% once ≥1 pick exists. Pick/submit semantics (rankSide/Confirm/speed-mode/Skip/long-press) untouched; cards block 324→128pt, screen ≈551pt vs the 614pt budget (see `docs/feedback/items/243-scroll-audit/build-trios-three-up.md`). **#244 (2026-08-05):** also the Rank tab's LAUNCH route for a no-pref user whose quick-tiers are complete for all four positions (TabNav completion-aware default — see `state/quicksetProgress.ts`); an explicit `rankingMethodPref` still wins |
| `TiersScreen` | Tiered roster view |
| `QuickSetTiersScreen` | Guided tier quick-set (1.5.4 #104; Rank-stack route `QuickSetTiers`; since #119 a first-class method, since #122 the DEFAULT — the Rank tab's launch route for users with no stored `rankingMethodPref`, with a "More ways to rank" header link back to the chooser; also entered from the Tiers header, the rank-home chooser, the Rank menu, or launch routing via `rankingMethodPref: 'quickset'`) — per position, walk the ladder top→FA (8 steps since #117) tapping player chips (name + team + age + current tier, #140) into each tier; one `/api/tiers/save` per tier. #161: an explicit save (≥1 picked) also sends `demoted_pids` — visible-but-unselected players whose current tier is the saved tier or higher get pinned unranked (skip ≠ demote; see `docs/feedback/items/161-quickset-demote/status.md`). Finishing offers Quick rank (#136). **#233 (2026-08-02, supersedes the #159 labels):** at 0 selected the primary reads position-aware "Continue — no QBs this high" (last tier: "Continue & finish") and Skip is HIDDEN (it duplicated the primary); ≥1 selected restores Back / Skip / "Save <tier> (N)" exactly — tap behavior unchanged, empty save still composes as a pure skip (never #161 demotion). **#244 (2026-08-05):** launch-routed and param-less entries start at the NEXT UNSET position (TabNav `initialParams` from `state/quicksetProgress.ts` — walk record ∪ cached tiers-status; explicit `position` params still win), and a no-pref user with all four positions complete launch-routes to Trios instead of here |
| `QuickRankScreen` | Quick rank (#136; Rank-stack route `QuickRank`) — within-tier ordering pass after Quick set: same guided tier-by-tier walk, but tapping stamps click-order rank numbers on the tier's players; Save posts the tier to `/api/rankings/reorder` (clicked order + unclicked appended in current order; subset-safe permutation, tier membership invariant). Skips tiers with <2 players. Entered from the Quick set finish prompt or the Rank menu; NOT a `rankingMethodPref` route (#122 unselected) |
| `PickAnchorScreen` | Pick Anchor wizard (Rank-stack route `Anchors`; reached from the rank home chooser or the Rank menu — the Tiers-header link was removed in 1.5.4 #99) — value one player at a time in draft-pick terms via `/api/anchor/save`. The #111 pick-value-scale pill row was hidden per #134 (backend `/api/anchor/scale` plumbing + stored scales intact; default 4). Scope pills (#133, PositionTabs construction) filter the queue to All/QB/RB/WR/TE (session-only, module-level mirror); pool query self-heals after the app-resume revalidation race via `useRecoverOnResume` (#121/#125), and the load-error state has pull-to-refresh + Retry (#128) |
| `ManualRanksScreen` | Editable drag/tap rank board — labeled "Overall Ranks" in the UI |
| `TradeFinderHubScreen` | Trade-Finding Hub (#156, flag `trades.finder_hub` — **ON since 2026-07-25**; Trades-stack route `TradesHome` when the flag is on) — the mode launcher: a **Trade DNA** panel (outlook + untouchables count + Chasing/Shopping position chips with `position_needs`/`position_surplus` recommendation chips from `GET /api/league/preferences`) over four cards. Guided/Player open `TradeDeck` with a `mode` param; Team opens a manager-picker sheet then `TradeDeck` with `mode:'team'` + `opponentUserId`; Calculator → `TradeCalculator`. "Edit prefs" reuses `OutlookSheet`. Finish batch (2026-07-25): Specific Player card shows LIVE pin counts from `useFinderTargets`; the untouchables row opens a management sheet (#173 — list + remove + how-to-add). #196/#197: no local `FeedbackFAB` — as a tab-stack screen it's covered by the RootNav global mount (the hub-finish local mount doubled the flag). #223: the "Trading in" `LeaguePill` row + local switcher-sheet mount are gone — the global TopBar carries the league + switcher; the in-page "Find a Trade" title stays. #218/#219 (2026-08-01) fit-to-screen density pass (approved mock `mockups/polish-lab-2026-08/hub-fit-to-screen.html`): screen pad 16→12, stack gap 12→8, DNA pad 12→10 / gap 8→6 with Chasing/Shopping labels INLINE beside the chips (64pt label column), section-label margin dropped, card pad-v 12→9, icon well 40→34 (glyph 22→20) — hub content ~491pt vs a 658pt viewport, no scroll in the nominal state (see `docs/feedback/items/218-hub-fit-to-screen/status.md`). #242 (2026-08-02): the Specific-Team manager-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%, shared with the untouchables sheet) so a 12-team league fits without scrolling |
| `TradesScreen` | Trade card browser. Also serves as the `TradeDeck` route under the finder hub (#156): reads `route.params.mode` (`guided`/`team`/`player`) + (team) `opponentUserId`/`opponentName`, renders the `TradeFinderModeBar` quick-switch row, hides the Trades/Portfolio/Calculator subnav, and threads `opponent_user_id` into `/api/trades/generate`. Flag off / no params ⇒ unchanged standalone home. Finish batch (2026-07-25): player mode renders the two-column FOR/AWAY pin board; #174 `PackageToggle` ("Trade as one package" → `pinned_give_mode:'all'`) with 2+ give pins; the mode bar's Team chip opens an in-screen manager sheet and switches scope IN PLACE (setParams; in-place change auto-regenerates); pins live in `useFinderTargets`; the top deck card carries #186 keep-side + #190 edit-in-calculator actions. 2026-07-26 (flag `trade.asset_ideas`): with exactly ONE pin (either direction) the asset-ideas surface renders under the controls card ALONGSIDE the deck — grouped Upgrade/Lateral/Downgrade ideas from `POST /api/trades/asset-ideas` (pin-driven `useQuery`, no button). **#216/#209 (2026-08-02, approved mocks `mockups/polish-lab-2026-08/asset-ideas-layout-v2/-v3`):** the single-pin surface is now the FEATURED TRADE window (`FeaturedTradeWindow` — best-difference idea as a read-only `TradeCard` with the Dynasty Value Swing `TradeValueBar` from the idea's server `favors`/`gap`, #190 edit-in-calculator kept) over the always-visible `AssetIdeasPanel` "More trades" list; row taps swap ideas INTO the window (screen owns `featuredIdea` + a ≤10-deep per-pin history stack, reset on pin change / fresh sweep; tap scrolls the window back into view via `mainScrollRef`), the ‹ Previous trade chip (Variant A) reverts, and the in-window row is tagged + inert. #241 (2026-08-02): in single-pin mode the window + list IS the page — the swipe deck, Find-a-Trade button and progress strip do NOT render (the #216 build had left the deck mounting beneath, showing a mystery second trade card); every other mode keeps the deck exactly as before. #242 (2026-08-02): the in-screen team-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%) so a 12-team league's 11 manager rows fit without scrolling. #243 (2026-08-03, approved mock `pin-mode-collapsed-controls.html` V1): in single-pin mode the Controls Card collapses by default to a one-line "Pinned: <name> · Edit" row (`trades.pin-summary`/`.edit`, ~44pt vs ~286pt); Edit expands the exact full card in place with a Done collapse header (`trades.pin-summary.done`), state resets to collapsed on pin change/mode entry; all other modes render the full card unchanged. #209: the player-mode pin board reordered to TRADE AWAY left / TRADE FOR right so board, featured card and idea rows all read give→get. F9 (flag `deck.first_session`): on the server-marked `first_deck` job, session dispositions/likes are tallied (refs, reset per job/league) → `first_session_like` / `first_session_deck_completed` events + at most one inline adaptation-moment card between deck cards (trigger math in `utils/firstSessionMoment.ts`; QuickSetPromptCard slot precedent; copy variants gated for literal truth — 'rerank' only when `deck.session_rerank` is operating); every board-refreshed deck renders the "Built from your updated board" header line (`job.board_refresh`, stacked ABOVE the F3 suppression note). 2026-07-27: top-card assets gain per-asset edit commands — a context-menu "Swap suggestions" row (SwapSuggestSheet: the calc eveners for the trade MINUS that asset, one-tap replacements via `evaluateForSwapSuggestions`) and #194 remove (✕ per row, min 1 asset/side hint, confirm when it breaks a #174 pinned package); both re-price through the shared `applyPackageEdit` → Mode B evaluate flow. #223/#224: the "Trading in" `LeaguePill` + local switcher-sheet mount are gone (league + switcher live in the global TopBar; the [leagueId] effect still resets the deck on a switch), and the classic standalone home renders a compact in-page "Find a Trade" heading (hidden in finder-mode launches — the mode bar titles those — and during first-run collapsed chrome) |
| `TradeFinderHubScreen` | Trade-Finding Hub (#156, flag `trades.finder_hub` — **ON since 2026-07-25**; Trades-stack route `TradesHome` when the flag is on) — the mode launcher: a **Trade DNA** panel over four cards. Guided/Player open `TradeDeck` with a `mode` param; Team opens a manager-picker sheet then `TradeDeck` with `mode:'team'` + `opponentUserId`; Calculator → `TradeCalculator`. Finish batch (2026-07-25): Specific Player card shows LIVE pin counts from `useFinderTargets`; #173 untouchables management sheet (list + remove + how-to-add). **#212/#231/#206 (2026-08-02, approved mock `mockups/polish-lab-2026-08/trade-dna-outlook-v3.html` + operator tweaks):** the DNA panel is collapsed by default — outlook chip with plain-words bias ("Rebuilding · leans young + picks"; inferred marked, `not_sure` = "no bias applied"), ONE summary line listing ALL explicit selections with position dots ("Chasing ●WR ●TE — Shopping ●QB ●RB", groups omitted when unset), untouchable mini-card chips (6px position dot + "A Jeanty" first-initial+last-name, cap 3 + "+N", read-only, no Manage collapsed). **Edit expands IN PLACE** (no OutlookSheet — the sheet's hub mount is gone; it survives on TradesScreen): four compact outlook cards + Chasing/Shopping rows of five multi-select toggles (QB RB WR TE + Picks storing `'PICK'`; selected = solid position-color fill + check glyph + bold dark label — check is the primary cue per the WR-contrast note; cross-row tap MOVES the position), count + Manage untouchables line, Done persists via `POST /api/league/preferences` ONLY when touched (an idle expand never invalidates the cached deck; no selection saves `not_sure`). **#236 (2026-08-02): the editor AUTOSAVES — every tap (outlook pick / chip toggle) POSTs the full payload immediately, one request in flight with trailing coalesce (last-write-wins), failure reverts drafts to last-saved prefs + inline error; Done is now a pure collapse (never POSTs, never disabled); an idle expand/collapse still fires no POST.** The #206 NEED/DEPTH recommendation chips were DROPPED (operator: "overkill") — `utils/dnaChips.ts` deleted with its only consumer; `position_needs`/`position_surplus` stay on the GET payload. Route param `editDna:true` (set by the deck receipt's Change link) auto-expands the editor. See `docs/feedback/items/212-trade-dna-redesign/status.md`. #196/#197: no local `FeedbackFAB` — as a tab-stack screen it's covered by the RootNav global mount (the hub-finish local mount doubled the flag). #223: the "Trading in" `LeaguePill` row + local switcher-sheet mount are gone — the global TopBar carries the league + switcher; the in-page "Find a Trade" title stays. #218/#219 (2026-08-01) fit-to-screen density pass (approved mock `mockups/polish-lab-2026-08/hub-fit-to-screen.html`): screen pad 16→12, stack gap 12→8, DNA pad 12→10 / gap 8→6 with Chasing/Shopping labels INLINE beside the chips (64pt label column), section-label margin dropped, card pad-v 12→9, icon well 40→34 (glyph 22→20) — hub content ~491pt vs a 658pt viewport, no scroll in the nominal state (see `docs/feedback/items/218-hub-fit-to-screen/status.md`). #242 (2026-08-02): the Specific-Team manager-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%, shared with the untouchables sheet) so a 12-team league fits without scrolling |
| `TradesScreen` | Trade card browser. Also serves as the `TradeDeck` route under the finder hub (#156): reads `route.params.mode` (`guided`/`team`/`player`) + (team) `opponentUserId`/`opponentName`, renders the `TradeFinderModeBar` quick-switch row, hides the Trades/Portfolio/Calculator subnav, and threads `opponent_user_id` into `/api/trades/generate`. Flag off / no params ⇒ unchanged standalone home. Finish batch (2026-07-25): player mode renders the two-column FOR/AWAY pin board; #174 `PackageToggle` ("Trade as one package" → `pinned_give_mode:'all'`) with 2+ give pins; the mode bar's Team chip opens an in-screen manager sheet and switches scope IN PLACE (setParams; in-place change auto-regenerates); pins live in `useFinderTargets`; the top deck card carries #186 keep-side + #190 edit-in-calculator actions. 2026-07-26 (flag `trade.asset_ideas`): with exactly ONE pin (either direction) an `AssetIdeasPanel` renders under the controls card ALONGSIDE the deck — grouped Upgrade/Lateral/Downgrade ideas from `POST /api/trades/asset-ideas` (pin-driven `useQuery`, no button); row tap opens `TradeCalculator` with the #190 `prefill`. F9 (flag `deck.first_session`): on the server-marked `first_deck` job, session dispositions/likes are tallied (refs, reset per job/league) → `first_session_like` / `first_session_deck_completed` events + at most one inline adaptation-moment card between deck cards (trigger math in `utils/firstSessionMoment.ts`; QuickSetPromptCard slot precedent; copy variants gated for literal truth — 'rerank' only when `deck.session_rerank` is operating); every board-refreshed deck renders the "Built from your updated board" header line (`job.board_refresh`, stacked ABOVE the F3 suppression note). 2026-07-27: top-card assets gain per-asset edit commands — a context-menu "Swap suggestions" row (SwapSuggestSheet: the calc eveners for the trade MINUS that asset, one-tap replacements via `evaluateForSwapSuggestions`) and #194 remove (✕ per row, min 1 asset/side hint, confirm when it breaks a #174 pinned package); both re-price through the shared `applyPackageEdit` → Mode B evaluate flow. #223/#224: the "Trading in" `LeaguePill` + local switcher-sheet mount are gone (league + switcher live in the global TopBar; the [leagueId] effect still resets the deck on a switch), and the classic standalone home renders a compact in-page "Find a Trade" heading (hidden in finder-mode launches — the mode bar titles those — and during first-run collapsed chrome). #231 (2026-08-02): finder-mode launches mount the self-contained `OutlookBiasReceipt` (one line under the mode bar) — the quiet outlook-bias receipt whose Change link routes to the hub's DNA editor |
| `TradeCalculatorScreen` | Manual trade builder — "Calculator" pill in Trades. Live mode: real consensus values via /api/trade/*; Demo mode: mock dual-board league. #190 (2026-07-25): optional `prefill` route param `{opponentUserId, giveIds, receiveIds}` (deck card's "Edit in calculator") lands in In-league mode with the package preloaded (draft-mode restore guarded; keyed remount on a new prefill). #213 (2026-08-01): one quiet "Want ideas instead? Find a trade →" text-link row under the mode tabs (testID `calc.find-a-trade`, all three modes) → `TradesHome` |
| `MatchesScreen` | Mutual trade matches inbox. #229/#234 (2026-08-02, ships live): the mutual empty state explains the mechanic ("A match needs two boards…"), leads with an always-on "Find a trade" primary (→ Trades hub; keeps testID `matches.go-to-trades`, superseding the S4 PRD-05 flag on this one state), and mounts the compact `LeagueProgressModule` (`matches.progress-module`, active league only) so League home and Matches tell one story; Refresh stays as a ghost (the empty state isn't scrollable — documented deviation from the mock's "Refresh is dropped"); the awaiting segment's empty state is unchanged (still flag-gated) |
| `SleeperConnectScreen` | WebView login to Sleeper → captures the JWT for "Send in Sleeper" (flagged beta). Doubles as account **verification** (account-auth P1): the backend proves the captured token live + claim-matched, marks the session verified, and the screen surfaces "Account verified" on success |
| `SettingsScreen` | Settings modal (gear icon; explicit header close control per #130): leagues (+ flag-gated `espn.link` "Link an ESPN league" row → LeaguePicker with the sheet auto-opened, #130), ranking pref, notifications, and the **Account** section — renders meaningful state for every session type (P2.7): linked identities, an Apple-link button for any session without an Apple identity (binds via `POST /api/auth/apple` on the live session and flips `useSession.verification`), Sleeper source row, link-Sleeper card for account-only users (P2.6 merge flow), verification status, Verify account (Sleeper sessions), Delete account |
| `LeagueSummaryScreen` | League rankings / power rankings (#142/#144; since #181 the League TAB's root — stack route `LeagueRankings`, the page the tab lands on, with a "League home" row (testID `league-summary.league-home`) pushing `LeagueHome`; the root-stack route `LeagueSummary` remains as a legacy variant for deep link `app/league/summary` + old entry points, same component, no home row) — every team ranked by summed roster value via `GET /api/league/power-rankings`, basis toggle Consensus \| My board (personal values, consensus fallback for unranked players) + a disabled "Redraft (soon)" chip (backend reserves basis=redraft but 501s — dynasty values only). Tapping a team opens an overlay of its roster grouped by position, value-sorted within each group, in dense PlayerCard rows. #14 completion (2026-07-20): neutral **Picks** filter pill + trailing bar segment + drill-in "Draft capital" section (team.picks from the generic ladder; renders only when the league holds picks — ESPN/demo/old servers degrade cleanly), and an "Updated <relative> · pull to refresh" line from `updated_at`. **2026-07-26 League Analyzer treatment** (DynastyGM teardown): VERTICAL stacked columns (x-axis rank 1..N, caller's numeral in an ice pill) in a chart card (league name + "Dynasty · <format>" caption + refresh affordance) over a ranked team list; **All · Starters · Bench** segmented control (`league-summary.subset.*`) — Starters = the server-DERIVED value-optimal lineup (`teams[].starters`, basis-aware, no per-week data), selecting re-ranks the whole league client-side; hidden when `starters_available` is false (ESPN/MFL/demo — honest degradation); drill-in focus grays every other bar and renders the roster INLINE below the chart (no Modal) with tercile-colored positional-rank chips and per-player positional value ranks ("RB2"/"NR"). #237 (2026-08-02): the drill-in roster panel mirrors the chart card's FULL filter set — the All/Starters/Bench `SubsetControl` (`league-summary.roster-subset.*`) plus the position pills — bound to the SAME `subset`/`posFilter` state as the chart (the panel's independent `drillPos` filter is gone), so the two sections always match. **#243 drill-in filter dedup, V1 (2026-08-03, approved mock `mockups/polish-lab-2026-08/drilldown-filter-dedup.html`):** WHILE a team is focused the chart card collapses to a slim strip — its own `SubsetControl`/`PosFilterPills` do NOT mount (a passive "Filtered by: <Subset>[ · <positions>]" caption, testID `league-summary.filter-caption`, stands in and points at the drill panel's copy — the single visible/interactive set; hint margins tighten via `hintTight`), the X close control becomes a "‹ All teams" back affordance (KEEPS testID `league-summary.roster-close`; inner label `league-summary.back-all-teams`), and the tab-root "League home" row hides — pre-roster focused content drops ~692pt → ~557pt so the first roster group + row clear the 658pt fold. Visibility-only: #237's shared-state model and the entire unfocused rendering (both control sets, home row, X-restores caption) are untouched — unfocusing restores the prior layout exactly (see `docs/feedback/items/243-scroll-audit/build-drilldown-dedup.md`) |
| `FreeAgentsScreen` | Free-agent finder (#143; root-stack route `FreeAgents`, entered from the League tab's "Free agents" row) — best available players in the league ranked by the caller's board values (consensus fallback per unranked player; `user_has_rankings:false` shows a "consensus values" notice), QB/RB/WR/TE filter pills, dense PlayerCard rows with value + FA positional rank, and a "Drop: <player> (+delta)" subline when the backend found a lower-valued same-position roster player to cut. Per-row **Add** (#179): Sleeper leagues open the claim-preparation sheet (v2 2026-07-26 — FAAB bid input + "Budget: $N remaining" for FAAB leagues, "waiver priority league" copy otherwise; open-slot detection; radio-select drop candidates least-valuable-first with untouchables withheld) ending in a deep-link to the league's Sleeper players page (Sleeper has no public write API — FTF prepares the claim, the user executes it in Sleeper); platform-linked/local leagues get a dimmed Add whose tap explains the missing write path. Also entered from the Trades home's Explore row (#182, `trades.explore.free-agents`). Backend `GET /api/league/free-agents` (#178: 503 `rosters_unavailable` surfaces its honest message instead of generic error copy) |
| `PlaceholderScreen` | Stub for unfinished routes |
| `TestStagesScreen` | Operator QA (root-stack `TestStages`; Settings → Testing row, flag `testing.stage_users` — delivered per-device via the experiment overlay, so only allowlisted operators see it; the spawn route is allowlist-gated server-side regardless): spawn a synthetic `qa_*` user at an adoption stage (fresh / activated / board_owner / converted / power) via `POST /api/test-users`, swap this device into it (token + user + demo-built league + verbatim `client_state` → `replaceOnboardingState`), plus device-only Factory reset (wipe first-run/tour state + sign out). Force-quit recommended after a swap (module-level once-per-session guards reset on relaunch) |
