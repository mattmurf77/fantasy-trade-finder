# mobile/src/screens/

One file per top-level route.

| Screen | Purpose |
|---|---|
| `SignInScreen` | Sign in with Apple is the primary portal behind `auth.accounts` (P2.6 account-first: a new Apple identity lands in an account-only session, no league needed); Sleeper username demoted to "Continue with Sleeper" below it (flow unchanged). With `onboarding.landing` on (ADR-006 account-later), the layout inverts: username field primary ("See trades for your team"), Apple demoted to a quiet re-entry link, voice-doc not-found copy, and a Sleeper-down demo escape in the error state |
| `LeaguePickerScreen` | Pick which league to use |
| `LeagueScreen` | League home (since #181 the pushed `LeagueHome` sub-route of the League tab's stack — the tab lands on the rankings view; this classic page is reached from its "League home" row): hero (IDENTITY ONLY since #223 — the hero Pressable/chevron and the bottom "Switch league" button are gone; switching + the sheet live in the global TopBar), Matches tiles, Explore rows, activity, contrarian, coverage, leaderboards. **#229/#230/#234 low-activity treatment (2026-08-02, ships live — approved mock `empty-states-progress-v3.html`):** while any unlock is outstanding the page adds an action row (Rank players secondary \| Find a trade solid-ice, `league.action.rank`/`league.action.find`), ONE `LeagueProgressModule` (`league.progress-module`: positions ring + ranked-members bar + unlock sentence + invite + fold line) and a "Works right now" card (`league.works-now`: flare-labeled EXAMPLE trade rendering the real TradeValueBar, solid-ice Find a trade); confirmed-zero sections fold (Matches tiles both-0, joined chip 0, coverage ranked-0, contrarian+leaderboards on server `insufficient_data`) and return automatically once counts > 0; a fully unlocked league (ring 4/4 + matches exist + contrarian live) renders exactly the classic populated layout. **#243 (2026-08-03, approved mocks `league-home-fold.html` V1 + `risers-fallers-cards.html` D1):** divider double-margin bug fixed (`styles.divider` dropped its `marginTop` — the ScrollView `gap` already separates siblings; applies to every divider on the screen, populated state included), Explore reflowed from 3 stacked hairline rows (~201pt) to ONE 3-across tile row (`ExploreTile`; `league.rookie_board_entry` adds the 3rd tile; testIDs unchanged), hero Card padding 16→12, and the progress module's invite button became an inline text link — the low-activity state's progress module now ends fully above the 658pt fold. `MarketPulseStrip` (flag `market.movers`) mounts below Explore — operator override of the D1 mock's top-of-page slot; renders null without flag/data. **draft-extensions W3 M-A (2026-08-06, flag `picks.assign`, ships OFF):** a dedicated **"Draft picks"** section (divider + `TickLabel` + full-width hairline row `league.draft-picks-row`) sits BELOW Explore and pushes the root-stack `PickAssignment` screen. Deliberately **not** a 4th Explore tile — that row is a fold-budgeted 3-across grid whose third slot is already a one-slot/two-occupant conditional, and the separate section also keeps assignment visibly "separate from the draft feature" as the operator framed it. Gated on **flag AND `isEspn`**, because ESPN is the only platform whose rookie draft FTF cannot read; the sub-line reads the live state through the shared `pickAssignmentSubline` formatter ("Not assigned yet" / "48 of 48 assigned · 3 traded") off a `['pick-assignments', leagueId]` query that shares its key with the screen, so the push renders warm and a save there updates this line on the way back. Flag off or non-ESPN ⇒ no section AND no request — the page is byte-identical |
| `RankHomeScreen` | Build-your-board chooser — reached from Quick Set's "More ways to rank" header link (no longer the no-pref default since #122); describes the five ranking flows by process (guided → manual) with Quick set leading as the lowest-effort "recommended" card (#119), saves `rankingMethodPref` so later launches route straight to the chosen flow |
| `LeagueScreen` | League home (since #181 the pushed `LeagueHome` sub-route of the League tab's stack — the tab lands on the rankings view; this classic page is reached from its "League home" row): hero (IDENTITY ONLY since #223 — the hero Pressable/chevron and the bottom "Switch league" button are gone; switching + the sheet live in the global TopBar), Matches tiles, Explore rows, activity, contrarian, coverage, leaderboards |
| `RankHomeScreen` | Build-your-board chooser — reached from Quick Set's "More ways to rank" header link (no longer the no-pref default since #122); saves `rankingMethodPref` so later launches route straight to the chosen flow. **#232 consolidation (2026-08-02, approved mocks `rank-method-consolidation-v2/-v3`):** THREE primary cards labeled by outcome — FASTEST = Quick set (recommended, #119, + Quick-rank follow-on subrow) · MOST PRECISE = Head-to-heads (Trios) · MOST CONTROL = Tiers board — with Pick Anchors / Overall ranks / Trends behind a collapsed "More ways to rank" disclosure; content from the shared model `navigation/rankChooserModel.ts` (also rendered by the RankMenu sheet, so the surfaces can't diverge). Flag `ranks.import` adds the v3 Variant A import entry right of the heading ("Have rankings already?", ice underline + upload glyph, testID `rank-home.import`) opening `RankImportSheet`; a successful apply toasts + lands on Overall ranks |
| `RankScreen` | 3-player swipe matchup loop (Trios). **#243 3-up (2026-08-03, approved mock `mockups/polish-lab-2026-08/trios-three-up-v2.html` Variant A):** the three stacked full `PlayerCard`s are now three side-by-side 128pt mini-cards built inline in `TrioPlayerCard` (PlayerCard no longer used here) — badge row, reserved TWO-LINE name box (13/18 bodySm metrics, fixed 36pt so 1-line names top-align and all cards stay equal height; whole-word wrap only via `textBreakStrategy="simple"` + hyphenation off; `glueSuffix()` NBSP-joins "Harrison Jr." so a suffix never orphans), `TEAM · AGE` meta. A2 winner tap-state: ranked card = ice border + ink2 fill + check tick + "RANKED 1ST/2ND/3RD" label, un-ranked cards dim 55% once ≥1 pick exists. Pick/submit semantics (rankSide/Confirm/speed-mode/Skip/long-press) untouched; cards block 324→128pt, screen ≈551pt vs the 614pt budget (see `docs/feedback/items/243-scroll-audit/build-trios-three-up.md`). **#244 (2026-08-05):** also the Rank tab's LAUNCH route for a no-pref user whose quick-tiers are complete for all four positions (TabNav completion-aware default — see `state/quicksetProgress.ts`); an explicit `rankingMethodPref` still wins |
| `TiersScreen` | Tiered roster view |
| `QuickSetTiersScreen` | Guided tier quick-set (1.5.4 #104; Rank-stack route `QuickSetTiers`; since #119 a first-class method, since #122 the DEFAULT — the Rank tab's launch route for users with no stored `rankingMethodPref`, with a "More ways to rank" header link back to the chooser; also entered from the Tiers header, the rank-home chooser, the Rank menu, or launch routing via `rankingMethodPref: 'quickset'`) — per position, walk the ladder top→FA (8 steps since #117) tapping player chips (name + team + age + current tier, #140) into each tier; one `/api/tiers/save` per tier. #161: an explicit save (≥1 picked) also sends `demoted_pids` — visible-but-unselected players whose current tier is the saved tier or higher get pinned unranked (skip ≠ demote; see `docs/feedback/items/161-quickset-demote/status.md`). Finishing offers Quick rank (#136). **#233 (2026-08-02, supersedes the #159 labels):** at 0 selected the primary reads position-aware "Continue — no QBs this high" (last tier: "Continue & finish") and Skip is HIDDEN (it duplicated the primary); ≥1 selected restores Back / Skip / "Save <tier> (N)" exactly — tap behavior unchanged, empty save still composes as a pure skip (never #161 demotion). **#244 (2026-08-05):** launch-routed and param-less entries start at the NEXT UNSET position (TabNav `initialParams` from `state/quicksetProgress.ts` — walk record ∪ cached tiers-status; explicit `position` params still win), and a no-pref user with all four positions complete launch-routes to Trios instead of here |
| `QuickRankScreen` | Quick rank (#136; Rank-stack route `QuickRank`) — within-tier ordering pass after Quick set: same guided tier-by-tier walk, but tapping stamps click-order rank numbers on the tier's players; Save posts the tier to `/api/rankings/reorder` (clicked order + unclicked appended in current order; subset-safe permutation, tier membership invariant). Skips tiers with <2 players. Entered from the Quick set finish prompt or the Rank menu; NOT a `rankingMethodPref` route (#122 unselected) |
| `PickAnchorScreen` | Pick Anchor wizard (Rank-stack route `Anchors`; reached from the rank home chooser or the Rank menu — the Tiers-header link was removed in 1.5.4 #99) — value one player at a time in draft-pick terms via `/api/anchor/save`. The #111 pick-value-scale pill row was hidden per #134 (backend `/api/anchor/scale` plumbing + stored scales intact; default 4). Scope pills (#133, PositionTabs construction) filter the queue to All/QB/RB/WR/TE (session-only, module-level mirror); pool query self-heals after the app-resume revalidation race via `useRecoverOnResume` (#121/#125), and the load-error state has pull-to-refresh + Retry (#128). **draft-extensions W1 (2026-08-06):** the rung grid moved to `utils/anchorRows.ts` when `components/AnchorSheet` became a second host of the same question — one table, so a rung can never diverge between the wizard and the sheet (the keys are a cross-client enum). Behavior here is otherwise untouched; the wizard still omits `via`, which sends the pre-W1 request body exactly |
| `ManualRanksScreen` | Editable drag/tap rank board — labeled "Overall Ranks" in the UI |
| `RookieRanksScreen` | **rookie-draft M2 / operator decision O1-expanded (2026-08-06, flag `ranks.rookie_subset`)** — the CONSOLIDATED cross-position rookie ranking view (Rank-stack route `RookieRanks`, deep link `app/rank/rookies`). Reads `GET /api/rankings?scope=rookie` with NO position, i.e. the one board through the one post-Elo view filter — so the values here and on the QB/RB/WR/TE boards are synced BY CONSTRUCTION, not by a sync step (this is why a separate rookie Elo space was rejected). Rows carry the cross-position rookie rank, position badge, `<POS><n> rookie` positional rookie rank, tier badge and 0–10k value; ALL/QB/RB/WR/TE pills filter the ONE list client-side (no second fetch). **EDITABLE since 2026-08-06 (operator decision, live TestFlight testing of build 82):** it shipped READ-ONLY on the argument that "a seventh drag board would just be Overall ranks under scope" — but the Draft Room's row is labeled "Rank the rookies", it lands here, and the screen could not rank. It is now the SHIPPED drag board, reused not reinvented: `DraggableFlatList` with `ManualRanksScreen`'s exact interaction (220ms long-press pickup, `DRAG_ACTIVATION_DISTANCE`, pickup/swipe haptics, 600ms debounced save, filtered-view splice-back, Move up / Move down VoiceOver custom actions, save-status pill `rookie-ranks.save-status`), plus drag handles `rookie-ranks.drag-handle.<pid>`. The left column keeps showing the CROSS-POSITION rookie rank even under a position filter — that is the screen's point. **THE REORDER LANE ONLY:** writes go through `POST /api/rankings/reorder` -> `apply_reorder`, which is subset-safe by construction (it permutes the sorted multiset of exactly the posted ids' OWN Elos and writes overrides for exactly those pids), so a rookie-scoped reorder is byte-identical to the equivalent unscoped reorder of that subsequence and no veteran ever moves — which is why `reorderRankings` takes no `scope`. `position:null` + every rookie id under ALL; `position:<POS>` + that position's rookie slice under a filter. Writes carry `via:'rookie_ranks'` (request-only — the route branches on `'quickrank'` alone — but it keeps a scoped edit identifiable). **HARD CONSTRAINT: this screen may NEVER reach `/api/tiers/save` / `save_tiers_position` / `apply_tiers` / the merged-band `apply_tiers_subset` path** — the one construction that can destroy a user's board. Pinned by behaviour + source tests in `backend/tests/test_rookie_ranks_editable.py`. Entered from any rank page's `RookieScopeControl` link and from the RankHome "Rookies" section (`rank-home.rookie-ranks`, which also flips the shared scope). Flag off ⇒ no entry point anywhere; reaching the route via a stale deep link renders an honest "not available yet" state. **draft-extensions W1 (2026-08-06):** the Draft Room bridge is TWO-way — when the room passes `returnTo:'DraftRoom'` (+ `returnLeagueId`) this screen renders a "‹ Back to the draft room" row (`rookie-ranks.back-to-draft`) above the header. Without it the bridge landed users two hops from any editing gesture with no way back, so acting on a rookie cost them their place on the board. Every other entry omits the params and the row does not render |
| `TradeFinderHubScreen` | **UNROUTED as of #246 (2026-08-05, guided-first landing)** — `TradesHome` now renders the guided deck directly (TradesScreen with default `mode:'guided'`); no navigator registers this screen and nothing imports it. Kept in the tree pending a later cleanup pass; its DNA editor body was lifted into `components/TradeDnaSheet.tsx` and its FA link superseded by the mode strip's Free agents chip. Historical row follows. Trade-Finding Hub (#156, flag `trades.finder_hub` — was the Trades-stack route `TradesHome` while routed) — the mode launcher: a **Trade DNA** panel (outlook + untouchables count + Chasing/Shopping position chips with `position_needs`/`position_surplus` recommendation chips from `GET /api/league/preferences`) over four cards. Guided/Player open `TradeDeck` with a `mode` param; Team opens a manager-picker sheet then `TradeDeck` with `mode:'team'` + `opponentUserId`; Calculator → `TradeCalculator`. "Edit prefs" reuses `OutlookSheet`. Finish batch (2026-07-25): Specific Player card shows LIVE pin counts from `useFinderTargets`; the untouchables row opens a management sheet (#173 — list + remove + how-to-add). #196/#197: no local `FeedbackFAB` — as a tab-stack screen it's covered by the RootNav global mount (the hub-finish local mount doubled the flag). #223: the "Trading in" `LeaguePill` row + local switcher-sheet mount are gone — the global TopBar carries the league + switcher; the in-page "Find a Trade" title stays. #218/#219 (2026-08-01) fit-to-screen density pass (approved mock `mockups/polish-lab-2026-08/hub-fit-to-screen.html`): screen pad 16→12, stack gap 12→8, DNA pad 12→10 / gap 8→6 with Chasing/Shopping labels INLINE beside the chips (64pt label column), section-label margin dropped, card pad-v 12→9, icon well 40→34 (glyph 22→20) — hub content ~491pt vs a 658pt viewport, no scroll in the nominal state (see `docs/feedback/items/218-hub-fit-to-screen/status.md`). #242 (2026-08-02): the Specific-Team manager-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%, shared with the untouchables sheet) so a 12-team league fits without scrolling. **#245 (2026-08-05): the tab is labeled "Acquire"** (route `Trades` unchanged); the in-page title is now "Acquire" (was "Find a Trade") and the hub splits into a TRADE TickLabel section (the existing DNA panel + mode cards) and a FREE AGENCY TickLabel section — one row (`finder-hub.card.free-agents`, plus icon) linking to the root-stack `FreeAgents` finder (#143; the League tab entry stays). Adds ~113pt → mid-state ~581pt vs the 658pt budget (see `docs/feedback/items/245-acquire-tab/status.md`) |
| `TradesScreen` | Trade card browser. Also serves as the `TradeDeck` route under the finder hub (#156): reads `route.params.mode` (`guided`/`team`/`player`) + (team) `opponentUserId`/`opponentName`, renders the `TradeFinderModeBar` quick-switch row, hides the Trades/Portfolio/Calculator subnav, and threads `opponent_user_id` into `/api/trades/generate`. Flag off / no params ⇒ unchanged standalone home. Finish batch (2026-07-25): player mode renders the two-column FOR/AWAY pin board; #174 `PackageToggle` ("Trade as one package" → `pinned_give_mode:'all'`) with 2+ give pins; the mode bar's Team chip opens an in-screen manager sheet and switches scope IN PLACE (setParams; in-place change auto-regenerates); pins live in `useFinderTargets`; the top deck card carries #186 keep-side + #190 edit-in-calculator actions. 2026-07-26 (flag `trade.asset_ideas`): with exactly ONE pin (either direction) the asset-ideas surface renders under the controls card ALONGSIDE the deck — grouped Upgrade/Lateral/Downgrade ideas from `POST /api/trades/asset-ideas` (pin-driven `useQuery`, no button). **#216/#209 (2026-08-02, approved mocks `mockups/polish-lab-2026-08/asset-ideas-layout-v2/-v3`):** the single-pin surface is now the FEATURED TRADE window (`FeaturedTradeWindow` — best-difference idea as a read-only `TradeCard` with the Dynasty Value Swing `TradeValueBar` from the idea's server `favors`/`gap`, #190 edit-in-calculator kept) over the always-visible `AssetIdeasPanel` "More trades" list; row taps swap ideas INTO the window (screen owns `featuredIdea` + a ≤10-deep per-pin history stack, reset on pin change / fresh sweep; tap scrolls the window back into view via `mainScrollRef`), the ‹ Previous trade chip (Variant A) reverts, and the in-window row is tagged + inert. #241 (2026-08-02): in single-pin mode the window + list IS the page — the swipe deck, Find-a-Trade button and progress strip do NOT render (the #216 build had left the deck mounting beneath, showing a mystery second trade card); every other mode keeps the deck exactly as before. #242 (2026-08-02): the in-screen team-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%) so a 12-team league's 11 manager rows fit without scrolling. #243 (2026-08-03, approved mock `pin-mode-collapsed-controls.html` V1): in single-pin mode the Controls Card collapses by default to a one-line "Pinned: <name> · Edit" row (`trades.pin-summary`/`.edit`, ~44pt vs ~286pt); Edit expands the exact full card in place with a Done collapse header (`trades.pin-summary.done`), state resets to collapsed on pin change/mode entry; all other modes render the full card unchanged. #209: the player-mode pin board reordered to TRADE AWAY left / TRADE FOR right so board, featured card and idea rows all read give→get. F9 (flag `deck.first_session`): on the server-marked `first_deck` job, session dispositions/likes are tallied (refs, reset per job/league) → `first_session_like` / `first_session_deck_completed` events + at most one inline adaptation-moment card between deck cards (trigger math in `utils/firstSessionMoment.ts`; QuickSetPromptCard slot precedent; copy variants gated for literal truth — 'rerank' only when `deck.session_rerank` is operating); every board-refreshed deck renders the "Built from your updated board" header line (`job.board_refresh`, stacked ABOVE the F3 suppression note). 2026-07-27: top-card assets gain per-asset edit commands — a context-menu "Swap suggestions" row (SwapSuggestSheet: the calc eveners for the trade MINUS that asset, one-tap replacements via `evaluateForSwapSuggestions`) and #194 remove (✕ per row, min 1 asset/side hint, confirm when it breaks a #174 pinned package); both re-price through the shared `applyPackageEdit` → Mode B evaluate flow. #223/#224: the "Trading in" `LeaguePill` + local switcher-sheet mount are gone (league + switcher live in the global TopBar; the [leagueId] effect still resets the deck on a switch), and the classic standalone home renders a compact in-page "Find a Trade" heading (hidden in finder-mode launches — the mode bar titles those — and during first-run collapsed chrome). **#246 (2026-08-05, guided-first landing — approved mock `mockups/polish-lab-2026-08/acquire-landing-guided-first.html` V1 + operator FA-chip override):** with `trades.finder_hub` on this screen IS the Acquire tab's landing — TabNav registers `TradesHome` as TradesScreen with `initialParams {mode:'guided'}` (the launcher hub is unrouted), so every `navigate('TradesHome')` call site lands on the deck. The `TradeFinderModeBar` chip strip is the persistent mode-switching home (no "‹ Hub" chevron; hint line only while `deck.length === 0`, the mock-B2 cold start; fifth chip **Free agents** pushes the root-stack `FreeAgents` — the deck's `!finderMode` Explore FA row never renders on the landing, so the chip is its FA entry). The `OutlookBiasReceipt`'s Change opens `TradeDnaSheet` — the hub's #212/#236 DNA editor as a bottom sheet OVER the deck (state `dnaSheetOpen`; the legacy `editDna:true` route param opens the same sheet and is consumed); the F10 deck-done summary's Done now always settles into the exhausted state (its finder-mode branch used to return to the hub); the `onboarding.trades_first` first-run (collapsed chrome, identity strip, auto-generate) now triggers on the landing itself. Flag off ⇒ classic standalone home, byte-identical |
| `TradeFinderHubScreen` | **UNROUTED as of #246 (2026-08-05, guided-first landing)** — `TradesHome` now renders the guided deck directly (TradesScreen with default `mode:'guided'`); no navigator registers this screen and nothing imports it. Kept in the tree pending a later cleanup pass; its DNA editor body was lifted into `components/TradeDnaSheet.tsx` and its FA link superseded by the mode strip's Free agents chip. Historical row follows. Trade-Finding Hub (#156, flag `trades.finder_hub` — was the Trades-stack route `TradesHome` while routed) — the mode launcher: a **Trade DNA** panel over four cards. Guided/Player open `TradeDeck` with a `mode` param; Team opens a manager-picker sheet then `TradeDeck` with `mode:'team'` + `opponentUserId`; Calculator → `TradeCalculator`. Finish batch (2026-07-25): Specific Player card shows LIVE pin counts from `useFinderTargets`; #173 untouchables management sheet (list + remove + how-to-add). **#212/#231/#206 (2026-08-02, approved mock `mockups/polish-lab-2026-08/trade-dna-outlook-v3.html` + operator tweaks):** the DNA panel is collapsed by default — outlook chip with plain-words bias ("Rebuilding · leans young + picks"; inferred marked, `not_sure` = "no bias applied"), ONE summary line listing ALL explicit selections with position dots ("Chasing ●WR ●TE — Shopping ●QB ●RB", groups omitted when unset), untouchable mini-card chips (6px position dot + "A Jeanty" first-initial+last-name, cap 3 + "+N", read-only, no Manage collapsed). **Edit expands IN PLACE** (no OutlookSheet — the sheet's hub mount is gone; it survives on TradesScreen): four compact outlook cards + Chasing/Shopping rows of five multi-select toggles (QB RB WR TE + Picks storing `'PICK'`; selected = solid position-color fill + check glyph + bold dark label — check is the primary cue per the WR-contrast note; cross-row tap MOVES the position), count + Manage untouchables line, Done persists via `POST /api/league/preferences` ONLY when touched (an idle expand never invalidates the cached deck; no selection saves `not_sure`). **#236 (2026-08-02): the editor AUTOSAVES — every tap (outlook pick / chip toggle) POSTs the full payload immediately, one request in flight with trailing coalesce (last-write-wins), failure reverts drafts to last-saved prefs + inline error; Done is now a pure collapse (never POSTs, never disabled); an idle expand/collapse still fires no POST.** The #206 NEED/DEPTH recommendation chips were DROPPED (operator: "overkill") — `utils/dnaChips.ts` deleted with its only consumer; `position_needs`/`position_surplus` stay on the GET payload. Route param `editDna:true` (set by the deck receipt's Change link) auto-expands the editor. See `docs/feedback/items/212-trade-dna-redesign/status.md`. #196/#197: no local `FeedbackFAB` — as a tab-stack screen it's covered by the RootNav global mount (the hub-finish local mount doubled the flag). #223: the "Trading in" `LeaguePill` row + local switcher-sheet mount are gone — the global TopBar carries the league + switcher; the in-page "Find a Trade" title stays. #218/#219 (2026-08-01) fit-to-screen density pass (approved mock `mockups/polish-lab-2026-08/hub-fit-to-screen.html`): screen pad 16→12, stack gap 12→8, DNA pad 12→10 / gap 8→6 with Chasing/Shopping labels INLINE beside the chips (64pt label column), section-label margin dropped, card pad-v 12→9, icon well 40→34 (glyph 22→20) — hub content ~491pt vs a 658pt viewport, no scroll in the nominal state (see `docs/feedback/items/218-hub-fit-to-screen/status.md`). #242 (2026-08-02): the Specific-Team manager-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%, shared with the untouchables sheet) so a 12-team league fits without scrolling. **#245 (2026-08-05): the tab is labeled "Acquire"** (route `Trades` unchanged); the in-page title is now "Acquire" (was "Find a Trade") and the hub splits into a TRADE TickLabel section (the existing DNA panel + mode cards) and a FREE AGENCY TickLabel section — one row (`finder-hub.card.free-agents`, plus icon) linking to the root-stack `FreeAgents` finder (#143; the League tab entry stays). Adds ~113pt → mid-state ~581pt vs the 658pt budget (see `docs/feedback/items/245-acquire-tab/status.md`) |
| `TradeFinderHubScreen` | **UNROUTED as of #246 (2026-08-05, guided-first landing)** — `TradesHome` now renders the guided deck directly (TradesScreen with default `mode:'guided'`); no navigator registers this screen and nothing imports it. Kept in the tree pending a later cleanup pass; its DNA editor body was lifted into `components/TradeDnaSheet.tsx` and its FA link superseded by the mode strip's Free agents chip. Historical row follows. Trade-Finding Hub (#156, flag `trades.finder_hub` — was the Trades-stack route `TradesHome` while routed) — the mode launcher: a **Trade DNA** panel (outlook + untouchables count + Chasing/Shopping position chips with `position_needs`/`position_surplus` recommendation chips from `GET /api/league/preferences`) over four cards. Guided/Player open `TradeDeck` with a `mode` param; Team opens a manager-picker sheet then `TradeDeck` with `mode:'team'` + `opponentUserId`; Calculator → `TradeCalculator`. "Edit prefs" reuses `OutlookSheet`. Finish batch (2026-07-25): Specific Player card shows LIVE pin counts from `useFinderTargets`; the untouchables row opens a management sheet (#173 — list + remove + how-to-add). #196/#197: no local `FeedbackFAB` — as a tab-stack screen it's covered by the RootNav global mount (the hub-finish local mount doubled the flag). #223: the "Trading in" `LeaguePill` row + local switcher-sheet mount are gone — the global TopBar carries the league + switcher; the in-page "Find a Trade" title stays. #218/#219 (2026-08-01) fit-to-screen density pass (approved mock `mockups/polish-lab-2026-08/hub-fit-to-screen.html`): screen pad 16→12, stack gap 12→8, DNA pad 12→10 / gap 8→6 with Chasing/Shopping labels INLINE beside the chips (64pt label column), section-label margin dropped, card pad-v 12→9, icon well 40→34 (glyph 22→20) — hub content ~491pt vs a 658pt viewport, no scroll in the nominal state (see `docs/feedback/items/218-hub-fit-to-screen/status.md`). #242 (2026-08-02): the Specific-Team manager-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%, shared with the untouchables sheet) so a 12-team league fits without scrolling |
| `TradesScreen` | Trade card browser. Also serves as the `TradeDeck` route under the finder hub (#156): reads `route.params.mode` (`guided`/`team`/`player`) + (team) `opponentUserId`/`opponentName`, renders the `TradeFinderModeBar` quick-switch row, hides the Trades/Portfolio/Calculator subnav, and threads `opponent_user_id` into `/api/trades/generate`. Flag off / no params ⇒ unchanged standalone home. Finish batch (2026-07-25): player mode renders the two-column FOR/AWAY pin board; #174 `PackageToggle` ("Trade as one package" → `pinned_give_mode:'all'`) with 2+ give pins; the mode bar's Team chip opens an in-screen manager sheet and switches scope IN PLACE (setParams; in-place change auto-regenerates); pins live in `useFinderTargets`; the top deck card carries #186 keep-side + #190 edit-in-calculator actions. 2026-07-26 (flag `trade.asset_ideas`): with exactly ONE pin (either direction) the asset-ideas surface renders under the controls card ALONGSIDE the deck — grouped Upgrade/Lateral/Downgrade ideas from `POST /api/trades/asset-ideas` (pin-driven `useQuery`, no button). **#216/#209 (2026-08-02, approved mocks `mockups/polish-lab-2026-08/asset-ideas-layout-v2/-v3`):** the single-pin surface is now the FEATURED TRADE window (`FeaturedTradeWindow` — best-difference idea as a read-only `TradeCard` with the Dynasty Value Swing `TradeValueBar` from the idea's server `favors`/`gap`, #190 edit-in-calculator kept) over the always-visible `AssetIdeasPanel` "More trades" list; row taps swap ideas INTO the window (screen owns `featuredIdea` + a ≤10-deep per-pin history stack, reset on pin change / fresh sweep; tap scrolls the window back into view via `mainScrollRef`), the ‹ Previous trade chip (Variant A) reverts, and the in-window row is tagged + inert. #241 (2026-08-02): in single-pin mode the window + list IS the page — the swipe deck, Find-a-Trade button and progress strip do NOT render (the #216 build had left the deck mounting beneath, showing a mystery second trade card); every other mode keeps the deck exactly as before. #242 (2026-08-02): the in-screen team-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%) so a 12-team league's 11 manager rows fit without scrolling. #243 (2026-08-03, approved mock `pin-mode-collapsed-controls.html` V1): in single-pin mode the Controls Card collapses by default to a one-line "Pinned: <name> · Edit" row (`trades.pin-summary`/`.edit`, ~44pt vs ~286pt); Edit expands the exact full card in place with a Done collapse header (`trades.pin-summary.done`), state resets to collapsed on pin change/mode entry; all other modes render the full card unchanged. #209: the player-mode pin board reordered to TRADE AWAY left / TRADE FOR right so board, featured card and idea rows all read give→get. #250 (2026-08-05): Specific-Team scope bounds every acquire surface — in `mode:'team'` the "Target players to acquire" picker pool draws ONLY the scoped opponent's roster (trade-away pool untouched) and the single-pin asset-ideas query passes `opponent_user_id` to `/api/trades/asset-ideas` (backend sweep filtered to that member; a receive pin owned by anyone else ⇒ empty groups); all other modes byte-identical. F9 (flag `deck.first_session`): on the server-marked `first_deck` job, session dispositions/likes are tallied (refs, reset per job/league) → `first_session_like` / `first_session_deck_completed` events + at most one inline adaptation-moment card between deck cards (trigger math in `utils/firstSessionMoment.ts`; QuickSetPromptCard slot precedent; copy variants gated for literal truth — 'rerank' only when `deck.session_rerank` is operating); every board-refreshed deck renders the "Built from your updated board" header line (`job.board_refresh`, stacked ABOVE the F3 suppression note). 2026-07-27: top-card assets gain per-asset edit commands — a context-menu "Swap suggestions" row (SwapSuggestSheet: the calc eveners for the trade MINUS that asset, one-tap replacements via `evaluateForSwapSuggestions`) and #194 remove (✕ per row, min 1 asset/side hint, confirm when it breaks a #174 pinned package); both re-price through the shared `applyPackageEdit` → Mode B evaluate flow. #223/#224: the "Trading in" `LeaguePill` + local switcher-sheet mount are gone (league + switcher live in the global TopBar; the [leagueId] effect still resets the deck on a switch), and the classic standalone home renders a compact in-page "Find a Trade" heading (hidden in finder-mode launches — the mode bar titles those — and during first-run collapsed chrome). **#246 (2026-08-05, guided-first landing — approved mock `mockups/polish-lab-2026-08/acquire-landing-guided-first.html` V1 + operator FA-chip override):** with `trades.finder_hub` on this screen IS the Acquire tab's landing — TabNav registers `TradesHome` as TradesScreen with `initialParams {mode:'guided'}` (the launcher hub is unrouted), so every `navigate('TradesHome')` call site lands on the deck. The `TradeFinderModeBar` chip strip is the persistent mode-switching home (no "‹ Hub" chevron; hint line only while `deck.length === 0`, the mock-B2 cold start; fifth chip **Free agents** pushes the root-stack `FreeAgents` — the deck's `!finderMode` Explore FA row never renders on the landing, so the chip is its FA entry). The `OutlookBiasReceipt`'s Change opens `TradeDnaSheet` — the hub's #212/#236 DNA editor as a bottom sheet OVER the deck (state `dnaSheetOpen`; the legacy `editDna:true` route param opens the same sheet and is consumed); the F10 deck-done summary's Done now always settles into the exhausted state (its finder-mode branch used to return to the hub); the `onboarding.trades_first` first-run (collapsed chrome, identity strip, auto-generate) now triggers on the landing itself. Flag off ⇒ classic standalone home, byte-identical |
| `TradeFinderHubScreen` | **UNROUTED as of #246 (2026-08-05, guided-first landing)** — `TradesHome` now renders the guided deck directly (TradesScreen with default `mode:'guided'`); no navigator registers this screen and nothing imports it. Kept in the tree pending a later cleanup pass; its DNA editor body was lifted into `components/TradeDnaSheet.tsx` and its FA link superseded by the mode strip's Free agents chip. Historical row follows. Trade-Finding Hub (#156, flag `trades.finder_hub` — was the Trades-stack route `TradesHome` while routed) — the mode launcher: a **Trade DNA** panel over four cards. Guided/Player open `TradeDeck` with a `mode` param; Team opens a manager-picker sheet then `TradeDeck` with `mode:'team'` + `opponentUserId`; Calculator → `TradeCalculator`. Finish batch (2026-07-25): Specific Player card shows LIVE pin counts from `useFinderTargets`; #173 untouchables management sheet (list + remove + how-to-add). **#212/#231/#206 (2026-08-02, approved mock `mockups/polish-lab-2026-08/trade-dna-outlook-v3.html` + operator tweaks):** the DNA panel is collapsed by default — outlook chip with plain-words bias ("Rebuilding · leans young + picks"; inferred marked, `not_sure` = "no bias applied"), ONE summary line listing ALL explicit selections with position dots ("Chasing ●WR ●TE — Shopping ●QB ●RB", groups omitted when unset), untouchable mini-card chips (6px position dot + "A Jeanty" first-initial+last-name, cap 3 + "+N", read-only, no Manage collapsed). **Edit expands IN PLACE** (no OutlookSheet — the sheet's hub mount is gone; it survives on TradesScreen): four compact outlook cards + Chasing/Shopping rows of five multi-select toggles (QB RB WR TE + Picks storing `'PICK'`; selected = solid position-color fill + check glyph + bold dark label — check is the primary cue per the WR-contrast note; cross-row tap MOVES the position), count + Manage untouchables line, Done persists via `POST /api/league/preferences` ONLY when touched (an idle expand never invalidates the cached deck; no selection saves `not_sure`). **#236 (2026-08-02): the editor AUTOSAVES — every tap (outlook pick / chip toggle) POSTs the full payload immediately, one request in flight with trailing coalesce (last-write-wins), failure reverts drafts to last-saved prefs + inline error; Done is now a pure collapse (never POSTs, never disabled); an idle expand/collapse still fires no POST.** The #206 NEED/DEPTH recommendation chips were DROPPED (operator: "overkill") — `utils/dnaChips.ts` deleted with its only consumer; `position_needs`/`position_surplus` stay on the GET payload. Route param `editDna:true` (set by the deck receipt's Change link) auto-expands the editor. See `docs/feedback/items/212-trade-dna-redesign/status.md`. #196/#197: no local `FeedbackFAB` — as a tab-stack screen it's covered by the RootNav global mount (the hub-finish local mount doubled the flag). #223: the "Trading in" `LeaguePill` row + local switcher-sheet mount are gone — the global TopBar carries the league + switcher; the in-page "Find a Trade" title stays. #218/#219 (2026-08-01) fit-to-screen density pass (approved mock `mockups/polish-lab-2026-08/hub-fit-to-screen.html`): screen pad 16→12, stack gap 12→8, DNA pad 12→10 / gap 8→6 with Chasing/Shopping labels INLINE beside the chips (64pt label column), section-label margin dropped, card pad-v 12→9, icon well 40→34 (glyph 22→20) — hub content ~491pt vs a 658pt viewport, no scroll in the nominal state (see `docs/feedback/items/218-hub-fit-to-screen/status.md`). #242 (2026-08-02): the Specific-Team manager-picker sheet sizes to content (list cap 360pt removed, sheet maxHeight 85%, shared with the untouchables sheet) so a 12-team league fits without scrolling |
| `TradesScreen` | Trade card browser. Also serves as the `TradeDeck` route under the finder hub (#156): reads `route.params.mode` (`guided`/`team`/`player`) + (team) `opponentUserId`/`opponentName`, renders the `TradeFinderModeBar` quick-switch row, hides the Trades/Portfolio/Calculator subnav, and threads `opponent_user_id` into `/api/trades/generate`. Flag off / no params ⇒ unchanged standalone home. Finish batch (2026-07-25): player mode renders the two-column FOR/AWAY pin board; #174 `PackageToggle` ("Trade as one package" → `pinned_give_mode:'all'`) with 2+ give pins; the mode bar's Team chip opens an in-screen manager sheet and switches scope IN PLACE (setParams; in-place change auto-regenerates); pins live in `useFinderTargets`; the top deck card carries #186 keep-side + #190 edit-in-calculator actions. 2026-07-26 (flag `trade.asset_ideas`): with exactly ONE pin (either direction) an `AssetIdeasPanel` renders under the controls card ALONGSIDE the deck — grouped Upgrade/Lateral/Downgrade ideas from `POST /api/trades/asset-ideas` (pin-driven `useQuery`, no button); row tap opens `TradeCalculator` with the #190 `prefill`. F9 (flag `deck.first_session`): on the server-marked `first_deck` job, session dispositions/likes are tallied (refs, reset per job/league) → `first_session_like` / `first_session_deck_completed` events + at most one inline adaptation-moment card between deck cards (trigger math in `utils/firstSessionMoment.ts`; QuickSetPromptCard slot precedent; copy variants gated for literal truth — 'rerank' only when `deck.session_rerank` is operating); every board-refreshed deck renders the "Built from your updated board" header line (`job.board_refresh`, stacked ABOVE the F3 suppression note). 2026-07-27: top-card assets gain per-asset edit commands — a context-menu "Swap suggestions" row (SwapSuggestSheet: the calc eveners for the trade MINUS that asset, one-tap replacements via `evaluateForSwapSuggestions`) and #194 remove (✕ per row, min 1 asset/side hint, confirm when it breaks a #174 pinned package); both re-price through the shared `applyPackageEdit` → Mode B evaluate flow. #223/#224: the "Trading in" `LeaguePill` + local switcher-sheet mount are gone (league + switcher live in the global TopBar; the [leagueId] effect still resets the deck on a switch), and the classic standalone home renders a compact in-page "Find a Trade" heading (hidden in finder-mode launches — the mode bar titles those — and during first-run collapsed chrome). #231 (2026-08-02): finder-mode launches mount the self-contained `OutlookBiasReceipt` (one line under the mode bar) — the quiet outlook-bias receipt; since #246 its Change opens the `TradeDnaSheet` over the deck (see the newer TradesScreen row above for the #246 guided-first landing) |
| `TradeCalculatorScreen` | Manual trade builder — "Calculator" pill in Trades. Live mode: real consensus values via /api/trade/*; Demo mode: mock dual-board league. #190 (2026-07-25): optional `prefill` route param `{opponentUserId, giveIds, receiveIds}` (deck card's "Edit in calculator") lands in In-league mode with the package preloaded (draft-mode restore guarded; keyed remount on a new prefill). #213 (2026-08-01): one quiet "Want ideas instead? Find a trade →" text-link row under the mode tabs (testID `calc.find-a-trade`, all three modes) → `TradesHome` |
| `MatchesScreen` | Mutual trade matches inbox. #229/#234 (2026-08-02, ships live): the mutual empty state explains the mechanic ("A match needs two boards…"), leads with an always-on "Find a trade" primary (→ Trades hub; keeps testID `matches.go-to-trades`, superseding the S4 PRD-05 flag on this one state), and mounts the compact `LeagueProgressModule` (`matches.progress-module`, active league only) so League home and Matches tell one story; Refresh stays as a ghost (the empty state isn't scrollable — documented deviation from the mock's "Refresh is dropped"); the awaiting segment's empty state is unchanged (still flag-gated). #249 (2026-08-05): both segments pass `hideLockButton` to TradeCard — the untouchable lock button is gone from this screen (operator call); untouchables stay reachable via the long-press player menu + a11y action |
| `SleeperConnectScreen` | WebView login to Sleeper → captures the JWT for "Send in Sleeper" (flagged beta). Doubles as account **verification** (account-auth P1): the backend proves the captured token live + claim-matched, marks the session verified, and the screen surfaces "Account verified" on success |
| `SettingsScreen` | Settings modal (gear icon; explicit header close control per #130): leagues (+ flag-gated `espn.link` "Link an ESPN league" row → LeaguePicker with the sheet auto-opened, #130), ranking pref, notifications, and the **Account** section — renders meaningful state for every session type (P2.7): linked identities, an Apple-link button for any session without an Apple identity (binds via `POST /api/auth/apple` on the live session and flips `useSession.verification`), Sleeper source row, link-Sleeper card for account-only users (P2.6 merge flow), verification status, Verify account (Sleeper sessions), Delete account |
| `LeagueSummaryScreen` | League rankings / power rankings (#142/#144; since #181 the League TAB's root — stack route `LeagueRankings`, the page the tab lands on, with a "League home" row (testID `league-summary.league-home`) pushing `LeagueHome`; the root-stack route `LeagueSummary` remains as a legacy variant for deep link `app/league/summary` + old entry points, same component, no home row) — every team ranked by summed roster value via `GET /api/league/power-rankings`, basis toggle Consensus \| My board (personal values, consensus fallback for unranked players) + a disabled "Redraft (soon)" chip (backend reserves basis=redraft but 501s — dynasty values only). Tapping a team opens an overlay of its roster grouped by position, value-sorted within each group, in dense PlayerCard rows. #14 completion (2026-07-20): neutral **Picks** filter pill + trailing bar segment + drill-in "Draft capital" section (team.picks from the generic ladder; renders only when the league holds picks — ESPN/demo/old servers degrade cleanly), and an "Updated <relative> · pull to refresh" line from `updated_at`. **2026-07-26 League Analyzer treatment** (DynastyGM teardown): VERTICAL stacked columns (x-axis rank 1..N, caller's numeral in an ice pill) in a chart card (league name + "Dynasty · <format>" caption + refresh affordance) over a ranked team list; **All · Starters · Bench** segmented control (`league-summary.subset.*`) — Starters = the server-DERIVED value-optimal lineup (`teams[].starters`, basis-aware, no per-week data), selecting re-ranks the whole league client-side; hidden when `starters_available` is false (ESPN/MFL/demo — honest degradation); drill-in focus grays every other bar and renders the roster INLINE below the chart (no Modal) with tercile-colored positional-rank chips and per-player positional value ranks ("RB2"/"NR"). #237 (2026-08-02): the drill-in roster panel mirrors the chart card's FULL filter set — the All/Starters/Bench `SubsetControl` (`league-summary.roster-subset.*`) plus the position pills — bound to the SAME `subset`/`posFilter` state as the chart (the panel's independent `drillPos` filter is gone), so the two sections always match. **#243 drill-in filter dedup, V1 (2026-08-03, approved mock `mockups/polish-lab-2026-08/drilldown-filter-dedup.html`):** WHILE a team is focused the chart card collapses to a slim strip — its own `SubsetControl`/`PosFilterPills` do NOT mount (a passive "Filtered by: <Subset>[ · <positions>]" caption, testID `league-summary.filter-caption`, stands in and points at the drill panel's copy — the single visible/interactive set; hint margins tighten via `hintTight`), the X close control becomes a "‹ All teams" back affordance (KEEPS testID `league-summary.roster-close`; inner label `league-summary.back-all-teams`), and the tab-root "League home" row hides — pre-roster focused content drops ~692pt → ~557pt so the first roster group + row clear the 658pt fold. Visibility-only: #237's shared-state model and the entire unfocused rendering (both control sets, home row, X-restores caption) are untouched — unfocusing restores the prior layout exactly (see `docs/feedback/items/243-scroll-audit/build-drilldown-dedup.md`). **#248 combined rank bars, V2 (2026-08-05, approved mock `mockups/polish-lab-2026-08/combined-rank-bars.html`):** ONE chart carries both boards — the screen runs TWO parallel queries (one per basis, same endpoint + pre-#248 queryKeys, no backend change); the basis toggle now picks which basis draws the BARS (labels flip to "Consensus sorts"/"My board sorts" once both boards load and differ) while the OTHER basis renders per column as a dashed-ice ghost tick (`league-summary.tick.<user_id>`, cross-client-invariants "dashed-ice tick = other-board marker") at its total on the SHARED max scale, plus a signed ▲/▼ delta chip (`league-summary.delta.<user_id>`, semantic pos/neg) when the bases' ranks differ by ≥2. Tick/delta math reuses the bars' exact `computeSubset`+`activeTotal` derivation over the other payload's own basis-aware rosters/starters, so All/Starters/Bench + position filters recompute both signals; ticks hide for starters/bench if the other payload lacks `starters_available`. Identical boards (user hasn't ranked — personal Elo = consensus seed) hide the whole overlay → exact pre-#248 rendering. Drill-in focus hides non-focused ticks/chips and the focus caption + drill subline state both ranks ("My board rank 7/12 · Consensus rank 2/12"). **#208 (2026-08-08):** the reported "ranks don't follow the position filter" symptom does NOT reproduce — every numeral (bar column, list row, focus caption, drill subline) is an index into the `posFilter`-dependent `ranked` memo and the server's `team.rank` is read nowhere; it was fixed incrementally by the 2026-07-26 chart rework and #248. What #208 did fix is that #248 made the overlay's VALUES filter-aware but not its DRAW decision: `ticksOn` gated on the whole-roster `boardsDiffer`, so filtering to a position the caller never re-ranked still drew a dashed tick on every bar top and printed "Consensus rank 3/12 · My board rank 3/12". `ticksOn` now gates on `boardsDifferInView` (the same comparison over the values on screen — a strict generalization of #248's identical-boards hide rule; the unfiltered view is unchanged), while `boardsDiffer` keeps its unfiltered identity meaning so the "… sorts" toggle labels don't flicker per pill; the dual-rank captions now denominate the other basis' rank by `otherCount` (that payload's own team count). `teamPosRank` / `playerPosRank` stay deliberately posFilter-INDEPENDENT — "rank of this team's RB value" and "RB2" must not fold a WR selection in. New invariant clause in `docs/cross-client-invariants.md` (same-view rule for the other-board marker); see `docs/feedback/items/208-ranks-follow-position-filter/` |
| `SettingsScreen` | Settings modal (gear icon; explicit header close control per #130): leagues (+ flag-gated `espn.link` "Link an ESPN league" row → LeaguePicker with the sheet auto-opened, #130), ranking pref, notifications, and the **Account** section — renders meaningful state for every session type (P2.7): linked identities, an Apple-link button for any session without an Apple identity (binds via `POST /api/auth/apple` on the live session and flips `useSession.verification`), Sleeper source row, link-Sleeper card for account-only users (P2.6 merge flow), verification status, Verify account (Sleeper sessions), Delete account · #214/#215 (2026-08-05): TRADE VALUES section with the 3-option "Stud tax" segmented row (`settings.stud-tax.<market|heavy|off>` — Market recommended / Heavy legacy / Off; optimistic PUT `/api/settings/stud-tax`, revert + toast on failure) |
| `LeagueSummaryScreen` | League rankings / power rankings (#142/#144; since #181 the League TAB's root — stack route `LeagueRankings`, the page the tab lands on, with a "League home" row (testID `league-summary.league-home`) pushing `LeagueHome`; the root-stack route `LeagueSummary` remains as a legacy variant for deep link `app/league/summary` + old entry points, same component, no home row) — every team ranked by summed roster value via `GET /api/league/power-rankings`, basis toggle Consensus \| My board (personal values, consensus fallback for unranked players) + a disabled "Redraft (soon)" chip (backend reserves basis=redraft but 501s — dynasty values only). Tapping a team opens an overlay of its roster grouped by position, value-sorted within each group, in dense PlayerCard rows. #14 completion (2026-07-20): neutral **Picks** filter pill + trailing bar segment + drill-in "Draft capital" section (team.picks from the generic ladder; renders only when the league holds picks — ESPN/demo/old servers degrade cleanly), and an "Updated <relative> · pull to refresh" line from `updated_at`. **2026-07-26 League Analyzer treatment** (DynastyGM teardown): VERTICAL stacked columns (x-axis rank 1..N, caller's numeral in an ice pill) in a chart card (league name + "Dynasty · <format>" caption + refresh affordance) over a ranked team list; **All · Starters · Bench** segmented control (`league-summary.subset.*`) — Starters = the server-DERIVED value-optimal lineup (`teams[].starters`, basis-aware, no per-week data), selecting re-ranks the whole league client-side; hidden when `starters_available` is false (ESPN/MFL/demo — honest degradation); drill-in focus grays every other bar and renders the roster INLINE below the chart (no Modal) with tercile-colored positional-rank chips and per-player positional value ranks ("RB2"/"NR"). #237 (2026-08-02): the drill-in roster panel mirrors the chart card's FULL filter set — the All/Starters/Bench `SubsetControl` (`league-summary.roster-subset.*`) plus the position pills — bound to the SAME `subset`/`posFilter` state as the chart (the panel's independent `drillPos` filter is gone), so the two sections always match. **#243 drill-in filter dedup, V1 (2026-08-03, approved mock `mockups/polish-lab-2026-08/drilldown-filter-dedup.html`):** WHILE a team is focused the chart card collapses to a slim strip — its own `SubsetControl`/`PosFilterPills` do NOT mount (a passive "Filtered by: <Subset>[ · <positions>]" caption, testID `league-summary.filter-caption`, stands in and points at the drill panel's copy — the single visible/interactive set; hint margins tighten via `hintTight`), the X close control becomes a "‹ All teams" back affordance (KEEPS testID `league-summary.roster-close`; inner label `league-summary.back-all-teams`), and the tab-root "League home" row hides — pre-roster focused content drops ~692pt → ~557pt so the first roster group + row clear the 658pt fold. Visibility-only: #237's shared-state model and the entire unfocused rendering (both control sets, home row, X-restores caption) are untouched — unfocusing restores the prior layout exactly (see `docs/feedback/items/243-scroll-audit/build-drilldown-dedup.md`). **draft-extensions W3 M-C (2026-08-08, flag `picks.assign_tradeable`, ships OFF):** the drill-in **Draft capital** group is one of D17's five priced surfaces — each item renders `MemberEnteredMarker` UNCONDITIONALLY beside its price (`league-summary.member-entered.<pick_id>`), because these picks are summed into the team's total AND into the league ranking, so a wrong assertion silently reorders the standings. The marker self-gates on the flag and on `source === 'user'`, and tapping it deep-links the assignment grid at that slot |
| `FreeAgentsScreen` | Free-agent finder (#143; root-stack route `FreeAgents`, entered from the League tab's "Free agents" row) — best available players in the league ranked by the caller's board values (consensus fallback per unranked player; `user_has_rankings:false` shows a "consensus values" notice), QB/RB/WR/TE filter pills, dense PlayerCard rows with value + FA positional rank, and a "Drop: <player> (+delta)" subline when the backend found a lower-valued same-position roster player to cut. Per-row **Add** (#179): Sleeper leagues open the claim-preparation sheet (v2 2026-07-26 — FAAB bid input + "Budget: $N remaining" for FAAB leagues, "waiver priority league" copy otherwise; open-slot detection; radio-select drop candidates least-valuable-first with untouchables withheld) ending in a deep-link to the league's Sleeper players page (Sleeper has no public write API — FTF prepares the claim, the user executes it in Sleeper); platform-linked/local leagues get a dimmed Add whose tap explains the missing write path. Also entered from the Trades home's Explore row (#182, `trades.explore.free-agents`). Backend `GET /api/league/free-agents` (#178: 503 `rosters_unavailable` surfaces its honest message instead of generic error copy) |
| `DraftRoomScreen` | Draft Room (rookie-draft M4, flag `draft.room`; root-stack route `DraftRoom`, deep link `app/league/draft-room`, entered from the League tab's third Explore tile — which is "Rookie draft" when the flag is on and reverts to today's "Rookie board" sheet when it's off, so flipping the flag never empties the slot). **Read-only**: `GET /api/draft/board` (one `schema:1` payload) renders three sections — your picks, the board (slot · owner · pick, with traded-pick provenance), and the undrafted rookies with a Consensus \| My-board toggle plus FreeAgents-style fallback notices (my-board fallback, and "no consensus value" rows listed last rather than hidden, D7). Every failure mode is a DESIGNED render, not a spinner: `order_not_set` (round-level ownership — an invented order is the bug this prevents), `class_not_loaded` (Feb–Apr is structurally empty; offers last year's class via a session-only toggle), `startup_draft` (labeled, rookie list suppressed), `platform_unsupported` (MFL lands with M5), `unavailable`, and `stale` — `as_of` is ALWAYS visible. Terminal CTA is a deep link into the platform's own draft room; FTF never writes a pick (D9). **Live polling is a SEPARATE flag, `draft.live_poll`** — the app's first recurring fetch: 15s `refetchInterval` against OUR endpoint, gated on flag + `useIsFocused` + `useAppActive` + `state === 'live'`, so blurred/backgrounded is ZERO requests; manual **Refresh** is present either way, so the room is fully usable with polling dark. **Placement wave (2026-08-06, operator decision "draft-surface placement"):** the room is now reached from THREE places — the League tile (C, unchanged), the Acquire mode strip's leading **Draft** chip (option B, the permanent home), and the seasonal **Draft** tab (option A′) — and is DUAL-REGISTERED (root stack + the Draft tab's stack) while keeping ONE deep-link path on the root-stack copy, so `app/league/draft-room` resolves identically with the tab present or hidden. Three additive changes here: an optional **`leagueId` route param** that overrides the session's active league (the multi-league tab rule lands on a specific league's room without paying a league switch — every pre-existing entry point omits it and behaves exactly as before); an **`inTabs` param** that suppresses the screen's own `FeedbackFAB` inside the tab (RootNav's global mount already covers tab screens — two FABs is the #196/#197 bug); and a **"Rank the rookies" row** (`draft-room.rank-rookies`, under the status bar, rendered in EVERY board state incl. `unavailable`) navigating `Main → Rank → RookieRanks`, gated on `ranks.rookie_subset` — required by the placement mock's finding #2, without which a draft surface teaches users that rookie ranking and rookie drafting are unrelated. **draft-extensions W1 (2026-08-06, flag `draft.rank_inline`, ships OFF):** the room shipped with INERT undrafted rows, three SHARED non-unique testIDs and ZERO `track()` calls, so the one job it could not do was the on-the-clock one ("this guy isn't priced and I have 90 seconds"). W1 fixes that shipped gap. Unflagged: the three row ids gain a stable-domain qualifier (`draft-room.undrafted-row.<player_id>`, `.order-row.<round>-<slot|r>`, `.pick-row.<pick_no>`) and the bridge row finally emits analytics (`draft_room_rank_rookies_tapped{state, from}` — this room's first-ever event). Behind the flag: an undrafted row becomes a `Pressable` with `onLongPress` + an `accessibilityActions` custom action (the shipped `TradeCard` vocabulary — **deliberately no "⋯" glyph**: none exists in `mobile/src/`, and a visible overflow affordance needs a `docs/design/components.md` spec under ADR-004/005) opening the shared `PlayerContextMenu` with **Set my value** → the new `AnchorSheet` (`components/AnchorSheet`, on the shipped `POST /api/anchor/save` lane with `via:'draft_room'`; the host re-prices its `['draft-board', leagueId, basis]` cache from the SERVER's returned value and invalidates the rank caches), **Rank the rookies** (the same bridge, now passing `returnTo:'DraftRoom'` so it is TWO-way) and **Add to targets** (the shipped asset-pref write + toast); plus the coverage nudge `draft-room.coverage-nudge` ("N of the top 25 have no value on your board") off the payload's own `undrafted[].valued`. **THE ANCHOR LANE ONLY** — nothing here may reach `/api/tiers/save`, `save_tiers_position` or the merged-band path, pinned by AST + runtime + source tests in `backend/tests/test_draft_extensions_w1.py`. Flag off ⇒ the rows are the plain `View`s they shipped as; the qualified testIDs are the only difference. **draft-extensions W2 (2026-08-06, flag `draft.mock`, ships OFF) — placement option C, operator decision:** a `Real draft | Mock` segmented control (`draft-room.mode.real` / `.mock`) pinned ABOVE the ScrollView, plus in Mock mode a pinned flare `MockRail`. The design pass recommended option B and argued against C on the ground that this screen prints the D9 no-platform-writes guarantee on itself; the obligations that answer it are structural and tested (`mobile/tests/check-mock-mode-marker.js`): in Mock mode the StatusBar/Refresh, the platform deep link AND the "never drafts for you" note do **not render**, and the rail is unconditional and outside the scroll. The Mock side renders `MockEntryPanel` only — start / resume (`mock-entry.resume` + `mock-entry.start-over`, where Start over is ONE `POST /api/mock-draft` because create abandons the prior active row in the same transaction) / recap, or a muted card for each honest refusal: `cpu_model_unvalidated` (POST-only, so it necessarily arrives after the tap — gap G2 — and is then remembered), `class_not_loaded` (derived from the board's own notice; the room's "Show last year's class" toggle deliberately does NOT arm it, because the mock pool has no season override — gap G7), `startup_draft`, `league_too_small` (client-side `MOCK_MIN_TEAMS = 6`; the engine has no floor — gap G5), and `live` / `complete` (correctness refusals: a mid-draft mock can never catch up, and a drafted class is the rejected replay mode). Starting opens `MockSetupSheet` and lands on the pushed `MockDraft`. `UndraftedRowView` is now EXPORTED from this file and used by the mock — it stays here rather than moving to `components/draft/DraftRows` (where the row styles, `BasisChip`, `positionOf`/`slotLabel` and the fallback copy did go) because `backend/tests/test_draft_extensions_w1.py` pins W1's kill switch and per-player testID to this source file. Flag off ⇒ no toggle, no mock query, no mock state: byte-identical to what shipped. **draft-extensions W3 M-B + operator note 2026-08-08 (flag `picks.assign`, ships OFF) — assign from HERE, not "go to the League tab":** ESPN publishes no draft object, so this room's only path to a real board is a member typing the pick grid in, and the shipped flow said so by pointing at another tab. Two additions close that. (1) The `picks_not_assigned` notice (new `NoticeCode` member — the ONE new vocabulary item of the wave, riding the OPEN `notice.code` set so an older binary renders `notice.message` and behaves correctly; `state` stays `unavailable`, `schema` stays 1) renders client copy that OFFERS the job — *"Nobody has set this league's draft picks yet. Set them here and the board fills in."* — over a primary `draft-room.assign-picks` CTA that PUSHES the root-stack `PickAssignment`. **Pushed, not a sheet:** the grid is a full working surface with four season tabs, collapsible rounds, a drag-order setup step and two sheets of its own (owner picker, CAS conflict), and iOS will not stack sibling Modals — the same constraint that forced `TradeDnaSheet` to nest its second layer inside one Modal; a push also inherits `pick-assignment.back-btn` and the #151 iOS-26 header workaround. (2) A partly-typed grid still renders a REAL board, which would be a silent dead end, so an assigned board carries `draft-room.assign-progress` — the shared `pickAssignmentSubline` line plus "Continue assigning" / "Edit the draft picks" — off a `['pick-assignments', leagueId]` query that is enabled ONLY for `order_confidence === 'assigned'` (the ESPN branch emits it and nothing else does), so every Sleeper/MFL room issues no extra request. Returning from either push invalidates `['draft-board', leagueId]` — and ONLY after such a push, never on ordinary focus — so the user who just typed a grid does not land back on the notice they fixed. The League tab's "Draft picks" section survives as a SECOND entry; this adds one. Flag off ⇒ neither affordance renders and the notice falls through to the server's own message |
| `MockDraftScreen` | **draft-extensions W2 (2026-08-06, flag `draft.mock`, ships OFF)** — the mock draft SESSION (root-stack route `MockDraft`, pushed from the Draft Room's `Real draft | Mock` switch; `leagueId` override param, same rule as `DraftRoom`; `FreeAgents` registration pattern incl. the #151 `headerBackVisible:false` + custom `HeaderBack` iOS-26 workaround, testID `mock-draft.back-btn`; own `<FeedbackFAB activeScreen="MockDraft" aboveTabBar={false} />` per #188). The ONE surface in the app where a pick can be made — hence the mode-marking contract, which is a hard requirement rather than polish: the design pass argued AGAINST placement C precisely because the room prints "Fantasy Trade Finder never drafts for you" on itself, and the operator chose C anyway. So: a **flare `MockRail` renders OUTSIDE the ScrollView above a SINGLE `return`**, i.e. in every sub-state (loading · error · every typed-empty refusal · the live board · the confirm step · the recap) at every scroll position; the D9 guarantee text and the platform deep link are **not rendered here at all**. Pinned by `mobile/tests/check-mock-mode-marker.js` (AST, not grep — it fails if the rail moves inside a branch or into the scroll). Sections: `OnTheClockCard` (pick label + you/CPU + persona sub-line, joined client-side from `settings_echo.personas` and `order[]` — gap G4) · `PickTicker` (last 8, ice tint = landed since your last pick, flare tint = yours) · Your picks chips · the room's undrafted list with the shipped `BasisChip` pair + fallback copy, tap-to-select while `on_the_clock.is_user` → `MockPickConfirm` bar → `POST /pick` (one request, whole CPU tail resolved in the response) · recap at `status:'complete'`. **No polling** (`staleTime: Infinity`, no interval, no refetch-on-focus): a mock only changes when its owner acts. Long-press row actions reuse `PlayerContextMenu` + W1's `AnchorSheet` and stay behind W1's own `draft.rank_inline` flag. **The recap's "+3 / −1 vs consensus" delta column from the design is deliberately NOT rendered** — `picks[]` carries no rank/value and the drafted players are gone from `undrafted[]` before first render (gap G3), so any delta would be invented; it returns when `state_payload()` adds `consensus_rank`. Imports `UndraftedRowView` from `DraftRoomScreen` (not a copy — see that row) |
| `PickAssignmentScreen` | **draft-extensions W3 M-A (2026-08-06, flag `picks.assign`, ships OFF)** — ESPN pick assignment (root-stack route `PickAssignment`, deep link `app/league/pick-assignments`, entered from the League tab's dedicated **"Draft picks"** section below Explore; `leagueId` override + `focusPickId` params, the latter being M-C's one-action correction path from a priced surface). ESPN hosts no rookie drafts, so an ESPN dynasty league's rookie draft happens off-platform and FTF becomes the league's system of record for pick OWNERSHIP — the user says who owns each slot, and **that is the entire input surface: no value entry anywhere, ever (D13)**, with prices computed server-side and not even displayed here. `FreeAgents` registration pattern incl. the #151 `headerBackVisible:false` + custom `HeaderBack` iOS-26 workaround (testID `pick-assignment.back-btn`); own `<FeedbackFAB activeScreen="PickAssignment" aboveTabBar={false} />` per #188; registered UNCONDITIONALLY (the flag gates the entry section, not the route — the `DraftRoom`/`MockDraft` rule). **The 192-slot problem is the design constraint** (operator decision 3 puts current + 3 seasons on the board; 12 teams × 4 rounds × 4 seasons ≈ 192) and three defaults answer it in the plan's priority order: (1) **the pristine grid is the default** — the server seeds every team owning its own picks, so a league with 3 trades leaves 189 slots untouched and the user records DEVIATIONS, not the board; (2) **order is set ONCE** — one `DraggableFlatList` of the league's teams for round 1 (`pick-assignment.order-list`/`.order-row.<user_id>`, `DRAG_ACTIVATION_DISTANCE` + pickup/swipe haptics, the `ManualRanks` interaction) plus a rounds stepper (1–8, default 4, clamped server-side because the clamp IS the conservation bound's only lever) and a **linear/snake** segmented toggle that changes slot NUMBERING ONLY, never ownership — which is why it is safe at any time and can never raise a CAS conflict; (3) **edit only the traded ones** — collapsible rounds (round 1 open, the rest closed), a flare deviation tick per row, and every deviating/contested/orphaned slot floating into the cross-season **"Traded picks"** summary pinned at the top. On top of those the screen **defaults to the current season** with the other three behind season tabs (`pick-assignment.season-tab.<year>`) and the confirm-the-board review step is **PER SEASON** (`pick-assignment.confirm-season.<year>`, marks persisted client-side under `ftf_pick_board_confirmed_v1` — a reading aid, never an input to pricing; there is no server field and inventing one would put a second writer on `draft_picks`). There is deliberately **no "review everything" view** — one 192-row scroll is the failure mode this design exists to avoid. Slot numbering (`1.03`) is computed from `settings.order` + `order_type` rather than stored, because the server's grain is (league, season, round, original_roster) and `overall` must never leak onto a `draft_picks` row (D18). Tapping any slot opens the owner sheet (the shipped manager-picker construction; options derived from the grid's own original-team pairs, so the picker can never offer an owner the PUT would reject). **Concurrency:** every save is one slot carrying the `assigned_at` this client read; a 409 `stale_assignment` opens the conflict sheet (`pick-assignment.conflict-sheet`) rendering the other person's row from the 409 body — "<Name> changed this pick 4 minutes ago — keep theirs, or use yours?" with `.conflict.keep-theirs` / `.conflict.use-mine`, where Use mine re-issues as another compare-and-swap so a THIRD editor conflicts again rather than losing. Never a silent overwrite; no giant dirty form to lose. **Provenance (D17):** assigned rows carry a MEMBER-ENTERED tag wherever they appear (row, summary, conflict sheet) plus the "ESPN never confirms them" note, and the correction path is one action — tap the row. Contested and orphaned slots say plainly that they are not priced. Flag off / no league / load failure each render their own honest state (`pick-assignment.unavailable` / `.no-league` / `.error`) rather than a spinner. **W3 M-C (2026-08-08):** the route now also accepts `season`, the third leg of the plan's `{leagueId, season, focusPickId}` correction triple — `focusPickId` still wins, and `season` is the fallback that lands the right season tab when a re-seed with fewer rounds has dropped that slot. The correction links arrive from the five priced surfaces' `MemberEnteredMarker`, and now also from the ESPN Draft Room's own `draft-room.assign-picks` / `draft-room.assign-progress` affordances. **ESPN auto-derived draft order (2026-08-08, no new flag — it improves the DEFAULT of this already-gated flow):** setup step (2) — "order is set ONCE" — no longer starts as a blank drag-order exercise. When the payload carries `suggested_order`, `SetupView` PREFILLS the round-1 `DraggableFlatList` from it (falling back to the stored order, then the members list, exactly as before; it only fills an EMPTY list, so a drag in progress is never rewritten by a background refetch) under a flare-tick caption `pick-assignment.espn-derived-note` — "Filled in from your 2025 ESPN standings — playoff teams by where they finished, everyone else in reverse regular-season order. Drag anything that's wrong; nothing is saved until you confirm below." Informational-highlight vocabulary, never an action color (ADR-005). A **client-side coverage check** gates the prefill (same length as the members list AND every id a known member) — a partially-resolvable order would render a derived prefix over an alphabetical tail, which looks authored and isn't. **An already-saved order is never touched and nothing is offered**: the server emits the key only while no order is stored, so this is structural, not client politeness, and there is deliberately NO "reset to the ESPN-derived order" affordance. Convention, refusals and the operator decision (non-playoff teams by inverse REGULAR-SEASON standings, NOT ESPN's consolation-ladder `rankCalculatedFinal`): `docs/plans/draft-extensions/build-espn-auto-order.md` |
| `PlaceholderScreen` | Stub for unfinished routes |
| **Rookie scope across the rank surfaces** | **rookie-draft M2 (2026-08-06, flag `ranks.rookie_subset`, ships OFF).** All six ranking surfaces mount the ONE shared `RookieScopeControl` (state + copy in `state/rookieScope.ts`), rolled out in plan order Anchors → Tiers → Quick Set → Overall/Quick Rank (inherit) → Trios (last; M0 measured 85 valued rookies, TE=21, so every position can still field a trio). Scope is session-only and rides each screen's react-query key as a `'rookie'` SUFFIX, so every existing prefix invalidation still covers the scoped cache. Per-surface behavior: **Anchors** — the queue narrows, saves are unchanged (`apply_anchor` is subset-safe). **Tiers** — scoped saves and the "Reset to suggested" clear both pass `{scope:'rookie', via:'rookie_tiers'}` (merged-band rule server-side); scope is part of the bucket identity so a flip can't strand the other scope's unsaved layout. **Quick Set** — the ladder STARTS at the first rookie-bearing rung (guarded on `isPlaceholderData`, or it would compute the rung from the outgoing pool), saves carry `via:'rookie_quickset'`, and a scoped walk **must not** write `quicksetCompletedPositions` or fire `quickset_completed` — the client mirror of server invariant I-4, because a rookies-only pass has not completed a position and `quicksetProgress` drives #244 launch routing. #161 demotion needs no scope branch: it derives from the visible grid, which under scope IS the rookie subset (operator decision O4). **Overall ranks / Quick Rank** — inherit; no save change, `apply_reorder` permutes exactly the posted ids. **Trios** — the subset constrains candidate selection only; picks still produce full-board Elo updates. Every surface renders the shared `RookieScopeEmpty` for the server's typed `{empty:true, reason}` response. Flag off ⇒ no control, no scope param, pre-M2 query keys — byte-identical |
| **TradesHome outlook cleanup (#253/#254/#255/#256/#259)** | **2026-08-08, ships live, no flag** (see `docs/feedback/items/253-outlook-cleanup/`). Four changes to the outlook/window strip of `TradesScreen` + the components it owns. **#254/#255 — one outlook surface:** the controls card's "Outlook · Edit" row (→ `OutlookSheet`) is suppressed whenever the #231 minimized bar (`OutlookBiasReceipt`) is on screen. The gate is the receipt's OWN exported predicate `outlookReceiptCovers(directionOn, declared, inferred)`, so the bar and the row can never both appear and can never both vanish — the row still renders on the classic flag-off home, with `trade.outlook_direction` off, and for a non-directional (`not_sure`/absent) outlook. Consequence: on the guided landing `TradeDnaSheet` is the ONE outlook + Chasing/Shopping editor (a strict superset of `OutlookSheet`'s outlook + acquire/trade-away plays). `OutlookSheet` stays mounted for its surviving callers (inferred-outlook banner Change, `trades.outlook-set-banner`, classic home). **Deliberately kept:** the FB-47 "Target players" section — despite the shared "trade away / acquire" wording it is player-level pinning (`pinned_give_players`/`pinned_receive_players`), which no bar or sheet can do. **#253 — canonical outlook display order** All-in → Contending → Rebuilding → Tanking, enforced in `TradeDnaSheet.OUTLOOK_CARDS` (was Rebuilding-first), with the order pinned by comment in `OutlookSheet`/`OutlookBiasReceipt` too; stored enum values untouched. **#256 — lane pill "Window moves" → "Team-fit moves"** (web filter button + `TEAM-FIT MOVE` card chip); the `window`/`value` enum and `lane-chip--*` classes are unchanged, label recorded in `docs/cross-client-invariants.md`. **#259 — untouchables are addable from your roster:** the #173 management layer (list + Remove) gains `untouchables.add-from-roster`, opening a roster picker as a THIRD layer inside the same `TradeDnaSheet` Modal (not `PlayerPickerModal` — that renders its own `<Modal>`, which iOS will not present over an open one). Pool = the caller's own roster via `['league-rosters']` ∩ the already-open `['calc-values']` pool, already-protected players filtered out, name search, value-sorted; writes through the shipped `setAssetPref(…, 'untouchable')` lane and invalidates `['asset-prefs', leagueId]` so count/list/deck locks move together |
| `TestStagesScreen` | Operator QA (root-stack `TestStages`; Settings → Testing row, flag `testing.stage_users` — delivered per-device via the experiment overlay, so only allowlisted operators see it; the spawn route is allowlist-gated server-side regardless): spawn a synthetic `qa_*` user at an adoption stage (fresh / activated / board_owner / converted / power) via `POST /api/test-users`, swap this device into it (token + user + demo-built league + verbatim `client_state` → `replaceOnboardingState`), plus device-only Factory reset (wipe first-run/tour state + sign out). Force-quit recommended after a swap (module-level once-per-session guards reset on relaunch) |
