# mobile/src/screens/

One file per top-level route. MAP, not a changelog — present behavior only, no dated amendments. History: `git log -- <this file>` and `living-memory/CHANGELOG.md`.

Route names rarely match file names — the route→file table and the conventions for adding a screen are in [README.md](README.md).

| Screen | Purpose |
|---|---|
| `SignInScreen` | Apple sign-in is the primary entry (`auth.accounts`); Sleeper username is the fallback. `onboarding.landing` flips Sleeper to primary with a demo escape when Sleeper is down |
| `LeaguePickerScreen` | Pick which league to use; footer links open the ESPN/MFL/Fleaflicker league-linking sheets |
| `LeagueScreen` | Classic league home (pushed `LeagueHome` sub-route): hero identity, Matches tiles, Explore rows, activity, contrarian, coverage, leaderboards. Adds a progress module + action row while any unlock is outstanding, and a "Draft picks" section for ESPN leagues (flag `picks.assign`) |
| `RankHomeScreen` | Build-your-board chooser: Quick set / Trios / Tiers as primary cards, Anchors / Overall ranks / Trends behind a "More ways to rank" disclosure; optional rankings-import link (flag `ranks.import`); saves `rankingMethodPref`. Also the **host of the whole import intake** — it owns `ImportRankingsSheet` (chooser + confirmation) and `RankImportSheet` (match → review → apply) as siblings, exactly one visible at a time (iOS will not stack sibling RN Modals), and subscribes to `rankImportBus` for the CSV the in-app browser captures |
| `RankScreen` | 3-player swipe matchup loop (Trios), three-across mini player cards. Also the Rank tab's launch route once all four quick-tiers positions are complete |
| `TiersScreen` | Tiered roster view |
| `QuickSetTiersScreen` | Guided per-position tier walk (top→FA); default Rank-tab launch route for no-pref users, resuming at the next unset position |
| `QuickRankScreen` | Within-tier click-order ranking pass offered after Quick Set finishes |
| `PickAnchorScreen` | Pick Anchor wizard: value one player at a time in draft-pick terms, position scope pills; shares its rung grid with `AnchorSheet`. Rung buttons carry `anchors.rung.<key>`; their LABELS are derived from `TIER_LABEL` via `anchorRows.ts` and must never be authored locally (`npm run test:anchor-labels` enforces it) |
| `ManualRanksScreen` | Editable drag/tap rank board, labeled "Overall Ranks" in the UI |
| `RookieRanksScreen` | Consolidated cross-position rookie board (flag `ranks.rookie_subset`), filterable by position; drag-reorder only — must never reach the tiers-save path; two-way bridge to/from Draft Room |
| `TradeFinderHubScreen` | UNROUTED since the guided-first landing shipped — kept in the tree, no navigator registers it. DNA editor moved to `TradeDnaSheet`; FA link superseded by the mode-bar chip |
| `TradesScreen` | Trade card browser and the `TradeDeck` route (`mode`: guided/team/player); also the Acquire tab's landing. Pin board, featured-trade window + asset-ideas list in single-pin mode, swap suggestions, per-asset edit/remove, mode-switch chip strip. Pass/Like live inside the top card (via `TradeCard`'s `disposition` prop), not below the deck (#169). **The deck has TWO sources (#384 W6-B, [D-153])**: the model's streaming job, and — when a consumed `FinderHandoff` carries a `fairAnchor` — one synchronous `getFairPackages` sweep whose ideas become the deck via `utils/ideaToCard`. The fork is taken inside the ONE #330 choke point, BEFORE the model gate and with a `return`, so a canvas arrival can never also dispatch a generate. `fairDeck` marks the result: it swaps the end-of-deck **unpin-retry** exit (meaningless — a fair deck has no pins, the anchor rode the request body) for **"Search all trades"**, which drops the anchor and runs the model for the same partner. Every model-search path clears `fairDeck`. **D-158 (Wave B0, flag `calc.inline_home`, dark) makes this screen the merged In-league page**: with the flag on the guided landing MOUNTS `TradeBuildCanvas` as its layout (not as the #270 experiment variant — `canvasHost` resolves the two claims to ONE mount and the flag wins), `handleInlineFindATrade` runs the search IN PLACE (`utils/canvasSearch.forkCanvasSearch` → the same D-153 fork → the same #330 choke point, reached by bumping `canvasRunSeq`; no navigation, no `FinderHandoff` write, no second `generateMutation.mutate` site) and stamps `deckOrigin:'calculator'` so the ✕→overlay and the calculator-first exits stay legal on an inline-built deck, an anchored deck renders the `trades.anchor-receipt` filter row above the results (Change = scroll to the canvas, which still holds the assets; Clear = `handleSearchAllTrades` verbatim, which is why both end-of-deck "Search all trades" buttons step aside for it), the ✓ goes through `utils/queueCalcTrade`, and the three `navigate('TradeCalculator', {prefill})` hand-offs load the inline canvas instead. Flag off: none of that renders and the three navigations are unchanged. **#376/#379/#394**: always-available minimized "Outlook & filters" row (`trades.outlook-fallback`) inside the receipt wrapper, gated exactly `consolidateOn && !outlookReceiptShown && !firstRun && canvasHost !== 'flag'` (whitelist pinned by `check-finder-conditions-reachable.js`); its Change opens the full `TradeDnaSheet`. The promised mortality was honored 2026-08-28 (**merged-view trim**, operator ruling in docs/feedback/items/402-more-offers-shop/merged-view-trim-2026-08-28.md): when the `calc.inline_home` flag path hosts the canvas, the row steps aside for the hosted calculator's own `calc.outlook-row` (T-1), BOTH page-level `trades.find-btn` bars step aside for the canvas action row — the page's only primary, covering fair-sweep and model-deck paths alike, with the deck-summary copy quoting whichever CTA renders (T-2) — and the hosted header drops its scoring-format chips via the `hideFormatChips` host prop (T-3). Host-gated (`canvasHost !== 'flag'`), never the bare flag: flag-on team/player deck modes mount no calculator and keep every surface (`check-inline-home.js` §11). **#402 canvas-results (flag `calc.canvas_results`, LIT):** on the flag-hosted merged landing (`canvasResultsLive = canvasResultsOn && canvasHost === 'flag'`) found ideas present as a **browse session** INSIDE the canvas — the working set stays in the existing `deck` state with `deckIdx` as the cursor, so the whole pass path (advance(), the decline-reason machinery, the swipe POST, D-067) is reused rather than forked; `browseSession` adds only existence, origin (`fair`/`model`), a passed tally and the per-idea edit map (ruling 3, fed by `InLeagueCalculator`'s new optional `onSidesChange`, replayed by the seeding effect through the prefill/prefillSeq remount). A chevron pager + `N / X` TickLabel sits above the canvas header (`trades.canvas-results.pager`; paging emits nothing, never wraps) with a model-path Clear. **The pager carries neither the ✕ nor the shop entry any more** (#410/#412, [D-169]): the screen hands both DOWN to the canvas as props, so exactly one of each is ever mounted. `browseDecline={browseLive && sortedDeck.length > 0 && declineReasonProps ? {onPress: handleBrowsePass} : null}` makes the calculator's action-row middle cell the decline ✕ — the pager ✕'s own predicate, unchanged, so under the `feedback.decline_reasons` kill switch it is **null** and the cell falls back to Clear rather than rendering a control `handleBrowsePass` early-returns out of; `handleBrowsePass` itself is untouched and still has exactly one caller (the SAME `trade_pass_overlay_*` overlay flow, host-mounted since no card renders; a completed pass splices the idea out via the browse-aware `commitReasonAdvance`). `giveBelowAdd` puts the browsed idea's "More offers" (`calc.give.more-offers`, renamed from `trades.canvas-results.more-offers`) under the GIVE column's "Add player" — same `openShopForCard` fork, same gate, same single `shop_opened` emitter, and still `rawTopCard` (the engine's give side, not the edited canvas — the same original-vs-edited rule the pass signal follows). **The deck tree does not render on this host at all** — session or not — and the honest empties render in the results area below the receipt (`trades.canvas-results.*`: fair-zero — the audit-Q5 fix — model-zero via the shared `modelZeroCopy()`, exhausted, progress, error, scoped-empty). The anchor receipt's Change/Clear fork under a live session (end the session; Change restores the anchor build). Sessions die on league switch, every `resetDeckForNewTargets`, and flag/host kill. **G22 — the activation moments follow the surface**: a real first ✓ queue success (not a refusal or an already-queued repeat) runs `recordCanvasQueueLike` — the same persisted swipe counters, the same Quick-Set-prompt trigger, the same F9 first-session tally and the same save-moment Apple chain a deck like runs — and the Quick-Set prompt card + F9 adaptation card get host-gated mounts in the top-of-results slot above the canvas (the deck-slot mounts are nulled with the deck tree, so exactly one renders); browse ✕ passes already ride `advance('pass')`, whose adaptation trigger stays outside the `deferDeckAdvance` fork. Flag off ⇒ byte-identical. Pinned by `mobile/tests/check-canvas-results.js` |
| `TodaysTradeScreen` | Presentation-v2 landing (flag `trades.presentation_v2`, route `TodaysTrade` in the Acquire stack; approved lab `mockups/trade-suggestion-redesign/` states 01/03/04/07). ONE endorsed hero per league per cycle — binary badge, asymmetric explanation (≤3 concrete bullets for the user's side, a confidence STATEMENT only for the counterparty), fairness as a league-normal RANGE band — plus the capped Featured tier and the honest empty state. **Purely additive**: it never touches `TradesScreen`, and with the flag off there is no entry chip and nothing here is reachable |
| `TradeBrowseAllScreen` | Presentation-v2's UNCAPPED ranked list (flag `trades.presentation_v2`, route `TradeBrowseAll`; lab state 09). Per-row dismiss with the in-place "we'll rank ideas like this lower · Undo" acknowledgement — the dismissed row STAYS, because removing it deletes both the visible proof the model learned and the training signal the engine programme runs on. Never slice this list |
| `TradeCalculatorScreen` | Manual trade builder — **two** modes, `Real values` (league-free, the cold-open default) and `In league`. The **Demo league** mode was removed 2026-08-22 (#384); a stored draft carrying `mode:'demo'` falls through to Real values. Optional `prefill` route param from a deck card's edit action (forces In league and suppresses the tour). Owns the #384 merged page's navigation half behind `calc.merged_layout`: the `onFindATrade` hand-off (analytics → `advanceGuideIfActive('n18')` → `setHandoff({origin:'calculator', fairAnchor?})` → `popTo('TradesHome')` — never `navigate`, see G-056). **W6-B ([D-153]) made that hand-off the FORK**: a canvas with a give side carries a `fairAnchor` and the deck runs the fairness-only `POST /api/trades/fair-packages` sweep; an empty canvas carries none and the deck runs the model exactly as before. A receive-only canvas counts as empty — the fair sweep prices a give package. **It writes NO pins at all any more** (W5 did, because the model's generate payload reads them; the sweep takes the canvas in its own request body, so a pin write would only strand a constraint on whatever searched next). It also owns the `outlookOpenerRef` for beat n11 and the calculator tour's two entry points, and its page `ScrollView` announces `notifyGuideTargetsMoved` (the spotlight caches an absolute window frame, so without it every tour beat below the fold is highlighted at its stale position the moment the page moves). Tour entry: **auto-start** on a first visit (gated on the flag, no prefill, `hasLeague`, a `calc_tour_completed` receipt, and n10's display cap — three abandoned auto-runs exhaust the offer rather than opening it on a later beat's degrade line) and **Show me around**, the re-runnable link the component renders only when a handler is passed. The #213 "Find a trade" text link steps aside on the merged In-league page so the canvas cannot be bypassed. **Also owns the ✓ cell's request half (W6-A, [D-152]):** `onLikeTrade` calls `POST /api/trades/queue`, emits the single `calc_trade_queued {queued, reason?}`, and shows a reason-specific `Toast` — `queueRefusalLine` has one line per server reason, because a generic failure is the dishonest state the disabled cell stood in for. The component owns the canvas and the in-flight lock; the screen owns the toast, and since **D-158** the request + analytics + refusal copy live in `utils/queueCalcTrade` because the canvas has two hosts. **D-158 (flag `calc.inline_home`, dark) trims this page to Real values**: the mode-tab row and the whole In-league branch are gone (the guided landing hosts that canvas now), a `prefill` arrival no longer forces league mode, and the calculator tour is suppressed ENTIRELY — both the auto-start and the "Show me around" re-entry — because beat n10 points at the In-league tab this wave deletes; Wave B rebuilds the tour against the inline module. The fork itself moved to `utils/canvasSearch.forkCanvasSearch`, which `TradesScreen` calls too. Flag off: two tabs, prefill-lands-in-league, tour intact |
| `ShopAssetScreen` | The #402/#403 "shop a player" window (rev-3): a root-stack push from the deck card's give-side "More offers" — one give asset navigates directly, several go through the "Shop which player?" chooser sheet on `TradesScreen`. Browses `POST /api/trades/asset-ideas` as Tier up / Tier down / Same value with one shared position filter row on all three modes; Same value is tier-scoped (`lateral_scope:'tier'`) with an auto-widen-on-zero notice; ✓ queues a real offer, ✕ is a 5 s held dismiss with a true Undo. Native back returns to the untouched deck; `gestureEnabled: false` (the pager owns the horizontal drag). Entry gated by the `trade.shop_asset && trade.asset_ideas && calc.merged_layout` conjunction; the ROUTE registers unconditionally (house rule), so a mid-session kill leaves an already-open window up until backed out — its data/queue calls then 404 honestly and the entry never re-offers. Root-stack push ⇒ mounts its own `FeedbackFAB` and its own `Toast` |
| `MatchesScreen` | Mutual trade matches inbox; progress module + "Find a trade" CTA on the empty state |
| `SleeperConnectScreen` | WebView Sleeper login; captures the JWT for Send-in-Sleeper (beta) and doubles as account verification. Runs `incognito` (2026-08-12): a persistent store would restore the last login and the poller would capture the wrong account within ~800ms — pinned by `tests/check-espn-connect-clear.js` |
| `SettingsScreen` | **Flag-off flat list only** (`account.settings_hub` false). The one-screen Settings: leagues (+ ESPN/MFL link rows), ranking pref, notifications, Account section (link/verify/delete + per-platform credential disconnects), Trade Values stud-tax mode. No longer a modal — it is a pushed page. Dies with the flag in phase 4 of the settings-IA plan. |
| `settings/` (subtree) | **Flag-on Settings** (`account.settings_hub` true). `SettingsHubScreen` (grouped nav rows + state previews, fires no settings queries) pushes seven second-level pages — `SettingsLeagues` (incl. all three platform credential disconnects, moved off Account), `SettingsRanking`, `SettingsTradeValues`, `SettingsNotifications`, `SettingsAccount` (identity → Sign out → data rights, Delete account last), `SettingsAbout` (incl. The Analyst toggle + version row), `SettingsTesting`. Rows live in `settings/sections/*`, each owning its own queries; shared row styles in `settings/styles.ts`, the toggle row in `settings/Row.tsx`. See `docs/plans/settings-ia-hub/`. || `LeagueSummaryScreen` | League/power rankings — the League tab's root: per-team bar chart + ranked list, Consensus/My-board toggle, All/Starters/Bench subset, draft-capital section, inline drill-in roster. Also hosts the **live** "Season outlook" section (flag `outlook.odds`, LIT 2026-08-19 by operator override, D-094): projected standings and playoff odds merged into one seed-ordered list — row order + cutline are the standings, a three-band chip is the odds; defaults to a collapsed one-line "your outlook" strip (per-league/user persisted, `state/outlookStrip.ts`) with the full section one tap away (#169 frame E). Its page `ScrollView` announces `notifyGuideTargetsMoved` from `onScroll` **and** `onLayout`/`onContentSizeChange` (#386 — the outlook section mounts/toggles after first paint and shifts the pills without a scroll; the props look unused but are pinned by rule 12 of `tests/check-guide-spotlight-tracking.js`) |
| `TeamReviewScreen` | Team Review (#357/#358/#359, flag `trades.team_review`): six stepped beats — standing / window / depth / divergence / partners / plan — each one finding, one plain read, one action. FOUR of the six write `league_preferences` through the existing `POST /api/league/preferences`, so the exit is a deck reshaped by what the user agreed to rather than a report. Registered in the TRADES stack (the operator's framing was a button INSIDE find-a-trade), entered from `TeamReviewEntryCard` on TradesHome — **not** a mode chip, because `TradeFinderModeBar`'s own measurement says its strip is already over-width. Reuses the Analyst persona but NOT the `AnalystGuide` overlay (that teaches a control; this presents data). Tab-stack screen, so no local `FeedbackFAB`. `meta.beats_skipped` is authoritative — the client never decides a beat is empty. **The `plan` beat is a STANDING SUMMARY, not a session receipt (#369, D-130/D-131):** it lists every lever the trade finder exposes — outlook / chasing / shopping / untouchables / targets / not-interested / scoped partner / fairness / trade idea / focus / pinned players — read from the SAVED preferences (`GET /api/league/preferences` on the `['league-prefs', leagueId]` key with `refetchOnMount:'always'`, plus `GET /api/league/asset-prefs`), never from session state and never from the team-review payload's mount-time snapshot. The three `league_preferences` levers are editable in place through the same `savePrefs`; everything else is shown with its home named. Every write goes through the single `savePrefs` helper, which backfills `team_outlook` because the route 400s without it. Pinned by `mobile/tests/check-team-review.js` (13 assertions) |
| `FreeAgentsScreen` | Free-agent finder: position-filtered best-available list by the caller's board values; per-row Add opens a claim sheet (Sleeper) or explains the missing write path (other platforms) |
| `DraftRoomScreen` | Read-only Draft Room (flag `draft.room`): picks/board/undrafted rookies, honest per-platform refusal states, deep link to the platform's draft room. Optional live polling (`draft.live_poll`), Mock-mode entry (`draft.mock`), inline rank/anchor actions (`draft.rank_inline`), ESPN assign-picks entry (`picks.assign`) |
| `MockDraftScreen` | FTF-native mock draft session (flag `draft.mock`): the user picks for their own team, CPU drafts the rest, nothing reaches the platform; persistent mode-marker rail distinguishes it from the real board |
| `PickAssignmentScreen` | ESPN pick-ownership grid (flag `picks.assign`): season tabs, one-time drag-order setup, collapsible rounds, optimistic-concurrency conflict sheet on a stale write. No value entry anywhere — prices are server-computed |
| `RecordPicksScreen` | Live off-platform pick recording (flag `draft.manual_picks`, ships OFF). ESPN hosts no rookie drafts, so once a league's picks are assigned (M-A) the ONLY way FTF learns who was actually taken is a member typing it in during the real draft — nothing else writes `recorded_picks`. Recording is **confirm, not select**: tap the player from the undrafted list, the team comes from the assignment grid's owner for the cursor's slot, the cursor auto-advances. Writes ride `api/recordedPicks`'s offline queue because live drafts happen on bad wifi |
| `TrendsScreen` | Movers + consensus-gap view; a Rank-stack sibling reached from the rank chooser. **Deliberately not prefetched** by TabNav — its queries take runtime args |
| `PortfolioScreen` | Roster-value portfolio; lives in the Acquire (`Trades`) stack rather than the tab bar, so the bottom nav still shows five tabs |
| `ProfileScreen` | Public profile, destination of the `/u/<username>` deep link |
| `FeedbackInboxScreen` | Tester feedback inbox: the user's own notes with status, plus the share button. Reached from Settings → Test feedback. The list still shows `fixed` notes even though the FAB badge excludes them (`utils/feedbackBadge`) |
| `LeagueJoinScreen` | Invite JOIN interstitial — destination of `<base>/app/league/join/<leagueId>?ref=<u>` while `growth.invite_join_link` is on. The LEGACY `<base>/?league=<id>&ref=<u>` form never reaches here: it has no path, so `utils/deepLinks` captures it directly into the same persisted intent. One owner per URL form; both end in the same place |
| `PaywallScreen` | Fleeced Pro purchase surface (flag `monetize.paywall`, DARK; root-stack **modal** `Paywall`, param `{source}`). Registered unconditionally and **self-guards twice**: on the local flag, and on the server's `{"enabled": false}` from `GET /api/paywall/config` — the server's answer wins over a stale client flag map. Renders the config's pages + products, preferring the StoreKit-localized `priceString` from the RevenueCat offering and falling back to the server's `display_price` when offerings are unavailable (no SDK key, Expo Go). Everything App Store guideline 3.1.2 requires is on-screen before purchase and is NOT conditional on the fetch succeeding: plan name, price+period, trial terms, the auto-renew + cancel disclosure, a working Restore Purchases, and tappable Privacy/Terms links (origin from `api/client.getBaseUrl` — the backend serves the web app at `/`). A user-cancelled purchase is silent; a real failure gets an inline Chalkline error. **Modal ⇒ no `FeedbackFAB`** (#188 exception). Fires `paywall_viewed` / `paywall_purchase_initiated` / `paywall_purchase_completed` / `paywall_purchase_failed` / `paywall_restore`. Pinned by `mobile/tests/check-paywall.js` (11 assertions) |
| `TipJarScreen` | Tip jar (flag `monetize.paywall`, DARK; root-stack **modal** `TipJar`, param `{source}`). Three `ftf_tip_*` consumables from `/api/paywall/config` `tips[]`, purchased via `purchaseTip` (StoreKit-localized price preferred, server `display_price` fallback). **A tip grants nothing and the screen enforces its own promise**: no entitlement-store usage anywhere, copy states "unlocks nothing", and the backend's `entitlements.is_tip_product()` stores the webhook event without writing a row. Same double self-guard as the paywall (local flag + server `enabled:false`); no Restore button and no auto-renew copy on purpose — consumables carry neither. Entered from `settings-tip-row` (both settings surfaces). **Modal ⇒ no `FeedbackFAB`** (#188). Reuses the `paywall_*` events with `source: 'tip_jar'` on view. Pinned by `mobile/tests/check-tipjar.js` (9 assertions) |
| `PlaceholderScreen` | Stub for unfinished routes |
| `TestStagesScreen` | Operator QA: spawn a synthetic adoption-stage user and swap this device into it; device-only factory reset (Settings → Testing, flag `testing.stage_users`) |
| `PremiumRankingsBrowserScreen` | Premium rankings in-app browser, lane 2a of Connected Rankings ([D-058]). Route param `source` ∈ `dynasty_nerds \| dlf` picks one of two CONSTANT URLs. The user logs in **on the site** and taps **the site's own** Export CSV button; FTF never touches, reads or prefills a credential and never operates the page. Registered unconditionally — `ranks.source.*` gates the entry rows in `ImportRankingsSheet`, not the route. The captured file goes back through `state/rankImportBus`. **Carries a passive download-capture shim** (scope.md §6.1): `onFileDownload` only sees navigations, and both sites build their CSV in-page (`data:` for DLF, `blob:` for DN), so an injected read-only script remembers page-created blobs and reads the href of an `<a download>` the user's own tap reached — it never clicks, navigates, fills, or mutates anything, and is gated on a real user gesture. No timers, no background fetch, no auto-navigation beyond the initial URL — pinned by `mobile/tests/check-premium-import.js` §5 |
| `EspnConnectScreen` | WebView ESPN login (flag `espn.webview_capture`): captures `espn_s2`/`SWID` from native cookies. Two entry reasons (route param `reason`): default = private-league link — pair goes back to `EspnLinkSheet` via the bus, which auto-advances; `'send'` = trade-send auth (`SendInEspnButton`) — the screen stores the pair server-side itself (credential-only `POST /api/espn/link`; the server verifies the pair against ESPN before storing), shows send-oriented banner copy (never "this league is private"), and skips the bus entirely. A failed store surfaces IN-flow (`espn-connect.store-error` banner + `espn-connect.store-retry`): rejected pair → fresh sign-in in place; ESPN unreachable → re-send the same captured pair; wrong ESPN account (#321 — 403 + `reason:'wrong_account'`, the pair works but its SWID doesn't own the user's bound team) → dedicated `espn-connect.wrong-account` banner naming the owning-account recovery, fresh sign-in on retry — never a silent success or a dead end. Store failures fire `espn_connect_store_rejected {reason, source, saw_otp}`; pinned by `tests/check-espn-wrong-account.js`. **Fresh-login guarantee (2026-08-12 incident):** the WebView only mounts after `clearEspnCookies` settles (`storeCleared` gate, `espn-connect.clearing` spinner meanwhile) — the clear covers espn.com AND the Disney SSO domains, scoped, never clearAll — so sign-in can never silently resume the previous account; pinned by `tests/check-espn-connect-clear.js` |

## Sharp edges

- `LeagueSummaryScreen`'s drill-in has exactly **one** `setSelectedId(null)`, inside `closeTeam` — every exit control (header back, in-card link, active-tab re-tap) routes through it — **Android hardware back is withdrawn, not wired**; `'hardware_back'` is a reserved `via` value with no emitter, pinned both ways, and `openTeam` emits `via:'refocus'` when jumping team-to-team. A control that clears focus directly disappears from analytics while the UI keeps working. Pinned by `mobile/tests/check-analytics-297-302.js`.
- `league-summary.header-title` (#302) — the stack-header title. Reads "League rankings" on the all-teams view and swaps to the focused team's name while drilled in. **Tab-root registration only**; the legacy root-stack `LeagueSummary` push keeps its static title.
- `league-summary.roster-close` / `league-summary.back-all-teams` (#302) — the drill-in exit, **moved** from the chart-card header (where it scrolled away) to the stack header's `headerLeft`. Ids deliberately unchanged, as in #243. On the legacy root-stack push it remains the in-card link, because that screen's `headerLeft` is already its own back control. Exactly one of the two renders at a time.

- `RookieRanksScreen` must never call `/api/tiers/save` or the merged-band `apply_tiers` path — reorder only, pinned by `backend/tests/test_rookie_ranks_editable.py`.
- Pick-assignment surfaces never send a value field — prices are computed server-side from (round, years_out, format); the server 400s any value-shaped key.
- `DraftRoomScreen`, `MockDraftScreen`, `PickAssignmentScreen` register UNCONDITIONALLY — their flags gate the entry point, not the route, so a stale deep link lands on an honest unavailable state instead of 404ing.
- Root-stack pushes over `headerShown:false` Main tabs (FreeAgents, LeagueSummary, TestStages, PickAssignment, MockDraft, EspnConnect) need the explicit `HeaderBack` control — native back is dead on iOS 26 (RNS#3294).
- `MockDraftScreen`'s mode-marker rail must render outside the ScrollView and every conditional (`mobile/tests/check-mock-mode-marker.js`) — it lets the room safely omit "never drafts for you" copy in Mock mode.
- `LeagueSummaryScreen`'s Season outlook section is a calibration result, not a style: no raw percentages, no `title_pct` at any week, no win-loss numbers while `meta.beta` is true, and bands read from `docs/cross-client-invariants.md` § "Playoff outlook bands". Non-Sleeper leagues get an explanatory row and no request — `backend/outlook/league_state.py` implements Sleeper only.
- Every new user-facing screen shows `FeedbackFAB`, but only **root-stack pushes mount one** (`<FeedbackFAB activeScreen="<RouteName>" aboveTabBar={false} />`). Tab-stack screens are covered by RootNav's single global mount inside `Main` — a second one is the #196/#197 double-FAB bug, which is why the tab copy of `DraftRoomScreen` gets `initialParams {inTabs:true}`. Screens with a pinned bottom bar call `setPinnedBottomBarHeight` (from `FeedbackFAB`) instead — Tiers, Quick Set, Quick Rank. Modals/sheets and onboarding flows are the exceptions.
- Anchors/Tiers/Quick Set/Overall/Quick Rank/Trios/RookieRanks share `RookieScopeControl` + `state/rookieScope.ts` (flag `ranks.rookie_subset`) — it narrows candidates only, never save semantics.
