# mobile/src/components/

Stateless / lightly-stateful reusable UI. No data fetching here — accept props.

| Component | Use |
|---|---|
| `PlayerCard` | Player tile with name, position, value |
| `TradeCard` | Give/receive trade summary card. Swipe-deck extras (2026-07-25): optional `onKeepSide` (#186 per-side "Keep · more offers" — pins that side via the FB-47 machinery + regenerates) and `onEditInCalculator` (#190 → calculator prefilled); 2026-07-27 `onRemoveAsset` (#194 — per-row ✕ drops the asset and re-prices; the last asset on a side renders dimmed, the tap voices the min-one-per-side hint). All render only in the swipe variant and only when the screen passes them. #216 (2026-08-02): optional `hideMatchStrength` — hides the StrengthBar for cards with no divergence match score (the featured-trade window's consensus asset ideas). #249 (2026-08-05): optional `hideLockButton` — hides the S3 PRD-02 untouchable lock "visible twin" only (long-press menu, a11y custom action and UNTOUCHABLE badge untouched); passed by MatchesScreen on both segments |
| `FeaturedTradeWindow` | #216/#209 (2026-08-02, single-pin find-a-trade): the FEATURED TRADE window — "Featured trade" tick-label (NO player name, v3 operator call) over a read-only reused `TradeCard` (give LEFT / receive RIGHT, `hideMatchStrength`, #190 `onEditInCalculator`, Dynasty Value Swing via the shipped `TradeValueBar` fed by the idea's server-stamped `favors`/`gap`). Back chip = v3 **Variant A**: "‹ Previous trade" on its own slim row ABOVE the header, rendered only while `onBack` is passed (host owns the history stack — the chip is part of the window pattern, any swap-in creates a back target). Also exports `assetIdeaKey(idea)` — the stable counterparty+package identity used for In-window tagging, history and testIDs. Approved mocks: `mockups/polish-lab-2026-08/asset-ideas-layout-v2.html` + `-v3.html` |
| `AssetIdeasPanel` | #172/#189/#198 grouped Upgrade / Lateral / Downgrade ideas for the single pinned asset (flag `trade.asset_ideas`). #216 rework: renders as the "More trades for <pin>" list UNDER the FeaturedTradeWindow — rows are tappable (chevron, pressed = surface shift) and call `onSelectIdea` to load their trade into the window; the row matching `featuredKey` shows an ice-bordered inert "IN WINDOW" tag; rows keep the signed diff chip and no longer show the raw value pair. #240 (2026-08-02): the give↔receive name line owns the full row width (chevron only beside it) and the diff chip + IN WINDOW tag sit on the meta line — the old fixed-width right rail starved/overlapped names; texts use the chalkline Text primitive (body/dense scale caps) |
| `RookieScopeControl` | rookie-draft M2 (flag `ranks.rookie_subset`) — THE rookie-scope control, rendered by **every** rank surface (Pick Anchors `anchors` · Tiers `tiers` · Quick Set `quick-set` · Quick Rank `quick-rank` · Overall ranks `manual-ranks` · Trios `trios`), each passing its own `surface` testID namespace → `<surface>.scope-all` / `.scope-rookie` / `.scope-consolidated`. Content + state come from `state/rookieScope.ts`, so the six surfaces can never diverge. #133 PositionTabs construction (hairline group, ink-3 fill + 2px underline — ice for All players, flare for Rookies, flare being the informational-highlight accent per ADR-005). Renders `null` with the flag off — that is the client-side kill switch, and it also removes the only in-app entry to the consolidated rookie view. When Rookies is active it carries the "See all rookies in one list" link → Rank-stack `RookieRanks`. `flush` drops the built-in gutter for hosts that already pad (PickAnchor, Trios). Also exports **`RookieScopeEmpty`** — the shared rendering of the server's typed `{empty:true, reason}` response (thin/unloaded class is a designed state, never an error) with the always-works "Show all players" escape |
| `TierBadge`, `TierBin` | Tier label + drop-zone bin |
| `PositionChip` | QB/RB/WR/TE chip with color |
| `StrengthBar` | Horizontal value/strength meter |
| `TradeValueBar` | The pick-denominated "Dynasty value swing" verdict (#157/#169) — diverging bar centered on even, margin spoken in draft-pick terms; presentation-only over `/api/trade/evaluate` fields (`favors`/`gap`), mounted on every trade surface (deck TradeCards, FeaturedTradeWindow, both calculator verdicts, League "Works right now" example). #243 density V1 (2026-08-03, mock `mockups/polish-lab-2026-08/tradevaluebar-density.html` B1): winner line stepped `type.heading`→`type.title`, verdict paragraph collapsed by default behind a one-line "Why?" disclosure (AdjustmentsDisclosure grammar, per-instance state — applies at every mount by design), verdict-box vertical padding `md`→`sm`, 9px scale labels fixed to the 11px Chalkline floor (~56pt saved/instance collapsed) |
| `TradeMeter` | Thin TRADE (tradeability) / GET (acquirability) bar, 0–1 score from `/api/rankings` — removed from Tiers tiles in 1.5.4 #100; kept available (payload fields still serialized) |
| `TradeSide` | Calculator: one side of a hand-built trade (players + add button). **W3 M-C (2026-08-08, flag `picks.assign_tradeable`):** renders `MemberEnteredMarker` UNCONDITIONALLY in the priced row and takes a `leagueId` prop as the correction target — the marker self-gates on the flag and on `source === 'user'`, so a host-side conditional could only suppress it (pinned by `mobile/tests/check-member-entered-marker.js`). Provenance rides `CalcPlayer.pickSource`/`pickSeason`, which only `InLeagueCalculator` sets |
| `VerdictPanel` | Calculator: dual-board fairness verdict + gives/gets bars (demo mode) |
| `ConsensusVerdictCard` | Calculator: server-authoritative consensus verdict from /api/trade/evaluate (live mode) |
| `InLeagueCalculator` | Calculator "In league" mode: real opponent + rosters, two-board mutual-gain verdict (evaluate Mode B), carries Send in Sleeper. #190: optional `initialOpponentId`/`initialGiveIds`/`initialReceiveIds` props (deck "Edit in calculator" prefill via TradeCalculatorScreen's `prefill` route param; initial values only). #204 (2026-07-27): the verdict's headline visual is the shared `TradeValueBar` (same consensus fields as live mode; the old consensus-basis headline sentence collapsed to a one-line provenance note; two-board delta rows + totals row stay). #202: a prefilled mount collapses the partner picker to one "Trading with @x · Change" row; Change expands today's chips. #203: computes the picker's "Suggested" rows (gap-closing assets + league-relative NEED signal from the already-fetched power-rankings read). #238 (2026-08-03, mock `mockups/polish-lab-2026-08/lineup-before-after.html` frame C1): when Mode B's `starter_impact.slots` is present, the verdict renders the full before/after starting-lineup table (`calc.lineup-impact` — all slots in template order, unchanged rows dimmed with a flat "—" chip, changed rows arrow + signed green/red delta chip, net "Starting lineup total B → A (+N)" line) in place of the old `calc.starter-impact` one-liner; pre-#238 servers (no `slots`) still get the sentence. Calculator only — deck/featured cards are explicitly phase 2. #251 (2026-08-05): `EvenerRows` moved ABOVE the LeagueVerdict — order is now trade sides → "Recommended to even it" → fairness summary (pure reorder, operator report via the featured-window edit-in-calc hand-off; live-mode `ConsensusVerdictCard` unchanged) . **W3 M-C (2026-08-08, flag `picks.assign_tradeable`):** the one mount with a real league, so it is the only place that copies `source`/`season` off `GET /api/league/picks` onto each owned-pick `CalcPlayer` (`pickSource`/`pickSeason`) and threads `leagueId` into `TradeSide`, both `PlayerPickerModal`s and `EvenerRows` — four of D17's five priced surfaces get their provenance from here, so `check-member-entered-marker.js` pins the mapping as well as the markers |
| `SuggestionCard` | Calculator: tappable fair-package suggestion |
| `EvenerRows` | Calculator: "Recommended to even it" one-tap balance rows from `/api/trade/evaluate` `eveners` (DynastyGM teardown 2026-07-26) — ≤3 single assets (players/owned picks, pick label shown) + at most one 2-piece package row; + button adds to the side `gap.add_to` points at, re-evaluate refreshes/clears the rows. Used by `InLeagueCalculator` (Mode B roster/pick eveners) and `ConsensusVerdictCard` via `onAddEvener` (open calculator's generic-pick evener). **W3 M-C (2026-08-08, flag `picks.assign_tradeable`):** renders `MemberEnteredMarker` UNCONDITIONALLY in the priced row and takes a `leagueId` prop as the correction target — the marker self-gates on the flag and on `source === 'user'`, so a host-side conditional could only suppress it (pinned by `mobile/tests/check-member-entered-marker.js`). The evener is the sweetener FTF ACTIVELY recommends, so it is the surface where an unmarked assertion does the most damage (plan §6.4 S4) |
| `AdjustmentsDisclosure` | Calculator: collapsed-by-default "Value adjustments" disclosure (DynastyDealer teardown 2026-07-26) — itemizes why each side's displayed package value differs from the naive sum of its assets (`/api/trade/evaluate` `adjustments` + `naive_totals`: package-depth discount, crown consolidation premium), signed green/red amounts + plain-language why + "Sum of parts N → M". Renders nothing without data. #215 (2026-08-05): optional `studTaxMode` prop — `'off'` renders a one-line dim "Value adjustments off" note (`calc.adjustments.off`) instead of vanishing (the user disabled the stud tax, so totals ARE the sums of parts). Mounted in `ConsensusVerdictCard` and the In-league verdict card |
| `ShareTradeImage` | Calculator: "Share image" action (DynastyDealer teardown 2026-07-26) — off-screen Chalkline share card (caption + both sides w/ values + verdict + text-only "Dynasty Trade Finder" watermark) captured to PNG via react-native-view-shot and handed to the native share sheet; text-share fallback on capture failure (and on Android). Mounted in `InLeagueCalculator` (In-league) + `TradeCalculatorScreen` (live) |
| `PlayerPickerModal` | Calculator: search + position-filter player picker. #203 (2026-07-27): optional `suggested` prop — ≤4 "Suggested" rows pinned above the list (standard row + flare NEED badge), hidden while a search/position filter is active. **W3 M-C (2026-08-08, flag `picks.assign_tradeable`):** renders `MemberEnteredMarker` UNCONDITIONALLY in the priced row and takes a `leagueId` prop as the correction target — the marker self-gates on the flag and on `source === 'user'`, so a host-side conditional could only suppress it (pinned by `mobile/tests/check-member-entered-marker.js`). Both variants share one row renderer, so the marker covers them together; the row is an accessible container, so the same sentence + a `correct` custom action also ride the ROW's a11y contract |
| `OutlookSheet` | Bottom sheet for team outlook selection. Since #212 the Trade-Finding Hub no longer mounts it (the hub's Trade DNA panel edits in place); the sheet survives for TradesScreen's own entry points (first-visit force-open, inline outlook banner, classic-deck Edit) |
| `OutlookBiasReceipt` | #231 deck bias receipt (v3 mock): one quiet line above the finder deck ("Leaning young + picks — you're Rebuilding · Change"). **#246: Change fires the host's `onChange` callback** (TradesScreen opens `TradeDnaSheet` over the deck) — the old `navigation` prop + `TradesHome {editDna:true}` hop are gone. Self-contained otherwise (own session/flag/prefs reads on the shared `['league-prefs']` cache); renders null unless flag `trade.outlook_direction` is on AND the resolved outlook (declared, else inferred — "you look" copy) is directional (`not_sure`/none ⇒ nothing) |
| `TradeDnaSheet` | **#246 (2026-08-05, mock `acquire-landing-guided-first.html` B3):** the hub's #212 Trade DNA editor LIFTED into a bottom sheet over the guided deck — opened by the receipt's Change link or a legacy `editDna:true` route param on `TradesHome`. Opens straight into the expanded editor (the receipt is the collapsed summary now): four outlook cards + Chasing/Shopping multi-select toggle rows + untouchables count/Manage line, with the #236 autosave machinery verbatim (every tap POSTs the full payload, in-flight coalesced last-write-wins, failure reverts drafts + inline error) — so **Done / scrim-tap is a pure dismiss** (no Save button). The #173 untouchables management sheet remains reachable via Manage and renders as a second layer INSIDE the same Modal (iOS won't stack sibling Modals). testIDs kept from the hub host: `dna.outlook.*` / `dna.chase.*` / `dna.shop.*` / `dna.done` / `finder-hub.dna.untouchables` / `finder-hub.untouchables.row/remove.<player_id>`. Modal/sheet ⇒ no FeedbackFAB |
| `SendInSleeperButton` | Flagged-beta ("Send in Sleeper"): propose a trade to Sleeper directly; routes to connect / deep-link fallback. Self-gates to Sleeper leagues (#146): renders null when its `leagueId` is an imported ESPN league (platform check against `useSession.leagues`) — mounts never need their own gate. #180: calls `POST /api/trades/validate` pre-confirm and surfaces advisory warnings (archived league / player moved / roster limit) with Cancel / Send anyway |
| `VerifyAccountBanner` | Account-auth P1: quiet dismissible "Verify your account" strip floated above the tab bar (mounted once in RootNav → Main). Renders null unless the session is unverified AND (a verified controller exists OR enforcement is on); routes to SleeperConnect |
| `SteerSlider` | Settings: "We steer ↔ You steer" 5-dot ranking-method selector (one dot per ranking flow, guided → manual; Quick set leads since #119) |
| `EspnLinkSheet` | Flag-gated (`espn.link`) ESPN league link flow (feedback #115): league ID/URL input (+ manual espn_s2/SWID paste for private leagues) → "which team is yours?" → import summary (match rate, skipped players, read-only expectations). Opened from LeaguePicker's "Link an ESPN league" footer |
| `PlatformLinkSheet` | Zero-auth platform-aware link flow for MFL (`mfl.link`) + Fleaflicker (`fleaflicker.link`): `platform` prop; league URL/ID input (MFL adds a season year; Fleaflicker adds an optional find-by-email lookup) → "which team is yours?" → import summary. Same three steps as EspnLinkSheet, no cookie paste. Opened from LeaguePicker's per-platform footer buttons. #177 (flag `mfl.auth_link`): MFL adds a "Sign in with MFL" path — username+password (secureTextEntry, password cleared post-call, never logged) → league list all pre-checked (franchise auto-bound from myleagues) → import-all summary |
| `MemberEnteredMarker` | **draft-extensions W3 M-C (2026-08-08, flag `picks.assign_tradeable`, ships OFF)** — D17's "provenance is inescapable" rule, as a component. An ESPN league has no platform record of pick ownership, so a LEAGUEMATE types the grid in; with this flag on those asserted rows price at full engine parity, and nothing on ESPN's side will ever contradict a wrong one. So every surface rendering a PRICE for a `source: 'user'` pick shows `MEMBER_ENTERED_COPY` — the registered cross-client string **"Member-entered — not verified with ESPN"**, exported from here and never re-typed by a host (`docs/cross-client-invariants.md`) — and the marker IS the one-action correction: tapping it calls `openPickCorrection` → `navigationRef.navigate('PickAssignment', {leagueId, season, focusPickId})`, and the grid switches to that season, expands the round and highlights the row. **It self-gates on BOTH the flag and `source === 'user'`** (plus the two ids the route needs), which is what lets the five hosts render it UNCONDITIONALLY inside their priced row — `mobile/tests/check-member-entered-marker.js` parses the real TSX and fails if it drifts into a ternary or `&&`, the regression a grep would miss. The five priced surfaces: `PlayerPickerModal` (trade-away/acquire picker) · `SwapSuggestSheet` · `EvenerRows` · `TradeSide` (calculator rows) · `LeagueSummaryScreen`'s draft-capital group. Also exports `isMemberEntered(source)` (THE predicate, one definition) and `usePicksTradeable()` — the two surfaces whose row is an accessible container (`PlayerPickerModal`, `SwapSuggestSheet`) use them to fold the same sentence + a `correct` `accessibilityActions` entry into the ROW's a11y contract, because iOS swallows a nested a11y node. Chalkline: flare tick (informational highlight, never an action color), 11px `fonts.ui` in `chalk.dim`, one 12px ice chevron — deliberately quieter than the `type.data` price it qualifies, and never a warning tone (an assigned grid is the intended state) |
| `ProvenanceChip` | Onboarding item 4 (flag `onboarding.trades_first`): deck-level tick-label chip — "CONSENSUS VALUES" → "YOUR BOARD" once `ob.quicksetCompletedPositions` is non-empty (flare = informational highlight). `onPress` inert until item 7 wires the Quick Set tap-through |
| `SkeletonTradeCard` | Onboarding item 4: static (shimmer-free) first-run deck placeholder with a one-line status while pregenerated cards stream in |
| `CoachMark` | Onboarding guided layer (v2.1): one-time inline dismissible callout — never modal, never stacked; callers own shown-once persistence + `coach_mark_shown/dismissed` events |
| `IdentityConfirmStrip` | Onboarding item 4 (F5): first-run "Trading as @user — not you?" strip (avatar + ice action → sign-out confirm; X = session dismiss) |
| `QuickSetPromptCard` | Onboarding item 7 (flag `onboarding.quickset_prompt`): inline deck-slot prompt card ("These trades use consensus values.") — accept deep-links to onboarding-mode QuickSetTiers, dismiss = snooze (caller owns bookkeeping); explicit buttons, not swipeable (documented deviation) |
| `AppleSaveMomentSheet` | Onboarding item 8 (flag `onboarding.apple_save_moment`, ADR-006): save-moment Apple ask modal — honest cross-device framing only, official Apple button, "Not now" decline; bind flow mirrors Settings' handleLinkApple (conflict/linked/no-session outcomes) |
| `Toast` | Transient notification. Teardown S4 PRD-03: unflagged VoiceOver announce on show + Reduce Motion fade fallback; flag `ux.toast_v2` = warn/error hold ≥5s (holdMs 0 = sticky); optional `action` slot (label+callback, e.g. Undo — callers pass it only under `ux.swipe_undo`) |
| `PlayerContextMenu` | Teardown S3 PRD-02 (flag `ux.player_context_menu`): shared player long-press bottom sheet — header = player info, rows = per-surface commands (untouchable / swap / swap suggestions / remove) passed by the caller; rows accept an optional `testID` override (2026-07-27 — defaults to `player-menu.<key>`); also exports `LockGlyph` (untouchable visible-twin icon, local Svg pending Icon.tsx fold-in) |
| `AnchorSheet` | **draft-extensions W1 (2026-08-06, flag `draft.rank_inline`)**: the anchor question asked WITHOUT leaving the screen you asked it on — the Draft Room's "Set my value". NET-NEW (HLD RB-11): `saveAnchor` had exactly one caller and it was a full SCREEN (`PickAnchorScreen`), so the plan's phrase "the anchor sheet" named a component that did not exist. What is REUSED is what matters: the shipped write LANE (`saveAnchor` → `POST /api/anchor/save`, unchanged apart from the request-only `via`) and the shipped rung GRID (`utils/anchorRows`, extracted rather than copied). **The anchor lane ONLY** — it must never reach `/api/tiers/save` or the merged-band path (pinned by `backend/tests/test_draft_extensions_w1.py`). Commits on the rung tap: D1's bar is "≤3 taps AND no navigation away", and long-press → *Set my value* → rung is already three, leaving no budget for a confirm step. Mis-taps recover by CORRECTION, not a fake undo — the sheet stays open with every rung live, because a true undo would need a restore-prior-Elo call the anchor lane does not have. Host supplies `via` (so a second host can't inherit the draft room's label) and `onSaved` (the host re-prices its own cache from the SERVER's value — the anchor→value map depends on the user's `#111` scale and lives in one place). Modal/sheet ⇒ no FeedbackFAB |
| `draft/DraftRows` | **draft-extensions W2 (2026-08-06)** — the Draft-surface pieces the room and the mock share, moved not rewritten: the `draftRow` StyleSheet (order row · slot/rank cells · undrafted row · basis chips · fallback note), `BasisChip`, `positionOf`, `slotLabel`, and the two fallback strings (`MY_BOARD_FALLBACK_COPY`, `NO_CONSENSUS_VALUE_COPY`) so the two lists cannot say different things about the same rookies. **`UndraftedRowView` is deliberately NOT here** — it stays exported from `screens/DraftRoomScreen` because `backend/tests/test_draft_extensions_w1.py` pins W1's kill switch and per-player testID to that source file, and W2 owns `mobile/` only. There is still exactly one undrafted row in the app; only its address is the compromise |
| `draft/MockChrome` | **draft-extensions W2 (flag `draft.mock`)** — `MockRail` (the persistent mode marker) + `DraftModeToggle` (the option-C `Real draft | Mock` segmented control, `draft-room.mode.real` / `.mock`). The rail is flare (ADR-005: flare = informational highlight, ice stays on actions) with a full-width bottom edge, so a crop of any part of a mock surface still reads as "mock". **Hosts must render it OUTSIDE their ScrollView and outside every conditional** — that is the whole obligation the operator's choice of placement C created, and `mobile/tests/check-mock-mode-marker.js` enforces it structurally |
| `draft/MockEntryPanel` | **draft-extensions W2** — the Mock side of the room's switch: `mock-entry.card` (headline + body + meta line + `mock-entry.start` / `.resume` / `.start-over` / `.recap` / `.run-it-back`), `mock-entry.loading`, `mock-entry.error`, and a MUTED card with a dead CTA for every refusal (`mock-entry.blocked.<cpu_model_unvalidated\|class_not_loaded\|startup_draft\|league_too_small\|live\|complete>`). Every refusal renders in the ROOM — the user is never pushed into a session screen to be told it isn't possible. Exports `MOCK_MIN_TEAMS = 6`, the client-side league-size floor the engine does not have (gap G5): if a server-side `league_too_small` ever lands, delete the constant rather than keep it in sync |
| `draft/MockSetupSheet` | **draft-extensions W2** — the setup sheet, built as a CONFIRMATION not an interrogation: `mock-setup-sheet` · read-only "You're drafting for" (multi-team drafting is out of v1, so a disabled picker would advertise a capability the engine lacks) · `mock-setup.rounds.minus|value|plus` (1–8, `ROOKIE_MAX_ROUNDS`; pre-filled from the board's `rounds`, because the route's own default is a flat 4 — gap G9) · `mock-setup.type.linear|snake` (defaults to linear and SAYS so: the board payload carries no draft `type` — gap G6) · `mock-setup.order-notice` (the KD-6 disclosure, shown before the commit when the league has no published order) · `mock-setup.start` / `.cancel` / `.error`. Modal/sheet ⇒ no FeedbackFAB |
| `HelpSheet` | Teardown S4 PRD-01 (flag `ux.help_surface`): lightweight help bottom sheet (2–3 sentences + "Read more" web link); exports `InfoButton` (ⓘ, 44pt effective target) |
| `RankImportSheet` | #232 follow-on (2026-08-02, flag `ranks.import`; mocks `rank-method-consolidation-v2` B1–B3): "Bring your rankings" paste-first import — one bottom sheet, two steps. Paste: mono textarea + live "N rows detected" (display-only mirror; server parse authoritative) + honesty line + "Match N players" → `POST /api/rankings/import-match`. Review: auto-matched rows with check (audit-visible), ambiguous rows with "Which one?" candidate chips + Skip, unmatched auto-skipped; "Apply N ranks — M to resolve" disabled until resolved → `POST /api/rankings/import-apply` (imported order on top, unlisted keep relative consensus order below). Opened from RankHome's `rank-home.import` link; parent owns success toast + navigation. Modal/sheet ⇒ no FeedbackFAB |
| `SwapSuggestSheet` | Deck swap-suggestions sheet (2026-07-27 player-changer — operator follow-up to the calc eveners): one-tap replacement candidates for ONE asset of the top deck card, from `/api/trade/evaluate` on the trade MINUS that asset (`one_sided_eveners` covers 1-for-1 cards). Honest loading/error/empty states; "Browse full roster" hands off to the classic #86 SwapPlayerSheet. Host (TradesScreen) owns the query + pick-to-reprice wiring. **W3 M-C (2026-08-08, flag `picks.assign_tradeable`):** renders `MemberEnteredMarker` UNCONDITIONALLY in the priced row and takes a `leagueId` prop as the correction target — the marker self-gates on the flag and on `source === 'user'`, so a host-side conditional could only suppress it (pinned by `mobile/tests/check-member-entered-marker.js`). Same accessible-container treatment as `PlayerPickerModal` |
| `TradeFinderModeBar` | The Acquire tab's persistent mode chip strip (#156 origin, flag `trades.finder_hub`; **#246 2026-08-05 guided-first landing** — approved mock `acquire-landing-guided-first.html` B1 + operator FA-chip override): chip row **Guided · Team · Player · Calc · Free agents** + mode title, carried atop `TradesScreen` in every finder mode — since #246 that includes the tab's landing itself, and this strip IS the mode-switching home. The "‹ Hub" back chevron is GONE (`onHub` prop + `trades.finder-mode.hub` retired with the unrouted hub); the hint line renders only when the host passes `showHint` (cold start, `deck.length === 0` — mock B2). Free agents is a NAVIGATION chip (pushes root-stack `FreeAgents` via `onFreeAgents`), not a deck mode — same chip construction as Calc, which set the push-chip precedent (deliberately no visual distinction). Presentational; host owns nav (guided/team/player switch in place via setParams, calc/free-agents navigate). **Rookie-draft placement (2026-08-06, flag `draft.room`, option B):** an optional `onDraft` prop prepends a sixth **Draft** push-chip so the strip reads **Draft · Guided · Team · Player · Calc · Free agents**. It must LEAD — the five shipped chips already overflow the usable width (≈402pt vs ≈361pt), so an appended chip would never be seen. Omitting `onDraft` renders the shipped five unchanged, which is how the flag gates it |
| `LeagueProgressModule` | #229/#230/#234 (approved mock `mockups/polish-lab-2026-08/empty-states-progress-v3.html`, ships live): ONE card owning every league unlock — SVG positions-ranked ring (0–4) beside a solid-ice compact "Rank players" (operator tweak: in-section buttons are BOTH solid ice), per-team segmented bar of ranked members ("N/M (you)"), one unlock sentence ("X more ranked leaguemates unlocks mutual matches", threshold 2 ranked leaguemates), and the fold line for collapsed zero-sections ("…once 3 leaguemates have ranked" — the `/api/league/contrarian` threshold). **#243 (2026-08-03, fold V1 mock `league-home-fold.html`):** the old ghost "Invite leaguemates" Button row is gone — the invite affordance is an INLINE ice text link on the unlock sentence ("…unlocks mutual matches. Invite them", `league.progress-invite`, same OS-share handler; falls back to a standalone "Invite leaguemates" link when the sentence isn't rendered because matches already unlocked), and the full variant's sentence dropped the "— trades both sides already like" tail per the mock. `compact` variant (Matches mutual empty state) = bar + short unlock line only, never an invite; null for totalTeams ≤ 1. Presentational — hosts fetch summary/coverage/progress/tiers-status |
| `MarketPulseStrip` | #243 (2026-08-03, approved mock `mockups/polish-lab-2026-08/risers-fallers-cards.html` frame D1; operator placement override: mounts BELOW League home's Explore section, not top-of-page): one compact card line — top riser + top faller (last name + signed %, chevron up/down glyphs, pos/neg colors) + ice "See all movers ›" — whole strip opens a #242-pattern bottom sheet (maxHeight 85%, size-to-content) with the full Risers/Fallers columns (pos-dot + name + `POS · TEAM` + signed %) under the flare "FTF community value · 30-day change" honesty label. Self-contained (own `market.movers` flag read, `useSession.activeFormat`, `['market-movers', format]` query on `GET /api/market/movers`); renders null without flag or data — loading, error (incl. the flag-off 404), and thin-history empty payloads all render nothing |
| `TopBar` | Global header (#223/#224): the left cluster is the ACTIVE league — brand tick + 11px "LEAGUE" micro-label over a truncating league name + ice chevron — and tapping it opens the app's ONE `LeagueSwitcherSheet` instance (mounted here, works from every tab; carries the #199 add-a-league wiring via navigationRef → root-stack `LeaguePicker`). No-league (account-only) sessions fall back to the "Trade Finder" wordmark. Right side: bell (inboard, so the unread badge sits off the screen edge) + settings gear. Bar is 52pt (`TOP_BAR_HEIGHT`). #247 (2026-08-05): the league-name row carries a solid-ice **format tile** (`topbar.format`) — the active league's scoring format from `useSession.activeFormat` ("1QB" / "SF TEP", the only two `ScoringFormat`s; TE-P is baked into `sf_tep`), `ice.base` fill + `ice.on` 11px dataSemi text, radii.xs; format also appended to the cluster's a11y label (the Pressable swallows child text on iOS). Hidden while format unknown / no league |

## testID registry (UI-test harness — docs/plans/mobile-testing/lld.md Appendix A)

**Grammar:** `testID = <screen> "." <element> [ "." <qualifier> ]` — kebab-case segments; qualifier is a stable domain id (`player_id`, `league_id`, `user_id`, tier, position), **never a list index** (lists reorder; single exception: synthetic stable lists like demo partner chips). State is asserted via distinct IDs or visible copy, never encoded in an ID. Every element a Maestro flow references must carry one (`mobile/scripts/testid-lint.sh` cross-checks). **Adding a screen = adding its IDs** — part of the definition of done.

**Maintenance tax (pay in the same change):** element rename → update IDs + affected flows (~10–20 min); new screen → 1–2 flows + profile touch (~1–2 h); new feature flag → boundary case pair (~30 min).

**RN caveat:** containers with implicit `accessible={true}` (Touchables) swallow child IDs on iOS — fix with `accessible={false}` on the container. `DraggableFlatList` rows are the likeliest offender.

**Landed so far (S1 spike set, 2026-07-11):**
- SignIn: `signin.username-input` · `signin.continue-btn` · `signin.hint-btn` · `signin.demo-link` · `signin.error-text`
- Tab bar (`tabBarButtonTestID`): `tab.rank` · `tab.trades` · `tab.matches` · `tab.league`
- LeaguePicker: `leagues.row.<league_id>` (S1 part B surfaced the container-accessibility hazard live: the row Pressable swallows child Text from the tree — text-asserts fail while pixels render; id selectors are the fix)
- Verify banner (Main root): `main.verify-banner` · `main.verify-banner.verify` · `main.verify-banner.dismiss`
- SleeperConnect: `sleeperconnect.done` (post-capture success overlay)

**W1 smoke-set tranche (2026-07-12):**
- RankMenu: `rankmenu.quickset|trios|anchors|tiers|manual|trends` · RankHome: `rank-home.card.<quickset|trio|anchor|tiers|manual>` (quickset added by #119)
- Trios: `trios.card.a|b|c` (fixed trio slots — stable domain qualifier) · `trios.pos-tab.<pos>` · `trios.speed-toggle` · `trios.confirm-btn` · `trios.skip-btn`
- Trades: `trades.find-btn` · `trades.card-top` · `trades.like-btn` · `trades.pass-btn` · `trades.subnav.<trades|portfolio|calculator>` · `trades.progress-strip` · `trades.empty-text`
- Calculator: `calc.mode-tab.<league|live|demo>` · `calc.side-a-add` · `calc.side-b-add` · `calc.picker.search` · `calc.picker.row.<player_id>` · `calc.verdict` · `calc.clear-btn`
- Matches: `matches.segment.<mutual|awaiting>` · `matches.empty-text` · League: `league.hero` · Tiers (minimal): `tiers.list` · `tiers.pos-tab.<pos|all>` (`all` = the #132 cross-position All board) · `tiers.save-btn`
- Pass-through props added: chalkline `Button.testID`, `PlayerCard.testID`, `TradeSide.addTestID`
- OutlookSheet: `outlook.save-btn` (the sheet AUTO-OPENS on first Trades visit — flows dismiss it conditionally) · Picker: `calc.picker.done` (onPick adds without closing; Done closes — flows must tap it)

**ESPN league linking tranche (2026-07-12, flag `espn.link`):**
- LeaguePicker: `leagues.link-espn` · `leagues.link-mfl` · `leagues.link-fleaflicker` · EspnLinkSheet: `espn-link.input` · `espn-link.private-toggle` · `espn-link.s2-input` · `espn-link.swid-input` · `espn-link.continue` · `espn-link.team.<team_id>` · `espn-link.open` · `espn-link.error` · League tab: `league.espn-resync`
- PlatformLinkSheet (MFL/Fleaflicker): `platform-link.input` · `platform-link.year` (MFL) · `platform-link.email-toggle`/`platform-link.email`/`platform-link.email-lookup`/`platform-link.discovered.<league_id>` (Fleaflicker) · `platform-link.continue` · `platform-link.team.<team_id>` · `platform-link.open` · `platform-link.error` · Settings: `settings.link-platform`

**Apple entitlement tranche (2026-07-12, feedback #131):**
- SignIn: `signin.apple-btn` (Apple sign-in button)
- Settings: `settings.link-apple-btn` (Link Apple card button)
- TopBar: `topbar.settings` (Settings gear — shared chrome, sibling of reserved `topbar.bell`/`topbar.bell-badge`)

**Feedback batch tranche (2026-07-12, #130/#136):**
- Settings: `settings.close-btn` (modal header close Icon Button, #130) · `settings.link-espn` (flag `espn.link` CTA row → LeaguePicker with the ESPN sheet auto-opened, #130)
- Quick Rank (#136): `rankmenu.quickrank` (Rank action-sheet row) · `quick-rank.pos-tab.<pos>` · `quick-rank.chip.<player_id>` · `quick-rank.save-btn`

**Legacy-smoke repair tranche (2026-07-12, QA F-1..F-3):**
- FeedbackFAB: `feedback.fab` (floating capture button — its accessibilityLabel "Capture feedback" also text-matches in Maestro, so flows must use the id)
- FeedbackSheet: `feedback.severity.<bug|polish|idea>` · `feedback.note-input` · `feedback.save-btn`

**Free-agent finder tranche (2026-07-17, #143):**
- FreeAgentsScreen: `free-agents.pos-tab.<all|qb|rb|wr|te>` (filter pills) · `free-agents.list` (FlatList) · `free-agents.row.<player_id>` (dense PlayerCard) · `free-agents.empty-text` (no-league AND empty-list states) · `free-agents.back-btn` (#151 explicit header back — native back dead on iOS 26, RNS#3294)

**Free-agent add + Trades entry tranche (2026-07-25, #179/#182):**
- FreeAgentsScreen: `free-agents.add.<player_id>` (per-row Add — Sleeper: opens the claim sheet since the 2026-07-26 v2; ESPN/MFL/Fleaflicker/local: ghost/dim, tap explains the missing write path)

**FA claim-sheet tranche (2026-07-26, #179 v2):**
- FreeAgentsScreen ClaimSheet (Sleeper leagues only — replaces the v1 explainer alert): `fa-claim.sheet` (sheet container) · `fa-claim.bid` (FAAB bid TextInput, number-pad — renders only when `waivers.type === 'faab'`; bid > remaining disables the CTA) · `fa-claim.drop.<player_id>` (radio-select drop-candidate rows, least valuable first — render when `open_slots` is 0/unknown) · `fa-claim.open-sleeper` (primary CTA → the league's Sleeper players page deep-link)
- TradesScreen: `trades.explore.free-agents` (Explore row on the standalone Trades home → root-stack `FreeAgents`; hidden first-run and in hub-launched deck modes)

**Header-back rollout tranche (2026-07-25, #151 pattern):**
- Root-stack pushes over headerShown:false Main tabs all use the explicit `HeaderBack` control (native back dead on iOS 26, RNS#3294): `league-summary.back-btn` · `profile.back-btn` · `test-stages.back-btn` (joining `free-agents.back-btn`)

**Quick-walk format + search tranche (2026-07-17, #137/#138):**
- QuickSetTiersScreen: `quick-set.format-toggle` (View hosting the SF/1QB FormatToggle — the segments themselves carry accessibilityLabel "<1QB PPR|SF TEP> scoring format", which is what Maestro text-matches) · `quick-set.search` (per-step name filter TextInput)
- QuickRankScreen: `quick-rank.format-toggle` · `quick-rank.search` (same pair, same semantics)

**Calculator suggestions tranche (2026-07-17, #78):**
- InLeagueCalculator: `calc.league-give-add` · `calc.league-receive-add` (TradeSide add buttons in the In-league mode; the picker/verdict IDs are shared with the calc screen)

**Onboarding trades-first tranche (2026-07-17, plan item 4, flags `onboarding.v2` + `onboarding.trades_first` / `onboarding.guided_layer`):**
- TradesScreen: `trades.provenance-chip` (deck-basis chip; disabled Pressable until item 7) · `trades.skeleton-card` (first-run streaming placeholder) · `trades.coach-mark.provenance` (guided-layer callout, tap dismisses) · `trades.identity-strip` (container) · `trades.identity-strip.switch` ("not you?" → sign-out confirm) · `trades.identity-strip.dismiss` (session hide)

**League rankings tranche (2026-07-17, #142/#144):**
- LeagueSummaryScreen: `league-summary.basis.<consensus|personal|redraft>` (basis chips — redraft is permanently disabled "(soon)") · `league-summary.team.<user_id>` (ranked team row → roster overlay) · `league-summary.roster-close` (overlay close Icon Button)
- LeagueScreen Explore rows: `league.rankings-row` (→ root-stack `LeagueSummary`) · `league.free-agents-row` (→ root-stack `FreeAgents`)

**League rankings bar-chart redesign tranche (2026-07-20, #169):**
- LeagueSummaryScreen: `league-summary.posfilter.<all|qb|rb|wr|te>` (chart position filter pills — single/multi select, reorders + rescales the stacked bars live) · `league-summary.roster-posfilter.<all|qb|rb|wr|te>` (drill-in overlay's own position filter). The team rows are now stacked bars but keep `league-summary.team.<user_id>`; basis + roster-close IDs unchanged

**Onboarding items 5–10 tranche (2026-07-17, flags `onboarding.*` per feature):**
- SignIn (item 5, `onboarding.landing`): `signin.apple-link` (quiet Apple re-entry text link) · `signin.error-demo-escape` (Sleeper-down "browse the sample league" escape)
- Trades (items 7/8/9/10): `trades.quickset-prompt` (+ `.accept` / `.dismiss`) · `trades.diff-banner` (post-Quick-Set regen receipt) · `trades.apple-sheet.<like|quickset_save|session2_banner>` (+ `trades.apple-sheet.signin` / `.decline`) · `trades.apple-session2-banner` (+ `.dismiss`) · `trades.share-liked` · `trades.trio-entry` (deck-exhausted CTA) · `trades.demo-bridge` · `trades.redraft-label`
- Rank stack (item 9, `onboarding.rank_routing`): `rank.more-ways` (QuickSetTiers header link → demoted RankHome chooser)

**Teardown Trades/engagement tranche (2026-07-19, W2B):**
- Trades: `trades.fairness-help` (flag `ux.help_surface`, ⓘ by the fairness toggle → HelpSheet) · `trades.outlook-set-banner` (flag `ux.outlook_inline_default`, no-inference set-outlook banner)
- Matches: `matches.go-to-trades` (flag `ux.empty_state_ctas`, both empty states) · `matches.matching-help` (flag `ux.help_surface`)
- Portfolio: `portfolio.open-settings` · FreeAgents: `free-agents.pick-league` (both flag `ux.empty_state_ctas`)
- Rank/Trios: `rank.unlock-payoff` (flag `ux.outlook_inline_default` pre-threshold caption) · `trios.info.<a|b|c>` (flag `ux.player_context_menu` ⓘ twins)
- Shared: `player-menu` (+ `player-menu.<action-key>`, keys `untouchable-add|untouchable-remove|swap`) · `help-sheet`

**Teardown growth/boards tranche (2026-07-19, W2D):**
- Board search (flag `ux.board_search`): `manual-ranks.search` · `tiers.search` (scroll-to + highlight inputs — the highlighted row reuses the active-drag ring, no ID of its own)
- League tab: `league.rookie-board-row` (flag `league.rookie_board_entry` → opens RookieDraftBoardSheet) · `league.whats-new` (flag `ux.whats_new`, CoachMark — tap dismisses)

**Teardown navigation tranche (2026-07-19, W2A):**
- TopBar bell sheet: `topbar.notif-row.<notification_id>` (tappable inbox row — only rendered with flag `notif.tap_routing_v2`; flag off the rows are inert Views with no ID)
- `rank.more-ways` note: with flag `ux.rank_tab_destination` on, the same ID appears in the header of EVERY rank surface (Trios/Anchors/Tiers/QuickSetTiers/QuickRank/ManualRanks/Trends) and opens the RankMenu sheet; flag off it remains QuickSetTiers-only → RankHome (unchanged)

Smoke flows: `mobile/.maestro/flows/smoke/01–11` (headers carry the TC ids). Full planned list: lld.md Appendix A (~90 IDs).

**Trade-Finding Hub tranche (2026-07-20, #156, flag `trades.finder_hub`):**
- TradeFinderHubScreen: `finder-hub.dna.edit` (opens OutlookSheet) · `finder-hub.card.<guided|team|player|calc>` (mode launcher cards) · `finder-hub.team-picker.<user_id>` (manager rows in the Specific Team sheet)
- TradeFinderModeBar (rendered in `TradesScreen` when opened as `TradeDeck` with a `mode` param): `trades.finder-mode.<guided|team|player|calc>` (quick-switch chips) · `trades.finder-mode.hub` (back to hub). The Trades/Portfolio/Calculator subnav is hidden in these launches

**Hub finish tranche (2026-07-25, #156 finish + #173/#174/#186/#190 — flag `trades.finder_hub` flipped ON):**
- TradesScreen player-mode two-column board: `trades.board.for.<player_id>` / `trades.board.away.<player_id>` (pinned mini chips, tap removes) · `trades.board.add-for` / `trades.board.add-away` (dashed add buttons → FB-47 picker pre-directed)
- `trades.package-toggle` (#174 "Trade as one package" switch — renders with 2+ pinned give players, both board layouts; ON ⇒ `pinned_give_mode:'all'`)
- `trades.team-picker.<user_id>` (#156 item 4 — in-screen manager sheet from the mode bar's Team chip; lands via setParams, in-place scope change auto-regenerates)
- TradeCard (swipe variant only): `trade-card.keep-give` / `trade-card.keep-receive` (#186 per-side "Keep · more offers" — pins that side + regenerates) · `trade-card.edit-in-calc` (#190 → `TradeCalculator` with a `prefill` route param)
- TradeFinderHubScreen: `finder-hub.dna.untouchables` (#173 — opens the untouchables management sheet) · `finder-hub.untouchables.row.<player_id>` · `finder-hub.untouchables.remove.<player_id>` (the hub's own `feedback.fab` mount was removed in #196 — RootNav's global mount covers it)

**League outlook-odds tranche (2026-07-23, #169, flag `outlook.odds` — DARK):**
- LeagueSummaryScreen: `league-summary.odds.section` (the gated playoff-picture container — present only when `outlook.odds` is on AND GET /api/league/outlook returns teams) · `league-summary.odds.beta-ribbon` (the load-bearing "Projected · preseason · beta" honesty label) · `league-summary.odds.source` (strength-source caption, e.g. "Preseason roster-value projection · 10,000 sims · top 6 make the playoffs") · `league-summary.odds.row.<roster_id>` (one team's projected playoff% / title% row, payload order). Flag off ⇒ none of these render and the endpoint is never called (it 404s while the modeling backend is dark). Basis toggle + dynasty-chart IDs unchanged.

**Guided avatar (The Analyst, flag `onboarding.guided_avatar`):** `analyst/` (six rn-svg pose components + `AnalystAvatar`; art source-of-truth = mockups/avatar-lab/analyst-poses.html) · `AnalystGuide.tsx` (RootNav-mounted overlay: scrim+cutout spotlight, bubble with in-bubble CTAs, ✕ skip / Skip-tour opt-out) · `analystScript.ts` (dialogue table = DATA; script doc: docs/plans/onboarding-conversion/guided-avatar-script.md). Guide testIDs: `guide.overlay` · `guide.bubble` · `guide.cta.<accept|dismiss>` · `guide.step-x` · `guide.dismiss-tour` (was `guide.skip-tour` pre-#187 — the permanent opt-out link on every bubble) · `guide.avatar.<pose>` · new targets `trades.card-body`

**Avatar dismiss/disable + FAB coverage tranche (2026-07-25, #187/#188):**
- Settings: `settings.guided-tour-toggle` (The Analyst on/off Switch — off = `dismissTour()`, on = `enableTour()` full-replay restart)
- FeedbackFAB `aboveTabBar={false}` mounts on root-stack pushes (FreeAgents, LeagueSummary's push variant) — same `feedback.fab` id, screens over the tabs now carry the button (see root CLAUDE.md convention)

**Rank-nav + calculator tranche (2026-07-25, #162/#165/#192):**
- TabNav shared HeaderBack (Rank/Trades/League stack sub-screens): `stack.back-btn` — always-on JS back control, falls back to the stack's home route (Rank surfaces → `RankHome`, the chooser, per #162/#165)
- InLeagueCalculator partner chips: the ranked-status flare dot is replaced by the RankedBadge text badge (`R` / `R*` derived / `NR`) — no new testID; chips assert via accessibilityLabel `@<user>, ranked|ranked in another format, values converted|not ranked`

**MFL auth-link tranche (2026-07-25, #177, flag `mfl.auth_link`):**
- PlatformLinkSheet (MFL only): `platform-link.mfl-auth-toggle` (reveals the sign-in fields) · `platform-link.mfl-username` · `platform-link.mfl-password` (secureTextEntry) · `platform-link.mfl-signin` · `platform-link.mfl-league.<league_id>` (checkbox row, pre-checked when the franchise auto-binds) · `platform-link.mfl-import` · `platform-link.mfl-open` (auth-done summary CTA); errors reuse `platform-link.error`

**Calculator eveners tranche (2026-07-26, DynastyGM teardown):**
- EvenerRows (In-league calculator + open calculator's ConsensusVerdictCard): `calc.evener.<id>` (row container; `<id>` = player_id, pick_id, or `<a>+<b>` for the 2-piece package row) · `calc.evener-add.<id>` (the + button — adds the asset(s) to the side `gap.add_to` points at and re-evaluates)

**Calculator polish tranche (2026-07-27, DTF/DynastyDealer teardowns):**
- InLeagueCalculator verdict: `calc.starter-impact` (dim one-line lineup-impact sentence under the Consensus totals — rendered only when `/api/trade/evaluate` Mode B returned `starter_impact`)
- InLeagueCalculator partner chips: `calc.partner-summary.<user_id>` (the positional value line inside the chip; NOTE the chip Pressable is an accessible container, so on iOS assert via the chip's a11y label — it spells the summary out — rather than the child id)
- ShareTradeImage (In-league actions + live-mode actions): `calc.share-image` (the "Share image" secondary button; the capture card itself is off-screen and carries no id)

**Calculator adjustments tranche (2026-07-26, DynastyDealer teardown):**
- AdjustmentsDisclosure (ConsensusVerdictCard + In-league verdict): `calc.adjustments.toggle` (the collapsed-by-default "Value adjustments" chevron row) · `calc.adjustments.row.<side>-<key>` (one itemized row; `<side>` = `give|receive`, `<key>` = `package_depth|consolidation` — side-qualified because the same key can appear on both sides). Rendered only when `/api/trade/evaluate` returned `adjustments`

**League Analyzer replication tranche (2026-07-26, DynastyGM teardown — LeagueSummaryScreen):**
- `league-summary.bar.<user_id>` — one VERTICAL stacked column in the rank-axis chart (a11y label "Rank N, TeamName, X total"); tapping focuses the team (its bar keeps position colors, every other bar goes muted-gray)
- `league-summary.subset.<all|starters|bench>` — the league-wide roster-subset segmented control. Rendered ONLY when the payload says `starters_available` (derived value-optimal lineup — never per-week lineup data); Starters/Bench recompute + re-rank the whole chart AND the drill-in
- `league-summary.refresh` — chart-card refresh affordance (pull-to-refresh also stays)
- `league-summary.focus-caption` — drill-in caption ("League rank: N/M") in the chart-card header while a team is focused
- `league-summary.avg-line` — dashed league-average line across the chart at the mean of the currently shown bar values (filters × subset × basis; "Avg <value>" label; a11y "League average N in this view"; hidden when the view sums to zero)
- UNCHANGED but re-homed: `league-summary.team.<user_id>` now lives on the ranked LIST rows below the chart; `league-summary.roster-close` is the chart-card X that exits team focus (the drill-in roster renders INLINE below the chart now, not in a Modal); `league-summary.posfilter.*` / `league-summary.roster-posfilter.*` / `league-summary.updated-at` / basis chips / `league-summary.roster-picks` all keep their ids. Drill-in group headers add tercile-colored "(rank/M)" chips (a11y "POS ranked N of M"); player rows show league-wide positional value ranks via PlayerCard `posRank` ("RB2", "NR")


**Calculator #202/#203 tranche (2026-07-27):**
- InLeagueCalculator: `calc.partner-collapsed` (the prefill-mount "Trading with @x" compact row — renders only when mounted with a resolved `initialOpponentId`) · `calc.partner-change` (its "Change" control — expands the full partner-chip picker)
- PlayerPickerModal: `calc.picker.suggested.<player_id>` (one Suggested row — present only while the trade is uneven, the picker adds to the `gap.add_to` side, and no search/position filter is active; the #204 value bar itself carries no id — the In-league verdict asserts via `calc.starter-impact` / visible copy as before)

**Deck player-changer + remove-asset tranche (2026-07-27, operator follow-up + #194):**
- TradeCard (swipe variant, top card only): `trade-card.remove-asset.<player_id>` (per-row ✕ — drops the asset + re-prices; dimmed with `accessibilityState.disabled` when it's the side's last asset, tap then voices the hint toast)
- PlayerContextMenu on the top deck card: `trade-card.swap-suggest.<player_id>` (the "Swap suggestions" row — registry-specced override of the default `player-menu.<key>` id) · `player-menu.remove-asset` ("Remove from trade" row)
- SwapSuggestSheet: `trade-card.swap-suggest-sheet` (sheet container) · `trade-card.swap-option.<id>` (one-tap replacement row; `<id>` = player_id, pick_id, or `<a>+<b>` for the 2-piece package) · `trade-card.swap-suggest-empty` (honest no-candidates state)

**League switcher add-league + FAB dedup tranche (2026-07-27, #199/#196):**
- LeagueSwitcherSheet: `league.switcher.add-league` (#199 — optional "Add a league" row pinned under the league list, rendered only when the host passes `onAddLeague`; since #223 the sheet's ONLY mount is TopBar's global instance, whose onAddLeague closes the sheet and navigates to the root-stack `LeaguePicker`, whose footer carries the ESPN/MFL/Fleaflicker link flows)
- TradeFinderHubScreen no longer mounts its own `feedback.fab` (#196/#197 — TradesHome is a tab-stack screen, RootNav's global mount covers it; the local mount rendered a second flag at a different height)

**League rankings (#14 completion, 2026-07-20):** `RankChipBadge` — "#3 of 12" consensus power-rank chip from the open `GET /api/league/rank-chip` read (60s server cache); silent-fail enrichment (error/old-server/demo → renders nothing); ice text when top-3. Mounted on LeaguePicker rows + the League tab hero chips. testID `league.rank-chip.<league_id>`

**Calculator finder hand-off + trade-card badge reflow tranche (2026-08-01, #213/#226):**
- TradeCalculatorScreen: `calc.find-a-trade` (#213 — the ONE quiet "Want ideas instead? Find a trade →" text-link row under the mode tabs; covers In-league/live/demo with a single affordance and navigates to the Trades stack home `TradesHome` — the finder hub with `trades.finder_hub` on, the classic deck off)
- No new IDs for #226, but the trade-card row layout changed: `PlayerCard` gained a `badgeSlot` prop (informational badges rendered INSIDE the wrapping header badge row) and its classic-branch `rightSlot` is now in-flow (reserves width) instead of absolutely positioned; `TradeCard` moved OTB/UNTOUCHABLE badges to `badgeSlot` on BOTH columns and stacks the lock/swap/remove controls vertically in `rightSlot` (`trade-card.remove-asset.<player_id>` etc. unchanged)

**Header league switcher tranche (2026-08-01, #223/#224):**
- TopBar: `topbar.league` (the tappable active-league cluster — micro-label + truncating name + ice chevron; opens the global LeagueSwitcherSheet; disabled/dimmed while a switch is in flight; NOT rendered in no-league sessions, which show the wordmark instead) · `topbar.settings` unchanged; the bell/gear ORDER swapped (bell inboard, gear at the edge) but both keep their ids/labels and the badge behavior
- `LeaguePill` was DELETED (its two mounts — Trades home "Trading in" + hub — were removed with #223; the header affordance replaced it). `league.hero` (LeagueScreen) still exists but is a plain identity View now — no press action, no chevron

**Featured-trade window tranche (2026-08-02, #216/#209 — flag `trade.asset_ideas`, single-pin player mode):**
- FeaturedTradeWindow: `featured-trade.window` (window container — back-chip row + "Featured trade" tick-label + read-only TradeCard) · `featured-trade.back` (the ‹ Previous trade chip, Variant A row above the header — renders only while the per-pin history stack is non-empty; capped at 10, reset on pin change or a fresh sweep)
- AssetIdeasPanel: `featured-trade.idea.<key>` (one tappable idea row; `<key>` = `assetIdeaKey(idea)` — `<counterparty_user_id>.<give_ids>-<receive_ids>`, a stable domain id, never a list index; the in-window row is disabled with `accessibilityState {selected, disabled}`) · panel container keeps `trades.asset-ideas`
- TradesScreen #209 note: the player-mode pin board's columns SWAPPED — `trades.board.away.*` / `trades.board.add-away` now render in the LEFT column, `trades.board.for.*` / `trades.board.add-for` in the RIGHT (ids unchanged; give→get reading order now matches the featured card and idea rows)
**Trade DNA panel v3 tranche (2026-08-02, #212/#231/#206):**
- TradeFinderHubScreen DNA panel (in-place editor replaces the OutlookSheet mount): `dna.edit` (collapsed header Edit — REPLACES `finder-hub.dna.edit`, which is gone) · `dna.done` (ice Done — persists via the preferences API only when something changed, then collapses) · `dna.outlook.<championship|contender|rebuilder|jets>` (outlook cards) · `dna.chase.<qb|rb|wr|te|picks>` / `dna.shop.<qb|rb|wr|te|picks>` (multi-select toggle rows; the Picks toggle stores `'PICK'` in `acquire_positions`/`trade_away_positions`; cross-row tap MOVES a position) · `dna.untouchable.<player_id>` (read-only collapsed mini-card chips, cap 3 + "+N", "A Jeanty" first-initial+last-name style)
- `finder-hub.dna.untouchables` SURVIVES but moved: it's now the expanded editor's "Manage" link (still opens the #173 management sheet; the collapsed card has no Manage affordance). The #206 NEED/DEPTH recommendation chips were dropped per operator decision — `utils/dnaChips.ts` + its node test were deleted with the panel's only consumer
- TradesScreen (finder modes only): `trades.outlook-receipt` (the #231 OutlookBiasReceipt quiet line) · `trades.outlook-receipt.change` (→ `TradesHome` `{editDna:true}`)
**Empty-states progress tranche (2026-08-02, #229/#230/#234 — ships live, no flag):**
- LeagueScreen: `league.action.rank` / `league.action.find` (day-one action row under the hero — Rank players left/secondary → Rank tab, Find a trade right/solid-ice → Trades hub; rendered only while the progress module is) · `league.progress-module` (the full LeagueProgressModule card) · `league.works-now` (the "Works right now" card — flare-labeled EXAMPLE trade + real `TradeValueBar` with static props + solid-ice "Find a trade")
- MatchesScreen: `matches.progress-module` (compact LeagueProgressModule on the mutual empty state — active league only) · `matches.empty-text` copy changed to "No mutual matches yet" (all-leagues view) · `matches.go-to-trades` KEPT but now always-on with label "Find a trade" (supersedes the S4 PRD-05 `ux.empty_state_ctas` gate on this one state; the awaiting segment still honors the flag)
**Rank chooser consolidation + rankings import tranche (2026-08-02, #232/#233, flag `ranks.import` for import only):**
- RankHomeScreen: `rank-home.import` (Variant A "Have rankings already?" heading-row link — flag `ranks.import`, opens RankImportSheet) · `rank-home.more-toggle` ("More ways to rank" disclosure header) · `rank-home.card.trends` (new disclosure row; `rank-home.card.<quickset|trio|anchor|tiers|manual>` unchanged, now split 3-primary/disclosure)
- RankMenu sheet (TabNav): `rankmenu.more-toggle` (disclosure header); `rankmenu.quickset|trios|tiers` = the 3 primary rows, `rankmenu.anchors|manual|trends` = disclosure rows. **`rankmenu.quickrank` RETIRED** — Quick rank left both chooser surfaces (it's the Quick set finish-prompt follow-on)
- RankImportSheet: `rank-import.paste` (textarea) · `rank-import.match` ("Match N players") · `rank-import.row.<n>` (review row, `<n>` = 1-based paste order — a stable domain order, same exception class as the trio slots) · `rank-import.apply` ("Apply N ranks — M to resolve", disabled until every ambiguous row is chosen/skipped)
- QuickSetTiers (#233, no new IDs): `quick-set.save-btn` copy at 0 selected is now "Continue — no <POS>s this high" / last tier "Continue & finish", and the Skip button does NOT render at 0 selected — flows asserting Skip must select a chip first

**TradeValueBar density tranche (2026-08-03, #243 V1):**
- TradeValueBar: `valuebar.why` (the collapsed-by-default "Why?" disclosure toggle above the verdict box — chevron flips when open; present at EVERY TradeValueBar mount: deck cards, featured window, calculator verdicts, League example card)
**Calculator lineup before/after tranche (2026-08-03, #238 V2 — In-league calculator only):**
- InLeagueCalculator verdict: `calc.lineup-impact` (the full before/after starting-lineup table — renders only when Mode B's `starter_impact.slots` is present; the container is `accessible` with the server's `note` sentence as its accessibilityLabel, so child rows carry NO ids and iOS asserts go through the container id / a11y label). `calc.starter-impact` SURVIVES as the fallback one-liner for servers without `slots` — flows should accept either
**Member-entered provenance tranche (draft-extensions W3 M-C, 2026-08-08 — flag `picks.assign_tradeable`, ships OFF):**
- One id per priced surface, all on the same shared `MemberEnteredMarker` and all rendered UNCONDITIONALLY (the component self-gates on the flag AND `source === 'user'`, so **with the flag off none of these exist and every surface is byte-identical**): `calc.picker.member-entered.<pick_id>` (PlayerPickerModal rows — both the plain and the `suggested` variants share the renderer) · `trade-card.swap-option.member-entered.<evener_id>` (SwapSuggestSheet) · `calc.evener.member-entered.<evener_id>` (EvenerRows) · `calc.member-entered.<pick_id>` (TradeSide, the calculator's own pick rows) · `league-summary.member-entered.<pick_id>` (the drill-in Draft-capital group; falls back to the pick's label when a server sends no `pick_id`)
- **iOS assertion caveat:** the PlayerPickerModal row and the SwapSuggestSheet option row are accessible containers, so those two ids are swallowed on iOS — assert via the ROW's accessibility label, which appends the same "Member-entered — not verified with ESPN" sentence, and which also carries a `correct` custom action ("Correct this pick") firing the same deep link. The other three rows are plain Views and expose the marker id normally
- **Draft Room, assign-in-place (operator, 2026-08-08, flag `picks.assign`):** `draft-room.assign-picks` (the `picks_not_assigned` notice's primary CTA — pushes the root-stack `PickAssignment`; the notice copy now OFFERS the job instead of directing the user to the League tab) · `draft-room.assign-progress` (the partially/fully-assigned row on a real ESPN board — "N of M assigned · Continue assigning"). Flag off ⇒ neither renders and the notice falls through to the server's own message

**Stud-tax mode tranche (2026-08-05, #214/#215):**
- SettingsScreen: `settings.stud-tax.<market|heavy|off>` (the 3-option "Stud tax" segmented control under the TRADE VALUES section — Chalkline pills, ice = selected; persists via PUT `/api/settings/stud-tax`, optimistic with revert-on-error toast)
- AdjustmentsDisclosure: `calc.adjustments.off` (the one-line "Value adjustments off" note rendered when the evaluate response carries `stud_tax_mode: 'off'` — both calculator verdict mounts)

**League home fold + market pulse tranche (2026-08-03, #243 — flag `market.movers` for the strip only):**
- LeagueScreen Explore reflow (fold V1): `league.rankings-row` · `league.free-agents-row` · `league.rookie-board-row` UNCHANGED but re-homed — they now sit on the 3-across `ExploreTile` cards (short labels "Rankings" / "Free agents" / "Rookie board"; the old full names survive as each tile's accessibilityLabel, so text-matching flows should switch to the ids)
- LeagueProgressModule: `league.progress-invite` (the inline "Invite them" text link on the unlock sentence — replaces the old ghost "Invite leaguemates" Button, same OS-share handler; renders as a standalone "Invite leaguemates" link when matches are already unlocked)
- MarketPulseStrip: `league.market-pulse` (the whole strip is the tap target — opens the movers sheet) · `league.market-pulse.see-all` (the "See all movers ›" text, inside the same Pressable) · `market-movers.sheet` (bottom-sheet container; closes via backdrop/onRequestClose, no explicit close control — #242 pattern)

**Acquire tab tranche (2026-08-05, #245):**
- Tab bar: `tab.trades` UNCHANGED — the tab's visible label + accessibility label are now "Acquire" (presentation-level rename; route name `Trades` kept, so text-matching flows must use the id or "Acquire")
- TradeFinderHubScreen: `finder-hub.card.free-agents` (the FREE AGENCY section's single row → root-stack `FreeAgents`; a11y label "Free agent finder"). The hub's in-page title copy changed "Find a Trade" → "Acquire"; the new TRADE / FREE AGENCY TickLabels are headers (no ids)
**Header format tile + matches lock removal tranche (2026-08-05, #247/#249/#251):**
- TopBar: `topbar.format` (solid-ice scoring-format tile beside the league name — inside the `topbar.league` Pressable, so iOS flows assert via the cluster's a11y label "League: <name>, <fmt> format"; not rendered in no-league sessions or before format hydration)
- #249: NO id change — the give-side lock button (never had its own testID) no longer renders anywhere on MatchesScreen (`hideLockButton` on both TradeCard mounts); the Trades deck keeps it. `player-menu.untouchable-add|-remove` remain the Matches path
- #251: NO id change — `calc.evener.<id>` / `calc.evener-add.<id>` rows in the In-league calculator now render ABOVE the verdict card instead of below it

**Guided-first landing tranche (2026-08-05, #246 — flag `trades.finder_hub`):**
- TradeFinderModeBar: **`trades.finder-mode.draft` (rookie-draft placement, 2026-08-06, flag `draft.room`, option B — the draft's PERMANENT home)** — pushes root-stack `DraftRoom`, same push-chip construction as Calc / Free agents. It **LEADS** the strip (`[DRAFT_CHIP, ...CHIPS]`), and that is load-bearing, not styling: the shipped five chips measure ≈402pt against ≈361pt of usable width, so the strip is genuinely scrolled and an APPENDED sixth chip would sit off-screen and never be seen. The host passes `onDraft` only when the flag is on, so the handler's presence is what creates the chip — flag off ⇒ today's five chips, same order · `trades.finder-mode.free-agents` (the fifth chip, pushes root-stack `FreeAgents`; operator override of the mock's tail-section FA placement) · **`trades.finder-mode.hub` RETIRED** (the "‹ Hub" chevron is gone with the unrouted hub) · `trades.finder-mode.<guided|team|player|calc>` unchanged, but now present on the tab's LANDING (TradesHome renders the deck with `mode:'guided'`), not just hub-launched decks
- TradeDnaSheet (changes HOST from the hub, ids kept): `dna.outlook.<championship|contender|rebuilder|jets>` · `dna.chase.<qb|rb|wr|te|picks>` / `dna.shop.<qb|rb|wr|te|picks>` · `dna.done` (sheet-header Done — pure dismiss, #236 autosave) · `finder-hub.dna.untouchables` (Manage link inside the sheet) · `finder-hub.untouchables.row/remove.<player_id>` (management layer inside the same Modal). **`dna.edit` and `dna.untouchable.<player_id>` are DORMANT** (they lived on the hub's collapsed panel, which is unrouted — flows should reach the editor via `trades.outlook-receipt.change`)
- Hub ids now unreachable in-app (screen unrouted, file kept): `finder-hub.card.<guided|team|player|calc|free-agents>` · `finder-hub.team-picker.<user_id>`; `trades.explore.free-agents` no longer renders on the landing (it was `!finderMode`-gated — the FA chip is the deck's FA entry; the classic flag-off home keeps the row)

**Draft-room per-player actions tranche (2026-08-06, draft-extensions W1 — flag `draft.rank_inline`, ships OFF):**
- **BREAKING for existing flows — three Draft Room ids gained a qualifier.** They shipped SHARED and non-unique across every row, which is exactly why this flow was untestable: `draft-room.undrafted-row` → **`draft-room.undrafted-row.<player_id>`** · `draft-room.order-row` → **`draft-room.order-row.<round>-<slot>`** (`<slot>` is `r` when the platform has not published an order — the row is round-level ownership, not a slot) · `draft-room.pick-row` → **`draft-room.pick-row.<pick_no>`**. Qualifiers are stable domain ids per the grammar, never the map index. All three ship **UNFLAGGED**: they are inert, and they are what makes the flag testable at all
- Undrafted rows with the flag ON are `Pressable` with `onLongPress` + `accessibilityActions [{name:'menu', label:'Player options'}]` — the shipped `TradeCard` vocabulary. **There is deliberately no "⋯" glyph:** none exists anywhere in `mobile/src/`, and a visible overflow affordance is a design decision needing a `docs/design/components.md` spec under ADR-004/005, not something a build agent invents inline. The row is an accessible container with its own label (name + position + "no value on your board"), so iOS asserts go through the id or that label
- `PlayerContextMenu` rows on this surface use registry-specced testID overrides: `draft-room.action.set-value` · `draft-room.action.rank-rookies` · `draft-room.action.add-target` (defaults would have been `player-menu.<key>`)
- `AnchorSheet`: `anchor-sheet` (sheet container) · `anchor-sheet.rung.<anchor_key>` (the eight rungs — `4_firsts`…`no_value`, the cross-client enum) · `anchor-sheet.result` (the post-save "Set to <tier> · ≈ N" line, with "Tap another rung to change it") · `anchor-sheet.error` · `anchor-sheet.done`
- `draft-room.coverage-nudge` — "N of the top 25 have no value on your board. Hold a row to set one." Renders only with the flag ON and only when the count is > 0
- RookieRanksScreen: `rookie-ranks.back-to-draft` — the RETURN leg of the two-way bridge. Renders only when the Draft Room passed `returnTo:'DraftRoom'`; every other entry to that screen omits the param and the row does not exist
- Flag OFF ⇒ none of the action/sheet/nudge ids exist and the rows are the plain `View`s they shipped as — the three qualified ids above are the only difference

**Mock draft tranche (2026-08-06, draft-extensions W2 — flag `draft.mock`, ships OFF; placement option C):**
- DraftRoomScreen: `draft-room.mode.real` / `draft-room.mode.mock` (the segmented mode switch, pinned above the ScrollView; rendered only with the flag on). Selecting Mock replaces the room's body with `MockEntryPanel` and pins `mock.rail` under the toggle — and REMOVES the StatusBar/Refresh, the `draft-room.deep-link` CTA and the "never drafts for you" note, which describe the real draft and would read as promises about a board that takes picks
- MockEntryPanel: `mock-entry.card` · `mock-entry.loading` · `mock-entry.error` · `mock-entry.start` · `mock-entry.resume` · `mock-entry.start-over` · `mock-entry.recap` · `mock-entry.run-it-back` · `mock-entry.blocked.<reason>` (`cpu_model_unvalidated` · `class_not_loaded` · `startup_draft` · `league_too_small` · `live` · `complete`)
- MockSetupSheet: `mock-setup-sheet` · `mock-setup.rounds.minus|value|plus` · `mock-setup.type.linear|snake` · `mock-setup.order-notice` · `mock-setup.start` · `mock-setup.cancel` · `mock-setup.error`
- MockDraftScreen: `mock-draft.rail` (**the mode marker — present in EVERY sub-state, outside the scroll; asserting it is how a flow proves it is not on the real board**) · `mock-draft.back-btn` (#151 header-back pattern) · `mock-draft.end` (header End → abandon, destructive confirm) · `mock-draft.scroll` · `mock-draft.on-the-clock` · `mock-draft.ticker-row.<pick_no>` · `mock-draft.basis.consensus` / `.my-board` · `mock-draft.undrafted-row.<player_id>` (the ROOM's `UndraftedRowView` with a `testID` override — same component, its own namespace) · `mock-draft.confirm` + `mock-draft.confirm.cancel` / `.draft` · `mock-draft.undrafted-empty` · `mock-draft.recap` + `mock-draft.recap-row.<pick_no>` · `mock-draft.empty-text` · `mock-draft.error-text` · `mock-draft.empty.<reason>` (typed-empty refusals: `no_active_mock` · `class_not_loaded` · `cpu_model_unvalidated`, and any future one — the reason is an OPEN string per plan D10)
- Long-press row actions on the mock reuse `PlayerContextMenu` behind W1's own flag: `mock-draft.action.set-value` · `mock-draft.action.add-target` (the `AnchorSheet` ids are unchanged)
- Flag OFF ⇒ none of these exist and the Draft Room is byte-identical to what shipped
